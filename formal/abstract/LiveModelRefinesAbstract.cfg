\* Same bound as live-sports/LiveModelDispatchUnderAmbiguity.cfg (matching
\* existing bound choices in the repo), plus TargetInv fixing the one
\* invocation id the refinement mapping projects through alpha.
CONSTANTS
    Invocations = {i1, i2}
    Attempts = {a1, a2, a3}
    Fingerprints = {fp1, fp2}
    Actors = {act1, act2}
    TrustedActors = {act1}
    VerifiedBalance = 2
    RequestCost = 1
    TargetInv = i1

SPECIFICATION Spec

PROPERTY AbstractRefinement
