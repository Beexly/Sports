\* UNCONTROLLED pending-class quotient: AtMostOne FAILS -- GE2 is reachable.
\* Expect a real AtMostOne violation with a counterexample trace tail in GE2.
CONSTANTS InvIds = {i1, i2, i3}
INIT Init
NEXT Next
INVARIANT TypeOK
INVARIANT AtMostOne
