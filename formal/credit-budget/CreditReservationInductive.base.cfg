\* BASE CASE (and reachable-truth sanity): run the ORIGINAL behavior spec
\* (Init /\ [][Next]_vars, inherited via EXTENDS) with every conjunct of
\* IndInv as an invariant. Passing confirms IndInv holds in ALL reachable
\* states, which subsumes the induction base case Init => IndInv (initial
\* states are reachable) and shows the auxiliary predicates are true of the
\* modeled system, not vacuous strengthening.
CONSTANTS
    Attempts = {t1, t2, t3, t4}
    VerifiedBalance = 3
    RequestCost = 1

SPECIFICATION Spec

INVARIANT TypeOKFinite
INVARIANT LedgerNeverExceedsBalance
INVARIANT NeverOverAdmit
INVARIANT AdmittedCountBoundedByStarted
INVARIANT CommittedCoveredByReserved
