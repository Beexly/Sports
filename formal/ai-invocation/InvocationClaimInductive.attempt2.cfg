\* CTI search, ATTEMPT 2: attempt 1 + AtMostOnePendingPerInvocation (added in
\* response to CTI #1 — two attempts Pending on the same invocation, which the
\* sequential route walk makes unreachable but no candidate conjunct forbade;
\* resolving one Ambiguous froze the invocation with the other still in
\* flight). Same shrunk constants as attempt 1.
CONSTANTS
    Invocations = {i1, i2}
    Attempts = {a1, a2}
    Fingerprints = {fp1, fp2}
    Actors = {act1, act2}

INIT InitIndInvAttempt2
NEXT Next

INVARIANT TypeOK
INVARIANT AtMostOneClaimOwner
INVARIANT AtMostOneExternalDispatchPerAttempt
INVARIANT DispatchAssignmentStable
INVARIANT SameIdDifferentFingerprintNeverExecutes
INVARIANT AmbiguousAttemptStopsFallback
INVARIANT AtMostOnePendingPerInvocation
