\* BASE CASE (and reachable-truth sanity) at the induction constants: run the
\* ORIGINAL composed behavior spec (Init /\ [][Next]_vars, inherited via
\* EXTENDS LiveModelDispatchUnderAmbiguity) with every conjunct of IndInv as an
\* invariant. Passing confirms IndInv holds in ALL reachable states, which
\* subsumes the induction base case Init => IndInv (initial states are
\* reachable) and shows the lifted auxiliaries + the one composition glue
\* conjunct are true of the modeled system, not vacuous strengthening.
\*
\* Constants match the closure run (see the module header and the strengthening
\* log for the documented shrink from LiveModelDispatchUnderAmbiguity.cfg).
CONSTANTS
    Invocations = {i1}
    Attempts = {a1, a2}
    Fingerprints = {fp1}
    Actors = {act1, act2}
    TrustedActors = {act1}
    VerifiedBalance = 1
    RequestCost = 1

SPECIFICATION Spec

INVARIANT TypeOKFinite
INVARIANT LiftedAtMostOneClaimOwner
INVARIANT LiftedAtMostOneExternalDispatchPerAttempt
INVARIANT LiftedDispatchAssignmentStable
INVARIANT LiftedSameIdDifferentFingerprintNeverExecutes
INVARIANT LiftedAmbiguousAttemptStopsFallback
INVARIANT AtMostOnePendingPerInvocation
INVARIANT RejectedImpliesBound
INVARIANT LiftedLedgerNeverExceedsBalance
INVARIANT LiftedNeverOverAdmit
INVARIANT AdmittedCountBoundedByStarted
INVARIANT CommittedCoveredByReserved
INVARIANT AmbiguousExposureHeldUntilTrustedResolution
INVARIANT ReservedNeverExceedsBudgetWindowCap
INVARIANT AvailableBudgetNeverNegative
INVARIANT NoDispatchWithoutExposureHold
INVARIANT InflightImpliesHeld
