import { Shell, SimpleTable } from "../_components";
import { askStatKing } from "@/lib/statking/product";
export default function Page({ searchParams }: { searchParams?: { q?: string } }){ const q=searchParams?.q ?? "best players"; const answer=askStatKing(q); return <Shell title="Ask StatKing"><form><input name="q" defaultValue={q} className="w-full border border-mineral bg-eclipse p-3 text-ion-white"/><button className="mt-3 border border-orbital-cyan px-4 py-2 text-orbital-cyan">Ask</button></form><h2 className="text-2xl text-ion-white">{answer.title}</h2><SimpleTable rows={answer.rows as any}/><p className="text-ion-1">Supported templates: best QB by fantasy edge, best RB by usage, most volatile WR, hidden value players, mirage players, sources needing activation, top YouTube sources.</p></Shell>}
