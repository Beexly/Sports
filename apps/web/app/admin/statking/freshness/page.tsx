import { Shell, Cards, SimpleTable } from "../../../stats/_components";
import { loadSources } from "@/lib/statking/product";
export default function Page(){ const rows=loadSources().slice(0,50).map(s=>({source:s.canonical_name,mode:s.source_mode,status:s.legal_gate_status.includes("review")?"review_required":"freshness_unknown",next_action:s.next_action})); return <Shell title="Freshness SLAs"><Cards items={[{label:"Sources sampled",value:rows.length},{label:"Needs review",value:rows.filter(r=>r.status.includes("review")).length},{label:"SLA",value:"modeled"},{label:"Next",value:"ingest timestamps"}]}/><SimpleTable rows={rows}/></Shell> }
