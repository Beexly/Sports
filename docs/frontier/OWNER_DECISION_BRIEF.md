# PROJECT PARALLAX — Owner Decision Brief

Branch `claude/keen-ptolemy-t38f1g` · scope: research + fixture build (reversible, autonomous) · no
merge, no live, no spend, no gate flip. The final report, in the twelve mandated points.

---

**1. What governing idea won.** **PARALLAX** — a counterfactual sports-reality instrument built on one
law: *Conserved Authority under valid intervention.* Every meaningful decision is a replayable,
time-aware, multi-observer object you can rewind, fork, and audit — and that refuses to claim more than
the evidence permits.

**2. What was rejected and why.** *The Calibration Republic* (forecasting-first) — needs settled history
and a crowd we don't have (0 settled); a platform, not an instrument; lowest novelty. *The Provenance
Vault* (proof-first) — descriptive, not counterfactual; closest to existing prior art (W3C PROV / flight
recorder); proves the past but can't fork it. (`04_MASTER_THESIS.md`.)

**3. What is genuinely novel.** The **combination**, never any single pillar. Verified across 18 sports
products + 8 science areas (`01`): no system fuses counterfactual **propagation** + **cross-domain**
observer comparison + first-class **refusal** + point-in-time knowability + replay. Closest (Betfair,
Unabated) reach ~2.5 of 5. Tagged `NOVEL-COMBINATION`; the six-pillar fusion is flagged
`POSSIBLY-FIRST-OF-KIND` (a universal negative we cannot fully prove). (`02_NOVELTY_LEDGER.md`.)

**4. What prior art exists.** Each pillar individually: counterfactual validity (Pearl/DoWhy), replay
(FDR/PROV), abstention (Chow/SelectiveNet), point-in-time calibration (Metaculus), discovery (PySR/
co-scientist/POPPER); and in sports: point-in-time rewind (Polymarket/Kalshi/Betfair), books-vs-fair
(OddsJam/Unabated), DFS weighting (FantasyLabs), calibration page (Polymarket). **We claim none of these
as ours.**

**5. What was built.** (a) The canonical **AuthorityVector** — one 8-layer composition closing GAP-1
(rights + evidence first-class) and GAP-2 (one recorded order), proven a faithful superset of the
production gate by the contraction lemma. (b) The **Reality Fork engine** — Decision Object, Time Lens,
Observer Arena, conservation-checked fork, boundary x*, refusal, deterministic replay, counterfactual
memory. (c) The **interactive instrument** `PARALLAX_REALITY_FORK.html`, render-verified. (d) 38 PARALLAX
tests + 14 frontier documents.

**6. What remains fixture/shadow.** Everything. Data mode is FIXTURE; the meet pins every expression to
INFO_ONLY; all priors are declared FIXTURE PRIORS. No fact is LIVE; the live path is designed, not armed
(`11`).

**7. What tests prove.** Future facts can't change an earlier decision; fixture/shadow can never be
public; rights/evidence bind directly; a fork conserves to <1e-9; missing facts and invalid
interventions are refused; one outcome moves no weight; the 8-layer composition equals the production
gate over all 72 contexts; the instrument renders with 0 network and 0 console errors. (`10`, `12`.)

**8. What failed.** Nothing in scope. Two checks are **ENVIRONMENT_BLOCKED** (all-workspace typecheck +
Next build — sandbox can't generate Prisma; resolve on hosted CI). Honestly classified, never called
green.

**9. What needs owner approval.** Merging this branch to main; contracting any paid feed; arming
`sourceReality = LIVE_REAL` per validated fact; `priced=true`; `canPublishProjections` (the backtest does
**not** beat naive today); `PERFORMANCE_STATS_ENABLED`; unfolding the production `authorityCeiling` to the
8-layer object. Each is a separate, explicit act; none was taken.

**10. The single highest-leverage next move.** **Adopt the AuthorityVector as the one authority object
every surface reads** — wire `composeAuthority` into the existing card/ladder/publish-gate paths (the
contraction lemma already proves it's safe), so GAP-1/GAP-2 close *in production behavior*, not just as a
parallel module. That turns "one organism, one source of truth" into a fact, and makes the Reality Fork
the surface that renders the live composition. (Owner-gated; propose-only until armed.)

**11. Branch and commit.** `claude/keen-ptolemy-t38f1g`; frontier checkpoints A–F (see `git log`); this
brief's commit is the Checkpoint F head.

**12. Preview location.** `docs/gse-packet/observatory/PARALLAX_REALITY_FORK.html` (open in any browser —
static, offline, fixture-watermarked). Companion: `GSE_FIELD_OBSERVATORY.html`. The full method:
`docs/frontier/00…12 + OWNER_DECISION_BRIEF.md`.

---

### The standard, met

A skeptic can open the slice, flip WR1 to *out*, watch WR2's read move to role-up while the permitted
claim stays **INFO_ONLY**, rewind to Monday and watch it **refuse to fork at all** — and say: *"I have
not seen a sports product behave like this before, and I can see exactly why it is not pretending."*
