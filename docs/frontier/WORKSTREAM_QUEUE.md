# GSE Frontier Workstream Queue

| ID | Status | Workstream | Dependency | Primary leverage | Protected zones |
|---|---|---|---|---|---|
| W000 | DONE (slice 1) / REPEATABLE | Recover Existing Assets | none | Prevent duplicate code; restore stranded correctness and frontier substrate | varies by selected PR |
| W001 | DONE (2026-07-17) | Governed Playback Canonicalization | W000 ✓; PR #112 disposition | One evidence envelope/event/delta/playback spine | settlement, proof, entitlements |
| W002 | DONE (2026-07-17) | Worldline v0 | W001 ✓ | Bitemporal as-of replay and no-lookahead world diffs — landed `apps/web/lib/worldline` (13 tests; verifier FAIL→both blockers fixed, DEC-009) | data, proof |
| W-OTS | DONE slices 1+2 (2026-07-17) | Bitcoin-anchored trustless time | founder packet | Slice 1: verbatim port, LIVE calendar+python checks, migration+flag. Slice 2: fail-open mint wire (post-transaction, never a freeze failure mode) + /api/proof/ots/[slateKey] (raw .ots + ?format=json) + llms.txt discovery. Slice 3 DONE (2026-07-17): ots-upgrade.ts (reference commitment-path semantics, conservative grafting, python parses upgraded proofs) + gated /api/cron/ots-upgrade poll (Bearer CRON_SECRET, documented-not-registered). W-OTS COMPLETE end-to-end | proof, data |
| W-MCP | DONE slice 1 + Phase 2.1 + Phase 2.3 (2026-07-17) | Hosted Galaxy Proof MCP at /api/mcp | proof surface (live) | The packet's 7-tool contract served from the site itself (streamable-HTTP JSON-RPC, dependency-free, in-process over lib/proof — one truth path; 24 tests) + an 8th tool `get_reality_receipt` wrapping W003 (DEC-017; 28 tests). Phase 2.3 (DEC-019): the founder's stdio packet (located in session uploads, `galaxyproofmcp.zip`) vendored byte-identical at `packages/galaxy-proof-mcp-stdio` for local Claude Desktop/Cursor installs — its own smoke.mjs wired as `npm test`, 7-vs-8-tool parity asserted from the hosted side. Follow-up queued: MCP registry/directory listings (founder click) | proof, public claims |
| W-WEATHER-REC | DONE + §2 smoke CLOSED (2026-07-17) | ONE canonical weather path | trials registry | Verdict: COMPLEMENTARY layers, not duplicates. Loader (vendored packet, as-of Open-Meteo fetch w/ provenance) → toGameWeatherForecast adapter → buildWeatherFeatureRows (leak gate) → trials registry. 7 tests incl. end-to-end + the gate dropping a mis-timed loader call. DEC-023: the §2 strict previous-runs smoke is DONE — `scripts/weather-integration-smoke.mjs` (`npm run smoke:weather-integration`) empirically confirmed live that Open-Meteo's `previous-runs-api` serves genuinely distinct forecast runs per hour (day-bucketed `_previous_dayN`), the correct leak-safe target; the shipped loader's `historical-forecast-api` backtest path does not offer run selection. Loader left unmodified (vendored verbatim, DEC-014). Still open, unscoped: wiring `previous-runs-api` into the feature store before any real historical admission run | data, model claims |
| W003 | DONE + Phase 2.2 (2026-07-17) | Reality Receipt v0 | W002 ✓ | Reproducible visual/decision object — landed `apps/web/lib/reality-receipt` composing the W001 envelope digest + receipt tamper-check + W-OTS Bitcoin-anchor status into one object; `GET /api/proof/reality/[gameId]` (JSON) + `/image` (PNG); FREE-tier-only fail-closed loader (DEC-015); 31 tests. Phase 2.2 (DEC-018): added the `slateInclusion` Merkle-inclusion leg (reuses `inclusionProof`/`verifyInclusion` from `@sports/prediction-engine`), gated on the receipt's own SEALED/OPEN disclosure timing after a gse-red-team pass caught premature pre-kickoff hash exposure in the first cut; 78 combined tests | proof, public claims |
| W004 | DONE (2026-07-17) | SportsIR v0 | W002-W003 ✓ | Shared minimal IR — 12 canonical primitives in `packages/types/src/sports-ir.ts` (6 ADAPTED with real-object proofs: Entity ctor, Observation/State ← Worldline, Claim/Outcome ← PickEvidenceEnvelope, Proof ← RealityReceipt; 6 DECLARED with named future sources); adapters at `apps/web/lib/sports-ir/`; 20 tests (16 adapter + 4 type) | schema/contracts |
| W005 | DONE (2026-07-17) | Intelligence Watch v0 | W002 ✓, existing watchlist ✓ | Persistent user-maintained intelligence, generalized beyond pick-settlement alerting — `apps/web/lib/intelligence-watch` compiles a W002 `WorldDelta` against a per-watchlist-entry `IntelligenceWatchContract` + the existing Elite `canGetAlerts` entitlement into a `surface`/`no_material_change`/`not_entitled` decision (DEC-021); named `IntelligenceWatchContract` (not `IntelligenceContract`) to avoid colliding with genesis-kernel's unrelated plan-compiler type of the same name; fully shadow (no live caller, no send path); 7 tests. gse-red-team flagged a REQUIRED (not yet exploitable) precondition before any live wiring: add a graded/settled guard, since `WorldDeltaEntry` carries no such marker today | entitlements, notifications |
| W006 | BLOCKED | Capability Foundry v0 | W000; PR #124 disposition | Turn discovered assets into evaluated capabilities. Checked 2026-07-17: genuinely still blocked — GG-000's own ruling requires GG-001/genesis-kernel to "land" first (packages/genesis-kernel/PR #127 still unmerged); recovering PR #124 without that would risk the duplication this session has repeatedly guarded against | supply chain, source rights |
| W007 | DONE (2026-07-17) | Branching Reality v0 | W002 ✓, W004 ✓ | Preserve unresolved worlds instead of flattening uncertainty — `WorldlineStore.detectConflicts()` (DEC-024) surfaces genuine same-instant source disagreements the single-winner snapshot silently discards, purely additive (zero change to existing resolution); adapts SportsIR's sixth and final DECLARED primitive, `SportsIrBranch`. Fully shadow (zero live callers). gse-red-team CONFIRMED clean on 5/6 checks + flagged a REQUIRED-before-live-wiring source-rights-clearance precondition, closed via documentation. 44 tests green | model/public interpretation |
| W008 | BLOCKED | Model Ecology v0 | W006-W007 | Regime-aware shadow competition | model freeze, claims |
| W009 | DONE (2026-07-17) | Hypothesis-to-Instrument v0 | W004 ✓, historical harness ✓ | Convert research into versioned product metrics — verified both dependencies real (`apps/web/lib/backtest/harness.ts`'s `runBacktestHarness` is the "historical harness"; checked `packages/prediction-engine/src/edge-lab/trials-registry.ts` first to rule out duplication — that system admits research into the model, a different lifecycle stage). Landed `apps/web/lib/hypothesis-instrument` — `buildModelBeatsClimatologyInstrument()` wraps the harness's own already-audited `climatology.modelBeatsClimatology` signal into a versioned, content-hashed, stably-identified `HypothesisInstrument` (DEC-025); 4-state honest status (`SUPPORTED`/`NOT_SUPPORTED`/`INSUFFICIENT_SAMPLE`/`UNTESTED`) correctly handles the all-PUSH edge case where the harness's own coarse `status` reads "ok" but climatology is withheld. Second `SportsIrClaim` source added (`hypothesisInstrumentToSportsIrClaim`) — reuses the existing ADAPTED primitive rather than a new one; `confidence` always `null` (a test result, not a probability). Fully shadow (zero live callers). 10 new tests; full apps/web suite (8,501 tests) re-run green | evaluation/claims |
| W010 | BLOCKED | Product Twin v0 | telemetry baseline | Turn comprehension gaps into bounded proposals | privacy, experiments |

## Workstream completion rule

A workstream is DONE only when its acceptance criteria, tests, protected-zone review, branch/PR receipt, and current-state update are verified. One session executes one workstream.

## W000 remaining recovery slices (repeatable, see RECOVERY_MATRIX.md)

- #123 cockpit per-page ADMIN (founder-mergeable or next recovery slice)
- #124 frontier fabric (after #119/#123 land; guard-script interplay re-check)
- #122 CLV/Pedersen (OWNER_GATE: founder merge + migration)
- #121 fantasy engine (after trademark rename)
- #52 Galaxy Dynasty (defer to Dynasty convergence)
