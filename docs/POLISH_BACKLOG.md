# Polish Backlog — owner directive: "polish EVERYTHING. mapping, data, stats"

Standing list (2026-06-12). Work top-down; each item ships with gates green.

1. **Optimizer real-pool path**: licensed salary feed env keys
   (SPORTSDATAIO/FANTASYDATA) light up /fantasy/dfs board automatically;
   DK CSV import is live today. Sample slate banner shipped.
2. **Players Lab stat polish**: spot-check each view's column math vs nflverse
   source (snap %, WOPR, RYOE); add per-view "rows + freshness" stamps.
3. **Galaxy Twin mapping**: live row events into the observatory canvas
   (board state → node pulses) — Crosswire-style deepening.
4. **Performance/calibration surfaces**: unify number formatting
   (tabular-nums everywhere, one decimal standard).
5. **Film Room**: render slate on Higgsfield credits (staged: media_id
   fe262e43…, ~18cr/clip, ~100cr full slate).
6. **Pundit lanes that ARE licensable**: owner notes Jeff Mans publishes a
   weekly show — evaluate its public feed (podcast/YouTube) through the
   rights registry as its own source. SiriusXM corporate licensing parked
   per owner.
7. **ADMIN_EMAILS** env still pending in Vercel → cockpit badge.
