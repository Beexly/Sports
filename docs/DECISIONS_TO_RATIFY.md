# GSE Intelligence Decisions To Ratify

This file records reversible choices and human-gated actions surfaced by the GSE Intelligence Core work. Codex may scaffold code paths behind OFF flags, but it may not self-approve owner, infra, data, schema, payment, entitlement, or calibration truth flips.

## 2026-06-23 - Slice 0

| Decision | Owner | Status | Rationale | Ratification Needed |
|---|---|---:|---|---|
| Use `codex/intelligence-core` in `C:\Users\Garrett\Sports-intelligence-core` instead of editing the dirty primary checkout | Codex | chosen | Preserves unrelated in-flight work on `codex/galaxy-dynasty-studio-rescue-v2` while starting from `origin/claude/sweet-fermi-sk9gws`. | No owner action required unless the owner wants work applied directly to another branch. |
| Treat the GSE Intelligence package as external operating specs, not committed repo docs | Codex | chosen | The package files exist under `C:\Users\Garrett\Documents\Claude\Projects\AI Sports` but are absent from this branch by filename. | Optional: owner/Claude can commit the package docs later for repo-native provenance. |
| Use `node --use-system-ca` for local nflverse probes on this host | Codex | chosen | Plain Node fetch failed TLS verification locally; system CA mode succeeded. | Optional environment cleanup if future scripts should run without `--use-system-ca`. |
| Add `LadderEvent` to Prisma schema without applying a migration | Codex | chosen | A1 needs the append-only model contract, but schema application to a shared database is a human/infra gate. | `[SCHEMA]` Generate and apply a reviewed migration in the target database environment. |
| Keep ladder reducer output shadow-only | Codex | chosen | A1 proves evidence derivation and two-track invariants without flipping `canPublishProjections`, pricing, performance stats, or model calibration. | `[OWNER]/[DATA]` Ratify any future runtime consumption of ladder state. |
| Use a runtime-neutral deterministic replay fingerprint in E1 | Codex | chosen | Exporting a Node-only crypto import through `@sports/prediction-engine` breaks the Next web bundle; E1 only needs deterministic replay identity. | No owner action required; D4 must choose the cryptographic hash-chain implementation for replayable provenance. |
| Keep B1 feature-store persistence as an injected R2/DuckDB seam | Codex | chosen | The feature-store contract needs a durable target shape, but no bucket, relation, credential, or write path should be created by Codex. | `[INFRA]` Provision `R2_FEATURE_STORE` and `feature_store.metric_snapshots`, then wire an implementation behind review. |
| Use `DEFAULT_PLAYER_RATE_SHRINKAGE_K = 12` for B2 posteriors | Codex | chosen | Twelve pseudo-samples gives visible shrinkage for thin player rates while letting real volume dominate; every posterior remains shadow and `priced=false`. | `[DATA]` Tune k by position and metric through purged/embargoed walk-forward evidence before any public weighting. |
| Start B3 with team-level physical conservation before pass/rush splits | Codex | chosen | The corrected invariant is yards and TD conservation, not fantasy-point conservation; team-level conservation keeps the first market-anchor slice reviewable while preserving downstream pass/rush/receiving split inputs for C3/C5. | `[DATA]` Fit historical points-to-yards/TD coefficients and split pass/rush/receiving rates before any public projection weighting. |
| Keep BT as a cleared-feature boosted-stump Tweedie scaffold | Codex | chosen | It gives a deterministic, reviewable Tweedie-family shadow baseline plus ACI and Clark-West hooks without introducing opaque model training dependencies or live projection effects. | `[DATA]/[OWNER]` Supply real historical feature rows and approve any promotion only after purged/embargoed out-of-sample evidence beats market-only and equal-weight baselines. |
| Use sequential Hedge weights for B4 | Codex | chosen | Updating weights only after each sample settles preserves out-of-sample discipline while bounded loss prevents one spike week from overwhelming the ensemble. | `[DATA]/[OWNER]` Ratify any promotion only after real walk-forward samples beat equal-weight and market-only baselines out of sample. |
| Use rolling Mondrian ACI windows for B5 | Codex | chosen | Disjoint fit, calibration, and test weeks preserve out-of-sample discipline while position-level alpha updates allow worse-calibrated positions to carry wider intervals. | `[DATA]/[OWNER]` Ratify real calibration data and public interval publication before any `canPublishProjections` or provider consumption. |
| Define B6 self-publishing criteria as draft artifact data | Codex | chosen | The artifact can show pre-game commit proof, position MAE, interval coverage, rank correlation, and Brier/log-loss/CRPS versus market without flipping any projection or public-performance gate. | `[DATA]/[OWNER]` Supply real settled projection rows and explicitly approve any future `canPublishProjections` or public artifact publication. |
| Keep C6 copula coefficients fixed and shadow-only | Codex | chosen | The first Gaussian-copula slice proves consumption by best-ball and Parlay MRI while avoiding learned coefficients, pricing changes, or betting recommendations without out-of-sample evidence. | `[DATA]/[OWNER]` Learn and approve sport/market-specific correlations before any priced use. |
| Keep C1 regression/breakout as a receiving-opportunity readout | Codex | chosen | xCatch/xTD deltas explain process pressure behind buy-low/sell-high signals without changing projections, pricing, or pick generation. | `[DATA]/[OWNER]` Learn process coefficients and approve any projection/public consumption only after out-of-sample validation. |
| Keep C2 role migration prior-smoothed and contextual | Codex | chosen | The Markov role-state matrix and vacated-touch redistribution explain where opportunity can flow, but sparse transitions are shrunk and not used as projection weights. | `[DATA]/[OWNER]` Learn transition priors from full historical data and approve any projection/public consumption only after out-of-sample validation. |
| Keep C3 game-script as a shadow market-derived scaffold | Codex | chosen | Vegas win-probability path and pass/run/pace mappings explain game environment without becoming projection weights. | `[DATA]/[OWNER]` Learn coefficients and approve any projection/public consumption only after out-of-sample validation. |
| Keep C4 availability and role-tenure coefficients shadow-only | Codex | chosen | Kaplan-Meier return curves, Cox-style hazard multipliers, and role half-life readouts expose availability uncertainty without wiring live injury feeds or projection weights. | `[DATA]/[OWNER]` Learn hazard/snap coefficients from historical availability data and approve any projection/public consumption only after out-of-sample validation. |
| Keep C5 divergence routes as shadow queues | Codex | chosen | Standardized divergence lets market-anchor, regression, role, script, and availability signals compound into one spine while preventing accidental live bets, public content, or projection changes. | `[DATA]/[OWNER]` Learn source weights and approve any betting, public content, or projection consumption only after out-of-sample validation. |
| Keep D1 prop-anchor triangulation as a third-market residual readout | Codex | chosen | Player props can challenge B3 anchors in yards, touchdowns, and derived fantasy points, but posted lines are medians unless a fair value is supplied and no live prop source is wired. | `[DATA]/[OWNER]/[INFRA]` Approve prop-source rights, fair-value estimation, learned thresholds, and any priced/public consumption only after out-of-sample validation. |
| Keep D2 options-style distributions as shadow draft readouts | Codex | chosen | Floor, ceiling, spike probability, bust risk, and convexity make best-ball risk visible while conformal/posterior inputs remain evidence fields, not live projection gates. | `[DATA]/[OWNER]` Wire real posterior/conformal feeds, learn thresholds, and approve any public/priced display only after validation. |
| Keep D3 model-parliament feed flagged off | Codex | chosen | A CRPS leaderboard can make internal model quality auditable, but only pre-game-committed settled rows count and the public feed remains disabled by code. | `[DATA]/[OWNER]/[INFRA]` Approve model-output registry, minimum samples, public feed enablement, and any model promotion. |
| Keep D4 replayable-provenance endpoint flagged off | Codex | chosen | SHA-256 hash-chain replay can re-derive calibration from settled-pick events and detect tampering, but the public endpoint returns a draft-only flagged response until source persistence and owner/data gates are approved. | `[DATA]/[OWNER]/[INFRA]` Approve durable hash-chain persistence, public endpoint enablement, and any public calibration claim. |
| Keep D5 community calibration tournament review-only | Codex | chosen | Pre-lock community forecasts can be scored with Brier/log-loss and calibration harness outputs, but identity, abuse controls, recognition, and public display are not approved. | `[OWNER]/[INFRA]/[DATA]` Approve participant identity, durable storage, moderation/abuse controls, recognition rules, and any public leaderboard. |
| Keep D6 active-learning uncertainty map as a review queue | Codex | chosen | Worst-calibrated and widest-interval segments can focus data/model review, but they must not automatically retrain, alter intervals, or change projection weights. | `[DATA]/[OWNER]/[INFRA]` Approve durable segment store, review ownership, learned thresholds, and any retraining or promotion workflow. |
| Keep E2 reliability reporting gated by public calibration data | Codex | chosen | Brier, ECE, max-gap, and reliability-diagram rows can render in the observatory only from `loadPublicCalibrationReport()` and must show collecting states instead of sample numbers. | `[DATA]/[OWNER]` Approve public calibration gate enablement, minimum sample thresholds, and any performance/publishing claim. |
| Keep E3 board trace and degradation metadata observational | Codex | chosen | Per-request trace IDs, `degradations[]`, and Board-health status make suppressed/stale/DB/empty states visible without changing pick generation, routing, publishing, or model gates. | `[INFRA]/[OWNER]` Approve durable trace storage, alerting destinations, SLA thresholds, and any external status claims. |

## Standing Human Gates

- `[OWNER]` Stripe price creation and live money actions.
- `[OWNER]/[DATA]` `PROJECTIONS_PROVIDER`, `canPublishProjections`, `PERFORMANCE_STATS_ENABLED`, `PUBLIC_PICKS_ENABLED`, `OUTCOME_LEARNING_ENABLED`, and `CALIBRATION_ADJUSTMENTS_ENABLED` flips.
- `[DATA]` Any `MODEL_VERSION` bump or `IMPLEMENTED` calibration proposal.
- `[INFRA]` R2/DuckDB/Oracle/prod DB provisioning.
- `[SCHEMA]` Applying migrations to any shared or production database.
