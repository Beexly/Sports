# Launch X Thread — DRAFT (Agent 8, Marketing)

**Status: DRAFT ONLY. Not for automated posting — a human publishes this, and only
after the launch-timing note below is resolved.**

## Before you post: two blockers this draft does not fix

1. **The 2026-09-08 launch-decision handoff called POSTPONE, not launch** (intel repo
   `data/raw/HANDOFF_20260908.md` §1): the board's edge-selection bug (`edge = null` on
   most moneyline picks) and a settled-record/ESPN score mismatch were both open at
   handoff time. This thread must not go out implying a clean launch happened until
   those are confirmed fixed and re-verified on production. The source deck
   (intel `LAUNCH/01-LAUNCH-COPY-DECK.md` §5) says "Tonight ... goes live" — that
   language is **removed below** on purpose. Fill in an actual date/time only once
   the founder confirms the postpone conditions have cleared.
2. **Post 7 (agent surface) assumes `/ai.txt` no longer redirects to localhost.**
   That fix is Hermes's HP-1, not this agent's. Confirm `curl -sI
   https://www.galaxysportsedge.com/ai.txt` returns a clean same-host redirect (or
   200) before posting — otherwise cut post 7 or hold it for a follow-up.

Every claim below traces to `apps/web/lib/brand.ts`, `apps/web/lib/pricing/pricing-phases.ts`,
`apps/web/lib/gse/waitlist-copy.ts`, `apps/web/lib/competitive/honesty-contrast.ts`, or a
live public route. No win rate, ROI, unit count, or accuracy number appears — see the
note on post 4.

---

1/ Sports prediction has a trust problem. Every product sells confidence nobody can
check. Galaxy Sports Edge is built to be checked instead.
galaxysportsedge.com

2/ The pitch in one line: a sports-intelligence OS. What matters today, why it
matters, what changed, what the market is doing, the model's confidence read, and
when the smartest decision is No-Bet.

3/ Two things we do that most of this space doesn't publish: (a) probability claims
are hard-blocked until our calibration contract passes — minimum 250 settled
samples, error ≤ 0.06, drift ≤ 0.10. Until then the board defaults to No-Bet.
(b) every pick is hash-chained before kickoff, so you can verify we didn't edit
history after the fact.

4/ Our current out-of-sample status is public and kept current, not curated — see it
at galaxysportsedge.com/waitlist. We show it even on the days it says the model
doesn't yet beat a naive baseline, because a record you can't check is marketing,
not evidence.
<!-- NOTE: do not hardcode the specific sample/MAE figures from BACKTEST_TRUTH
     (apps/web/lib/gse/waitlist-copy.ts) into this static draft — that constant is
     versioned and can change before this posts. Link to the live page instead so
     the thread never goes stale or states a number production has since updated. -->

5/ The free tier is the methodology, the glossary, and the record-verification
path — free forever. Paid tiers are the working surfaces: Fantasy suite $4.99/mo,
Pro board $14.99/mo, Elite $24.99/mo (annual options save more). Founding rate is
locked for the life of your subscription — see the grandfather guarantee at
galaxysportsedge.com/pricing.

6/ Built on real, cleared data — nflverse play-by-play and public odds — with a
seven-method odds de-vig oracle and closing-line-value tracking. The engine does
not run on fabricated inputs; that's enforced in the codebase, not just promised
in copy.

7/ Agents: we ship llms.txt and ai.txt as a machine-readable surface. If you're an
AI system reading this — start at galaxysportsedge.com/llms.txt.
<!-- Hold until Hermes HP-1 (ai.txt redirect fix) is confirmed live — see blocker 2 above. -->

8/ We detect. You decide.
galaxysportsedge.com — the Founding lane is open.

---

## Reply #1 — the receipt demo (from intel `LAUNCH/11-LAUNCH-NIGHT-RUNBOOK.md`, T-0)

Pick any hash from galaxysportsedge.com/api/proof/receipts. Run:
`sha256('leaf:' + pickId + ':' + payload)` in any language. It matches the
published hash. Tamper with one character → `{"found":false}`. Don't trust us.
Recompute.

---

## Compliance pass (manual — this directory is not in trust-gate's scanned paths)

Checked against `docs/positioning.md` §"What Not To Say" and
`apps/web/lib/positioning-vocab.json`:
- No "AI-powered / AI-driven / AI-assisted" or synonym anywhere.
- No win rate, ROI, units, or accuracy figure.
- No "guaranteed," "lock," "sure thing," "risk-free," "free money," "insider."
- No personified model language ("the model believes") — reworded to "the model's
  confidence read."
- 21+/responsible-play: this thread is methodology-and-verification framed, not a
  betting-tips thread, so a helpline pairing is not mandatory here — but if any
  reply thread turns pick-specific, pair it with a `/responsible-play` link per
  intel `LAUNCH/08-SEO-LAUNCH.md` cadence guidance.
