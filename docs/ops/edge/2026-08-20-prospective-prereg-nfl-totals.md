# Prospective pre-registration DRAFT — NFL totals

Status: **NOT RECORDED / not fired.** Not a live track. Not a public claim.

This is a draft so that when CLOSE-labeled NFL totals exist, the rules are
frozen before anyone looks at a capital path.

## Frozen e-process (same form as H-F5)

Side-adaptive asymmetric fractional, λ = 0.3:

E_t = 1 + 0.3 * (W_t * (q_bet / m_bet) + (1 - W_t) * (1 - q_bet) - 1)

Miss term is (1 - q_bet), never (1-q)/(1-m).

Side rule: q_t > m_t → OVER; else UNDER (ties UNDER).
One bet per game. Entry window 6–3h. m = Shin-devigged median at entry
from ≥3 books, quote age ≤ 15 min. Exclusions counted.

Checkpoints every 50 graded starting at n=50.
Certify: E ≥ 20 at a scheduled checkpoint.
Kill: E ≤ 0.10 at any checkpoint.
Early abort: capital < 0.01 after 50 graded.

## Model subset that actually exists today

Do not promise player-tracking, next-gen stats, or order flow.
Until schema has a starting-QB id that is causal as-of, the NFL cycle uses
team-level log totals (or the frozen R-9 NB-RBPF synthetic engine promoted
only after a NEW hash on forward data). Pitcher-equivalents that cannot be
joined are out.

## Archive gate

NFL CLOSE count at draft time: 0
A real-data cycle may not start until CLOSE-labeled 6–3h NFL totals games
≥ 50. Today this draft is not armed.

## Founder signature

[ ] FOUNDER_YES — not signed.
