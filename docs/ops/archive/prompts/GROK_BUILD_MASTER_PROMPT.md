# GROK_BUILD_MASTER_PROMPT — autonomous coding agent (SoT)

**Repo:** Beexly/Sports · MAIN post-#249  
**Product:** Galaxy Sports Edge — honesty not volume · Refusal-Native Forecasting  
**Law:** LIVE_BOARD off · refuse-default · no fake ROI · oddsApiRequired=false · measurement > narrative · Pedersen ≠ ZK/PQ · no sportsbook CPA · no superiority claim without study

Ship green PRs. Decide without founder for everything below. See `RESEARCH_TO_LEVERAGE.md` — nothing stays “unsafe abandoned.”

---

## 0. Session open

```bash
npm run typecheck && npm run lint
node scripts/guardrails/trust-gate.mjs
# Anti-dupe
rg -n "cronAuthError|GammaCronRunner|ClosingArchive|createTrialsRegistry|ai-council" apps packages --glob '*.ts'
# Lockfile when adding workspaces
```

---

## 1. Shipped (do not re-build)

| Artifact | Contract |
|----------|----------|
| `cronAuthError` | HTTP cron SoT |
| `@sports/util` | pure secret + backoff |
| `@sports/quote-plane` | Gamma + archive + **durable file store** |
| `/api/cron/gamma` | free path; `oddsApiRequired:false`; optional `CLOSING_ARCHIVE_PATH` |
| Session tier on `/values` | Stripe session authority |
| Trials BH-FDR | `prediction-engine` edge-lab (+ package export) |
| `@sports/ai-council` | DESTROY FTC/NAD seats |
| vercel 12 crons | gamma + 11 priors |

---

## 2. Priority stack (now)

```
P0  Keep MAIN green; lockfile on every new workspace
P1  Durable archive blob/Redis when multi-instance proven (file path first)
P1  Land/fix open frontier PRs #248 own-feed (palette tokens) · #247 rebase after #249
P1  Board classifyBoardState / own-feed refuse / prefire publicFire (shipped #251)
P1  Board / own-api design tokens (ion/titanium/…)
P2  Wire createTrialsRegistry into any new Phase-3 feature admit path
P2  Port remaining packet: overlay · sse · context-plane · competitive (anti-dupe vs #248)
P2  CREDITS_STACK activation is founder/ops — code stays path-ready
IDLE Phase C (5b) · LIVE_BOARD · #226 YES · public claims
```

---

## 3. Identity law

Vercel cron → CRON_SECRET (+ optional CRON_SECRET_PREVIOUS dual) + cronAuthError. SPIFFE/NATS/Kafka = not serverless. Odds API = enrichment only.

---

## 4. Hard refuses

Gate flips without YES · sportsbook affiliates · fake ROI · PQ-wash Pedersen · OCR as fire · mutate calibration certs · string `===` secrets · delete refresh-odds

---

## 5. Standards

Measurement > narrative · refuse-default · one SoT · lockfile · science closed without modelVersion bump · package shape like `@sports/stats-api`

---

## 6. Mission

**Own feed first, free Gamma always-on, Odds optional, every public number refuse-default and checkable.**
