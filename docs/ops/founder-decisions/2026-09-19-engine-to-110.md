# FOUNDER DECISIONS — 2026-09-19 (engine to 110%)

Three decisions unlock everything measured this week. Each is stated with its
evidence and the exact action. Nothing here can be pulled by an agent: every
one is yours by law (MODEL_VERSION, gates, operator runs).

---

## 1. THE OOM FIRE (fixed, needs your merge — PR #868)

The truth surface, the calibration cron, and the autonomy cycle were
OOM-killed in production. The external watchdog has not seen green since
2026-09-13 16:53 UTC. Root cause: the odds-table calibration loader fetched
every H2H row ever stored for every candidate game — measured 1,641,812 rows
against a 7.7M-row table, every call.

Fixed in PR #868 (two-phase bounded fetch, bit-exact pre-filter, 48h floor).
Verified locally: 22+22+6+23+17+6+18 tests, typecheck 0, guardrails 26/26.
**ACTION: merge #868.** Watchdog goes green on its next run; the calibration
streak restarts on `market_anchored_v5` (the floor strands only the ~1% of
picks that were being priced from 12-100-day-old quotes — measured p99 gap
288.8h).

## 2. EPA PATH ACTIVATION (PR #867 now unblocked)

The trust-gate blocker on #867 was AGENTS.md quoting banned tokens (the
scanners read AGENTS.md too). Three phrases reworded, pushed — CI re-runs.

**ACTION: merge #867 after CI green.**

### 2a. The one operator run that matters most (only you can run it)

The EPA source is wired and self-activates Week 5, but ZERO settled rows carry
it because `forceReprice` has never run. One run backfills 2025-settled
history with `nfl_epa_adj` so the calibration heads can actually train on it:

    # dry run first
    curl -H "Authorization: Bearer $CRON_SECRET" \
      "https://www.galaxysportsedge.com/api/cron/backfill-independent-trueprob?force=1&dryRun=1&limit=5"

    # then the real run
    curl -H "Authorization: Bearer $CRON_SECRET" \
      "https://www.galaxysportsedge.com/api/cron/backfill-independent-trueprob?force=1&limit=500"

### 2b. W5 CONSTANTS — the hazard that fires 2026-10-08

Measured out-of-sample on 2025 (fit W1-8, test W9-18, n=151): the fitted
conversion is **~45.4 points per EPA/play net diff** with **~2.1 points
effective home field**. The repo constants the EPA path will use at
self-activation are **scale 0.12** (≈12 points per 1.0 EPA) and **HFA 0.025
EPA (~1.1 points)** — the EPA fair values would open Week 5 with the margin
scale roughly 3.8x too small and home field half-strength.

**DECISION (yours):** approve a scoring change that sets the EPA path's
constants to the fitted values (45.4 points per EPA net diff, 2.1 pts HFA),
which requires the next **MODEL_VERSION bump and a calibration pass** per the
freeze doctrine. Say "approved" and a worker lands the diff with tests on
#867 so it ships before 10-08. Do nothing and Week 5 EPA outputs are
on by construction.

## 3. CLV GATE — the definition question is now answered (code, not opinion)

`evaluateRevenueLadder` compares `beatCloseCount / gradedSampleSize` against
0.524. `gradedSampleSize` INCLUDES `matched_close` rows (686 of 1,587 graded).
0.524 is the break-even win rate at -110 — a DECIDED-GAMES quantity. So the
gate as written demands 52.4% over a denominator carrying ~43% pushes:

- as written (beat / all graded): **23.2%** [0.212-0.253] vs 52.4
- like-for-like (beat / decided): **40.8%** [0.377-0.441] vs 52.4

**DECISION (yours):** keep the floor as written (23.2% is the honest number
under that definition) or redefine the ESTABLISHED milestone as
beat/(beat+lost) — which is what 52.4% actually means. Either is defensible;
the one thing that was not defensible was not knowing which quantity the gate
read. Do not pick the flattering one; pick the one you mean.

## 4. GATES INVENTORY (state of every switch, verified 2026-09-19)

| Gate / capability | State |
|---|---|
| PERFORMANCE_STATS_ENABLED | ON (your flip) |
| PRICING_PHASE | PROVEN |
| LINE_ARCHIVE_ENABLED | ON — writing ~146K rows/day, healthy |
| EVENT_ODDS_INGEST_ENABLED | ON — **verified landing player props** (pass_tds, receptions; hundreds of players, fresh today) |
| EPA path | Wired; self-activates W5; needs backfill (2a) + constants (2b) to be GOOD |
| Conviction gate `requireEvidence` | OFF (founder-only). With only book-agreement live today, flipping it now changes nothing — revisit when beat/prop signals have real sources |
| Props hierarchical-Bayes engine | Code live, ingest live — first backtest vs 2025 closing props is the next build |
| Ranking orderings (RANKING_ORDERING_SWITCH) | Comparators landed; measurement pre-registered, NOT RUN (needs settled-row pull; next session) |
| Line-archive freshness monitor | TESTED AND UNWIRED (one call-site + field on public-surface-truth = founder's call, it changes a production API response) |

## 5. MOVE-37 (the invention lane)

W5-W8 lab duels running today with pre-registered kill lines (W5<0.02, W6<
0.01, W7<0.02, W8<0.02 R² vs named dumb baselines, season-split test set).
Results land in C:/workspace/gse-lab-20260919/move37_wfamily_results.md.
Doctrine holds: kill lines are binding, no rescue, most families expected to
die — the survivors are the machine-discovered structures nobody priced.
