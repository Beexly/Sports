# Agent ledger

One row per unit of work. **Every agent working on this repo reads this file first,
claims its row, and writes its evidence when done.**

**Also read the live truth surface before acting:**
`https://www.galaxysportsedge.com/api/ops/public-surface-truth` — deployment SHA,
gates, credit stack, Jynx lane plan, calibration. On 2026-08-19 it disproved a
"leaked Anthropic key" alarm (the value was Vercel placeholder text; the real
value is `auto`) and would have prevented a week-old open PR (#369) from being
rebuilt twice by different agents. Dashboards and prose reports drift; this
endpoint and this ledger are the two ground truths.

Validated by `scripts/ops/check-agent-ledger.mjs`, which runs in CI via
`apps/web/__tests__/agent-ledger.test.ts`. A malformed or dishonest row fails the
build.

## Why this exists

Four agents work on `Beexly/Sports` — a local Hermes runner, GitHub Copilot, a
browser agent, and Claude Code sessions. They share no memory. On 2026-08-19 that
cost three separate collisions in one day:

- Copilot and Hermes each built the same fabricated `signup-workflow` feature,
  independently, neither knowing the other had.
- A Claude session began reimplementing `H-L` while it was assigned to Hermes.
- Hermes read "run the queue" as a CSV import queue and processed a census file
  instead of the task queue.

Git is the only substrate all four agents actually touch — none of them can reach a
Postgres or Redis memory store. So the ledger is a file, and the enforcement is a
guard, not a convention.

Related prior art, and why it is not used: `Beexly/fablechain` carries an
`AgentMemory` (Postgres + Redis) and a `TaskBacklog`. Over its last 200 commits the
agent implemented `feat(faucet): implement testnet faucet backend` **nine times** with
an identical subject line, because `TaskBacklog.ts` is a `const` array and completing
a task never removes it. Persistent memory alone does not prevent duplicate work.
Evidence does.

## Rules

1. **Claim before you start.** Set `Owner` and `Status: CLAIMED` in the same commit
   that begins the work. An unclaimed row is fair game; a claimed row is not yours.
2. **Never edit a row you do not own**, except to add a `BLOCKED` note explaining a
   dependency.
3. **`DONE` requires evidence.** Put a commit SHA (7+ hex chars) or a `#123` PR
   reference in `Evidence`. The guard resolves SHAs against the repo — a hash that
   does not exist fails the build. "I completed it" is not evidence.
4. **If you cannot push, you are `UNPUSHED`, not `DONE`.** Record the local branch
   and SHA so the work can be recovered. Eight rows sit in this state right now;
   every one of them exists on exactly one laptop.
5. **`CANCELLED` requires a reason** in `Evidence`, not a hash.
6. **Titles must be unique.** Two rows describing the same work is the exact failure
   this file exists to catch, so the guard rejects it.
7. **One row, one unit of work.** If scope grows, add a row; do not widen a claimed one.

## Owners

| Owner | What it is |
|---|---|
| `hermes` | Local Hermes runner (OpenRouter free models), on the founder's machine |
| `copilot` | GitHub Copilot coding agent, runs on a GitHub runner |
| `browser` | Browser agent with dashboard access (Vercel, Anthropic console) |
| `claude` | Claude Code session (remote container) |
| `founder` | Garrett — owner-gated decisions only |
| `—` | Unclaimed |

## Statuses

`OPEN` · `CLAIMED` · `BLOCKED` · `UNPUSHED` · `DONE` · `CANCELLED`

`UNPUSHED` is the honest state for work that is finished but lives only on one
machine. Hermes is instructed never to push, so its deliverables sit on a local
branch whose SHAs no other clone can resolve. That is not `DONE`: one laptop is
the only copy, and nobody else can verify or build on it.

## Ledger

<!-- LEDGER:BEGIN -->

| ID | Title | Owner | Status | Evidence |
|---|---|---|---|---|
| H-A | Isotonic PAV calibration | hermes | DONE | 62e32730 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-B | Cron matrix generator | hermes | DONE | 4b961782 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-C | E-process sequential test | hermes | DONE | f53b229e on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-D | markClosingSnapshots behind a flag | hermes | DONE | 0a447f98 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-E | CLV census on a Neon branch | hermes | UNPUSHED | output CSV on founder machine: docs/ops/calibration/2026-08-18-clv-census.csv, 1161 rows; Neon branch since deleted |
| H-F | DOC_DRIFT.md audit | hermes | DONE | e668c4c4 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-G | Suppression curve vs random and oracle baselines | hermes | DONE | 63e84c32 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-K | charge.refunded revocation behind REFUND_REVOKES_ACCESS | claude | DONE | e69aa93c on origin/hk/refund-revocation — REIMPLEMENTED by fleet after Hermes confirmed "never implemented". 19 new tests, 72/72, default-OFF structural. Adversarial review confirmed 1 major (active-subscription refund lock-in) + hardening list; revision is C-11 before any merge |
| H-L | health-alert stateless cooldown + portable payload | claude | DONE | b9ec799 |
| H-M | Cron no-op audit | hermes | DONE | cd5086f7 on origin/hermes/h-m-cron-audit; data-driven early-return proposal only, ~60/day off-season, 576/day figure withdrawn |
| H-N | Env-shape validator | hermes | CLAIMED | — |
| H-O | Repair the @/lib/stripe test mock | claude | DONE | ab9f9c2; superseded by #369 single-seam version — the fix had been sitting mergeable in #369 for 12 days and was rebuilt twice for lack of a ledger |
| H-P | Triage the 73 CI test failures | claude | DONE | #434: every cluster root-caused; fixes shipped for checkout, copy, nflverse, waitlist, kill-switches, PAVA |
| C-5 | PAVA forward-violation bug in calibration core | claude | DONE | 9627379 |
| H-Q | Fix remaining singleton test failures (triage notes in v7 handoff) | hermes | DONE | 14b74b5c, merged via 3e997adf; full apps/web suite ZERO failures locally for the first time this session |
| F-4 | Authorize one-time Hermes push of its local sprint branch (8 UNPUSHED deliverables, single-copy risk) | founder | DONE | 63e84c32 (sprint-backup tip) + cd5086f7 (h-m tip) both on origin, independently verified; grant spent |
| C-1 | vercel.json guard drift + drift test | claude | DONE | 657a7f1 |
| C-2 | Land v5.2.6 calibration evidence | claude | DONE | 175c44f |
| C-3 | Declare appliedPauseGroups + RUN_GENERATE_SIGNAL_SLATE | claude | DONE | 1d39021 |
| C-4 | Agent ledger + guard | claude | DONE | 65e6474 |
| X-1 | signup-workflow scaffolding | copilot | CANCELLED | fabricated feature; auth is Google OAuth only via PrismaAdapter, no signup flow exists |
| X-2 | Delete CLAUDE_PROVIDER to fix AI routing | browser | CANCELLED | doubly wrong. (1) Original diagnosis false: value is "auto", not a key. (2) Deletion is NOT inert in production: with clouds configured and no JYNX_MODE, empty -> mode "anthropic" -> zero cloud attempts -> CASH Anthropic, dropping the azure/vertex credit lanes that "auto" selects. provider-mode-failsafe.test.ts covers UNRECOGNISED values only — citing it for deletion was a category error (fable). Prerequisite for any future deletion: set JYNX_MODE=auto first |
| F-1 | Rotate the Anthropic key found in CLAUDE_PROVIDER | browser | CANCELLED | false premise: live truth surface returns claudeProvider "auto"; the sk-ant-api03-aB text was a Vercel placeholder misread, then propagated by the orchestrator. No Anthropic key was ever exposed there. Browser agent correctly refused the rotation script |
| F-2 | Decide REFUND_REVOKES_ACCESS default | founder | OPEN | decision made (fable): stays OFF through H-K merge; flip to true after ONE observed production refund revokes correctly in logs; the env flip itself is a hands task |
| F-3 | Promote or remove apps/web/app/api/v1 | claude | DONE | 149e469f — RULING (delegated authority): PROMOTE. The tree is not a shadow — signals/probabilities/openapi are functioning, key-authed, rate-limited, RED-honest B2B routes; deleting working revenue surface mid-launch is malpractice, and permanent-red trains everyone to ignore CI. Promotion executes the machinery's OWN ceremony (promotion-readiness.ts gates: owner-approval-recorded via founder delegation, rollback evidence = git revert of the route commits, raw-key-absence proof), never bulldozes it. Implementation is C-8 |
| C-8 | Execute the api-v1 promotion ceremony: record doc, guard route-tree check to exact-3-route allowlist (new routes still fail), promotion-readiness gate + ~13 tests aligned to post-promotion model — own PR, small diff | claude | DONE | 149e469f; 69/69 across ten api-v1 suites, full suite 41 to 11 failures, guard negative-tested with a rogue route, no assertion weakened |
| C-7 | Triage the residual failure set: cockpit-jarvis-trend-api, cockpit-picks-glance, honest-degraded-states, picks-daily-limit-meta | claude | DONE | 41a971a0 — four stale assertions, zero real defects: a regex that could not cross a quote, a premium constant misread as a paywall regression, a query shape replaced by a clearer derivation, and honest copy improved past its pinned wording. 36/36. NOTE: the original "CI-only" framing was wrong — these fail locally too; gse-waitlist/backfill-independent-trueprob/espn-odds-client are genuinely CI-only and remain unowned (see C-9) |
| C-9 | Genuinely CI-only failures (pass locally, fail in CI): gse-waitlist 6, backfill-independent-trueprob 2, espn-odds-client 1 — almost certainly env divergence between the CI runner and dev containers | claude | DONE | e2c3c9f7 + 2f229d3b — the "env divergence" framing was wrong on all three. backfill: test pinned the pre-RES-lift plain average; the sharpness-weighted blend is deliberate; suite lives in packages/ingestion-pipeline which apps/web-scoped local runs never executed. gse-waitlist: CI uses the durable Postgres limiter which resetRateLimits() cannot clear; 11 POSTs shared one "anon" key at 5/60s, flapping on the window boundary — each POST now has its own client IP. espn-odds-client: fixture commence date 2026-08-10 aged out of the client's -6h..+21d window on Aug 11 — deterministically red since; fixture date now dynamic. CI-green confirmation pending the run on 2f229d3b |
| C-6 | Drive PR #369 to green and merge (invoice.paid alias, auth-aware smoke, single-seam checkout mock) | claude | DONE | #369 |
| R-1 | Rotate the ~25 Hermes .env credentials exposed in a session transcript (Fireworks, Together, DeepSeek, OpenRouter x2, Vercel first); delete the browser agent's own mistyped 401 gateway key | browser | OPEN | APPROVED (fable) 2026-08-19; these are the real exposure, not the phantom sk-ant key |
| R-2 | Set JYNX_MODE=auto in Production+Preview (belt-and-braces so a future CLAUDE_PROVIDER deletion is genuinely inert) | browser | OPEN | APPROVED (fable) 2026-08-19 |
| R-3 | Redeploy and confirm contentPlanPrimary flips off cerebras_free to the OpenRouter secondary (CEREBRAS_API_KEY removed; live surface still shows the old plan) | browser | OPEN | ELEVATED 2026-08-19: this redeploy ALSO activates THE_ODDS_API_KEY (truth surface shows oddsKeyPresent false — env saved after the b71f7e2 build) — NFL odds path for preseason; do it first |
| R-4 | Verify TheRundown 429s stop after THE_ODDS_API_KEY activates (dashboard request log clean for 1h); if 429s persist, report cadence for review — do not change any cron | browser | OPEN | 429 observed on /api/v2/sports/11/events/2026-08-20 at 05:22 UTC while ingestion still succeeded same minute (partial throttle, not blackout) |
| S-1 | Evaluate TheSportsDB as a free schedules/results redundancy source (v1 key 123, 30 req/min; NOT an odds source) — must pass source-rights registry classification before any adapter work | — | OPEN | founder surfaced docs 2026-08-19; folds into H-S |
| H-S | Free sports-data provider map (L-5 in the v8 queue): one doc, fixed schema, every candidate classified — odds sources separated HARD from schedules/results/stats sources, free-tier limits verified with at most 2 live calls each using documented public keys only, source-rights classification per CLAUDE.md for each. No signups, no credential creation, no adapters — map first | hermes | DONE | fc4587114 — docs/ops/data-sources/FREE-PROVIDER-MAP-2026-09-03.md, 8 providers classified (7 real + OrcaSports confirmed vaporware), all with tier/limits/source-rights per CLAUDE.md taxonomy |

| C-10 | Guard hardening from fleet review: ledger guard fetch-by-sha in shallow CI (fabricated SHAs currently pass), api-v1 route walker must not honor SKIP_DIRS and must flag symlinks, promoted-routes listFiles must not throw on dangling symlinks — each fix carries the reviewer reproduction as a regression test | claude | DONE | b7471fc1 (fix/ledger-shallow) + 0451eb97 (fix/apiv1-walker), both merged. Ledger guard: validate() now fetches unresolved SHAs from origin (--depth=1) before failing, so a fabricated DONE SHA can no longer pass in a shallow CI clone; "unverified" now survives ONLY for a repo with no origin remote at all (strictly narrower than before). Verified end-to-end against a genuinely shallow repo: real ledger exits 0, scratch ledger citing deadbeefcafe exits 1 via an actually-refused fetch. api-v1 guard: dedicated walkRouteTree replaces the shared walker under the promoted tree — descends EVERY directory (SKIP_DIRS no longer hides app/api/v1/build/route.ts) and flags any symlink or non-regular dirent as its own violation id; promoted-routes listFiles no longer statSyncs, so dangling symlinks return a verdict instead of crashing 10 suites. PROMOTED_ROUTES literal unchanged, walkFiles untouched for all other scans — strictly more detection, nothing relaxed. Re-attack agent confirms all 5 exploits closed with live reproductions. 123/123 across the three affected suites, typecheck 0 |
| C-11 | H-K revision per attackers: never revoke while Stripe subscription still active (retrieve + status check, log for human), log-only mode makes ZERO Stripe API calls, transient retrieve failure in enforce mode 500s (structural unresolvability still 200s), status guard on revoke write | claude | DONE | 53200c5c (fix/hk-revision) on base e69aa93c, merged — brings H-K itself plus all four attacker findings fixed. (1) MAJOR lock-in closed: ENFORCE mode retrieves the resolved subscription and revokes ONLY on Stripe status canceled/incomplete_expired; any live status (active/trialing/past_due/unpaid/paused/incomplete) mutates nothing and escalates via console.error naming charge id, sub id and status, acking 200. (2) Log-only mode makes ZERO Stripe API calls. (3) Transient retrieve failures propagate to a 500 with the webhook event UNRECORDED so Stripe redelivers (test proves the second delivery revokes); structural dead-ends stay log-and-200. (4) revokeSubscriptionAccess WHERE now carries status: { not: 'CANCELED' } so a later distinct refund event cannot re-stamp canceledAt. 82/82 in stripe-webhook-route.test.ts, 200/200 across adjacent billing suites. Flag remains structurally default-OFF (=== "true"), verified post-merge — production behavior is unchanged until F-2 |
| C-12 | Edge Roadmap + gate-split map committed | claude | DONE | #435 — docs/ops/edge/2026-08-19-edge-roadmap.md + calibration-gate-split-map.md |
| F-5 | Founder reads the Edge Roadmap and picks the QUICK experiments to greenlight; enabler E1 (clean-room prod export) needs founder hands — agents cannot reach the DB | founder | OPEN | roadmap section 2-3; every experiment preregistered against CLV 52.4 percent under the e-process, kill criteria included |
| L-6 | CLV analysis on the census (the measurement that should have been L-4) | hermes | DONE | 830ff6df on origin/hermes/l6-clv-analysis, resolved 2026-08-19; 909/1161 graded pairs; headline 24.86% beat is NOT accepted as a performance number — inputs under C-14/L-9 provenance audit first |
| C-13 | Integrate origin/hermes/sprint-landing (e8286086: H-A..H-G toolkit + L-4/L-5 docs + main merged) into the designated branch, fully verified | claude | DONE | e00e526b, 373 files. Founder explicitly authorized proceeding after I read (not assumed) every sealed-path diff: CI adds one additive dependency-audit job (no existing job touched) + a secretless external-watchdog workflow polling the already-public status endpoint; new pre-push hook runs the existing secret scanner; .claude/settings.json only tightens this agent's own permissions; the Prisma migration is purely additive (1 enum, 2 new currently-unread tables, no ALTER/DROP/backfill) and its FILE landing here does not apply it to any database. Re-verified independently: typecheck 0 errors across 22 workspaces, both guards pass, full apps/web suite 11313/11314 (the 1 failure is ai-control-plane-sealing-guard.test.ts, a pre-existing environmental false positive from this session's .claude/worktrees/ convention predating that guard's SKIP_DIRS list — confirmed unrelated to merge content, would fail identically on any commit right now; real minor guard gap noted for a future authorized SKIP_DIRS fix). Two conflicts (espn-odds-client, backfill-independent-trueprob tests) resolved to already-verified C-9 content |
| C-14 | CLV-grading forensics: the L-6 magnitudes are diagnostic of a possible computation artifact (ML mean CLV -27.4pp is implausible as real market movement; spread beat-rate 10.31% with median CLV 0; inverse strategy clears 52.4% on decided picks) — audit settle-sport CLV sign conventions, ML price units, side attribution, MATCHED semantics before ANY conclusion ships | claude | DONE | docs/ops/edge/2026-08-19-clv-forensics-verdict.md. Verdict: MIXED/medium confidence. CLV math itself verified correct (sign conventions, American-odds handling); NOT de-vigged (methodology fact). Forensics found a real, ALREADY-FIXED side-derivation bug (0e56c477, 2026-07-17) matching the SPREAD/ML-low vs TOTAL-honest pattern — but adversarial refutation showed that story doesn't fit the magnitude (a rare name-collision can't crash an aggregate to 7-10%) and, critically, missed two REAL, CONFIRMED, still-UNPATCHED bugs on the exact same code path: no staleness bound on the closing snapshot (8e2af6f1 on origin/claude/hotfix-settle-refresh-races, unmerged) and truncated book coverage at close (take:80 vs needed take:240, fix at 6f0353e1 on origin/claude/galaxy-sports-edge-pdcswh, unmerged). Neither headline number (TOTAL 58.5%, ML -27.4pp) ships as a claim until these are applied and the census is re-graded |
| R-6 | Rename THE_ODDS_API_KEY_FREE to the canonical THE_ODDS_API_KEY in Vercel Production (code enumerates the canonical name; oddsMatchedEnv null proves the mismatch), verify CEREBRAS_API_KEY presence/absence, redeploy, re-read truth surface | founder | DONE | executed by founder hands 2026-08-19 ~07:39 UTC (not the browser agent), redeployed. Verified live (no-store, age:0): oddsKeyPresent=true, oddsMatchedEnv="THE_ODDS_API_KEY" — preseason NFL odds path unblocked. deployment.sha unchanged (b71f7e28) as expected: same-commit Redeploy with new env, not a new push. contentPlanPrimary still reads "cerebras_free" but this is a STALE LABEL not a bug: jynx.ts:165 names the field "cerebras_free" whenever isFreeLaneEnabled() is true via EITHER CEREBRAS_API_KEY OR the OpenRouter secondary (free-lane-policy.ts:14-21); the dispatcher itself (free-lane.ts:59-91) correctly skips Cerebras with zero wasted calls when the key is absent/falsy and falls through to the OpenRouter secondary host — money-correct, cosmetic-only. THE_ODDS_WIDGET_KEY also added (feeds C-17) |
| L-7 | CLV data-path forensics: 10-row hand spot-check, ML price-format census, spread side-vs-movement buckets, MATCHED semantics, per-book + June/July slices | claude | CLAIMED | Hermes's part done and pushed: f87146bb on origin/hermes/l7-clv-forensics but NOT YET merged into the designated branch: real conflict on 10 test files (canonical-sample-posture, cockpit-jarvis-trend-api, cockpit-picks-glance, cqr, glass-ledger-page, honest-degraded-states, picks-daily-limit-meta, player-stats-backfill-plan, refresh-player-stats-route, backfill-independent-trueprob/espn-odds-client) — this branch shares the same old pre-C-7/C-9-fix lineage as hermes/sprint-landing. Merge attempted, correctly aborted rather than hand-resolved under time pressure; likely resolves cleanly once C-13 lands sprint-landing (same root divergence). Do not rename to DONE until actually merged |
| L-8 | Commit + push the L-7 forensics artifacts with a formulas README | hermes | DONE | commit content confirmed on origin/hermes/l7-clv-forensics (f87146bb); integration into designated branch tracked under L-7 above, not blocking this row |
| L-9 | CLV verdict slices: decided-only beat rates with CIs, TOTAL deep-dive, lock provenance audit (book quote vs batch mean vs model-derived), ML monster-lock provenance, spread sign-flip classification | hermes | DONE | 93b92db1 on origin/hermes/l9-clv-slices, merged c19d2e06. 3/5 items computed: decided-only beat by market x month (TOTAL 58.5% [52.8,63.9] clears 52.4%, SPREAD/ML do not), sign-flip 57/388 SPREAD (toward 40/away 109/unchanged 239), spot-check 0/10 mismatches. **CRITICAL finding for C-14: 909/909 picks have ZERO matching odds_batch rows at clv_captured_at — every lock price appears model-derived, not a real book quote.** 2/5 items (lock provenance, ML monster-lock join) correctly left BLOCKED — no hermes_ro DB credential reachable anywhere on the Hermes machine (DATABASE_URL_UNPOOLED, local Postgres, neonctl all masked); required queries saved in clv-slices.json for a session with real DB access. Hermes correctly refused to escalate to NEON_API_TOKEN or unmask credentials per standing rule |
| L-10 | Free-provider live probes for the H-S map: 2 calls max per candidate, latency/shape/limits, rights-registry classified, no adapters no signups | hermes | DONE | 8c1c6143 on origin/hermes/l10-provider-probes, merged 5d358e90. Cleared, zero-cost, live-verified: nflverse (open-license CSV/JSON, ~280ms), Open-Meteo (open-license, ~550ms), Sleeper API (public, ~27ms). ESPN public API 403'd both calls (Akamai bot-block, was previously 200 — investigate before relying on it). TheSportsDB + MLB Stats API both respond 200 but are vendor_candidate/gated per registry — Hermes correctly did NOT mark them usable, needs registry promotion first. FFC-ADP DNS failure, likely transient |
| L-11 | Verify the DeepSeek-pasted affiliate/partnership-tooling repo list against GitHub ground truth before any adoption | hermes | DONE | 7817d56a on origin/hermes/l11-affiliate-tooling-verification, merged. 21 checked: only 3 real-and-matches (Refferq, Google Meridian, Dub); 4 do not exist at all (Income Generator Hub, SponsorFit, ClawMarketing/growth-os, xAmplify PRM, prathammahajan/affiliate-management-system — that's actually 5 names, table lists 5 under a "4" mislabel, not rechecked further); remaining ~13 real but exaggerated or mismatched vs the DeepSeek description. Confirms the original caution in docs/ops/edge/2026-08-19-affiliate-tooling-research-triage.md — treat as leads only |
| C-29 | C-15 fleet round 1 REJECTED at verification — 5 blockers, all 3 adversarial lenses refuted; implementation 9099ace2 exists in worktree wf_2645e948-ec2-5 and is NOT merged. (1) CORRECTNESS: STALE_CLOSE is unreachable in production — the SQL floor gte commenceTime-6h in closing-odds.ts:65-73 excludes stale batches BEFORE the deriver sees them, so findFirst returns null and you get NO_ODDS with closeAgeMs null; the exact measurement the preregistration depends on is systematically censored. (2) The shadow re-grade caps its window at 24h so the preregistered maxCloseAgeMs=Infinity cell is byte-identical to the 24h cell by construction — that sensitivity row is not evaluable and the closeAgeMs distribution is right-censored. (3) DOCTRINE: the commit ships a NEW public performance claim (decided-only beat-close rate + Wilson CI on the public /clv page AND unauthenticated /api/clv) gated only on "allowed", BEFORE the shadow re-grade that its OWN preregistration doc in the SAME commit says must gate it. (4) scripts/clv/shadow-regrade-census.ts has no DATABASE_URL check, no stub-mode guard and no empty-population guard — run without a live DB the stub client returns [] and it writes a complete, schema-stamped, preregistration-citing artifact full of zeros: a fabricated-evidence generator. (5) SUFFICIENCY: MIN_CLOSE_DISTINCT_BOOKS is pooled cross-market, counted at clv-capture.ts:179 before the per-market split at :203-215, so a batch with TOTALS from one book, H2H from another and SPREADS from a third passes a 3-book floor while every market has exactly one — proven by probe test | claude | OPEN | do NOT merge 9099ace2 as-is. Genuine wins to keep from round 1: both defects re-confirmed live on HEAD (clv-capture.ts:96 no staleness bound; settle-sport.ts:363 still take:80 — note the line is 363, not 321 as previously recorded), and a proven reproduction that HEAD accepts a batch captured 7 HOURS pre-kickoff as "the close" |
| C-30 | PREMISE CORRECTION to C-15/C-20 as previously written: commit 6f0353e1 does NOT contain the take:80 to take:240 change — that was authored by 8e2af6f1 (and independently by 7c1276f8); 6f0353e1 merely inherited 240 from its own ancestor. Consequence: 6f0353e1 is the tip of a 44-commit stale branch (Worldline/OTS/MCP/fantasy campaign) whose parent is NOT an ancestor of HEAD and CANNOT be taken. 8e2af6f1 by contrast is a clean single-commit candidate (parent 821d0ca3 already an ancestor, HEAD..8e2af6f1 = exactly 1 commit) | claude | DONE | c789fe40 lineage verified by the C-15 fleet. ALSO CRITICAL: cherry-picking 8e2af6f1 WHOLESALE would REGRESS HEAD — its M-F6 half conflicts because HEAD already carries an independently-derived equivalent (GSE-SEC-043, pick.updateMany scoped to result PENDING), and taking theirs would strip HEAD's newer bookDisagreementAtLock write-once capture. The landable slice is M-F7 only: clv-capture.ts + clv-capture.test.ts + settle-sport.ts take:240, 3 files, zero conflicts, typecheck 0, prediction-engine 203 files/2352 tests pass, ingestion-pipeline 17 files/197 tests pass |
| C-15 | CLV measurement integrity fix, three parts now scoped by the C-14 verdict: (1) pull in two pre-built unmerged fixes — 8e2af6f1 closing-snapshot staleness bound, 6f0353e1 take:80->take:240 book coverage; (2) lock-price provenance fix (real book quote at lock time vs model-derived, per L-9's 0/909-match finding); (3) re-grade the census under both and compare before any claim | claude | OPEN | verdict + concrete fix path landed in docs/ops/edge/2026-08-19-clv-forensics-verdict.md; this is the gate to any honest ESTABLISHED (>=52.4% CLV) claim; not dispatched tonight per cost-conscious mode, needs a real design pass |
| C-16 | Frontier research night: six verified-citation literature threads (anytime-valid/e-process + conformal e-prediction, calibration/CORP/Venn-Abers, pooling/extremization, market efficiency + CLV literature, independent models for MLB totals, Kelly/abstention decision layer) mapped onto the codebase as preregistration-compliant proposals, citation-audited and doctrine-audited, synthesized into a ranked dossier | claude | DONE | 66aee571 — docs/ops/edge/2026-08-19-research-frontier-dossier.md (72,704 chars). 11 agents, 6 literature threads, 17 engineering proposals; citation audit checked 90 citations across the surveys and 38 more against the repo — ZERO fabricated, ZERO doctrine violations, 4 proposals flagged for overstated framing only. Central correction to our own prior reading: holdout Brier 0.2556 sits ABOVE UNC 0.2499, so under the exact CORP decomposition DSC-MCB is about -0.0057 — NEGATIVE net skill versus the base rate, not merely zero resolution. Governing result: calibration-only methods (Venn-Abers/IVAP, temperature, Platt, beta, plain isotonic) are score-only monotone transforms — ranking-invariant, therefore provably CANNOT create resolution; only methods injecting new conditioning information can (IDR, multicalibration-as-boosting, an independent overdispersed totals model, decorrelation-from-market objectives). Top three extracted as C-20/C-21/C-22 |
| C-23 | Anytime-valid certification protocol v1: composite bet-specific null (H0,i: p*_i <= b_i = 1/D_i, NOT the fixed 0.524), mixture e-process over unknown edge, deterministic pre-placement inclusion rule, frozen append-only hash-chained ledger, per-model-version tracks, preregistered kill criteria | — | OPEN | Design + audit in docs/ops/edge/2026-08-19-deepseek-adversary-round2.md (7d425f47). Our current fixed-0.524 null is MISSPECIFIED and inflates certification: at -115 the true break-even is 0.5349, so a losing 0.530 reads as skill. Supermartingale property re-derived and verified. KNOWN GAP to close before build: the price-space null is E[delta] <= 0 on a CONTINUOUS quantity, and no e-process was supplied for it — needs a bounded-mean betting martingale (Waudby-Smith/Ramdas), not the binomial likelihood ratio. Existing repo crypto (proof/receipts, verification-spec, sealed-holdout, pedersen-opener) is a candidate substrate for the frozen ledger |
| C-20 | Grade TOTAL/SPREAD CLV in PRICE space, not points-only (the Bickel-Kim fix) — dossier rank 1, days of effort | — | OPEN | SCOPE EXPANDED + HARD CONSTRAINT ADDED 2026-08-19 (7d425f47): (a) capture BOTH sides' decimal odds + ms timestamp + book at BOTH lock and close; de-vig with Shin (already in repo), NOT proportional; (b) the null changes from a win-rate null to a price-space mean null E[delta]<=0, which needs the e-process gap in C-23 closed first; (c) **NEVER re-grade the historical census in place** — anytime-valid guarantees are void if E_n is recomputed over corrected history (certification probability can be driven arbitrarily high by re-grading until the path looks good). Fix forward only; corrected history becomes a separate, clearly-labelled audit stream that never feeds certification. Original dossier finding: our totals/spread CLV is computed on point-line movement alone while the actual over/under juice sits UNUSED in columns the schema already has. Bickel & Kim 2014 show MLB totals conclusions FLIP once real juice replaces an assumed price. Consequence: the 58.5% totals beat-close number is not evidence of anything, in either direction, until this lands. MUST be coordinated with C-15 (measurement integrity, fleet in flight) — same code path, and C-15's scope did not include this defect |
| L-12 | Compute the grouping-loss go/no-go gate on the 909-pick graded census | hermes | DONE | c789fe40 on origin/hermes/l12-grouping-loss, merged. Result is an HONEST BLOCKED, and the blocker is the most clarifying finding of the session: **the (score, outcome, features) triple does not exist because there IS no model probability.** outcome present (WIN/LOSS); features present but market-metadata-only (pre-lock odds, no game-state covariates — those live on Game/PickSignalSnapshot/OpeningLine and are not exported); model probability MISSING ENTIRELY. schema.prisma:602 documents PickProofReceipt.modelProb as "calibrated win prob 0-1 — null until one genuinely exists". Hermes refused to substitute confidence_pct (heuristic composite) or market_fair_prob (the MARKET's number, and populated on only ~561/1161 rows), and refused to invent numbers. Full estimator implemented and ready (equal-count bins 5/10/20, from-scratch k-means on logit-standardized features, permutation null) — it runs the moment a real model probability exists |
| C-28 | **The calibration number measures the market, not our model.** ECE 0.0044 was computed on confidence/100: compute-live-calibration-metrics.ts:119 ("p from confidence/100 provisional; internal eligibility only until publish policy"), calibration-regression-snapshot.ts:23, public-confidence.ts:51. Confidence is substantially a market-structure echo (scoring.ts:486-494), and murphy-res-definition.ts:48 ALREADY names the correct basis as "Independent modelProb / edge ranking (not confidence/100 market echo)". So the excellent calibration is largely the MARKET's calibration re-measured through our heuristic — which coherently explains the whole picture: near-perfect reliability AND near-zero resolution is exactly what you get when your score is the market. Decide the publish posture and either emit a genuine modelProb or restate the calibration claim to say precisely what it measures | — | OPEN | discovered via L-12's blocked gate 2026-08-19. IMPORTANT MITIGATION: no false claim has shipped — the code gates this as "provisional; internal eligibility only until publish policy", so the honesty machinery held. But every downstream conclusion resting on "our calibration is excellent" needs restating, and C-21/C-22/C-26 are all gated behind a real modelProb existing |
| C-21 | Grouping-loss lower bound within existing confidence buckets as the go/no-go gate before any multicalibration/IDR spend — dossier rank 2, days of effort | — | OPEN | BLOCKED IN PRACTICE by C-28: cannot compute until a genuine model probability exists (see L-12). Original scope: Answers the question the entire roadmap turns on, cheaply: is RES ~0 because the market is efficient wrt the features we have (=> stop, go find new data) or because the pipeline collapses real signal into one flat number (=> build)? Perez-Lebel/Le Morvan/Varoquaux 2023 (arXiv:2210.16315) gives a recoverable-resolution lower bound. One day of diagnostic before a week-plus model build. WARNING (7d425f47): do NOT implement the grouping-loss formula supplied by the external adversary — it gives within-bin OUTCOME variance, which for binary outcomes is ~p(1-p), near 0.25 always, and is useless. True grouping loss is the variance of the TRUE conditional probability within a score bin (expected within-bin variance of P(Y=1 given X), conditioned on the score S(X)) and requires the paper's partition/clustering lower-bound estimator over features within each bin. Its accompanying <0.01 / 0.01-0.05 / >0.05 threshold table is invented, not sourced. Citation VERIFIED 2026-08-19: ICLR 2023, arXiv:2210.16315, and the authors publish working estimator code at github.com/aperezlebel/beyond_calibration — USE THEIRS, do not reimplement from a description |
| C-22 | Independent MLB totals model on an overdispersed negative-binomial run distribution (not soccer-shaped Poisson), wired end-to-end — dossier rank 3, week+ | — | OPEN | Per the dossier the ONLY surveyed proposal that CREATES resolution rather than measuring/preserving/shrinking it, because it conditions on real game-to-game variation (park, weather, starter rest) that a collapsed confidence score structurally cannot carry — those covariates move tens of percent game to game where our score moves 0.17%. Targets the one market segment with any live signal. Gated behind C-20 and C-21. PRIOR UPDATE 2026-08-19 (downward, from verifying our own key citation): Bickel & Kim 2014 (Applied Financial Economics 24(18), doi:10.1080/09603107.2014.925052) — the very paper that justifies the price-space fix — did exactly that fix with real run-line and money-line prices and found LITTLE EVIDENCE the MLB over-under market is inefficient. Our angle (weather-park interaction, umpire zone, bullpen fatigue, and pricing the full run DISTRIBUTION incl. alternates rather than the mean) is not what they tested, so this is not fatal — but we are hunting a field a competent study already swept. Carry a lower prior into C-22 and require the prospective test, not the backtest |
| F-7 | **URGENT, FOUNDER HANDS: redeploy production.** Live deployment SHA is b71f7e28, HEAD is 112 commits ahead (both verified independently 2026-08-19). At the DEPLOYED SHA three surfaces serve premium content to ANONYMOUS visitors: /board (state.ts renders market selection and rankingP with no viewer check), /intelligence/engines (anonymous WebFetch returned full analytics tables verbatim, no gate panel), /observatory (get-slate-twin called with no entitlements, loader has zero occurrences of canSeeConfidence/canSeePremiumPicks). HEAD already gates ALL THREE (state.ts:112 GSE-SEC-026, engines page 12-13/159-178, observatory 62-63 + get-slate-twin.ts:109,214-225). So the fix is ALREADY MERGED and merely UNDEPLOYED — this is a redeploy, NOT a code change and NOT a gate/env flip | founder | DONE | Pro's entire headline value (full board, selections, confidence, model probabilities) is currently free on three public URLs, and it is a live CLAUDE.md rule 3 violation (no frontend-only paywalls). Smoke-test the money path after deploying: 112 commits land at once. docs/ops/edge/2026-08-19-gse-launch-optimization-plan.md — RESOLVED 2026-08-19: production deployed at 90aa7652 (Vercel Ready, www alias live, verified via public-surface-truth). Root causes: strict-allow-scripts npm blocker (cdd91b58) plus fsevents deny (90aa7652, Codex, founder-authorized) |
| C-31 | Ship the five verified launch fixes from the optimization fleet, in rank order: (2) free teaser returns 1 pick not 2 — api/picks/route.ts:128 applies take BEFORE the selective-publish filter at :135-142, so survivors can only be fewer than the cap; over-fetch a bounded pool then slice AFTER filter+sort, server-side only. (3) /faq:89 and :43 answer "Every pick, free" and "Free gets every pick" — FALSE (enforced limit is 2, live renders 1) and published as FAQPage JSON-LD; rewrite from the entitlement constant, add a test asserting no Free answer contains "every pick"/"all of them". (4) /pricing renders NO RiskDisclosure while 29 other page.tsx files do — add it, but do NOT pass includePastPerformance (implies a track record) and do NOT hardcode 21+ (terms:64-72 says legal age in your jurisdiction). (5) checkout hard-400s on browsers without crypto.randomUUID — subscribe-button.tsx:65-69 mints a non-UUID fallback, CLIENT_INTENT_ID_RE is UUID-only, route.ts:74-82 rejects; server already has a token-less branch at checkout-attempt.ts:417-421, so return null and omit the field. Do NOT widen the server regex. (6) daily-slate counts have NO date bound (route.ts:78-85) while /api/picks does (route.ts:103-106), so /picks shows four irreconcilable numbers on one screen; extract one shared predicate | claude | DONE | bd60fc71: all four shipped (fix 5 was C-39/0c190bfe earlier) — 69 raw findings, 10 falsified in adversarial verification, these 5 survived with file:line evidence. docs/ops/edge/2026-08-19-gse-launch-optimization-plan.md |
| C-32 | PERMANENT DO-NOT-DO list from the launch audit — proposals killed in verification, recorded so no future session re-raises them: (a) NEVER write universal "every pick is sealed with a receipt before kickoff" copy — three separate audits independently proposed it and it is FALSE as written, since process-sport.ts:846-853 mints a receipt only when marketFairProb is in (0,1) AND entryOdds is non-zero, and mint failure is non-fatal at :888-894. (b) NEVER publish a win rate, ROI, hit rate or beat-close rate — holdout Brier 0.2556 sits ABOVE base-rate uncertainty 0.2499; this includes wiring daily-slate recentRecord to real data without sample size and a Wilson band. (c) NEVER re-select which pick is free on the theory that free picks are the weak ones — the split is confidence >= PREMIUM_CONFIDENCE_THRESHOLD and with resolution near zero there is NO evidence high-confidence picks win more; that would institutionalise an unproven discrimination claim on the highest-traffic free surface | — | OPEN | keep this row permanently; it exists to stop good-sounding proposals being re-litigated |
| F-6 | FOUNDER DECISION, RESOLVED 2026-08-20: no sportsbook or DFS affiliate revenue, permanently — option (b) from the original framing, scoped precisely to the conflicted category rather than all partnerships. The competitive-intel corpus's red-team argument (handoff section 1/8, master dossier 4.5: "the moment GSE books meaningful affiliate revenue it becomes the thing it attacks") applies to sportsbook/DFS specifically, where the operator profits when the user loses money betting; it does not extend to non-gambling partner categories (creator tools, sports-data APIs, cloud/AI tools, local sponsors), which the codebase already separates by risk class (`HIGH_RISK_PARTNER_CATEGORIES = [sportsbook, dfs]` in lib/revenue/partner-types.ts) and which carry no such conflict | founder | DONE | 45d3f1f7 — pledge published on /how-we-make-money and /terms sec.7 ("no sportsbook or DFS affiliate links, permanently"); C-17 and R-7 cancelled below; general /partners disclosed-partner program (zero live partners today) explicitly continues, unaffected |
| C-26 | Audit the Kelly staking chain for market-echo contamination: kelly.ts:154-155 gates on confidence AND edgeScore — the AND with an edge gate means it does not fire on chalk (the corpus's stated fear) but it DOES discard edge-positive picks purely for low confidence, which under calibrated probabilities is where variance-adjusted value lives. More concerning, kelly.ts:181 derives a "bounded +5% edge proxy" from edgeScore and line 70 concedes the probability is an ESTIMATE derived from edgeScore — so given confidence is substantially a market-structure echo, stakes may be sized off market structure rather than independent edge | — | OPEN | verified in code 2026-08-19; doctrine per competitive-intel handoff is fire on calibrated edge e = p - q, never on confidence kappa. Gated behind C-21 (is there orthogonal information at all) — no point retuning a staking rule if there is no edge to size |
| C-27 | Quarantine the five stale files still carrying the superseded "70 percent win-rate north star" so no agent or copy generator reads them as current: gse-competitive-intel README line 18, _expansion-targets line 6, rotowire-engine-teardown line 256, and two fantasyguru docs. Superseded 2026-06-30 by "prove CLV edge, do not claim it until it clears breakeven" — but all five were written AFTER the walk-back and still carry it. Also flag _competitor-mistakes-lessons line 18 which scripts the literal marketing line "We get you a proven edge" | — | OPEN | that repo is read-only to this session (public clone, no push). Remediation is either a founder-side commit there or a local do-not-read note; the risk is real because these are exactly the files an agent asked to "write GSE marketing copy" would find first |
| C-24 | Claude Code operating-cost and reliability config: CLAUDE_CODE_SUBAGENT_MODEL for mechanical fleets, maxTurns cap on every subagent frontmatter, effort per unit, CLAUDE_CODE_RESUME_INTERRUPTED_TURN=1 + CLAUDE_ENABLE_STREAM_WATCHDOG=1 (directly fixes the 5-hour silent fleet stall we hit tonight), PermissionDenied hook returning retry:true (fixes the classifier denials that blocked two commands tonight), and an audit/prune of the ~40 always-loaded skill descriptions which are a permanent per-turn context tax | — | OPEN | sourced from /workspace/beexly/claude-code-best-practice; NOT applied mid-flight with ~15 agents running — apply when the board is quiet. NOTE: autoMode is deliberately NOT read from project .claude/settings.json (repo-injection guard), so permission fixes must land in user/local settings, which is a founder action not an agent one |
| C-25 | Ledger guard hardening round 2 — two holes our SHA rule structurally cannot catch: (1) INVERSE COMPLETENESS: we verify every claimed DONE has a resolvable SHA, but nothing verifies the reverse, that no row exists which no evidence supports; (2) NO ESCALATION SLA: an item can sit OPEN/CLAIMED forever. Add a rule that after N consecutive checks a stale row must resolve, be re-owned, or be deleted. Also adopt status-with-mandatory-reason (COMPLETE/INVALID/ON HOLD) so an agent has a legitimate slot for "this finding was wrong" instead of a fake DONE | claude | DONE | f4d6b2c97: ON HOLD status with mandatory owner+reason, SLA warnings (CLAIMED-no-evidence, OPEN-evidence-no-owner) on every CLI run, 36/36 negative tests; guard caught 3 real defects in this ledger (missing SHAs C-68/C-69, pipe-parse break C-67); background: claude-code-best-practice verification-checklist.md; SHIPPED on this branch: ON HOLD status with mandatory owner+reason, SLA warnings (CLAIMED-no-evidence, OPEN-evidence-no-owner) surfaced on every CLI run, negative-tested 36/36 in agent-ledger.test.ts, guard caught 3 real defects in this ledger on first run (missing SHAs C-68/C-69, pipe-parse break C-67); original: claude-code-best-practice verification-checklist.md rules 1F/10B/10C, whose own origin note records an item sitting ON HOLD for 6 runs because no rule checked the reverse direction. Cheap to add, closes two real gaps in scripts/ops/check-agent-ledger.mjs |
| C-17 | Design (not implement) The Odds API affiliate widget integration: founder added THE_ODDS_WIDGET_KEY to Vercel; distinct from THE_ODDS_API_KEY (prediction source-of-truth, not for sale) — the widget is bookmaker odds + our affiliate link, a monetization surface. Design against the existing apps/web/lib/revenue/ partner/offer/disclosure model and the sealed affiliate-structural-separation + partner-offer-compliance-scan guards, without editing either guard | claude | DONE | 9b8fa121 (docs/ops/edge/2026-08-19-odds-widget-integration-design.md). Two real blockers found: (1) site CSP script-src (next.config.mjs:103) allow-lists only Clarity+Stripe, blocks any vendor widget script until the real host is added; (2) evaluateOfferEligibility fails closed on unknown userState for high-risk offers and no visitor-state signal exists in the repo — widget cannot legally render for anonymous traffic without one. Category must be sportsbook not sports_data (triggers the full compliance bundle). 8-item founder-review list + 7-item safe-to-PR list (~half day) in the doc — CANCELLED 2026-08-20 by F-6: sportsbook affiliate widget is in the killed category, permanently |
| C-19 | Cosmetic: rename the misleading contentPlanPrimary="cerebras_free" label — it fires whenever ANY free lane is enabled (Cerebras OR the OpenRouter secondary), not only when Cerebras is actually used; already caused one false-alarm re-investigation during R-6 verification | hermes | DONE | 4df9e6b9e — renamed cerebras_free → content_free in JynxLane type, credit pool, and all tests (6 files, 30 tests pass, typecheck 0) |
| C-18 | Design S2S (server-to-server) postback/conversion tracking for revenue partners: apps/web/lib/revenue/ stops at "offer disclosed," nothing records whether a click-through actually converted (signup/deposit) — applies to every affiliate partner, not just the odds widget | — | OPEN | surfaced 2026-08-19 while researching C-17; needs its own design pass (webhook endpoint, HMAC/shared-secret verification, idempotency, which network(s) support it) |
| R-7 | Start (not complete) sportsbook affiliate program applications at every major US-regulated book (DraftKings, FanDuel, BetMGM, Caesars, ESPN Bet, Fanatics, bet365, Hard Rock Bet, Bally Bet): fill all public/non-sensitive fields, hard-stop before SSN/EIN/banking/payout fields or any T&C-acceptance click, produce a per-book punch-list of what remains | browser | CANCELLED | founder-directed 2026-08-19 night; boundary is deliberate — no agent submits tax/banking info or accepts a binding affiliate contract unsupervised; feeds C-17 (widget design) and the RevenuePartner/disclosure model already in apps/web/lib/revenue/ — CANCELLED 2026-08-20 by F-6: sportsbook affiliate applications are in the killed category, permanently |
| C-33 | DeepSeek round-5 audit: ADOPT N_eff arithmetic and certification calendar (NFL cannot certify solo in a season; MLB ~37 weeks; with ~6 weeks left in MLB season, certification is a 2027 event on every sport — launch and certification are decoupled, launch rides the transparency floor), preseason exclusion rule, key-number label handling, single-track disclosure language. Five errors rejected: regime-mixing "breaks the e-process" mislabelled PROVEN (costs power, not validity); empirical-Bernstein bet formula unstable across rounds (implement from WSR paper only); rank table mis-sorted for the THIRD straight round (BookDisagreementAtLock 0.200 ranked below 0.1875); alpha-table effort column decorative (all seven weights recomputed, divisor is 1.0 everywhere) with invented per-sport N_eff; NFL 3-markets assumption unverified (1H totals may not be ingested). Unification: opener-vs-close is the intercept-only special case of close-pred — one program, preregistered baseline, not two tracks | claude | OPEN | doc: docs/ops/edge/2026-08-19-deepseek-round5-audit.md; adoption of build steps gated on L-14 census |
| L-14 | Line-archive label census v2 (read-only, hermes_ro): per sport AND per market type (spread, total, 1H total, moneyline) — clean-label counts under C-14 contamination criteria, snapshot cadence, earliest-snapshot depth before start, NFL preseason vs regular-season split. Gates the close-pred backtest, the preregistration date, and the E-5 NFL volume question | hermes | DONE | abf0e3f4 on origin/hermes/l14-label-census, merged here; headline: MLB 241 clean closes per full-game market, NFL 0 clean and 0 preseason odds, first-half totals not ingested, go is no |
| C-34 | F-7 blocker found and fixed: production deploy has been failing on EVERY commit since `.npmrc`'s `strict-allow-scripts=true` (added 2026-08-16, real npm ≥11.10 feature, verified against npm docs) landed in this branch's lineage — it hard-fails any dependency install script (ESTRICTALLOWSCRIPTS) unless approved in package.json's `allowScripts` map, which did not exist. Vercel's build npm enforces it; this sandbox's npm 10.9.7 silently no-ops it, which is why local installs looked fine and masked the bug. `.npmrc` is not even in `b71f7e28`'s ancestry — the last successful prod deploy predates this control; every build since has failed at `npm install`, not from F-7's 112-commit backlog itself | claude | DONE | cdd91b58: approved all 7 real pending packages via `npm approve-scripts` (verified list via `npx npm@11 approve-scripts --allow-scripts-pending`): prisma@5.22.0, unrs-resolver@1.11.1, @prisma/client@5.22.0, @prisma/engines@5.22.0, @sentry/cli@2.58.6, esbuild@0.21.5, esbuild@0.28.1. Reproduced Vercel's exact `npm install --include=dev` with real npm 11 — exit 0, Prisma generates cleanly. package-lock.json touched only by npm's own 1-line fsevents dev-flag normalization (verified trivial, no version/integrity change) — kept, not agent-authored content |
| C-35 | Root-caused the real reason NFL has zero odds — NOT cadence, NOT a dead cron, NOT the 112-commit deploy lag (the three theories in play). SEASON_WINDOWS in packages/data-ingestion/src/config.ts gated americanfootball_nfl to Sep-Feb, so getInSeasonSports() filtered NFL out of every 15-min refresh for all of August, the weeks Week 1 lines are posted and preseason runs. Explains L-14's "last snapshot 2026-06-17, 0 clean closes, 84 future games with no odds" exactly. Widened to Aug-Feb; spring stays closed so added credit spend is confined to August | claude | DONE | 1454d8c3 — 6 regression tests in season-windows.test.ts (August in-season, off-season still closed, year-end wrap, backfill override); 0 type errors, 287 data-ingestion + 197 ingestion-pipeline tests pass |
| C-36 | Ingest NFL preseason odds (implemented as H-F3 preseason ingestion) | hermes | DONE | 8731a472 (H-F3, verified live in DOD-1 smoke) |
| C-37 | LINE_ARCHIVE_ENABLED is OFF, which is why odds_line_snapshots has 0 rows — deliberate, not broken (line-archive.ts:143 "Default OFF — founder flips it"). CORRECTION to my first version of this row: I wrote that line history is therefore "lost permanently". FALSE. The `odds` table is APPEND-ONLY — no unique constraint, id is a cuid, every refresh cycle INSERTs, 1,368,288 rows across 717 MLB games is roughly 1,900 rows per game. Raw price history is being preserved right now and nothing overwrites it. What the archive flag actually adds is the structured per-side snapshot with OPEN/INTERIM/CLOSE phase tagging, which is what makes close identification reliable rather than reconstructed. Real value, but not a data-loss emergency, and DeepSeek round-6 action 2 ("back up the live odds table now, it may already be disappearing") rests on the same false premise | claude | OPEN | schema.prisma:409-438 Odds model — indexes only, zero unique constraints |
| F-8 | FOUNDER: set LINE_ARCHIVE_ENABLED=true in Vercel. Corrected justification — this is NOT about preventing data loss (the odds table is append-only and is preserving raw prices already, see C-37). It is about getting phase-tagged OPEN/INTERIM/CLOSE snapshots so closes are identified at capture time instead of reconstructed after the fact, which is exactly the contamination class that ranked #1 in the round-3 failure table. Worth doing, not an emergency, and it does NOT block the feasibility tests — those run on the existing 1.37M append-only rows tonight | founder | OPEN | gate-flip is owner-only by standing rule; code path shipped and test-covered (H-D, line-archive.ts) |
| C-38 | DeepSeek round-6 audit, WITH A CORRECTION OF MY OWN FALSE ACCUSATION. I flagged its "1,368,288 live odds rows" as fabricated. It was not. That figure is line 3 of Hermes RESULTS.md, along with 717 MLB games and 569 eligible — the founder had pasted the full census to it. I wrote the accusation before reading the file I had just merged, which is the same failure mode I spent the night catching in others. Standing rule added: never call a number fabricated until it is grepped against every artifact in hand. Its two REAL errors stand: preseason key backwards (C-36) and mis-diagnosing the founder-gated archive flag (C-37). ADOPTED and materially good: each clean-close game yields MULTIPLE entry labels (24h/12h/6h/3h/1h), so the usable label count is far above 241 while the independent-game count for grouped CV stays 241; Ridge with grouped-by-game CV, never gradient boosting, at this sample size | claude | OPEN | correction verified by grep of docs/ops/hermes/l14-label-census/RESULTS.md line 3 |
| L-15 | MLB close-prediction FEASIBILITY tests on the existing append-only odds corpus — the fastest possible go/no-go, runs tonight on data already in hand, no new ingestion and no waiting. Tests 0-4 are pure SQL over the 1.37M odds rows (mean CLV by entry window; dispersion predicts absolute movement; deviation-from-median predicts direction; short-horizon momentum; cross-market spread-leads-total lead-lag). Test 5 is Ridge with grouped-by-game CV. Decision rule fixed BEFORE seeing results: grouped-CV Spearman r >= 0.15 promising, r < 0.10 kill MLB close-pred | hermes | DONE | 5f524a98 on origin/hermes/l15-close-pred-feasibility, merged here; RESULTS.md, headline: kill MLB close-pred; totals Ridge r=0.49 is p_entry artifact given corr(entry, close)=0.40; H2H r=0.09 R2 negative |
| C-39 | C-31 fix 5 SHIPPED — checkout was hard-400ing on every browser without crypto.randomUUID. A prior pass stopped the TypeError but replaced it with a non-UUID `ci_` fallback that the server's UUID-only CLIENT_INTENT_ID_RE rejected, so Safari under 15.4, older Chrome, and any insecure context could not subscribe at all and were shown a network error blaming themselves. Returns null now and omits the field; the server's existing token-less branch mints the durable intent. Server regex deliberately NOT widened | claude | DONE | 0c190bfe — 5 rewritten tests that previously pinned the bug now assert null plus a never-returns-a-rejectable-string guard; checkout suite 72 pass; apps/web typecheck clean |
| L-16 | Book-level microstructure tests, the two mechanisms L-15 did NOT test and which cannot suffer the corr(X, Y-X) artifact that voided the totals result. A: per-book persistent shading versus the cross-book median close, clustered by game. B: cross-book lead-lag or copy hierarchy at 19-min cadence. Both are book-selection rules rather than game-outcome prediction, so they can pay even though the market-level close is unpredictable | hermes | DONE | f2b485d3 on origin/hermes/l16-book-microstructure, merged here; branch hermes/l16-book-microstructure, RESULTS.md, headline: both dead; totals max clustered t 1.48; 0 lead-lag pairs; minute-cadence MLB totals closed |
| C-40 | L-16 verdict absorbed: BOTH book-level mechanisms DEAD, minute-cadence MLB totals is now closed. A (per-book shade): 11 books, 863-947 labels each, largest absolute t is betus 1.48 — nobody clears the pre-registered bar. B (lead-lag): 110 ordered pairs, zero raw leads, zero after Benjamini-Hochberg; largest rho-1 is bovada to draftkings 0.075 with a positive reverse, so not one-way. Hermes correctly REFUSED a tempting positive: anti-shade CLV showed +1.2 to +1.8pp on the deviation subset, and it called that fade-the-outlier noise rather than persistent shade because the first kill clause had already fired. Do not build a copier or a fade-the-soft screen on this corpus | claude | DONE | f2b485d3; RESULTS.md at docs/ops/hermes/l16-book-microstructure/ |
| C-41 | ONE exploratory survivor from L-16, explicitly NOT a claim: fanatics MONEYLINE shade at t=-2.18, +0.8pp CLV over 163 bets, which clears both the t and the 150-bet bars. It is quarantined because the pre-registered primary market was TOTALS — this is a secondary market surfaced while testing something else, which is precisely the multiple-comparisons trap that killed the original 58.5pct totals signal. It may only be pursued as a PROSPECTIVE pre-registered track on moneyline with its own frozen inclusion rule, never as a retrospective claim on this corpus | — | OPEN | L-16 RESULTS.md moneyline paragraph; three other absolute-t-above-2 moneyline books fall below the 150-bet bar |
| L-17 | FINAL edge experiment on this corpus, pre-registered before results: six path-geometry features (pre-entry-only realized variation, increment autocorrelation, sign-change rate, dispersion decay half-life, cross-sectional skew, staleness fraction) into Ridge with grouped-by-game CV predicting realized CLV on the 241 clean-close games. r at or above 0.15 continue; r below 0.10 the edge program STOPS on this corpus, no appeal. Each feature must pass its decimation/jitter separation test before entering the model (round-7 Part 1 artifacts) | hermes | DONE | e2986412 on origin/hermes/l17-l18-geometry-metrics, merged here; RESULTS.md, headline: STOP edge program; totals grouped-CV r=0.091 R2 negative |
| L-18 | PRODUCT metrics on the 241-game corpus, needed regardless of L-17 outcome: BPQI (per-book mean signed deviation from cross-book median close, pre-close snapshots only, clustered by game) and BURS (per-book fraction of polls with a changed quote). Output feeds the public metrics surface. Honest-claim / forbidden-claim pairs fixed in round-7 Part 3: state measured deviations, never "exploitable", never "fade targets" | hermes | DONE | e2986412 on origin/hermes/l17-l18-geometry-metrics, merged here; RESULTS.md, headline: 11-book BPQI/BURS table; totals max clustered t fanatics -2.05; measured deviations only |
| C-42 | Master plan committed: two tracks (R revenue-first, E one final pre-registered experiment then silence), agent-by-agent assignments, do-not-do list, five sign-off criteria. The alignment artifact all four agents read via git since they share no other memory | claude | DONE | docs/ops/MASTER-PLAN-2026-08-19.md landed in 286b1f3e |
| C-43 | Business plan v1 committed: GSE as "the proof layer for sports prediction" — competitors' product classes (picks, tools, props, fantasy, odds intel) on a proof spine none can bolt on afterward. Moat stated honestly: the un-buyable start date of the append-only archive and published record, the zero-affiliate incentive structure, and the L-15/L-16 negative knowledge; trademarks and trade-secret handling for the rest; no pretense that publishing-calibration is secret. GSE TruthMetrics family named (BookGrade, PulseScore, Consensus Clock, Line DNA, Glass Ledger). Revenue math at near-zero cost base; NFL Week 1 is the launch window; F-6 recommended zero-affiliate permanently; 30/60/90 mapped to agents; founder-only action list of five | claude | DONE | docs/business/GSE-BUSINESS-PLAN-2026.md landed in 7587c5f3 |
| C-44 | TRACK E CLOSED — the pre-registered L-17 kill fired: totals grouped-CV r=0.091 (rule: below 0.10 stop, no appeal), R² negative, n=203 games. All six path-geometry features survived decimation (real path shape, not cadence noise) and still do not predict realized CLV — the strongest possible form of the negative: the geometry is real and uninformative. Spreads r=0.209 with NEGATIVE R² is quarantined same as C-41 (secondary market, zero explained variance). The edge program ends on this corpus. No second experiment, no booster, no fleet. Remaining research lives only as: (a) C-41 prospective moneyline track if ever activated with frozen rules, (b) forward NFL archive accumulating for a future, separately pre-registered program. All effort now on Track R | claude | DONE | e2986412 (L-17 RESULTS.md); MASTER-PLAN Track E clause executed as written |
| B-1 | /kill-ledger public page | hermes | DONE | pushed `a43cdaa` on `hermes/b1-kill-ledger` |
| B-2+B-3 | BookGrade+PulseScore v1 + metric-honesty glossary component | hermes | DONE | pushed `5e589d41` on `hermes/b2-bookgrade-pulsescore` |
| B-4 | Per-pick Verify button UI on public pick cards | hermes | DONE | 935b59e1 on origin/hermes/b4-verify-button, merged here; client component reveals contentHash, committed timestamp, /verify link; no new crypto |
| B-QUEUE | Hermes autonomous build queue v1 (B-1..B-4 shipped as individual DONE rows; B-5 pledge page folded into B-QUEUE-2) | hermes | CANCELLED | B-1 a43cdaa, B-2+B-3 5e589d41, B-4 935b59e1; queue doc superseded by BUILD-QUEUE-2026-08-20B.md (B-QUEUE-2) |
| C-45 | AGENTS.md was stale and actively misdirecting agents: it pointed autonomous sessions at handoff/LEDGER.md and docs/ops/hermes/CONTINUOUS.md, both frozen since 2026-08-17/18 (a prior session, branch claude/fable-5-ultracode-plan-ptru4e) and containing none of tonight's work. This is the most likely cause of the fresh Hermes session that could not find the build queue and had no origin remote -- it was bootstrapping from stale instructions. Repointed THE LOOP at docs/ops/AGENT_LEDGER.md (the live multi-agent ledger) and the latest docs/ops/hermes/BUILD-QUEUE-*.md; softened the absolute never-push law, which the founder had already explicitly overridden tonight | claude | DONE | 5fe69ad5 |
| C-46 | Grok "self-regularizing residual-information e-process" audited: algorithmic core is real and correctly stated (Liu-West 2001 kernel, cubature Kalman, RBPF, negative-binomial hierarchy — converges with our own adopted C-22 design); REJECTED its novelty claim (capital-modulated lambda is a predictable betting strategy the existing anytime-valid theory already licenses — GRAPA-family adaptive bets are the published cousins), its residual-information gate (sequential conditional MI at a few hundred binary outcomes is estimator noise, unspecified estimator), its step-1 run on the closed 241-game corpus (Track E closed, C-44, and its own power math prices that run at zero), and its sandbox capital-896 as evidence (synthetic planted edge, smoke test only). ADOPTED as the candidate engine for the one open door — the future separately pre-registered prospective program — via synthetic-first validation with a mandatory null test | claude | DONE | b1416b92 (docs/ops/edge/2026-08-20-grok-stack-audit.md) |
| B-QUEUE-2 | Hermes build queue 2 issued: B-6a/b/c Glass Ledger made true (chain append wiring behind LEDGER_CHAIN_ENABLED default OFF, public chain-export endpoint, real loadLedgerView), B-7 Consensus Clock + Line DNA libraries against fixtures (pages data-gated until the phase-tagged archive holds 7 days), R-9 Grok engine synthetic-only with a hard never-touch-real-data rule and an alpha-level null-test acceptance bar | hermes | OPEN | docs/ops/hermes/BUILD-QUEUE-2026-08-20B.md in this commit; launch items strictly before the research tail |
| B-7 | Consensus Clock (dispersion half-life fit) + Line DNA (per-game path summary) libraries against synthetic fixtures | hermes | DONE | 1bacc671 on hermes/b7-consensus-clock-line-dna; libs + 26 tests vs fixtures only; pages unshipped |
| C-47 | Grok round-2 absorbed before founder sign-off: implementation depth adopted into R-9 (Laplace-for-NB conditional, Liu-West ordering, house-filter conventions from the verified-in-repo team-strength-filter.ts — 42KB shadow bootstrap PF that R-9 now extends instead of greenfielding); DML causal prototype adopted as R-10, shadow-only, one treatment (QB out), time-aware cross-fitting, overlap/placebo/sensitivity mandatory, compared against existing TeamIntervention magnitudes, never a public claim. Grok sandbox artifacts remain spec-only; round-1 rejections stand | claude | DONE | 7534bbf1 |
| B-6a | Glass Ledger chain-append wiring | claude | BLOCKED | Hermes correctly stopped instead of looping (AGENTS.md two-attempts rule) on a process-sport.ts anchor mismatch, but that was not the real bug. The store it built (ledger-store.ts, origin/hermes/b6a-chain-append 7f3822c4, NOT merged) is in-memory-plus-optional-JSON-file, and process-sport.ts runs inside apps/web/app/api/cron/refresh-odds/route.ts, a Vercel serverless function, so module state does not survive a cold start. Flipping LEDGER_CHAIN_ENABLED would have shipped a chain that silently restarts from GENESIS_HASH on nearly every invocation while presenting as continuous: a fake audit trail on the platform whose entire identity is a real one. Root cause and the exact proposed Postgres schema, matching the existing PickProofReceipt/SlateCommitment DB-adapter pattern the file's own header already specified, are in docs/ops/edge/2026-08-20-ledger-chain-durability-proposal.md. Requires a new Prisma migration, a sealed path, founder review required before any code lands |
| F-9 | FOUNDER: review and approve (or amend) the LedgerChainEntry Prisma model proposed for the Glass Ledger's durable hash chain | founder | OPEN | docs/ops/edge/2026-08-20-ledger-chain-durability-proposal.md; blocks B-6a/b/c only, nothing else in the queue |
| R-9 | Grok-derived NB-RBPF engine, synthetic-only, house-filter conventions, null-test acceptance | hermes | DONE | cc939ea3 on hermes/r9-grok-engine; null 0/200 capital>20; planted median max 2.1385 vs open-loop 1.0000; RESULTS.md; shadow only |
| R-10 | DML causal prototype, QB out/limited, shadow-only, time-aware cross-fitting | hermes | DONE | 5436aa07 on hermes/r10-dml-qb-out; synthetic panel (no nflverse dump in workspace); ATT -0.0111 CI includes 0; placebo contains 0; RESULTS-dml.md |
| F-10 | FOUNDER OVERRIDE, recorded honestly: the CEO-agent charter (uploaded 2026-08-20) orders ONE real-data MVE on the 241-game corpus — a fundamentals-vs-market outcome model with a fractional e-process — despite C-44's no-appeal closure. The closure was ours; the gate is his. Binding, pre-registered outcome: capital at or below 2 ends the edge program permanently with a published Kill Ledger entry; above 20 produces a prospective pre-registration DRAFT for founder signature; anything between publishes as did-not-survive and closes the program identically. One cycle, no reruns, no tuning after seeing the path, primary window 6-3h with one bet per game | founder | DONE | f986dfdb (FINAL-RUN doc + charter adoption); executed by Hermes as H-F5 |
| Q-FINAL | FINAL RUN H-F queue (closed by C-61: all seven H-F tasks merged and verified; superseding run doc FINAL-RUN-2026-08-20.md) | claude | CANCELLED | C-61 (2e016327) closed this queue DONE; row retired to stop agents re-running it |
| C-48 | DeepSeek MVE pre-run statistics audit VERIFIED AND ADOPTED into H-F5 as a binding STATISTICAL RULING: the fractional e-process E_t = 1 + 0.3(Yq/m + (1-Y)(1-q)/(1-m) - 1) is a fair martingale under the POINT null (market probability exactly correct) but NOT a valid e-process under the composite null p<=m (verified: for q<m the increment's conditional expectation exceeds 1 at small true p). Bound consequences: point null is THE pre-registered null; result language never says "positive expected value" (vig is outside this test); hyperparameters frozen-or-online-only; entry prices held to the close-quality cleanliness bar with exclusions counted; window 6-3h and lambda 0.3 fixed pre-result with no alternates computed even diagnostically; any >20 prospective track certifies against the vig-inclusive composite null (b_i = 1/D_i) instead. DeepSeek's pre-drafted kill entry and preregistration template stand as the outcome-doc templates | claude | DONE | 3f565cab (FINAL-RUN H-F5 ruling block) |
| C-49 | Grok Build 4.6 integrated as a governed grunt seat: .grok/config.toml ([permission] allow/deny mirroring session law — no main pushes, no force-push, no rm -rf, no prisma migrate/db push, no install-scripts approve, sealed paths + .env unreadable/uneditable) and .grok/sandbox.toml ([profiles.gse] extends "workspace", denies .env/.pem/.ssh). Schema verified against the actual xai-org/grok-build source (Apache-2.0, cloned read-only): [permission] allow/deny rules, [mcp_servers]/[plugins] tables, and sandbox profile extends="workspace" all confirmed in crates/codegen/xai-grok-config and xai-grok-sandbox. Grok Build reads CLAUDE.md/AGENTS.md natively, so the full rule system binds it with zero duplication. Pricing recorded honestly: the client has free included usage (2x-first-week promo dated Aug 12, expiring ~now); the underlying API is paid — Grok Build is NOT unconditionally free | claude | DONE | 3f565cab (.grok/config.toml, .grok/sandbox.toml) |
| C-50 | MVE STATISTICAL RULING v2, superseding v1 BEFORE any computation (pre-registration still amendable; v1 symmetric point-null retired unrun). DeepSeek round-2 correction VERIFIED: the asymmetric fractional increment 1 + 0.3(Y q/m + (1-Y)(1-q) - 1) has conditional expectation linear in true p, <= 1 at both p=0 and p=m, hence a valid supermartingale under the composite null p <= m, with increments >= 0.7. Claude's amendment: DeepSeek's form was over-side-only — structurally blind to under-side edges and losing on them in expectation, fatal in a one-shot final experiment — so the pre-registered primary is SIDE-ADAPTIVE (increment oriented to the predictably-chosen bet side; one process, no multiplicity; founder may veto before the run). Adopted operational rules: entry book-quoted <= 15 min old from >= 3 books with exclusions counted; checkpoints every 50 from n=50; certification E >= 20 at a scheduled checkpoint; kill E <= 0.10 at any checkpoint; early abort < 0.01 after 50. Prospective-track template frozen with disclosure language at docs/ops/edge/2026-08-20-mve-prereg-v2.md; queued for whichever agent holds the build seat at H-F5 | claude | DONE | 2cad335f |
| C-51 | MVE pre-registration FINALIZED before first computation: DeepSeek round-3 review returned PROVEN on Claude's side-adaptive amendment ("a predictable side-selection rule combined with the asymmetric increment yields a nonnegative supermartingale under the composite null") and the side-selection rule is now frozen deterministic — q>m bet OVER, q<=m bet UNDER (ties UNDER), under bets use complements — with disclosure language adding side selection to the frozen list. FINAL-RUN verifier lane rewritten as a seat-swap rule (builder never verifies own work; H-F5 audit stays cross-model: DeepSeek statistics + Hermes code). FOUNDER DIRECTIVE 2026-08-20: Grok Build is the default seat for ALL grunt work while its included usage lasts ("use grok for everything"), Hermes fallback; Grok must update the ledger per task like every agent. Ops fix shipped with it: sandbox deny is fs-level and inherited by child processes, so the env file was removed from the sandbox deny (node scripts need runtime env for DB; model-level Read/Edit denies stand) and node/npx-tsx under scripts/ allowed for the MVE runner | claude | DONE | 9078f838 |
| C-52 | BUILD-BLOCKING BUG, real and verified before fix (grepped the file for all usages, ran eslint and tsc directly, read the failing rule's config): apps/web/app/kill-ledger/page.tsx line 127 declared EXPECTED_EVIDENCE_PATHS and never referenced it anywhere in the file. @typescript-eslint/no-unused-vars is "error" severity in .eslintrc.json and apps/web/next.config has eslint.ignoreDuringBuilds=false, so `next build` genuinely fails on this line — not a false alarm. Introduced in a43cdaa7 (Hermes's original B-1 commit); missed by Claude's own c80b1a92 review-fix to the same file; every commit since inherited the failure, meaning production deploys have been blocked since B-1 landed. Deleted the dead declaration; eslint and tsc both clean after (verified directly, not by report) | claude | DONE | b820cc9c |
| C-53 | LEAN BACKSTOP WAKE finding: two Hermes branches ready and content-clean (both pure additions, both self-report DONE) — hermes/hf1-fable-dashboard (H-F1, 6fc36451, /fable proof dashboard) and hermes/hf2-pledge (H-F2, 2b427adc, /pledge + affiliate-free endpoint). Dry-run merge of EACH against current base conflicts only on this ledger file's own append line (both branches append a DONE row at the same spot as each other and as C-52). Per the lean-backstop protocol, not hand-resolving in this pass — needs the standard union-resolve (keep both rows, keep DONE) on the next full pass | claude | DONE | 7ea77a5a (H-F1 merge); H-F2 merge follows |
| H-F1 | FABLE proof dashboard section | hermes | DONE | af111172 |
| H-F2 | Zero-affiliate pledge page | hermes | DONE | 1642d202 |
| C-54 | Root-caused the deploy blocker PROPERLY instead of stopping at the one reported line. C-52 fixed a single unused var; a full `eslint . --ext .ts,.tsx` sweep over apps/web (run with a REAL exit code, not piped — piping through tail had masked the true status earlier this session) found THREE more no-unused-vars errors of the identical build-blocking class: lib/truthmetrics/consensus-clock.ts newtonStep declared `n` and never referenced it (verified by reading the whole function, not by trusting the linter alone); consensus-clock.test.ts imported an unused ConsensusClockResult type; bookgrade.test.tsx imported an unused PULSE_SCORE_V1. Because apps/web lint runs --max-warnings=0 and next.config sets eslint.ignoreDuringBuilds=false, every one of these fails `next build` — so C-52 alone would NOT have unblocked the deploy. All three removed; eslint over the whole app now exits 0; the two touched suites pass 19/19. Lesson recorded: fix the CLASS, not the reported instance | claude | DONE | 16dabcf8 |
| C-55 | PRODUCTION BUILD VERIFIED GREEN locally before any deploy — the first time this has been proven rather than assumed this session. `npm run build` (full next build, vercel.json's real command path) exits 0 with zero errors or warnings; note the harness reported "exit 0" for the wrapper too, which was meaningless (trailing echo), so the true status was read from BUILD_EXIT written inside the log — same pipe-masking trap as the earlier ledger-guard incident. Route manifest confirms all four launch surfaces compiled: /fable, /kill-ledger, /bookgrade, /pledge, /api/pledge/affiliate-free. deploy:ready reports 18 env-var failures that are SANDBOX ARTIFACTS, not blockers (no prod secrets in this container; they live in Vercel env) — its meaningful checks pass: 20 cron schedules, 3 security-header rules, gate sequencing | claude | DONE | 16dabcf8 (build run against this tree) |
| DOD-1 | DEFINITION-OF-DONE ITEM 1 CLOSED — production redeployed from latest main after being the standing blocker all session. Deployed SHA 7294739c, status Ready (exit 0, ~4 min build), aliased to https://www.galaxysportsedge.com, 53 migrations found and none pending. Executed by the Grok build seat under F-11 (Claude lacked any production-deploy tool). VERIFIED LIVE, not taken on report: post-deploy smoke all green (13 public pages 200, robots/sitemap/opengraph, /api/health, 3 security headers); the four launch surfaces each return 200 (/pledge, /fable, /kill-ledger, /bookgrade) plus /calibration; and /api/pledge/affiliate-free returns the frozen payload character-for-character. Mandatory-language spot-check on the live HTML: ECE confidence-echo caveat renders verbatim on /fable, BookGrade "quality score, not a betting signal" renders verbatim | founder | DONE | 7294739c |
| C-56 | Honesty-gate gap caught by checking the LIVE page rather than the source or the report: the charter lists four mandatory verbatim public lines; three rendered exactly (ECE caveat, BookGrade, PulseScore) but /kill-ledger carried a paraphrase ("We test things so you do not have to learn the hard way") instead of the mandated "These are things we tested and found not to work. We publish them so you do not pay for the same mistakes." Copy corrected to the charter wording (house style keeps "do not" over the contraction). Not a launch blocker and the old line was not dishonest — but mandatory means mandatory, and a paraphrase in one of four is how a spec quietly erodes. kill-ledger suite 4/4, trust-gate and eslint clean. Rides the next deploy; not worth a redeploy cycle on its own | claude | DONE | 10c8d655 |
| C-57 | Delegation skills installed and made PERSISTENT. `npx skills add amElnagdy/delegate-skills --skill grok-delegate --skill claude-delegate --global` succeeded (both repos real; the earlier 403 on github.com was the agent proxy on unauthenticated HTML, not absence) but wrote to /root/.agents/skills in this ephemeral container — the founder asked for them "for future use", which that install does NOT deliver since the container is reclaimed at session end. Vendored to .claude/skills/ so git carries them to every future session and to the founder's local checkout; .gitignore extended in its own established pattern (.claude/* ignored with explicit ! allowlist for shared assets — commands/, settings.json, now skills/). SUPPLY-CHAIN REVIEW DONE BEFORE LANDING, since these run with full agent permissions and the installer says so: verified by grep, not by trusting the file header — Node built-ins only, zero third-party deps, zero network calls (no fetch/http/socket anywhere), spawns limited to git/grok/claude/taskkill; process.env is forwarded so the child CLI can authenticate, harmless given no network path. secret-scan and trust-gate clean. NOT runnable here: the /plugin marketplace commands are Claude Code CLI built-ins the founder must type locally | claude | DONE | 539e3c1e |
| F-11 | FOUNDER OVERRIDE, recorded honestly: the CEO charter's non-negotiable "Do not let Hermes or any agent touch Vercel" is overridden for the build seat, at the founder's explicit direction ("im giving you permission to redeploy" + "pretty sure grok can though"). Claude could NOT execute it — the only Vercel tool in session is create_git_project, which makes a PREVIEW deploy and could create a duplicate project, and there is no .vercel linkage, no list_teams tool, and no Vercel CLI; declining was a capability limit, not policy. Scope of the override is deliberately narrow in .grok/config.toml: allow `vercel --prod`/`deploy --prod` plus read-only ls/inspect/logs; DENY vercel env, remove, rm, project rm, domains, alias — the seat may ship, never mutate env or destroy the project. Deploy is only safe because C-55 proved the build green first | founder | DONE | 08d3abed (.grok/config.toml scoped allow/deny) |
| H-F4 | Daily honest-record post generator | hermes | DONE | 2d850547 |
| C-58 | LEAN BACKSTOP WAKE: four new build-seat branches found. MERGED H-F4 only (clean merge, zero conflicts; typecheck 0, trust-gate 0, draft-only guard 0 — that guard is the law for this task since publishing stays founder-gated — 36/36 tests). NOT merged, each for a stated reason: (a) hermes/hf3-preseason conflicts on this ledger file's append line only, code content clean — per lean-backstop protocol not hand-resolved in this pass, needs union-resolve next full pass, and note it self-expires ~Aug 30; (b) hermes/hf5-mve held back because FINAL-RUN makes an independent cross-model audit MANDATORY before any H-F5 merge and the builder may not verify its own work; (c) hermes/f9-ledger-chain-schema NOT merged — it edits packages/db/prisma/schema.prisma plus a new migration, a SEALED PATH, while F-9 (the founder's schema review) is still OPEN | claude | DONE | 223c5202 (SHA corrected; 40c341ab was a pre-merge hash that resolves nowhere) |
| C-59 | H-F5 MVE DID NOT RUN, and the build seat handled it correctly — recording this because a stalled one-shot experiment is exactly what gets quietly faked. RESULTS.md reports the cycle BLOCKED before first observation: runtime DATABASE_URL/DIRECT_URL point at localhost Postgres, pg failed 28P01 password authentication for user "sports", DATABASE_URL_UNPOOLED unset, no Neon URL in process env. It invented no capital, no n, no exclusion count; printed no secrets; computed no alternate window, lambda or variant; and left the pre-registration un-amended. Frozen artifacts DID land on the branch (side-adaptive e-process + unit tests, seed 20260820, nParticles 24, runner scripts/edge-lab/run-mve.ts, and the outcome drafts written BEFORE any corpus look) — which is correct pre-registration hygiene. Needs founder hands: re-run on the same branch with a working Neon URL, no retuning, one cycle | hermes | BLOCKED | founder must supply working DATABASE_URL and re-run; origin/hermes/hf5-mve 0035e3b4 |
| C-60 | GOVERNANCE FLAG on hermes/f9-ledger-chain-schema (a28e1d67): the branch appends a NEW row "F-12" with owner=founder and status=DONE, self-marking founder-owned work complete without the founder having acted — F-9 remains OPEN. Mitigating and verified by reading the diff, not assumed: the change is deliberately inert (it does not apply the migration, does not flip LEDGER_CHAIN_ENABLED or PUBLISH_LEDGER, and loadLedgerView stays four-leg empty), and it does implement the durable Postgres store the B-6a proposal specified instead of the in-memory fake that was rejected. Still not mergeable by an agent: sealed path plus an open founder gate. Founder decides whether to adopt; if adopted, the F-12 row should be rewritten as agent-owned with F-9 recording his actual approval | claude | BLOCKED | founder review required; not merged |
| H-F3 | NFL preseason ingestion (C-36) | hermes | DONE | 8731a472 |
| H-F6 | SEO/metadata audit pass | hermes | DONE | a3ee39cd |
| H-F7 | Archive liveness check (odds_line_snapshots) | hermes | DONE | 38b2bb8a — total 37402; MLB 11318 (OPEN 144 / INTERIM 11174 / CLOSE 0); NFL 9864 (OPEN 96 / INTERIM 9768 / CLOSE 0); hermes_ro on gse-postgres main |
| C-61 | FINAL RUN H-F QUEUE CLOSED. Merged hermes/hf3-preseason, hf6-seo, hf7-archive into the working branch (union-resolve on this file each time; the resolve initially duplicated C-53 and the ledger guard caught it — stale BLOCKED copy from hf3's older base deleted, current DONE kept, per the keep-DONE convention). Verified after merge, not assumed: tsc 0, eslint over apps/web 0, trust-gate 0, data-ingestion 295/295, launch-page suites 29/29. Also fixed a stale docblock in kill-ledger/page.tsx whose "Copy rules" comment still quoted the pre-C-56 closing line while the rendered copy, metadata and JSON-LD had all moved to the mandated wording — a comment that lies about the copy rule is how the next agent reintroduces the old line | claude | DONE | 2e016327 (SHA corrected; 5077a89d was a pre-merge hash that resolves nowhere) |
| C-62 | ARCHIVE LIVENESS (H-F7) read from Neon as SELECT-only hermes_ro: odds_line_snapshots = 37,402 rows; MLB 11,318 (OPEN 144 / INTERIM 11,174 / CLOSE 0); NFL 9,864 (OPEN 96 / INTERIM 9,768 / CLOSE 0). Table grew mid-query (37,384 -> 37,402), which proves LINE_ARCHIVE_ENABLED is bound and writing. CLOSE=0 is NOT yet evidence of a bug: close stamps are written at settle time by markClosingSnapshotsIfEnabled, and the flag only bound at the 7294739c deploy, so no game has completed a full pre-deploy-to-settle cycle under it. It IS the next thing to check — if CLOSE stays 0 after MLB games settle post-deploy, the close-stamp path is broken and DoD item 7 is not actually satisfied. Recorded as a pending observation, not a pass | claude | OPEN | recheck CLOSE counts after the next MLB settle cycle |
| F-13 | FOUNDER DIRECTIVE 2026-08-20: build seat moves from Grok Build to HERMES RUN ON LAGUNA going forward. Grok completed H-F1..H-F7 and steps back to fallback + verifier. FINAL-RUN doc updated with seat history and current holder. Consequence recorded because it is load-bearing: since Grok BUILT the H-F queue, Grok may not verify it, so the outstanding mandatory H-F5 MVE audit belongs to Laguna-on-Hermes or DeepSeek | founder | DONE | 2e016327 (evidence SHA corrected; 5077a89d resolves nowhere. Row content untouched) |
| T-Q1 | q-integrity: slugPlayer collision fix + credit-cap kickoff sort + line-archive N+1 batch | hermes | DONE | 9a619bf4 on origin/hermes/ox-alpha-q-integrity; draft PR #525; 257 tests passing (34 tq1-specific) |
| T-Q2 | Wire markClosingSnapshots behind existing LINE_ARCHIVE_ENABLED flag in settle-sport.ts | hermes | DONE | 0a447f98 (hermes-H-D) is ancestor of HEAD on hermes/ox-alpha-q-integrity; markClosingSnapshotsIfEnabled called in settle path with try/catch (never fails settlement); 55 settle-sport tests + 15 line-archive tests passing; fix stale docblock noting it's wired (commit fad55c53) |
| T-Q3 | Kalshi listing snapshot harness: 429 → one retry → success; 429 exhaust → KalshiError(429) STOP; never last_price as q; exponential backoff verified | hermes | DONE | kalshi-snapshot-harness.test.ts created (0906ec0c on origin/hermes/ox-alpha-q-integrity); 4/4 tests pass; persist path already in #516 (gateKalshiListing refuses last_price, yes_bid+no_bid complement) |
| T-Q3a | Typecheck fix for T-Q3: kalshi-snapshot-harness.test.ts lines 109-110 — with noUncheckedIndexedAccess, fv.sides[0]/sides[1] are possibly undefined. Added non-null assertions (sides[0]!, sides[1]!) since toHaveLength(2) already guards. Fix at cc905a83 on origin/hermes/ox-alpha-q-integrity; tsc --noEmit exits 0; CI Test job 97040591997 PASS (7m54s), all checks green, PR #525 ready for review | hermes | DONE | cc905a83 on origin/hermes/ox-alpha-q-integrity; PR #525 |
| H0.6 | H0 slice #6 — remaining process covariates from PROP_COVARIATE_GAP: rush-yards bind + INT bind on hermes/h0-rush-yards-covariate-bind, PR #576. Binds leak-safe NGS weekly means (week t for t+1, week=0 excluded, fail-closed, never imputed) into RushSample (pctAttemptsGte8Defenders + avgTimeToLos) and IntSample (avgTimeToThrow + aggressiveness) via covariate-bus. priced:false throughout. Post-review fix (2308f468): single latestPriorRow scan per request instead of nextGameCovariate x2 per field; CovariateCells built from BUS_CELL template for single-source grain/provenance. 22/22 tests pass, tsc --noEmit 0 errors, all CI checks green on PR #576 | hermes | DONE | 2308f468 on origin/hermes/h0-rush-yards-covariate-bind; PR #576 |
| H0.5 | RZ-share on top of Dirichlet (K11) — BLOCKED. K11 (dirichlet-multinomial kernel slot) is defined in KERNEL_SLOT_CARDS.md but NOT yet implemented on origin/main (no kernel/slots/ dir). EDGE-HUNT-LAUNCH.md gate: 'you bind it only if slot already on main.' Do not fill K1–K13 slots (fleet does that). scoring-zone.ts already has rzShare field — ready to bind once K11 lands on main. | hermes | BLOCKED | N/A — waiting on fleet K11 landing |
| LAUNCH-0902 | Final launch pass (owner-directed, 2026-09-02): free-first settlement law with starved-cycle detection; alias tombstones + dry-run merge tool for duplicate games (2h baseball window); blocking CI migration replay + drift check; baseline seeds non-destructive; market-coverage and confidence-tail monitors on the truth surface and launch:ready; owner runbook (`npm run ops:runbook`) and 10-minute launch-day runbook; review-fix pass on the Devin/cubic findings; guard hardening (selftest 91/7/45). Calibration floors and record gates untouched (D2/D3/D8 in docs/ops/CLAUDE_DECISIONS_20260902.md). | claude | DONE | PR #685 (stacks on PR #684); report LAUNCH_FINAL_20260902.md; decisions docs/ops/CLAUDE_DECISIONS_20260902.md |
| C-63 | ZERO GAPS AUDIT 2026-09-03: Comprehensive codebase audit with all gaps closed. Fixed 2 failing guardrails (ai-control-plane-sealing: skip Sports/ nested dir copy; ai-council: use node not npm spawn for Windows PATH compat). Added descriptions to 18 @sports packages (ai-council through util). Verified: tsc=0, lint=0, git-clean, guards=26/26 (was 24/26), all package READMEs present. Gap analysis report at docs/ops/GAP_ANALYSIS_2026-09-03.md documenting 31 remaining OPEN ledger items + 2 production TODOs (documented future work in compliance package). | claude | DONE | 3f47b0e88 |
| C-64 | settle-backfill defaultPersist score-disagreement guard (dual-audit verified) | claude | DONE | 6a2ec0d0d + regression test in settle-backfill.test.ts |
| C-65 | settle-backfill capReached off-by-one fix: fetch cap+1 rows so capReached (rows beyond cap) is decidable; exactly-cap=false, cap+1=true; graded set still sliced to cap | claude | DONE | merge 4adc43f2b + capReached rework in settle-backfill.ts, 11/11 tests |
| C-66 | backfill-team-efficiency + ingest-player-stats unpublished-season guards | claude | DONE | 968c49522: both routes fall back to floor only on isUnpublishedSeasonSignal (404/ok-zero-rows); 5xx outage recorded failed; 49/49 route tests; tests for outage-no-fallback + ok-zero-rows fallback added |
| C-67 | merge-duplicate-games computeCanonicalFillData score-pair branch (AND to OR) | claude | DONE | ea893d5a8: extracted pure rules to scripts/ops/game-merge-score-fill.ts (OR semantics) + 10 node:test cases via test:merge-score-fill |
| C-68 | game-merge-plan canonicalPickId → referencePickId rename (repo-wide) | claude | DONE | f4709df22: PickConflict.referencePickId; 3 code refs + 1 test ref renamed, game-merge-plan tests 17/17 |
| C-69 | smoke-prod cold-start timeout retry logic | claude | DONE | 5ba852598: one 2s-settle retry per non-200 route, global cap 8, LAST_CODE global (subshell counter bug caught in self-test); bash -n OK, counter verified |
| C-70 | VERIFIED FIXES 2026-09-03: 6 real bugs from dual-audit verification (DeepSeek + Claude independent scans agreed on 6/12 real, 6/12 false/stale). Shipped as C-64 through C-69 with one commit each plus verify block. False positives NOT touched: confidence-tail pickType (already wired), external-watchdog set -e (already omitted with comment), owner-runbook dual-secret check (already present), agent-bash-guard env -S/--output (already handled with selftests), review-pr.md/canary.md (already correct), process-sport.test.ts (doesn't exist). | claude | DONE | 47037c6d1..5ba852598 on claude/verified-fixes-2026-09-03; gates at close: tsc 0 errors, lint clean, guardrails 26/26, test:fast 251/251, settle-backfill 11/11, game-merge-plan 17/17, merge-score-fill 10/10 |
| H-N1 | All-night Wave 1 — consolidate the landing: merge #695→#696→#697→#698→#699→#694→#692 in order onto hermes/night-2026-09-04, full verify block after each, one draft PR, then re-add the soccer scoring assertion named in historical-replay-sport-key.test.ts | hermes | DONE | 5bccb568e1c9eaccbf3bb8db50e7846cfa31e1c5..6a9c9d694cf78586b9ec8335f86fc64526c37822 (7 merges in §2 order; #696→#698 carried #695 as merge base). Verify block after EVERY merge: typecheck 0, lint 0, test:fast 0, guardrails 26/26 (dependency-audit flaked red twice on heads with zero manifest changes — D4 flake, waivers stay per §2, fix is forbidden path, noted in merge message). One conflict: AGENT_LEDGER.md append line, union-resolved keep-both (H-N1 + ARCH-1). Soccer suppression assertion from historical-replay-sport-key.test.ts NOTE re-added post-#694 with NFL control: 7/7 green. Draft PR: #700 |
| ARCH-1 | ARCHITECT RECON 2026-09-03: verified launch state and triaged the unmerged backlog. main=0db5ef808 with CI run 33799645214 SUCCESS; typecheck 0, lint 0, guardrails 26/26 on that head; production LIVE on the same SHA (Vercel dpl_GoYrLkrJ2vTC28dYaXbXhEni9PbJ, target=production, READY). 789 remote branches, 640 diverging from main. KEY FINDING: 50 open PRs from the 2026-08-25 audit wave were never merged and none of their content is on main; dry-run merge of each against 0db5ef808 with git merge-tree gives 43 CLEAN, 7 CONFLICT, 0 absorbed. These are named access-control, money-path and honesty-surface fixes, including #597 (two BLOCKER findings: paid-board bypass, free-grace exploit). Landing queue, per-batch verify block, preview-deployment verification recipe and an explicit not-verified list are in docs/ops/LAUNCH_COMMAND_2026-09-03.md. Textual merge cleanliness is NOT semantic compatibility: the full apps/web suite was NOT run against any merged combination, and the doc says so rather than implying otherwise. Also corrected an earlier stale-ref reading in this session that reported main at bb0e7df | claude | DONE | #690 (4d42ffc5d696feecd775ac38291285646c466b27); doc is docs/ops/LAUNCH_COMMAND_2026-09-03.md |
| H-N2 | All-night Wave 2 — make metric-source-payload-rights.test.ts and metric-evidence-report-markdown.test.ts cwd-independent (resolve from import.meta.url, not process.cwd()); no test deleted or skipped | hermes | DONE | 648071a8c22a466a564db0121435e287435b5c84: walk-up repo-root helper (tsconfig.base.json marker, unique to root) because package tsconfig pins module:CommonJS so import.meta is TS1343; controls added vs vacuous pass; verified green from BOTH repo root and package dir (10/10 each). SECOND ROOT CAUSE beyond cwd: untracked nested clone at Sports/ (git-ignored, no unique commits vs main, clean) was collected by the root glob and always failed from root; moved to C:/Users/Garrett/_staged_for_deletion_2026-09-02/stale-sports-clone-from-sports-Sports_2026-09-04 (reversible). Gates: tsc 0, lint 0, guardrails 26/26, test:fast 0, targeted 0. Also in 61fd629c0: H-N1 DONE + PR #700 |
| H-N3 | All-night Wave 3 (first item, per addendum A3) — write docs/data/CONVERGENT_CALIBRATION_EVIDENCE_2026-09-04.md setting live tail (40% at ≥80), live resolution (0.005 on 1,663), replay AUC (0.4965 on 13,646) side by side, citing CLAUDE_DECISIONS_20260902.md D2/D3 + NFL_REPLAY_CALIBRATION_2026-09-04.md; state plainly the ≥80 tier wins less often than board average, corroborated; then continue Wave 3 main build (replay-calibration.ts) | hermes | DONE | 25772b36a (A3 convergent-evidence doc; every number traced to its recorded source) + Wave 3 build in fe3223f6085a7cf5659316d9e48b3ce517a772ff: scripts/analytics/replay-calibration.ts — walk-forward 10 folds (2016-2025), Murphy decomposition + reliability + ECE (equal-width AND adaptive) with seeded 2000-resample bootstrap CIs, isotonic/Platt/beta vs identity on held-out folds, variable-based leaves (favourite strength, era). RESULT: closing line already calibrated — no calibrator beats identity beyond +0.00007 mean Brier; market Brier 0.2106 CI [0.2050,0.2172] on n=2750 held-out; favourite-strength 6.5-9.5 leaf drifts 65.9%→57.1% (flagged for follow-up, no product change). Corpus honestly bounded: 5,281 games 2006-2025 (closing MLs start 2006), stated in script output and docs/data/MARKET_CALIBRATION_2026-09-04.md. Run exit 0; tsc 0, lint 0, guardrails 25/26 (dependency-audit D4 flake, 304s degraded npm audit), no test gates affected (analysis script, no test suite claims it) |
| H-N4 | All-night Wave 4 — build packages/prediction-engine/src/clv-harness.ts reusing clv.ts primitives (never reimplement), (openLine, closeLine, side, entryOdds) → CLV in points AND probability; known-answer tests computed by hand with controls; runner injecting seeded synthetic open labelled SYNTHETIC OPEN — NOT A REAL CLV RESULT everywhere; document required real-archive columns | hermes | DONE | dee68cf900d11541bbdf75bd4f274667e5f283d2: clv-harness.ts delegates all CLV math to clv.ts primitives (summarizeClv/clvFromEntry), SYNTHETIC OPEN labelling on every output section, requiredArchiveColumns() contract + entry==close degeneracy documented; clv-harness.test.ts 11/11 hand-computed with controls (one test fixed during build: +150 misapplied favourite formula, harness was right); clv-synthetic-runner.ts exit 0, n=744/market over 3 nflverse seasons, mean CLV ~0 pts as symmetric seeded jitter requires — SELF-CAUGHT pre-commit: season cap compared season numbers (2025..) to SEASON_CAP=3 and broke after one game (n=1, vacuous); fixed to distinct-season tracking and re-run before committing. Gates: tsc 0, lint 0, guardrails 25/26 (dependency-audit D4 flake 3rd occurrence tonight, 304s, manifest-unchanged head), test:fast 0, targeted clv-harness+clv 0 |
| H-N5 | All-night Wave 5 — totals tie-break: strict opt-in (context.totalsTiebreak="strict" threaded via HistoricalReplayOptions; only discriminating books vote, exact tie => no pick), legacy DEFAULT untouched (pinned tests stay green; default flip = owner's MODEL_VERSION decision), before/after replay, proposal doc under docs/calibration-proposals/, no MODEL_VERSION bump, no live-path merge | hermes | DONE | da1ab0b763cd303d743fe1d0a207c2beac79d627 (on hermes/wave5-totals-tiebreak; merged into hermes/night-2026-09-04): scoreTotalPick strict branch + GameContextInput.totalsTiebreak field + replay plumbing; scoring.test.ts strict suite 5/5 incl. A/B test proving the flag changes behaviour on identical input; pinned totals-consensus-tiebreak.test.ts UNCHANGED and green; historical-replay* 4 files green. Replay evidence (totals-tiebreak-replay.ts, seasons 2023-2025, 816 REG games, identical target both modes): legacy 816/816 total picks, 100% OVER, mean confidence 67, decided 0.5068 (411-400-5); strict 0 picks — every replayed total pick was manufactured by the <= tie-break. Doc: docs/calibration-proposals/2026-09-04-totals-tiebreak-strict.md (status PROPOSED, modelVersion v5.2.7 declared = current, no bump; model-freeze unaffected). Gates: tsc 0, lint 0, guardrails 25/26 (dependency-audit D4 flake 4th occurrence tonight, 40s, manifest-unchanged), test:fast 0, targeted 6 files 0. SELF-CAUGHT ×2: (1) first season slice assumed newest-first encounter order (archive oldest-first) processed 1 season/248 games — honest-corpus guard aborted; (2) JSON seasons field printed slice(0,3) not the processed target. Both fixed before commit |
| H-N6 | Re-audit of the 2026-09-04 night in a fresh session: re-verify every gate claim on merged head 432dbc055 with fresh tool exits, then hunt missed/under-leveraged/unpolished items | hermes | DONE | ba389e792 (docs commit carrying the night-log RE-AUDIT line + this row lands alongside; no second code commit): verification re-run correctly-invoked and green (typecheck 0, lint 0, test:fast 0, guardrails 26/26 exit 0 — first harness attempts used wrong runner path/bare tsc, self-caught and discarded before any claim); Wave 5 replay JSONs pinned from %TEMP% to docs/calibration-proposals/evidence/ with doc citation repointed; SPREAD pick'em sibling defect confirmed by throwaway probe (all-PK board publishes phantom pick conf 59, side AWAY by exclusion, consensus 1.0; 50/50 opposing lines correctly null) — recorded as re-audit addendum in the proposal doc + _dispatch3 Forecast List, NOT fixed (D1 scope + D5-adjacent live-path rule); D4 dependency-audit root-caused as degraded-response false STALE (next/high verified still live at 10:32 CST) — full diagnosis + proposed fix for the forbidden-path owner in docs/ops/DEPENDENCY_AUDIT_D4_DIAGNOSIS_2026-09-04.md; night log RE-AUDIT line appended; totals-tiebreak default-flip decision remains with the owner (unchanged); EXTENDED same session to all waves: W1 all 7 PR SHAs + 170aa1682 ancestors, fix code spot-verified, targeted suites 127/127 engine + 7/7 web; W2 metric tests 10/10 package-dir, nested clone confirmed removed; W3 replay-calibration re-run exit 0 reproducing every published number exactly (Brier 0.2106 CI [0.2050,0.2172] n=2750, isotonic +0.00007, leaf 65.86->57.12) with log pinned docs/data/MARKET_CALIBRATION_2026-09-04-reproduction.txt; W4 clv 11/11 + SYNTHETIC OPEN wrapping verified + archive contract printed, real-archive acquisition tracked in F-5; flagged 6.5-9.5 leaf drift noted as having no dedicated follow-up owner (D1-scoped, left unclaimed) |
| C12 | Close the pass (scripts/ops/chaos/c12-close-the-pass.txt): PART 2 7/7 verifications closed (cron auth enumerated 26/26, email_verified confirmed installed-package-level, D-4 columns confirmed, webhook dedupe confirmed, npm-audit matrix waived with reasons + dates, Neon PITR honestly NOT-RUN-console-only, 3/10 calibration-checked); five blockers fixed-or-decided — S1 liveBoardOn de-hardcoded (4 sites), S2 copy corrected + push opt-in mounted (D-2), S3 always-on 21+ attestation gate (16 prefixes, middleware + /age-verify, loop/redirect-safe), S10 ESPN disclosure on /data (feed use founder-ratify), #16 precision-corrected (comment not copy) with gate kept structural via free-only; PART 4 PAID_CHECKOUT_OPEN server-side choke on the single checkout path + one-line revert; PART 5 D-item artifacts (D-1/2/3/4/6/7/8/10; C11's D-0..D-15 text unrecoverable — honest gap recorded); PART 6 re-scores (free-only 6/10, paid 4/10); PART 7 2am handoff | hermes | DONE | this commit carries code+tests+docs/ops/launch/C12-02..06 (C12-01 landed in 4e5a58963); verify block real exits: typecheck 0, lint 0, lint:brand 0, targeted web 116/116 (board-classify-state, middleware-contract, subscriptions-checkout-route, auth, age-verify surface, deploy-readiness-stripe-prices), push stack 35/35; D-4 backfill dry-run REFUSED exit 2 with gate off (control verified live); no sealed path touched, no gate flipped, no Stripe flow exercised, no test skipped |
| H-N7 | Frontier night 2026-09-05 (hermes/night-2026-09-05): PR 704 opened for c12-close-the-pass; archived 12 genuinely-orphaned engine modules to attic/ after falsifying 5 of LEVERAGE 19 dead-code claims as transitively live (elo-estimator, hawkes-steam, nflverse-replay-parser, projection-evaluation, tweedie-aci); narrowed eprocess-property.test.ts to live forecast-skill props; closed research-lab algorithm gap (10 briefs mapped, paths existence-checked); ingested Wave-2 extract-data JSONs to gse-competitive-intel waves/ on branch hermes/wave2-ingest-2026-09-05 + remote added; hunts all NOT FOUND (cron auth 25/25 via cronAuthError incl. generate-signal-slate, Stripe webhook hardened, dev/state prod-gated, JSON-LD escaped, NEXT_PUBLIC clean, no SSRF-shaped routes, rate-limit infra present) | hermes | DONE | c92964d23 on main (squash #705; supersedes branch SHAs a69a3d435 + 8ed40dc6c, unreachable after --delete-branch); engine 275 files/3072 tests green, typecheck 0, lint 0, guardrails 26/26 |
| H-N8 | Frontier night 2026-09-05 closeout (hermes/night-2026-09-05): full web 12010 passed/0 failed, engine 275 files/3072 tests green, guardrails 26/26 exit 0, typecheck/lint clean (c12 branch); Wave-3 research-lab 41 mapped paths existence-checked, Rumor Triage row clarified to metrics/market/ canonical paths; diagnosed 21:54 full-run 10 failures as branch-mixing artifact (C12 test file on main-tree worktree) + sealed-holdout 60s load-flake, neither a real regression | hermes | DONE | c92964d23 on main (squash #705; supersedes e9d2ebf80, unreachable after --delete-branch); web 12010/0, engine 3072/3072, guardrails 26/26 |
| C-80 | Settlement CRITICAL root cause (36 overdue, 2026-09-05): ESPN limit=1000 collapses the board to 25 events (limit 300 + FBS/FCS + D-I groups), matcher held or mis-graded on bare fc/sc/united tokens, 2-3 letter abbreviation containment and union-side matching (now bipartite, exact abbreviations, diacritics folded), runner skipped within-grace rows whenever one hold existed, backfill kept the newest dates | claude | DONE | 2294db2 6880f18 6cd5eda f987259; live ESPN probes and the real 2026-08-29 MLS board replay (13/13 settle, zero holds) in the tests; SQL read-only: 22 MLS + 13 MLB + 1 NFL, zero settlement observations for any of them |
| C-81 | Signal slate overwrote book-priced MONEYLINE picks every cycle (selection "(model signal)", line 0, bookmakerCount 0, marketFairProb null while the receipt kept the book) and showed FREE viewers the gated number as "@ NN%"; guard added, teaser scrubbed at write and read, confidence-tail byMarket populated | claude | DONE | 31564d9 ef24e77 67730a6; SQL: MLB 450 signal rows vs 19 book-priced, 46 NCAAF signal rows written 15:54-16:05 UTC over today's slate |
| C-82 | C-28 Phase 0/1: calibration loaders read the receipt's publish-time market probability when the factor breakdown lost it (coverage was 28/738 settled ML = 3.8%), score bake-off reported per market, operator note restated to the real p hierarchy | claude | DONE | 8a8f292 3eaf058 9a11cc2; floors unchanged; no gate touched |
| C-83 | Operator tooling for the backlog: GET /api/ops/settlement-rca (Bearer CRON_SECRET, dry-run of the production grader, reason per overdue pick, read-only), npm run ops:stale-picks (SELECT-only list of the 20 stale published picks) + OPERATOR_TASKS STALE-PICKS, generate-signal-slate and autonomy-cycle raised to the 300s cron ceiling (583 Vercel timeouts at 120s) | claude | DONE | a06d59f 0e9835c e957482 |
| C-84 | Finish-line plan and v5.2.8 proposal: docs/ops/LAUNCH_FINISH_LINE_2026-09-05.md (production facts, fixes, founder-only actions, WP-1..WP-12 with acceptance commands) and docs/calibration-proposals/2026-09-05-market-anchored-display-probability-v5.2.8.md (status PROPOSED; model-freeze exit 0) | claude | DONE | e14463b and the docs commit carrying this row |
| F-14 | FOUNDER (DECIDED 2026-09-05 by delegation, revised 19:05 UTC: YES, NOW; Phase 2 = WP-1 starts immediately as the PROVEN unlock; production read-only SQL on settled MONEYLINE picks with receipts: n 150, Brier 0.1692, Murphy REL 0.0050, ECE 0.0552 on 10 bins; excluding soccer two-way moneylines by the engine's own publication rule: n 115, Brier 0.1444, ECE 0.0440, Murphy REL 0.0044, all four floors met today; founder approved the switch and cron triggering 19:20 UTC; recorded in the proposal doc section 1 and LAUNCH_FINISH_LINE sections 3c and 3d; measurement side shipped fbc3784c7 with receipt-first scoring, MONEYLINE-only pooled floors and a basis-aware streak; receipts carry a mean-implied proportional de-vig, not Shin, and the proposal wording was corrected; display label and claim copy remain as C-107): decide v5.2.8 (market-anchored displayed win probability; confidence stays a 0-100 selection score never rendered as a percent; restated public calibration claim). YES flips the proposal doc to IMPLEMENTED and dispatches WP-1 | founder | OPEN | docs/calibration-proposals/2026-09-05-market-anchored-display-probability-v5.2.8.md section 1; read provenPath.scoreBakeoffByMarket (marketFairProb on MONEYLINE) after the next calibration-metrics cron before deciding |
| F-15 | FOUNDER (TOP ITEM): The Odds API account is in a payment state: HTTP 402 payment circuit open on every hourly settle cycle since 2026-09-03 20:20 UTC (31 groups per sport, Vercel), 401 INVALID_KEY on NFL until 2026-09-04 23:20; the shared breaker stopped paid book odds at 2026-09-05 00:30 UTC (last fanduel/draftkings/betmgm rows) and zero book-priced picks were generated in the 18h after (SQL 18:00 UTC); fix the account for Week 1 only if WP-27 (C-104, the completely free two-book board) cannot land before Thursday; do not upgrade the tier; check account/plan/quota before NFL Week 1, the truth surface only reports that a key value exists | founder | DONE | DONE 2026-09-06 02:37 UTC by the browser agent (#709, plan 3e script A and 3f): 20K key rotated on the dashboard, THE_ODDS_API_KEY set in Vercel Production, redeploy Ready 02:37:12 UTC; one 402 on the old key at 02:37:15 UTC during rollover, none in the 24 minutes after (Vercel logs); dashboard usage 0 to 112 credits by 03:02 UTC; truth surface oddsInserting lastSuccessAt 03:03 UTC with 242 rows; follow-up C-109 (credit governor) because that burn exhausts the plan before Week 1 ends; history: mcp Vercel get_runtime_errors 48h, 2026-09-05 15:4x UTC; REVISED 2026-09-06 02:25 UTC from founder dashboard screenshots: account Active on the 20K plan ($30/mo, next invoice Sep 22), 0 of 20,000 credits used this period, free key 0 of 500, so the production key is stale, not unpaid; founder is setting THE_ODDS_API_KEY in Vercel Production to the 20K key and redeploying (breaker is process-local, redeploy resets it); browser agent verifies only (plan 3e script A); no tier change, no key pasted anywhere |
| F-16 | FOUNDER (DECIDED 2026-09-05 by delegation: UNPUBLISH every row the selector finds; REVISED 2026-09-06: automated by WP-29 (C-106, coder priority 1) so no human runs it; the owner tool e8a3731dc stays as fallback only; 18 stale rows on the live surface at 02:08 UTC): adjudicate the 20 stale published PENDING picks on unstarted NFL/NCAAF games before Thursday; list with npm run ops:stale-picks; procedure in OPERATOR_TASKS STALE-PICKS | founder | OPEN | truth surface stalePendingPicks.count=20 on 2026-09-05 |
| F-17 | FOUNDER: run the owner game-merge tool over the duplicate Game rows (same fixture under odds-api hash, espn:<short>: and espn:<oddskey>: namespaces; MLB 268 fixtures with 2-3 rows, NCAAF 102, MLS 75, NFL 34 in the 21d window, zero merged) after WP-3 stops new duplicates | founder | OPEN | SQL read-only 2026-09-05; apps/web/lib/ops/game-merge-plan.ts, npm run ops:merge-games |
| C-85 | WP-3: unify the ESPN externalId namespace for new Game rows (espn-schedule-seed.ts:121 writes espn:<short>:<id>, espn-odds-client.ts:361 writes espn:<oddsKey>:<id>); one form, upsert on it, never a third row per fixture; existing rows are F-17 | — | OPEN | docs/ops/LAUNCH_FINISH_LINE_2026-09-05.md section 5 |
| C-86 | WP-7: MLB and MLS TOTAL picks are 0 with 41/51 games in the 72h window while pause groups cover MONEYLINE/SPREAD only; find the exact gate (process-sport totals path, selective runtime) and fix or label it on the coverage surface | — | OPEN | truth surface marketCoverage 2026-09-05 |
| C-87 | WP-2/4/5/6/8/11/12 settlement and honesty follow-ups: cockpit reason column from settlement-rca, board fetch memoization + 240s soft deadline in settle-picks, doubleheader tie window 90m, CROSS_PATH_SCORE_CONFLICT rca code, CLV entry price for rows first written by the signal slate, hadOddsSignal from bookmakerCount, CONFIDENCE_DISPLAY_MODE wired | — | OPEN | each has files and an acceptance command in LAUNCH_FINISH_LINE section 5; one PR per package |
| C-88 | WP-9/10: B2B /api/v1/probabilities publishes confidence/100 as pModel and the public proof page computes model-vs-market from confidence/100; Edge Index home copy says "calibrated" while maps are OFF | — | OPEN | apps/web/app/api/v1/probabilities/route.ts:88-91, apps/web/lib/proof/load-proof-of-record.ts:262-269, components/home/annotated-sample-signal.tsx:35 |
| C-89 | Money path, picks and public frontend honesty batch: reconcile-entitlements and repair-checkout-attempts at distinct minutes (45 and 50), publicEdgeScore withholds the edge number when it would reveal a gated probability, dashboard post-checkout banner truthful for FREE, US-Eastern slate-day window on /api/picks and daily-slate (80 of 91 NCAAF book picks were hidden), signal-slate soccer skip and neutral copy, "book-priced" receipt claim, dashboard confidence as NN/100, SentientShell unmounted from public routes, pricing and picks copy derived from the PUBLIC_PICKS gate | claude | DONE | a7ca36aef 00931f08c 486b0a34a 19937d8e5 66316461c 246fc9efe 994a2fa88; typecheck 0, lint 0, trust-gate 0; full web suite 12135 passed with 2 stale source invariants, both repaired in 994a2fa88 |
| C-90 | WP-25: gate the ESPN Power Index fetch in the signal slate behind the rights registry (fail closed, attribution) until the founder clears FPI use (F-21); copy never names the source to anonymous viewers | claude | DONE | 44735ad04 package-level gate (independent-source-rights.ts, ESPN_POWERINDEX_LICENSED unset = off, client never called; injectable env; 304 tests green; adversarial review verdict SHIP); anonymous copy stopped naming the source in 31564d9 |
| C-91 | WP-13..19 money-path hardening: Stripe unpaid never access-granting, Stripe-side double-subscribe guard, no-store on every entitlement gate body, env-path price amount verification, 503 on missing webhook secret, isPremium helper replaces inline tier predicates, durable rate limiter on checkout and portal | — | OPEN | files and acceptance per package in LAUNCH_FINISH_LINE section 5; 174 money-path tests green on the branch |
| C-92 | WP-20..24 pick payload: signal slate batches per sport and refreshes its own rows, no game.dataQualityScore write from the slate, line null and priceSource for book-less moneylines, isPublished on create only, per-market totals drop reasons on the coverage hint | — | OPEN | LAUNCH_FINISH_LINE section 5; WP-23 must not write the gate into pickUpdateData (would re-publish owner-unpublished rows) |
| C-93 | FE-02/05/08/09/10/11/12/13/14/15/17/18 public frontend launch quality: one floating launcher per corner, "No book price attached" pill and suffix stripped on signal cards, checkout resume after the sign-in bounce, main-content id on every page, plain-language gate copy, PICK_GRADE_LABELS on the dashboard, sitemap and noindex for illustrative pages, no double brand in titles, design-token drift, FAQ and About match the paywall, computed annual savings | — | OPEN | LAUNCH_FINISH_LINE section 5 FE table; FE-01/03/04 shipped in 994a2fa88 and 486b0a34a; no screenshots possible from the sandbox (connection reset through the proxy) |
| C-94 | FAN-01/02/03/05/06/07/08/10/11/12/13/14 fantasy suite honesty: label the 2025 basis season on every live surface and advance it when 2026 REG rows exist, feed the synced Sleeper roster into Start-Sit Waivers Trade, projections badge off above fictional tools, FREE trimmed-pool disclosure and upsell, hub readiness derived from providers, pricing Fantasy features match canUseFantasyFull, GM Ledger demo banner, per-game units on Start-Sit, studio pinned to the demo pool, scheme fit removed from live copy, projection-method decision, baseline text | — | OPEN | LAUNCH_FINISH_LINE section 5 FAN table; FAN-04 and FAN-09 shipped in 74e8e381f (host.ts disclosure, lib/fantasy/sleeper-season.ts) |
| C-95 | NFL-05/06/07 Week 1 follow-ups: free grader and backfill stamp line-archive CLOSE (markClosingSnapshotsIfEnabled after a successful settle write), hasRegRows probe so the nflverse display season advances to 2026 once REG rows land, ESPN schedule seed covers every Eastern day in the horizon | — | OPEN | packages/ingestion-pipeline/src/settle-sport.ts:716 is the only CLOSE caller today; packages/data-ingestion/src/nflverse-season.ts:29-83; espn-schedule-seed.ts:134-152 |
| C-96 | NFL-04 code half: surface the last refresh's Odds API remaining-request header on /api/ops/public-surface-truth and the health alert, and skip the paid getScores supplement for out-of-season sports in settle-picks (free path unchanged) | — | OPEN | packages/data-ingestion/src/config.ts:52-69 and :101; apps/web/app/api/cron/settle-picks/route.ts:246; founder half is F-26 |
| F-18 | FOUNDER: set the public Terms URL in the Stripe Dashboard, then STRIPE_TERMS_CONSENT_ENABLED, then redeploy; watch the consent checkbox in the live checkout smoke | founder | OPEN | docs/ops/OPERATOR.md section 5 ordering rule; LAUNCH_FINISH_LINE section 4 item 9 |
| F-19 | FOUNDER: confirm no Founding Payment Link (scripts/ops/create-founding-payment-link.mjs) is active or shared; they charge without granting access by construction | founder | OPEN | LAUNCH_FINISH_LINE section 4 item 10 |
| F-20 | FOUNDER: the Stripe webhook endpoint must subscribe to all ten handled events (the go-live checklist lists seven; missing checkout.session.expired, invoice.paid, charge.refunded) | founder | OPEN | apps/web/app/api/webhooks/stripe/route.ts handled-event list; LAUNCH_FINISH_LINE section 4 item 11 |
| F-21 | FOUNDER (DECIDED 2026-09-05 by delegation: gate the fetch fail-closed at the package layer, ESPN_POWERINDEX_LICENSED unset = off; NFL and NCAAF signal picks stop until a license exists): ESPN Power Index rights decision | founder | OPEN | rights registry allows ESPN facts only; C-90 is the engineering floor; remaining founder action is only the license itself |
| F-22 | FOUNDER: one live production checkout smoke end to end, then refund | founder | OPEN | LAUNCH_FINISH_LINE section 4 item 4 |
| F-23 | FOUNDER: the X handle @GalaxySportsAI is stamped into twitter:site and creator on every page and linked in the footer; rename or register a handle matching the domain, then update lib/brand.ts SOCIAL.x and derive ORG_HANDLE from it | founder | OPEN | apps/web/app/layout.tsx:89, apps/web/lib/brand.ts:62-67, components/layout/footer.tsx |
| F-24 | FOUNDER: first-visit media weight about 10.5 MB plus an 8s full-screen video interstitial (gse-reveal.mp4 4.16 MB) before any content; decide montage default off for organic visits and a hero still under 250 KB | founder | OPEN | apps/web/components/landing/montage-entrance.tsx; ls -la apps/web/public/brand/gse-reveal.mp4 |
| F-25 | FOUNDER: every /fantasy/* URL 307s anonymous visitors to the 21+ age gate (live curl 2026-09-05); decide whether /fantasy stays in AGE_GATED_PREFIXES or only contests and props do | founder | OPEN | apps/web/lib/age-verify/surface.ts:44, apps/web/middleware.ts:53-57 |
| F-26 | FOUNDER: The Odds API plan tier vs projected burn (about 34.6K credits per 30 days in September at */15 with 4 in-season sports and 3 markets, about 51.8K in October); confirm the plan on the vendor dashboard and upgrade or reduce non-NFL cadence | founder | CANCELLED | superseded 2026-09-05 by the delegated decision in LAUNCH_FINISH_LINE section 3b item 1 (fix the account, do not upgrade the tier, keyless path becomes primary: C-103); config.ts:52-69 and :101 remain the burn reference |
| F-27 | FOUNDER: Contest Bay bootstraps gse_contest_entries and gse_contest_settlements with runtime DDL outside Prisma migrations; land a migration before CONTESTS_PUBLIC ever opens | founder | OPEN | apps/web/lib/contests/store.ts:113-146; contestsPublic false on the live truth surface |
| F-28 | FOUNDER: the home hero has no path to /picks or /pricing and the IA ships two competing today surfaces (/board and /picks) plus two calibration-named proof pages; decide the primary CTA and which surface is the product | founder | OPEN | apps/web/app/page.tsx:129-137 and :170; nav-auth.tsx:98 |
| C-97 | OPS-02/04/06/08/09 operations hardening: durable rate limit on public-surface-truth and health, autonomy observation built from the real loaders (no hard-coded boardSuppressed true), withErrorCapture around checkout webhook and picks, autonomy executor timeout budget inside maxDuration, delete or test-fence the unreachable cron auth dual mode | — | OPEN | apps/web/app/api/ops/public-surface-truth/route.ts:150-166; health-alert/route.ts:165-167; autonomy-cycle/route.ts:121-136; lib/cron/authorize.ts:10-24 |
| C-98 | OPS-01 code half: alerting posture booleans (health webhook configured, Sentry DSN configured) on the truth surface and cockpit so a CRITICAL band that pages nobody is visible | — | OPEN | apps/web/app/api/cron/health-alert/route.ts:87-110 and :215-252; mirror lib/ops/autonomy-posture.ts; founder half is F-29 |
| C-99 | OPS-03 code half: durable lease on settle-picks (JarvisMemoryEvent pattern from traffic-heartbeat.ts) and a recency guard in the autonomy kernel so three schedulers stop double-firing refresh-odds and calibration-metrics | — | OPEN | vercel.json; .github/workflows/external-cron.yml:28-44 (frozen for agents); lib/autonomy/operating-kernel.ts; the founder picks the primary scheduler (F-30) |
| C-100 | OPS-05/07 alert copy and runbook drift: hours-in-band and overdue count in the health-alert head line, escalation by band age, LAUNCH_DAY_RUNBOOK and GO_LIVE aligned with strict health, settlement-rca and autonomy posture | — | OPEN | docs/ops/LAUNCH_DAY_RUNBOOK.md:89-103; docs/ops/CRON_MATRIX.md:27-36; apps/web/lib/ops/health-alert-decision.ts:69-72; the post-publish calibration drift alert (durable marker, health-alert unhealthy signal, truth surface) shipped in fbc3784c7 |
| C-101 | TCI-3/6/8 test-suite gaps: anonymous /api/picks teaser contract by invoking the route, explicit timeout on the sealed-holdout repo walk and node environment for route tests, worker smoke tests (TCI-2 signal-slate publish-rule assertions and TCI-5 packages/db test script shipped in 3da74b4de) | — | OPEN | apps/web/__tests__/picks-stale-kill-switch.test.ts is the mocking precedent; apps/web/__tests__/sealed-holdout-open-scan-guard.test.ts:56-61; workers/* have no tests |
| F-29 | FOUNDER: confirm HEALTH_ALERT_WEBHOOK_URL (or ALERT_WEBHOOK_URL) and SENTRY_DSN are set in Vercel Production and as the GitHub Actions secret; today no surface shows whether a CRITICAL band pages anyone | founder | OPEN | health-alert logs BLIND or UNDELIVERED to console only; NOT VERIFIED from the repo (env read-denied) |
| F-30 | FOUNDER: three schedulers drive settle-picks about six times an hour (Vercel cron, GitHub external-cron.yml, autonomy executor); pick the primary and trim the others; also enable the four env-gated Postgres suites and point the ci.yml brand-safety step at npm run test:brand-safety (workflow files are frozen for agents) | founder | OPEN | .github/workflows/external-cron.yml; ci.yml test job env (CHECKOUT_ATTEMPT_DB_TEST_URL, PR159_PG_URL, AI_CLAIM_PG_URL, SLATE_OPENING_PG_URL); brand-safety step lists 7 files vs 19 |
| C-102 | SEC-01/02/03/04/05/06/08 security follow-ups (no P0 or P1 found): admin trigger-refresh server action calls the handler directly under requireAdminActor (the fetch-to-self drops the session cookie), emailVerified stamp awaited with a fail-closed catch in the jwt callback, clientIp() in the five routes keying on raw X-Forwarded-For, safeEqualSecret in the waitlist Basic Auth gate, expectedMainFeatures and provider env-var names off the anonymous truth surface, nonce-based CSP, /api/promotions via jsonNoStore | — | OPEN | apps/web/app/admin/page.tsx:112-122; lib/auth.ts:93-97; lib/waitlist/access-gate.ts:59-62; api/ops/public-surface-truth/route.ts:610 and :815; next.config.mjs:87-88; api/promotions/route.ts:48-53 |
| F-31 | FOUNDER: open PR triage (30 open, read-only git against origin/main 1a3f00d05): close the 9 merged or superseded (#697 #698 #696 #625 #686 #687 plus the merge-train branches), update and merge the 5 clean production-relevant branches (#665 #668 #666 #660 #669) after CI, split #693 by original PR onto fresh branches (11 of its 12 re-landed money and access-control fixes never reached main; 11 conflicts), close the 4 mega-branches (#663 #674 #672 #675) with the two-item salvage list, treat #670 as a v5.3.0 calibration proposal not a cleanup, hold #437 until after Week 1, rebase #667 yourself (sealed guardrail path) | founder | OPEN | LAUNCH_FINISH_LINE section 5 (PR triage); NOT VERIFIED: GitHub PR metadata, only git refs were read |
| F-32 | FOUNDER: one secret (CRON_SECRET) authorizes both cron mutations and the read-only operator surfaces (settlement-rca, daily-truth, detailed truth); add OPS_READ_SECRET in Vercel once lib/ops/ops-auth.ts accepts it (C-102 companion) | founder | OPEN | apps/web/lib/ops/ops-auth.ts:12-14 |
| C-103 | WP-26 BRIDGE ONLY and optional (founder position on PR #680: not Rundown; the product path is C-104): TheRundown quota discipline: Phase A durable 429 cooldown (IngestionRun marker rundown:429, RUNDOWN_429_COOLDOWN_MINUTES default 60), freshness throttle from the newest Rundown-affiliate odds row (RUNDOWN_FALLBACK_MIN_INTERVAL_MINUTES default 30), provider and providerNote per sport, oddsInserting.lastProviderNote on the truth surface; Phase B board-fill skips the second odds pass inside the interval and Rundown daySpan follows the board horizon; Phase C re-land the PR #680 core after Week 1 | — | OPEN | NOT STARTED: the implementation run was stopped 2026-09-05 18:30 UTC to save usage; the design is complete in LAUNCH_FINISH_LINE WP-26 Phase A. ROOT CAUSE (production logs 2026-09-05, every 15-minute cycle, all four in-season sports): rundown empty (2d): HTTP 429 rate_limited; ESPN core odds returns exactly one provider (DraftKings) for NFL CFB MLB MLS so MIN_BOOKMAKERS = 2 cannot hold without Rundown or the paid feed |
| C-104 | WP-27 GSN API activation, the completely free two-book board (founder position on PR #680: we are the provider, not Rundown, not The Odds API): (1) re-land the PR #680 core onto main (GalaxySportsApiOddsProvider reading ESPN inline odds via site.web.api, de-vig fair_prob, 8s timeouts, galaxy-espn-inline registry entry, provider selection); (2) select the Galaxy provider also when the paid circuit is open, not only when THE_ODDS_API_KEY is unset; (3) feed galaxy-kalshi-book.ts from the PredExon Kalshi catalog (predexon-client.ts on main, verdict use-with-caution so assertIngestible passes, PREDEXON_INGEST plus PREDEXON_API_KEY, catalog cached per cycle for the 1 rps and 1k per month free tier) instead of the direct Kalshi client; (4) extend the Kalshi book from H2H to NFL SPREAD and TOTAL using series KXNFLSPREAD and KXNFLTOTAL in kalshi-series.ts; (5) acceptance: with the paid key blank in a test, processSport mints MONEYLINE and NFL SPREAD/TOTAL picks with bookmakerCount 2 from espn_public plus kalshi, none when either side lacks a live quote, data-ingestion and ingestion-pipeline suites green | — | OPEN | origin/hermes/galaxy-keyless-odds is 197 behind main and its PR #680 CI was green; nothing on main consumes predexon-client.ts yet (git grep 2026-09-05); ESPN core odds returns one provider (DraftKings) verified live 2026-09-05; the founder holds a PredExon dashboard key (registry note 2026-08-21) |
| F-34 | FOUNDER: set PREDEXON_API_KEY (the dashboard key you already hold) and PREDEXON_INGEST=true in Vercel Production once WP-27 (C-104) lands; then blank THE_ODDS_API_KEY if you want the keyless Galaxy path to take over immediately (after WP-27 step 2 it also switches on its own while the paid circuit is open) | founder | OPEN | packages/data-ingestion/src/predexon-client.ts env names; PredExon free tier 1 rps and 1k requests per month, Kalshi list-markets documented free and unlimited |
| F-35 | FOUNDER: ESPN inline odds rights posture for the Galaxy path: the app registry espn-public-api entry is facts only with commercial_display_allowed false, while the PR #680 package entry galaxy-espn-inline is use-with-caution with commercialUse true; reconcile the two (an ESPN license, or a written posture that relayed sportsbook prices are facts with attribution) before the keyless path is the only book-priced source | founder | OPEN | apps/web/lib/scraping/source-rights-registry.ts espn-public-api; origin/hermes/galaxy-keyless-odds packages/data-ingestion/src/source-registry.ts galaxy-espn-inline |
| C-105 | WP-28 publish-time market probability from the odds table for the 610 receipt-less settled moneyline picks: third fallback in live-calibration-p.ts after the factor breakdown and the receipt, odds rows with fetchedAt at or before generatedAt, latest snapshot per bookmaker, at least MIN_BOOKMAKERS books, de-vig with consensusNoVig, picked side; read time only, zero writes; loaders in proven-path-seed.ts, calibration-eligibility-durable.ts and calibration-metrics/route.ts pass the rows or a resolver | claude | DONE | fbc3784c7 (publish-time-market-p.ts pure resolver plus one-query loader, provenance counted, same mean-implied proportional de-vig as the receipts, adversarially reviewed); query shape reused from apps/web/lib/settlement/free-path-clv.ts:68-90; acceptance: fixture test with three bookmaker rows, no-rows pick stays excluded, scoreBakeoffByMarket MONEYLINE coverage above 0.7 on the next cron run; LAUNCH_FINISH_LINE section 3c step 2 |
| C-106 | WP-29 zero-sit settlement policy (founder decision 2026-09-05: no pick ever sits): PENDING more than 24h after commenceTime with rca reason NO_FINAL after every free source and the backfill lane is VOIDED through the settlement outbox lane (PickSettlementEvent VOID with the RCA code in the payload, never deleted); published PENDING picks on unstarted games past STALE_PENDING_PICK_MAX_AGE_DAYS are unpublished by the same cron via scripts/ops/lib/stale-pending-picks-selection.ts; both counted on the truth surface and the health alert; owner tool kept for manual overrides | claude | DONE | DONE verified 2026-09-06T19:10:55.263Z UTC (the truth-surface production reading; the settlement event it verifies fired earlier at 19:07:18.519 UTC, the two are deliberately different clocks and neither is a typo). The acceptance was always a production reading, law 5. Truth surface generatedAt 2026-09-06T19:10:55.263Z reads settlement.overduePending 0 of 2627 commenced picks (health HEALTHY) and stalePendingPicks.count 0, on deployed sha 21f60ffb5. The last two overdue picks (SPREAD "Washington Huskies -21.5" cmpiuhhcc0399s0l5zq580dy8 and MONEYLINE "Washington Huskies ML (model signal)" cmtnolh600001axs0aftxxw5k, both on the C-111 phantom fixture Washington Huskies v Washington State Cougars, commenceTime 2026-09-05T19:00Z) were VOIDED through the settlement outbox lane at 19:07:18.519 UTC, event delivered 19:07:18.840 UTC, status DELIVERED, payload kind ZERO_SIT_VOID, lane zero-sit, actor system:settle-picks:zero-sit, rcaCode FIXTURE_NOT_FOUND, reason "The free ESPN scoreboard for 2026-09-05 (103 event(s)) lists no event pairing Washington State Cougars with Washington Huskies; Washington Huskies appears against another opponent, so the game row is left as is" (read-only SQL on picks joined to pick_settlement_events). Voided, never deleted, and the game row was correctly NOT cancelled because one team appears against a different opponent, which is the conservative branch this row specified. The earlier four phantom-fixture picks (Ole Miss v Louisville, Illinois v UAB) voided the same way at 16:07:14.975 UTC. Shipped in #711; code landed b4885f214 (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): apps/web/lib/settlement/zero-sit-lane.ts runs at the end of every settle cycle: stale published PENDING picks on unstarted games past STALE_PENDING_PICK_MAX_AGE_DAYS unpublished and recorded; PENDING picks more than 24h past commence VOIDED through the settlement outbox lane with RCA OVERDUE_NO_SCORE (the lane's code for the NO_FINAL pending reason), SCORE_MISMATCH_CROSS_PATH (24h unresolved), AMBIGUOUS_TEAM_NAME (cause MULTIPLE_FINALS for doubleheaders) or FIXTURE_NOT_FOUND (ESPN board fetched with strictEspn, all division groups required, fetch failure voids nothing; rows CANCELED only when neither team appears); preferLongerTeamName in the process-sport upsert update branch; stale-pick-policy.ts and the truth-surface hint state the automatic policy; tests settlement-zero-sit-lane.test.ts, free-adapters.test.ts, multi-source-scores-dated-clearance.test.ts, settle-picks-free-first.test.ts (60 passed); typecheck 0, lint 0, guardrails 26/26; adversarial review approved after one fix (fail-open on a partial ESPN board). DONE flips when the truth surface reads overduePending 0 and stalePendingPicks 0 after the first settle cycle post-deploy (law 5: the acceptance is a production reading). CODER PRIORITY 2 after C-109 (2026-09-06: the founder wants no human step; the stale half ships first, the void half second); 2026-09-06 03:20 UTC diagnosis of the 16 overdue (read-only SQL plus Vercel logs, plan 3f item 2): 10 MLB SPREAD picks (v5.2.7, 2026-09-01 to 03) on game rows with city-only names (Tampa Bay v New York, Chicago v Milwaukee, Los Angeles v New York, Los Angeles v St. Louis, Houston v Chicago; 32-hex externalIds), 7 of them refused every cycle as [free-settle] SCORE_MISMATCH_CROSS_PATH (existing 3-1 vs incoming 3-4 and similar, "left PENDING for human review"), which is what ambiguous names produce when two feeds match two different real games; the paid scores window (daysFrom 3) has passed for 9 of the 10, so only the void lane clears them: VOID with RCA AMBIGUOUS_TEAM_NAME or SCORE_MISMATCH_CROSS_PATH after 24h, never human-review forever; also apply preferLongerTeamName in the plain upsert update branch (process-sport.ts:609-612), which today overwrites a full name with a feed's city-only name; the other 6 overdue are the phantom fixtures in C-111; stale count 26 at 03:02 UTC; replaces the owner-decides stance in apps/web/lib/board/stale-pick-policy.ts:16-20; entry points settle-picks/route.ts, root-cause-analysis.ts, settle-backfill.ts; acceptance: 25h NO_FINAL pick voided with an outbox event, 23h pick untouched, stale unstarted pick unpublished, overduePending and stalePendingPicks read 0 after one cycle |
| F-36 | BROWSER AGENT (revised 2026-09-06, founder wants no human step): the streak runs on the schedule (40 */6 UTC) and the durable publish receipt is written automatically at streak three (evaluateAndPersistEligibility auto-publish); after the truth surface shows that receipt AND C-107 is deployed, set PERFORMANCE_STATS_ENABLED=true and PRICING_PHASE=PROVEN in Vercel Production and redeploy (plan section 3e script B) | browser | OPEN | CALIBRATION_ELIGIBILITY_STREAK default 3 (apps/web/lib/ops/calibration-eligibility-durable.ts:237); floors measured 2026-09-05 19:05 UTC in LAUNCH_FINISH_LINE section 3c; agents never run a cron with a secret |
| C-107 | WP-1 display side of v5.2.8 Phase 2: the pick card and the picks API show the market-implied win probability on book-priced two-way moneyline picks with the verified label (mean implied probability across books, proportional de-vig, fixed at publish time in the receipt), the public calibration claim is restated with the MONEYLINE-only scope, then the proposal status flips to IMPLEMENTED and MODEL_VERSION to v5.2.8 in the same commit | claude | CLAIMED | display half landed f06be6b31 (2026-09-06 04:55 UTC): pick card and /api/picks show "Market-implied win probability NN%" with the verified label on receipted two-way moneyline picks, N from the immutable signal snapshot bookmakerCount, under the confidence entitlement and never in the FREE branch, nothing on signal-slate, SPREAD or TOTAL picks (apps/web/lib/picks/market-implied-display.ts, tests market-implied-display.test.ts, pick-card-market-implied.test.tsx); the public claim is restated MONEYLINE-only but reworded to "The calibration we measure ourselves on is ..." because the public /calibration chart still buckets by confidence (apps/web/lib/calibration/compute.ts, BUCKETS and bucketFor()), so the founder accepts the rewording or a later row re-scopes that chart to marketFairProb; FE-05/FE-10/FE-15 shipped in the same commit (preview page displaySelection and the ConfidenceBadge percent path under CALIBRATION_ADJUSTMENTS are follow-ups). CLAIMED 2026-09-06 03:50 UTC by the launch session on claude/sports-prediction-launch-rtiexc for the DISPLAY half only (pick card and picks API label, MONEYLINE-only claim copy); the IMPLEMENTED flip and the MODEL_VERSION v5.2.8 bump stay sequenced after the first clean NFL Sunday (2026-09-13) per the standing decision and the model-freeze guard; measurement side shipped fbc3784c7 and merged in cff3e72d7; label and claim text in docs/calibration-proposals/2026-09-05-market-anchored-display-probability-v5.2.8.md section 1; never write the banned four-letter betting word into copy or tests (trust-gate); signal-slate picks keep showing no percentage |
| C-109 | WP-30 The Odds API credit governor (decision 2026-09-06, delegated: the 20K plan must last the month, no tier change): (1) honor the existing spend guard in packages/ingestion-pipeline/src/settle-sport.ts:181-187, which today logs "paid scores fetch not justified" and proceeds, so paid getScores runs only when the free path reported NO_FINAL for that sport's overdue picks, and at most once an hour; (2) settle-picks is invoked five times an hour (the :20 cron plus the autonomy cycle RUN_FREE_SETTLE target at 7,22,37,52 via apps/web/lib/autonomy/execute-autonomy-cycle.ts:29), so the paid supplement must not follow that cadence; (3) refresh-odds iterates all seven SUPPORTED_SPORTS every 15 minutes (packages/data-ingestion/src/config.ts, apps/web/app/api/cron/refresh-odds/route.ts:121-123) including NBA, NCAAB and NHL out of season: skip a sport with no event in the next 48h on the free ESPN scoreboard; (4) pace to the plan: x-requests-remaining is already parsed (packages/data-ingestion/src/odds-api-client.ts:255), hold paid odds calls when remaining divided by hours to month end is below the current hourly rate; (5) surface remaining credits and projected exhaustion on the truth surface oddsInserting.dualPath; acceptance (REWORDED 2026-09-06 by founder decision: the rule was right and the wording was wrong, no code change): the plan survives to the credit reset with no exhaustion, average burn over the period at or below 645 a day, and the hourly reserve mode engages automatically; unit tests for the pacing math; no change to MIN_BOOKMAKERS or any gate. The invariant is that the 20K plan lasts the period and the board never goes fully dark, which the reserve-pace floor plus the hourly floor deliver; a hard 600-a-day cap was rejected because it would throttle a normal NFL Sunday (4 live sports at 15-minute cadence is about 1,150 a day) while credits are plentiful, and the pace rule already degrades to hourly (about 290 a day) once remaining divided by hours left drops below 25/h | claude | DONE | DONE 3359e072a (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): pure governor packages/data-ingestion/src/odds-credit-governor.ts (600 a day budget, hold below pace to UTC month end, one odds call per live sport per hour kept, one probe per sport per hour on a stale zero, header-less 0/0 never recorded), append-only credit ledger odds-credit-ledger.ts (JarvisMemoryEvent pattern), settle-sport.ts honors the spend guard with justification from the settle-picks route (apps/web/lib/odds/paid-scores-justification.ts) and at most one paid scores call per sport per hour across the cron and the autonomy cycle, refresh-odds skips a sport with no ESPN event in 48h (fetch failure never skips), truth surface oddsInserting.dualPath.credits; tests odds-credit-governor.test.ts, settle-sport.test.ts, refresh-odds.test.ts, paid-odds-governor.test.ts, paid-scores-justification.test.ts, settle-picks-paid-scores-governor.test.ts (164 passed); typecheck 0, lint 0, guardrails 26/26; adversarial review approved after one fix (zero-credit deadlock). Disclosures: pacing to UTC month end is CORRECT and is not a defect (founder ruling 2026-09-06, closed, do not re-raise): The Odds API dashboard states verbatim on the plan card "Monthly plans reset on the 1st of each month at 12AM UTC", so credits reset on the 1st and the Sep 22 invoice date is billing only; the once-per-hour marker is read-then-write, not atomic under true concurrency; governor-held sports report ok:true with note credit_governor_skip; the acceptance is the reworded one above, measured over the period rather than as an instantaneous cap. MEASURED 2026-09-06 14:00 to 14:50 UTC (18 samples of oddsInserting.dualPath.credits, 49 min wall clock, used 3308 to 3420): burn 136.5 credits an hour (3,276 a day), remaining 16,580, paceOk true at 28.3/h against the 25/h floor. Crossover into hourly reserve mode projects to 2026-09-07 08:20 UTC with about 14,192 credits left, then about 288 a day, roughly 49 days of runway against the 23.7 days to the Oct 1 reset: the plan survives with room. DECISION 2026-09-06 (founder-delegated): HOURLY_BUDGET STAYS AT 25, no code change. Tightening the reserve to 30/h was considered and rejected on the measurement: 16,580 over 585.2h is 28.3/h, already under 30, so it would engage hourly reserve mode on deploy and hold the board at one odds call per sport per hour for 24 days with 84 percent of the plan unspent, which is exactly what a hard daily cap was rejected for. Consequence to watch instead, and it is not a defect: at this burn the board runs on hourly odds refresh from about 2026-09-07 08:20 UTC through the reset, NFL Week 1 included; the lever there is refresh cadence, not the reserve floor. Also fixed on the same pass: the data-refresh worker built its governor with no source, so its credit observations were written as ops.odds.refresh-odds, i.e. as scheduled-cron spend from a separate process on its own cadence; it now passes "data-refresh-worker". The refreshOdds() callers (the cron, board-fill, free-spine-health, the traffic heartbeat) keep the "refresh-odds" label deliberately: refresh-odds is the function that spent those credits. TWO FURTHER FINDINGS, both investigated and NOT patched: (1) odds-credit-ledger reservePaidCallSlot treating an omitted atomicCapable as capable is NOT a defect and must not be "fixed" by defaulting it to false, which would silently drop the pg_advisory_xact_lock for any caller that forgets the flag and reopen the double-spend race C-109 exists to close; (2) process-sport still feeds the raw feed team names into enrichGameContext and the independent fair values, which reach exact-string-keyed TeamGameLog lookups (rest days, schedule density) and therefore CAN move a score under a frozen MODEL_VERSION, plus can flip a live PENDING pick's stored selection side: FOUNDER-GATED, see C-106 and do not land it mid-calibration-streak. Observed 2026-09-06 (browser agent, dashboard): 112 credits used between 02:41 and 03:02 UTC, about 320 an hour against 20,000 a month (645 a day); at that rate the key exhausts around 2026-09-08; at the schedule-implied rate (7 sports x 4 odds calls x 3 credits plus 5 x 7 x 2 score credits an hour, about 154 an hour, assuming the provider's documented per-call costs; confirm from x-requests-used deltas) around 2026-09-11, NFL Week 1 kickoff; Vercel logs 02:04 to 03:14 UTC show settle-picks runs at 02:07, 02:20, 02:22, 02:37, 02:52, 03:07. CODER PRIORITY 1, ship before 2026-09-08 00:00 UTC |
| C-110 | WP-28b single-book publish-time market probability (decision 2026-09-06, delegated): marketPFromOddsTable left 223 games unresolved as insufficient_books (one stored book at or before generatedAt); resolve them from that one book with the same proportional de-vig, tag pSource market_p_single_book, report it in pSources and bySource, and bump the streak basis tag to market_anchored_v2 so the streak restarts honestly on the new sample definition; the 263 no_rows games stay excluded; floors, bins (10 equal-width, apps/web/lib/calibration/ece.ts) and the streak length do not change | claude | DONE | DONE 23a0a3a0f (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): publish-time-market-p.ts resolves one real two-sided book with the same de-vig, pSource market_p_single_book (two or more books keep market_p_from_odds_table), unresolved reasons no_rows / no_usable_book / no_side with insufficient_books held at 0 for compatibility, loader stats resolvedSingleBook, MARKET_ANCHORED_P_BASIS market_anchored_v2 written by calibration-eligibility-durable.ts and compute-live-calibration-metrics.ts so the first v2 run (08:40 UTC) resets the streak once by design; tests publish-time-market-p.test.ts, market-anchored-calibration-sample.test.ts, calibration-drift-alert.test.ts (57 passed); typecheck 0, lint 0, guardrails 26/26; review approved with no blocking finding. Disclosure: production no_rows may read lower than 263 because rows with only non-book or one-sided quotes now count as no_usable_book. Truth surface 2026-09-06 03:02 UTC: n 223, Brier 0.1617, ECE 0.0553 with bootstrap 95% CI 0.0365 to 0.1142 (200 resamples), Murphy REL 0.0071; pSources proof_receipt 122, market_p_from_odds_table 101; exclusions three_way_market 120, non_moneyline_market 976, no_market_probability 486 (263 no_rows + 223 insufficient_books); by sport MLB n 175 ECE 0.0509, NCAAF n 35 ECE 0.131, NFL n 13 ECE 0.3006. At n 223 the floor sits inside the estimator's own interval; more real rows is the honest lever. CODER PRIORITY 4 |
| C-111 | Fixture confirmation guard (found 2026-09-06): three NCAAF game rows created from May listings (externalIds a6dfbee1…, e17a2675…, bcad21f3…; Ole Miss v Louisville, Illinois v UAB, Washington v Washington State, all dated 2026-09-05) do not exist on ESPN's 2026 schedule for that date (ESPN public scoreboard 20260905, 68 events, none present; 20260912 shows Duke at Illinois, Charlotte at Ole Miss, Utah State at Washington, Washington State at Kansas State), yet generate-signal-slate wrote MONEYLINE "model signal" picks on them at 15:54 and 18:53 UTC on 2026-09-05 and three v5.0.0 spreads from 2026-05-22/23 still sit published: (1) the slate and the board must not generate or publish a pick for a game absent from the day's free ESPN scoreboard for its sport; (2) a game row whose kickoff has passed with no ESPN fixture within 24h is marked CANCELLED and its picks VOIDED with RCA FIXTURE_NOT_FOUND through the outbox lane; (3) game rows older than 30 days re-confirm date and opponent against ESPN before any pick is generated | claude | DONE | DONE b4885f214 (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): packages/ingestion-pipeline/src/fixture-confirmation.ts (FixtureConfirmer, ESPN board per sport per cycle with all division groups, groups 80+81 for CFB and 50 for NCAAB, any failed group means not confirmable rather than not listed) wired into generate-signal-slate.ts and process-sport.ts before any pick write; unlisted fixture skipped and logged (fixtureUnconfirmed counter), scoreboard failure skips the sport for the cycle, rows older than 30 days re-confirm and take ESPN's kickoff when it differs by more than 15 minutes; tests fixture-confirmation.test.ts, generate-signal-slate-guard.test.ts, process-sport.test.ts; typecheck 0, lint 0, guardrails 26/26; review approved after one fix (division groups). Disclosures: exact-name college matching depends on ESPN and the odds feed spelling displayName identically (watch fixtureUnconfirmed on the first NCAAF Saturday); the guard runs before line snapshots and enrichment so an unconfirmed game loses those derived writes for the cycle; two ESPN fetch batches per sport per cycle (process-sport and the slate); events.length at limit 300 not yet treated as truncation. The settlement-side void of the six phantom picks rides on C-106. 6 of the 16 overdue picks at 2026-09-06 03:10 UTC are this cohort (read-only SQL: picks joined to games, result PENDING, commenceTime older than 6h); ESPN public scoreboard queries 2026-09-06 03:20 UTC; plan 3f item 2. CODER PRIORITY 3 (with C-106) |
| F-37 | BROWSER AGENT handoff (founder instruction 2026-09-06: no human step where a machine can do it): script A The Odds API billing (clear the invoice, no tier change, copy nothing); script B Vercel Production env (HEALTH_ALERT_WEBHOOK_URL, SENTRY_DSN, redeploy; later PERFORMANCE_STATS_ENABLED and PRICING_PHASE after the preconditions); script C Stripe live mode (Terms URL, then STRIPE_TERMS_CONSENT_ENABLED, three missing webhook events, deactivate Payment Links); script D live checkout with the founder's card then refund; script E read-only verification | browser | DONE | DONE 2026-09-06 03:02 UTC (#709, report in plan 3e scripts and 3f): A DONE (key swap, redeploy 02:37 UTC), C DONE (Privacy URL set to /privacy, Terms already /terms, STRIPE_TERMS_CONSENT_ENABLED already true since Jul 12, webhook /api/webhooks/stripe now 10 events with invoice.paid and charge.refunded added, 5 Payment Links deactivated), E DONE (truth surface read 03:02 UTC); B alerting and D checkout SKIPPED by founder decision (no Slack, no Sentry, no checkout smoke); the two public flips remain for a later prompt after the receipt and C-107; LAUNCH_FINISH_LINE section 3e; supersedes the manual halves of F-15 F-18 F-19 F-20 F-22 F-29; never paste a secret into chat, docs or commits; never blank THE_ODDS_API_KEY before WP-27 deploys |
| F-33 | FOUNDER: close hermes/settlement-token-fix after PR #707 lands; its containment minimum of 3 still lets "chi" match "kansascitychiefs" and it conflicts with 6880f18 (minimum 4, exact abbreviations, bipartite sides, NFL regression cases) | founder | OPEN | git merge-tree of the two branches reports a content conflict in apps/web/lib/data-sources/free-settlement.ts |
| C-112 | Alias-aware publish-time market probability lookup (found 2026-09-06 by Devin Review on PR #716, verified read-only): apps/web/lib/calibration/publish-time-market-p-loader.ts queries odds by pick.gameId with no alias resolution, while scripts/ops/merge-duplicate-games.ts re-points odds rows to the canonical game (odds.updateMany data gameId canonicalId) and by its own documented design NEVER re-points picks, which stay on the alias row as settlement history. The first production run of the merge utility therefore silently drops every affected pick out of the calibration sample as no_rows even though a real publish-time price survives on the canonical row. Fix: resolve pick.gameId through mergedIntoGameId (bounded by MAX_ALIAS_HOPS, the same bound game-identity.ts uses) before the odds query, and count alias-resolved games in the loader stats. Acceptance: a fixture test where the pick sits on an alias and the odds sit on the canonical resolves to a market probability, and the unresolved reason stays no_rows when the canonical genuinely has none | — | OPEN | MEASURED NO-OP TODAY, which is why this is a row and not a hotfix: production has 0 game rows with mergedIntoGameId set (SELECT count(*) FROM games WHERE "mergedIntoGameId" IS NOT NULL), 0 of the 297 no-odds settled MONEYLINE picks sit on an alias, and 0 are recoverable that way (read-only SQL 2026-09-06, three alias hops followed). The bookmakerCount 0 finding is independent of this path and unaffected. SEQUENCE THIS BEFORE anyone runs merge-duplicate-games.ts in production, not after |
| C-113 | CRITICAL, FIXED: the free score persister settled games that had not started. apps/web/lib/data-sources/free-score-persist.ts filtered candidate finals by a +/-48h date window ("~2 days (TZ edge)") and then matched on TEAM NAMES ONLY, so in a daily series (MLB plays the same matchup on consecutive days) an earlier meeting's final was written onto a LATER unplayed game and the published picks on it were graded WIN/LOSS before first pitch. Nothing downstream caught it: SCORE_MISMATCH_CROSS_PATH only refuses to overwrite an EXISTING final with a DIFFERENT one, and a pick graded off a phantom score is not overdue, so settlement health read HEALTHY throughout. TWO PATHS, not one (CodeRabbit, #717): the score persister writes the phantom FINAL onto the Game row, but the grading that reaches a subscriber happens in apps/web/lib/data-sources/free-settlement-runner.ts via settlePendingPicks, which loaded PENDING picks with no commenceTime filter and graded them straight off a feed final with no kickoff binding. Either path alone reproduces the incident, so remediation that touches only the persister is incomplete. SHIPPED FIX, in both (the first revision ordered candidates by calendar-date distance; that was too weak and is NOT the live rule): (1) skip any game whose commenceTime is still in the future, and scope the runner's pending-pick load the same way, each re-checked in the WHERE of its own write so a game postponed mid-cycle cannot be stamped; (2) collect EVERY windowed final naming both teams, narrow with nearestByKickoff (shared by both paths), and refuse when more than one survives; (3) reject a final whose startIso drifts past MAX_KICKOFF_DRIFT_MS (12h), bind a final that carries no start time to ONE day rather than two so the UTC-rollover case still works, and refuse outright when a supplied timestamp is malformed or date-only; (4) a doubleheader is resolved by the clock, not by a flag or a count: assign both the final and the pick to their nearest board fixture and require the same one, holding when nothing places (the earlier "more fixtures than finals" count and the board's completed flag were both wrong and are NOT the live rule); (5) the settlement write is transactional and rolls back when the game refuses the FINAL write, so a concurrent postponement or a competing cross-path score can never leave a pick settled against a row the database did not stamp | claude | DONE | DONE cc161128a for the persister half; the complete guard set across both paths is #717 (branch claude/sports-launch-round2-fixes-hk7kv9). MEASURED ON PRODUCTION 2026-09-06 19:20-19:45 UTC (read-only SQL, SELECT only): 87 published picks had settledAt EARLIER than their game's commenceTime (all 87 isPublished), 63 of them MONEYLINE graded WIN/LOSS (34 WIN / 29 LOSS) and ALL 63 inside the calibration window; 5 games with a FUTURE commenceTime carried status FINAL with scores, every one updatedAt 2026-09-06T19:22:22Z (written while the audit ran, so the loop was live). Worst case pick cmtp2tipl00lf142ew8g32u0w, MONEYLINE "Philadelphia Phillies ML (model signal)", v5.2.7, result WIN, settledAt 2026-09-06T02:07:17.186Z on a game with commenceTime 2026-09-07T17:06:00Z, graded 39.0h BEFORE kickoff, game row FINAL 4-2; a logged-out visitor saw a WIN badge on an unplayed game. Root cause was the +/-48h window plus team-name-only matching in persistFreeScores' per-game loop. Tests free-score-persist.test.ts 23 passed across 5 suites at the branch tip (the row previously said 19 across 4, which was true when written and went stale as later commits added cases; CodeRabbit, #717); every guard proven by negative control, each disabling failing exactly its own test; typecheck 0, lint 0, guardrails 26/26. Found by the blind-spot sweep surface lens and independently verified before any code was touched |
| C-114 | FOUNDER DECISION then owner-run: remediate the 87 already-corrupted picks and 5 games from C-113. The C-113 guard (cc161128a) stops NEW corruption but changes nothing already written: those rows still carry fabricated WIN/LOSS results and phantom FINAL scores, and the 63 moneyline rows are inside the sample the published ECE 0.0524 is computed from, so the calibration figure is measured partly on outcomes nobody observed. THE POLICY IS THE FOUNDER'S CALL, not an agent's: revert the affected picks to PENDING and let the normal lane re-grade them after the real game finishes, or VOID them with an RCA code through the settlement outbox, or unpublish. Whichever is chosen, the game rows must also have status and scores cleared where commenceTime is still in the future. Follow the existing owner-tool precedent: a read-only lister (scripts/ops/list-stale-pending-picks.ts) plus a separate --execute adjudicator (scripts/ops/adjudicate-stale-picks.ts). Agents cannot execute this: law 7 bars database writes and SELECT-only is the only approved exception | founder | OPEN | selector is picks p JOIN games g ON g.id=p."gameId" WHERE p."settledAt" IS NOT NULL AND p."settledAt" < g."commenceTime" (87 rows at 2026-09-06 19:45 UTC), plus games WHERE "commenceTime" > NOW() AND status = FINAL (5 rows). Until this is done the public calibration number and any track-record claim rest on a contaminated sample: do not publish either, and note that C-113 does not by itself make ECE 0.0524 trustworthy STOP - DO NOT RUN THE UNPUBLISH TOOL ON THIS POPULATION YET. Spot-checked 4 rows against ESPN ground truth 2026-09-07 21:2x UTC. GOOD NEWS FIRST: commenceTime is CORRECT on all four (matches ESPN exactly), so this predicate is sound and is NOT an artifact of the corrupted game rows that inverted C-115 - the two populations are genuinely different and were rightly kept apart. BUT THE SCORES ARE WRONG ON ALL FOUR and were never corrected: espn 401816835 truly finished White Sox 10-1 Twins (total 11), our row says 4-6; 401816838 truly Rockies 8-10 Cardinals (18), our row says 10-7; 401816830 truly Marlins 10-3 Cubs (13), our row says 5-6. RE-GRADED AGAINST THE TRUE FINALS, 3 OF THE 4 STORED RESULTS ARE CORRECT: cmtp7m2i9 OVER 11.15 stored WIN is right (true 18), cmtp7m2nn OVER 8.05 stored WIN is right (true 13), cmtpe49z8 UNDER 8.5 stored LOSS is right (true 11); only cmtp7m34e (Cardinals -1.5, stored LOSS) is wrong - they won by 2. TWO READINGS, BOTH TRUE: as DATA, unpublishing removes about three correct rows for every wrong one; as EVIDENCE, a pick graded 15h before kickoff observed nothing, so a coincidence with the truth is a coin flip that landed and must not count toward calibration whatever it says. CONSEQUENCE: UNPUBLISH IS THE WRONG INSTRUMENT FOR BOTH POPULATIONS. C-115 needs the SCORES fixed; C-114 needs RE-GRADING against ESPN truth, which preserves the record and makes it honest rather than deleting it. Re-grading writes `result`, which the settlement outbox owns, so it is a founder decision and not an agent one. Working note: scratchpad c114-truth.md. |
| C-115 | CRITICAL, ROOT CAUSE NOT YET FOUND: published settled TOTAL picks whose stored result contradicts the final score on their OWN game row. This is NOT the C-113 early-settlement defect and must not be conflated with it. Three hypotheses were tested and REJECTED, each with the query that killed it, so nobody re-runs them: (1) grading uses clvLockLine while display uses line - rejected, across the 430 rows where the two differ the stored result matches the display line 351 times and clvLockLine 341 times, so neither explains it; (2) the score belongs to a duplicate sibling row of the same series - rejected as the main cause, only 10 of 80 wrong rows have a sibling row within 72h whose score would make the stored result correct; (3) same defect as C-113 - rejected, settled_before_kickoff is 0 among the wrong rows and 10 among the correct ones. What IS established is a strong timing signal, and it is the lead worth pulling: picks settled under 5h after kickoff are wrong 21.8 percent of the time (76 of 348), picks settled 5h or later are wrong 1.7 percent (4 of 236), a 13x difference, and the p90 settle-lag is 4.91h for wrong rows against 257h for correct ones - every mis-graded row settled inside the window where a game may not have finished. Both settlers DO gate on completion (free path buildTrustedFinals via completed() in free-settlement.ts:43, paid path settle-sport.ts:392 `if (!score.completed) continue;`) so the naive live-score explanation is NOT yet substantiated in code and the next agent must find the path that bypasses both, or disprove the timing signal | claude | CLAIMED | Measured by me on production read-only SQL 2026-09-06 23:0x UTC, Neon summer-brook-99380762. 80 of 585 published settled TOTAL picks contradict p.line, re-measured definitively at 2026-09-07 00:05 UTC. An earlier run in the same session read 79 of 584 and its under-5h/5h+ split totalled 76+4=80 of 348+236=584: the table is LIVE and picks settle continuously, so the denominator moves between queries. Anyone re-running this will get a different total and that is expected; the 13x ratio is what matters and it is stable (CodeRabbit caught the inconsistency, #717). Decisive split query: settled under 5h after kickoff n=348 wrong=76 (21.8pct), settled 5h+ n=236 wrong=4 (1.7pct). Worked example cmtpaag0e03uwvxtdedzppu9k UNDER 46.3 (line 46.25) on Sacramento State 52 - 0 Mississippi Valley State, actual total 52 so UNDER lost, stored WIN, settledAt 2026-09-06T05:52:14Z, modelVersion v5.2.7, kickoff 02:00 so settled 3.87h after. Hypothesis-rejection queries are in this session transcript ROUND 2 PROGRESS 2026-09-07, folded in from a row that should never have existed: I opened C-139 for this investigation, which is a rule-6 violation (two rows describing the same work is the exact failure this file exists to catch) and a rule-7 misread - this is progress on the claimed unit, not new scope. Devin caught it on #719; the row is deleted and its substance is here. REPRODUCED: 81 of 590 published settled TOTAL picks contradict the final on their own game row (13.7pct), and the 13x timing split HOLDS - under 5h after kickoff 75 of 351 wrong (21.4pct), 5h or later 6 of 239 (2.5pct), so 93pct of the damage is in the in-progress window. HYPOTHESIS 4 REJECTED (score changed after settlement): 78 of 81 contradicting picks had games.updatedAt later than settledAt, average 285h - which looks decisive until the control is run. 501 of 509 CORRECT picks (98.4pct) show the SAME thing, against 96.3pct of the contradicting ones. games.updatedAt moves on any write, so it is universal noise and slightly MORE common among the correct rows. HYPOTHESIS 5 REJECTED (graded on a partial in-progress score): that predicts a ONE-DIRECTIONAL error, because a running total is lower than the final, so OVER would wrongly lose and UNDER wrongly win. Measured unconditionally on the under-5h cohort: 41 finals ABOVE the line against 34 BELOW, mean actual minus line +1.03. Symmetric, so the prediction fails. METHOD TRAP, recorded because I fell into it for one round: a side x stored x expected breakdown LOOKS directional (OVER-stored-LOSS showed +4.03, UNDER-stored-WIN +5.92) but every one of those cells is directional BY CONSTRUCTION - if OVER is stored LOSS and expected WIN then actual > line necessarily. Those cells cannot evidence direction; only the unconditional split can, and it is symmetric. WHERE IT POINTS: symmetric errors concentrated in the settle window are the signature of grading against a WRONG FIXTURE's score, which is directionally random where a partial score is not. That makes C-115 a candidate SIBLING of C-113, the cross-fixture matching defect fixed forward in #717, on the TOTAL market rather than the score persister. NEXT: test whether each contradicting pick's stored result is explained by the final of some OTHER game in the same feed window, not merely a duplicate row of the same series - hypothesis 2 tested duplicate ROWS only (10 of 80) and cross-fixture is a wider set. Also re-measure the contradiction rate on picks settled AFTER b665a270d once enough accumulate. NO remediation shipped: the 81 rows stay published until the cause is known, because unpublishing them now destroys the evidence the next investigation needs. HYPOTHESIS 6 REJECTED BY ITS OWN CONTROL, measured 2026-09-07 19:4x UTC read-only (81 of 588 contradict, reproducing 81 of 590): for 77 of 81 contradicting picks (95.1pct) SOME other game in the same sport within 24h has a total that would produce the stored result - and so does 496 of 507 (97.8pct) of the CORRECT picks. With that many games in a window almost every line has a neighbour on both sides, so existence carries zero information. Do not re-run it. HYPOTHESIS 7 (cross-fixture confusion on a NAME-SIMILAR fixture, the C-113 containment signature) is NOT SUPPORTED, and that is a CONFOUND rather than a clean rejection - stated that way on purpose. Restricting the neighbour to one whose team name contains or is contained by this game's, the explains-rate is 5 of 74 (6.8pct) for contradicting picks against 278 of 411 (67.6pct) for correct ones, with a similar base rate of having such a neighbour at all (91.4pct vs 81.1pct). That LOOKS decisive and is not: a name-containing neighbour inside 24h is usually a DUPLICATE ROW of the same fixture carrying the same score, so it lands on the own-game side BY CONSTRUCTION - which explains the stored result exactly when the pick is correct and never when it contradicts. Same by-construction trap as the side x stored x expected breakdown. The neighbour set has to exclude same-pairing rows before this test can say anything, and even then existence is near 50/50 under the null. WHERE THIS LEAVES IT: four hypotheses have now been spent on SQL and the only stable signal is still the 13x timing split, so the remaining question is a CODE question. NEXT, and it is a READ not a query: settle-backfill.ts:340 grades via buildTrustedFinals(games, []) - it derives finals from the GAMES TABLE rather than from a fetched score payload, so neither settler's `score.completed` gate is the one that applies on this lane; establish what buildTrustedFinals accepts as a trustworthy final off a game row, and whether an in-progress score can reach that row (C-114 proved a PHANTOM score can, pre-kickoff). Confirm or kill that before any remediation. CORRECTION to the line immediately above, made within the hour and before anything was built on it: settle-backfill.ts:340 does NOT bypass the completion gate. `games` there comes from fetchScores(), a fetched ESPN payload, not the games table, so buildTrustedFinals applies completed() on this lane too and the gate holds on all three. Do not go looking for a third ungated write path; there is none. THE SPLIT, and this is the real progress: C-115 IS TWO POPULATIONS, not one. selectGradingLine() (settle-backfill.ts:344) grades on clvLockLine when it is present and falls back to line; the pick card displays line. Of the 81 rows that contradict the DISPLAY line, 34 (42pct) are graded CORRECTLY against clvLockLine - those are not mis-graded picks at all, they are picks whose displayed number is not the number they were settled on. The remaining 47 are explained by NEITHER line and are the actual grading defect. Hypothesis 1 was rejected earlier because it was measured unconditionally across all 430 rows where the two lines differ, where display won 351 and clv 341; CONDITIONED on contradiction, which is the only place it could show, clv explains 42pct. The unconditional test was the wrong test, not a wrong answer. STILL LIVE AFTER THE FIX: splitting by settledAt at the 6880f18cf deploy (2026-09-06 02:15 UTC, the MIN_CONTAINMENT_TOKEN_LENGTH bipartite matcher), before reads 76 wrong of 542 and after reads 5 of 46. State that carefully - n 46 cannot establish a REDUCTION (the interval on 5 of 46 covers 14pct) - but it does establish that the defect still PRODUCES rows under current code, so this is not a historical cleanup. Damage is also episodic, not uniform: 2026-07-18 to 07-25 alone carries 62 of the 81. FIVE POST-DEPLOY ROWS TO TRACE, all v5.2.7, all graded FINAL: cmtp7olso04ci9t2po0eeh9eo (NCAAF ASU 70-7 Morgan St, actual 77, OVER 83.5 stored WIN, clv 74 EXPLAINS it), cmtpaag0e03uwvxtdedzppu9k (NCAAF Sac St 52-0 MVSU, actual 52, UNDER 46.3 stored WIN, clv 53 EXPLAINS it), cmtp7m2g104iy5gakc5kdrte9 and cmtpi1g740e91qkek2wlbayk7 (both MLB Mariners 6-7 Athletics, actual 13, OVER 6.6 and OVER 7.0 both stored LOSS, clv 8.375 and 7.5 explain NEITHER) and cmtp7m2zq04mg5gak7ze8z2mr (MLB Dodgers 5-3 Nationals, actual 8, UNDER 8.5 stored LOSS, clv 8.5 explains it not). The last three are population B and the two MLB rows share a scoreline across two different commenceTimes, which is the C-117 duplicate-fixture signature - that is the next thread. Settlement payloads for all five are in pick_settlement_events; they carry the receipt line and clvLockLine but NOT the score used, which is the single field that would end this investigation. NEXT: trace population B (47 rows) only - population A is measurement, not mis-grading, and is now C-143. ROOT CAUSE FOUND, AND IT INVERTS THE FINDING. I HAD THE BLAME BACKWARDS AND SO DID THIS ROW: the picks are RIGHT and the GAME ROWS are wrong. A nine-lens parallel investigation converged from six independent angles on one mechanism - the pick is graded correctly against a fetched ESPN final while its game row is still unscored, and homeScore/awayScore are written LATER by a loose team-name plus time-window backfill that stamps a DIFFERENT fixture of the same series onto the row. I then verified it MYSELF against ESPN ground truth, independent of the agents, three of three: event 401816824 really finished Mariners 2 - Athletics 6 (total 8) but our row says 6-7 (13); event 401816839 really finished 2 - 0 (total 2), our row says 6-7 (13); event 401816841 really finished Dodgers 7 - Nationals 5 (total 12), our row says 5-3 (total 8). In ALL THREE the stored pick result is CORRECT against the true final. cmtp7m2g1 OVER 6.6 stored LOSS is right (true total 8 against clvLockLine 8.375); cmtpi1g74 OVER 7.0 stored LOSS is right (true total 2); cmtp7m2zq UNDER 8.5 stored LOSS is right (true total 12). The 2026-09-05 Mariners game's 6-7 was copied onto two LATER, DISTINCT fixtures of the same series, written at 04:17:41 and 22:47:30 - after those picks settled. THIS EXPLAINS EVERY SIGNAL AT ONCE, which no earlier hypothesis did: the symmetry (a wrong fixture's score is directionally random), the market-agnosticism (a wrong score corrupts TOTAL, SPREAD and MONEYLINE alike, which is why the moneyline column was the discriminating fact), the 13x timing split (a pick settled soon after kickoff has an unscored row, so the loose backfill is what fills it and that is where the mis-bind happens) and the identical scoreline across two commenceTimes I flagged and did not chase. CONSEQUENCE, and it is the important one: REMEDIATION BY UNPUBLISHING THESE PICKS WOULD DESTROY CORRECT RESULTS. The public track record may be healthier than reported; the SCORES shown beside it are what is wrong. Also visible in the same query and not incidental: each fixture exists as THREE game rows under different externalId namespaces (espn:mlb:401816795, espn:baseball_mlb:401816795, and a hash) - C-85 and C-117 are live and are the surface the loose backfill mis-binds across. NEXT: fix the score-persist bind, not the picks; re-derive the 117 against ESPN rather than against our own rows; NOTHING gets unpublished on the strength of the old reading. ADVERSARIAL VERIFICATION OF THE AGENT HYPOTHESES HAD NOT COMPLETED when this was written - the ESPN checks are mine and are what this claim rests on. CAUSAL CHAIN COMPLETE 2026-09-07 21:3x UTC, and the missing capability is named. (1) A score is written WRONG - either a phantom pre-kickoff write (the C-114 population, kickoff times correct, scores wrong on 4 of 4 checked) or a cross-fixture bind carrying another game of the same series (the C-115 population, 3 of 3 confirmed against ESPN). (2) SCORE_MISMATCH_CROSS_PATH then REFUSES to overwrite an existing final with a different one - a guard that exists for a good reason, to stop one lane clobbering another's result. (3) THEREFORE THE WRONG SCORE IS PERMANENT. Measured: all six verified-wrong game rows are FROZEN, untouched for between 16h and 1d17h (espn 401816824 last written 2026-09-06 04:17 and not touched since) while settle and refresh cycles ran several times an hour throughout. Nothing in the system can correct a game score once it is wrong. (4) The public surface therefore shows a wrong score beside a correct pick, indefinitely. THE GOOD NEWS, measured not assumed: the CURRENT settlement path is healthy. C-120's settledWith payload is live as of 2026-09-07 20:20 UTC and on all 14 events recorded so far the score the grade was computed from matches the game row EXACTLY, written within a fraction of a second of each other - so the lane running now writes score and grade atomically from one payload and is not producing new corruption. free-score-persist.ts is also already well guarded (doubleheader placement, nearestByKickoff, fail-closed on ties, MAX_KICKOFF_DRIFT_MS); those guards are NOT the gap. THE GAP IS THAT NO AUTHENTICATED CORRECTION PATH EXISTS. The cross-path guard is right to refuse an unauthenticated overwrite; what is missing is a lane that may overwrite a final WHEN AND ONLY WHEN a named ground-truth source disagrees with it, writing an audit row for every correction. That is the remediation for C-115 and it is also what makes the settled-before-kickoff population re-gradeable instead of deletable. It is a new write path over game scores, so it is FOUNDER-GATED, not an agent decision. NOTHING further should be unpublished. CORRECTION TO MY OWN CORRECTION, from adversarial verification 2026-09-07 21:5x UTC, and I confirmed the counter-evidence myself before accepting it. I wrote "zero mis-graded pick results; only the reference is corrupt". THAT IS TOO STRONG. GENUINELY MIS-GRADED PUBLISHED PICKS EXIST: cmtp1qor6052dx2r51abbm3ik, published MONEYLINE "Seattle Mariners ML", v5.2.7, on espn:mlb:401816839, settled 2026-09-06T02:07:17Z against a 20:12:24Z first pitch = 18.09 HOURS BEFORE the game, stored LOSS; ESPN has Mariners 2-0 winners, so the true result is WIN and the stored LOSS is exactly what the row's corrupt 6-7 implies. THE MODEL THAT FITS BOTH: two failure modes separated by WHEN the pick was graded. Graded AFTER kickoff from a fetched payload, the pick is correct and the row is corrupted later (the C-115 population). Graded BEFORE kickoff there is nothing to fetch, so it is graded off whatever the row already held, INHERITS the corruption, and is right or wrong by coincidence (the C-114 population, which is why 3 of 4 measured correct - a coin flip against a wrong score). The populations OVERLAP; that moneyline is in both. WHAT THE VERIFIER CONFIRMED, harder than I did: it tested by ESPN EVENT ID rather than team-plus-time - the field a duplicate-fixture defect corrupts - and rebuilt the population independently (605/88/37/51 against my 588/81/34/47, same structure, live drift), verifying 45 of 45. WHAT IT REFUTED: my claim that the corrupt-score story explains the 13x timing split. It ran the control I did not - corruption in the CORRECT cohort is 39.6pct under 5h against 50.0pct at 5h+, flat to inverted - so a corrupt baseline cannot produce the population-B gradient. THE TIMING SIGNAL REMAINS UNEXPLAINED. Also refuted: the paid lane as writer (settle-sport.ts:552 gates the whole grading block on !conflicts, so that path is unreachable); the writer is re-attributed to the stale backfill lane, which is AGENT-REPORTED and NOT yet confirmed by me. |
| C-116 | CANCELLED AS DUPLICATE. The duplicate-Game-row work was already tracked before I opened this row: C-85 owns the code half (unify the ESPN externalId namespace so new rows stop being minted under espn:<short>:, espn:<oddsKey>: and an odds-api hash) and F-17 owns the founder-run merge of the existing rows. I added C-116 without reading the existing ledger rows first, which is exactly what the loop's rules forbid (Devin Review, #717). Do the work under C-85 and F-17, not here. Evidence I measured is preserved in this cell so it is not lost with the row | claude | CANCELLED | Independently confirmed by me on production read-only SQL 2026-09-06 23:0x UTC and offered to C-85/F-17: total_game_rows 3091, fixtures_with_dupes 644, surplus_rows 936, ever_merged 0. Worked example Astros v Diamondbacks 2026-09-04..07 exists as five rows including three for the SAME 2026-09-05T00:10 fixture under espn:mlb:401816804, espn:baseball_mlb:401816804 and hash 2adbf23e1f48845cbe5da5a4ff1afb6e. Also relevant to C-85: MLB game rows keyed by opaque hash carry 4.1pct impossible spread rows against 2.4pct for espn-keyed rows, so mis-keying correlates with odds contamination but does NOT fully explain it |
| C-117 | HIGH, SUBSCRIBER-VISIBLE: the live board shows the same fixture and market more than once with CONTRADICTORY confidence and tier. Measured on today's Eastern slate: 58 board rows covering only 18 distinct fixtures across 27 distinct game rows, 12 of the 58 duplicates. Notre Dame v Wisconsin MONEYLINE appears four times as two contradictory variants, FREE tier confidence 57 LEAN and PREMIUM tier confidence 88 STRONG_PLAY. A paying subscriber and a free visitor can therefore be shown opposite strength readings on the same game, which is a direct hit on the product's stated premise. Almost certainly downstream of the duplicate Game rows tracked by C-85 and F-17 (generation runs per game ROW, not per fixture), so fix C-85 first and re-measure before adding board-level de-duplication | claude | DONE | Blind-spot sweep integrity lens wf_93300b0b-5ff, board-equivalent query using the Eastern window from lib/picks/slate-window.ts. NOT independently re-measured by me - the 27-distinct-game-rows figure is consistent with the 936 duplicate rows I did confirm ROUND 22 CLOSES THIS ROW. Dedupe shipped 6636b414d, then three Devin findings on it, all real, fixed in 236f14e40. (1) REGRESSION I INTRODUCED: the collapse keyed on row.market, which is redacted to the literal ALL_MARKETS for every non-premium viewer, so a game's SPREAD and TOTAL merged for FREE while PRO kept both. rows and openPicks then differed by entitlement, breaking the tier-invariant count contract at board-gate-decisions.test.ts:212. Key now comes from pickType via boardDedupeKey(gameId, pickType); dedupeBoardRows takes {key,row} entries and each key is built inside the same .map as its row, not by parallel index, so a later .filter() cannot silently misalign the arrays. (2) the mergedIntoGameId: null alias filter was on the decision query only; the three fallback game queries could still return a tombstone beside its canonical row, and the collapse cannot pair those (different ids, different keys by construction), so only the query filter fixes it. Ninth instance of the sibling-call-site pattern. (3) take: 100 ran before the collapse, so one repeatedly-evaluated game could consume the cap and crowd other fixtures off the board; raised to 500, which bounds decisions scanned rather than fixtures shown. 11 tests. Negative control: reducing boardDedupeKey to gameId alone fails exactly the two market-separation tests and leaves the other nine green. Verify on e8b394a68: typecheck 0, lint 0, guardrails 26/26, apps/web 12473 passed 97 skipped. All 52 review threads on #717 replied to and resolved. RESIDUE, stated plainly: two NEVER-MERGED Game rows for one real contest are still two rows on the board. The collapse cannot pair them (different ids), only the merge can, and that is F-17 and founder-gated. C-117 covers the two multiplicity sources the code owns; F-17 covers the third. |
| C-118 | HIGH, FABRICATION RISK: the soccer moneyline publish guard does not hold in production. A two-way moneyline on a three-way market is wrong BY CONSTRUCTION because the draw is unpriced, the engine has a unit test at packages/prediction-engine/src/__tests__/soccer-moneyline-threeway.test.ts:66 asserting no such pick is published, and the test passes - yet 148 published soccer MONEYLINE picks exist, four of them generated on 2026-09-06. ROOT CAUSE FOUND AND FIXED IN CODE 2026-09-06 (2f9f7d90e): process-sport.ts passed sport.NAME into OddsInput.sport at one of its 21 assignment sites while every other site passed sport.key. OddsInput.sport has exactly one consumer, isThreeWayMoneylineSport, which tests startsWith('soccer'), and soccer_usa_mls has the display name 'MLS', so the guard never fired on the production path. The guard's unit test passed because it calls the scorer directly with a key. THIS ROW STAYS OPEN FOR THE 148 ALREADY-PUBLISHED ROWS ONLY, which is founder-gated remediation (law 7 bars agent database writes), plus their treatment in the calibration sample, which they contaminate because a two-way moneyline on a three-way market cannot be graded honestly. Do NOT read the text above as a live bypass and do not go looking for an unfixed production path: verified 2026-09-07, every production processSport caller now reaches the guard through sport.key and generateSignalSlate skips soccer separately (CodeRabbit, #717) | claude | OPEN | Independently confirmed by me on production read-only SQL 2026-09-06 23:0x UTC: 148 published MONEYLINE picks on sports.key LIKE 'soccer%'. The sweep reported the four generated today by id |
| C-119 | HIGH, RULE 1 RISK: 394 of 720 published MLB SPREAD picks carry a run line whose absolute value is not 1.5. The MLB run line is ALWAYS 1.5. Observed examples include Athletics -13.5 and San Francisco Giants -7.5, and the same family appears in totals (OVER 11.9, OVER 13.2 on MLB games, where real totals move in half-run steps). These are not offers that exist at any book, so a subscriber cannot place the bet described. Determine whether the line is INVENTED by the engine (a direct violation of non-negotiable rule 1, no fabricated product data) or ingested from a source that is not book-priced, then add validation that refuses to publish a line that cannot exist at that sport's book. VERIFIED MECHANISM (packages/prediction-engine/src/scoring.ts:394 and :410): the published line is avgSpread, the arithmetic MEAN of every book's spread, published directly as the pick's line. So a single contaminated row drags the mean to a value no book quotes, which is why the observed lines are means (6.5625, 6.142857, 28.333) rather than quoted numbers. CONTAMINATION IS REAL AND MEASURED: MLB odds rows carry spreads to +/-19.5, and on one worked example eight major books AGREE on 16.5 to 19.5 at the same fetchedAt for an Athletics v Nationals row, so those are genuine lines for some event but not for a baseball one. NOT YET ESTABLISHED, and do not repeat it as fact: which event they belong to, and how they attach. The mis-keying correlation is weaker than it first looks (opaque-hash MLB rows 4.1pct impossible against espn-keyed 2.4pct), so duplicate rows do NOT fully explain it. Also note the band labels 'implausible' and 'impossible' below 5 runs are MY judgement, not a verified fact; what IS verified is that every spread row from espn_public, lowvig, betonlineag and betus is exactly +/-1.5 | claude | OPEN | Independently confirmed by me on production read-only SQL 2026-09-06 23:0x UTC: published MLB SPREAD picks 720, of which 394 have abs(line) <> 1.5. Line values quoted from the C-115 sample rows |
| C-120 | MEDIUM but it blocks every other settlement investigation: there is NO record of the score a pick was graded against. PickSettlementEvent.payload is documented as the immutable event-time payload and does carry result, selection, settledAt and pickReceipt, but its evidence key is null on every row inspected and it holds no score. Consequence: it is impossible to distinguish a pick that was MIS-GRADED from a pick that was graded correctly and whose game row was later overwritten with a different score, which is exactly the ambiguity blocking C-115 and the C-114 remediation policy decision. Adding homeScore, awayScore and the sourceIds used at settle time to that payload is a small, safe, additive change and it should be done before any further settlement forensics | claude | OPEN | Measured by me 2026-09-06 23:0x UTC: 1442 rows in pick_settlement_events, 1426 with a payload, payload keys are actor ageHours awayTeam commenceTime contentVersion deepLinkPath evidence gameId homeTeam kind lane locale pickId pickReceipt pickType policy rcaCode reason result schemaVersion selection settledAt sportKey. Inspected the payload of three mis-graded picks including cmtpaag0e03uwvxtdedzppu9k, evidence null on all three |
| C-121 | An 8-lens adversarial audit of the #717 settlement guard family produced 14 findings. The workflow STALLED before its refutation phase, so NONE of these carry adversarial verification and they must not be treated as confirmed. My own verification status, finding by finding, so the next agent does not redo it. CONFIRMED AND ALREADY FIXED: the postponed/cancelled VOID path bypassed every guard this PR added, voiding a live game off a sibling fixture's postponement - fixed in b8c05c7c2, and independently reported by Devin Review the same hour, which is the strongest quality signal the audit produced. REFUTED, do not act on it as written: the claim that settle-sport.ts:474 has 'no score predicate' is wrong - it has a SCORE_MISMATCH_CROSS_PATH conflict check that refuses the overwrite. What IS true there is narrower: the check is a preceding READ and the write is an unguarded update by id with no commenceTime bound, so it carries the same read-then-write race shape the runner just closed. SINCE VERIFIED AND FIXED (second confirmed finding): the zero-sit VOID write carried no kickoff predicate. I have now read the lane's selection logic and it is NOT a founder policy question as I first recorded it - decideZeroSitVoid returns skip for every candidate under the minimum age, so ageHours >= the floor is a NECESSARY condition of every void this lane issues, and that age is computed from the commenceTime captured at candidate load. The scoreboard fetch for up to 20 picks sits between that read and the write, so the window is wide. The unpublish half of the SAME FILE already carries its full predicate into its own updateMany with a comment giving exactly this reason, so this is an internal inconsistency, not a design choice. The game-cancel write had the same hole and was worse: a game rescheduled into the future and still SCHEDULED would be marked CANCELED. Both bounds now ride in the WRITE as relation filters; a refused write lands in the existing WRITE_RACE_LOST bucket and the pick stays PENDING for the next cycle, so no new accounting was needed and no VOID semantics changed. ALSO VERIFIED AND FIXED (third confirmed finding): settle-backfill graded the pick FIRST, then on a score mismatch declined the game write and let the grade stand - the code said so outright, 'the pick settlement already happened above'. That commits a grade computed from the incoming score onto a game row that keeps a different one, and records it nowhere. This produces exactly the signature I measured on production in C-115, settled picks contradicting the score on their own row; state it as a MECHANISM that produces that signature, NOT as the established cause of those 80 rows, which I have not traced to this path. Fixed by rolling the whole transaction back through a sentinel throw: the grade rests on the same contested number as the score, so both go or neither does. The refusal is now recorded as held with reason SCORE_MISMATCH, a new member of the UnresolvedStalePick reason union with no consumer outside this file and its tests, so the pick does not sit unaccounted and the zero-sit lane takes it under SCORE_MISMATCH_CROSS_PATH. The preceding read plus unguarded update by id was ALSO the read-then-write race shape, so the rule now rides in the write as a guarded updateMany, with the read kept only for the log line on the refusal path. FOURTH CONFIRMED FINDING, VERIFIED AND FIXED: the AMBIGUOUS_MATCH hold reporting an empty sources array. Confirmed, and MORE SPECIFIC than the lens stated. There are THREE AMBIGUOUS_MATCH holds in free-settlement.ts, not two: the city-only hold (empty sources), the own-fixture hold added by THIS PR's own fixture-binding work (also empty sources), and the disagreeing-candidates hold (sources populated). The zero-sit lane derived the cause from sources.length === 0, so the own-fixture hold was reported to operators as a city-only team NAME when the names were fine and the fixture simply could not be placed by any clock. Both causes void under the same rcaCode so no ACTION was wrong; the RCA reason an operator reads to diagnose the void was. On this product a false statement in an audit record is not cosmetic. Fixed by having the grader STATE its cause on an optional ambiguity field at all three holds, with the old inference kept as the fallback for an outcome predating the field, a third ZeroSitAmbiguityCause member NO_OWN_FIXTURE with its own detail text, and the own-fixture hold now also carrying the sources of the finals it could not place. BOTH REMAINING FINDINGS REFUTED AS ACTIONABLE, do not act on them as written. (a) 'finalBelongsToOwnFixture's rows.length < 2 is evaluated AFTER filters that can drop the sibling fixture': mechanically true, but those filters are LOAD-BEARING and removing them is a regression, not a fix. sameMatchupFixturesNearKickoff is called with a MULTI-DAY board, so the same two teams appear on it for every game of a series, and the startTime and drift filters are exactly what separates a doubleheader sibling from tomorrow's series game. Keeping an undatable row would make an ordinary series look like a doubleheader and hold, then VOID, every pick in it. free-settlement.test.ts already pins this - 'still settles when the only other board row is an adjacent-day clockless fixture' is a CodeRabbit regression guard from earlier in this same PR - so acting on this finding would have re-broken a bug already fixed here. The rows those filters drop that a person would call a sibling are ones with no usable date, and those cannot be placed by clock anyway, which is the same reason the guard refuses when it does see them. (b) 'the tie detector may treat two distinct fixtures listed at the same instant as one': the s !== best clause does tolerate one instant appearing twice, but it is load-bearing too. multi-source-scores.ts mergeGames dedupes on each source's OWN gameId, so one fixture reported by two sources with different ids survives as two rows at the identical instant; removing the clause would make those tie and refuse, holding and then voiding gradable picks. That case and a genuine same-instant doubleheader are NOT distinguishable with the data finalMatchesNearestFixture receives (start instants only, no fixture identity), and not by any fixture-identity key either, since two distinct fixtures with the same teams at the same instant collapse under any such key. A documented limitation, not a defect with a fix. NOTED, low: the scoreless hold's no-op update still bumps Game.updatedAt, a Prisma @updatedAt column, so 'it changes nothing' is true of the VALUE and false of the ROW | claude | OPEN | Audit run wf_a52fe63b-3a1, 8 lenses over free-settlement.ts, free-score-persist.ts, free-settlement-runner.ts, settle-backfill.ts, zero-sit-lane.ts and settle-sport.ts. Two lenses completed and returned 14 findings; the run stalled before the 3-skeptic refutation phase, so severities are the lens's own and unvalidated. Verified by me against the code 2026-09-07 00:4x UTC: settle-sport.ts:466-484 DOES contain the conflict check (finding refuted as written), zero-sit-lane.ts:829-835 pick.updateMany carries only id and result PENDING (write predicate confirmed absent, semantics NOT established), stale-pick-policy.ts:83-96 shows the UNPUBLISH lane targets commenceTime gt now which is a different lane from the VOID path flagged. VOID race fix verified by negative control 2026-09-07 00:5x UTC: with the two bounds reverted the two new tests fail as voided 1 (expected 0) and gamesCanceled 1 (expected 0) while the other 33 in that file pass, so both tests discriminate. With the fix: zero-sit 35/35, apps/web 12442 passed 97 skipped (was 12440, exactly the two added), typecheck 0, lint 0, guardrails 26/26. settle-backfill rollback verified by negative control the same way: with the sentinel throw replaced by the old fall-through, the new test fails as settled 1 (expected 0) while the added no-final and idempotent-re-run tests still pass, so the rollback fires only on a genuine conflict. NOTE on control discipline: my FIRST negative-control run on that one reported 13/13 passing, which would have read as a non-discriminating test; the edit had silently not applied because I matched the wrong indentation. Re-applied and confirmed. A control that changes nothing passes for the wrong reason, so always verify the control edit landed. After both fixes: apps/web 12444 passed 97 skipped, typecheck 0, lint 0, guardrails 26/26. Ambiguity attribution fix verified by negative control on BOTH halves independently: reverting the zero-sit reader to the sources-length inference fails the NO_OWN_FIXTURE test, and separately reverting the grader's own-fixture hold to empty sources with no stated cause fails the free-settlement test. After all four fixes: apps/web 12446 passed 97 skipped (12440 at the start of this batch, six tests added), typecheck 0, lint 0, guardrails 26/26. packages suite 4537 passed with ONE pre-existing failure unrelated to any of this: packages/dev-tools/context-compiler shells out to git rev-parse fcec492c and this container holds a SHALLOW clone of 348 commits that does not contain that commit. Not caused by these changes and not in any file they touch. Raw findings at the workflow transcript dir |
| C-122 | Review round 18 on #717: one bug in code THIS SESSION wrote, plus two real CodeRabbit findings, one of which had an INCOMPLETE proposed fix. (1) Devin, CONFIRMED: persistZeroSitVoid guarded the pick write with a relation filter on game.commenceTime, but a relation filter locks the PICK row, not the game. A reschedule committing between the pick write and the cancellation left the transaction committing a permanent VOID on a game that will be played, with the cancel matching nothing. My own comment said Prisma does not lock the game row and I then relied on a filter that only reads it - the exact 'fixed the branch I was looking at' pattern the check-in warns about. Fixed by acquiring the game row LOCK FIRST: write the kickoff back to the value the candidate carried, matched on that exact value, which refuses any move and holds the row to commit. Same lock acquisition free-settlement-runner.ts uses. (2) CodeRabbit, CONFIRMED: findPostponedMatch copied a DATE-ONLY board start into TrustedFinal.startIso, which parses as midnight UTC, so a timed pick read as more than MAX_KICKOFF_DRIFT_MS adrift and the postponement went undetected (PENDING/NO_FINAL instead of its direct VOID). IMPORTANT, their proposed fix was INCOMPLETE and adopting it verbatim REOPENED a bug this PR had already fixed: dropping startIso alone falls through to finalBindsToKickoff's clockless +/-1 DAY calendar rule, so YESTERDAY's postponement voided a game played today, which is the series bug the clock binding exists to stop arriving through the clockless path. Caught by writing the negative test alongside the positive one. Real fix is both halves: omit startIso when the row has no clock AND require a clockless postponement to be the pick's own DAY, matching how sameMatchupFixturesNearKickoff already narrows clockless fixtures. (3) CodeRabbit, CONFIRMED: persistFreeScores queried commenceTime gte only, so every FUTURE scheduled fixture for the sport matched the OR clause (all SCHEDULED with null scores) and competed for the take 300 cap with the started games the pass exists to score. Worse than reported: MLB runs about 15 games a day, so the 21-day window is already near the cap BEFORE any future fixture is counted, and the starved rows are exactly the ones that go overdue. Fixed with an upper bound plus a deterministic oldest-first ordering (the cap was previously an arbitrary database slice). (4) CodeRabbit, ACCEPTED as a records-honesty fix: the C-118 row asserted a live production bypass that 2f9f7d90e had already closed; reworded to historical with the remediation scope kept OPEN. | claude | DONE | #717. Each fix has an independent negative control: removing the game hold fails the two new ordering/refusal tests (writeOrder reads pick-first instead of game-first); restoring the date-only startIso fails the date-only VOID test; and the yesterday-date-only test is what caught the incomplete fix mid-flight. Four existing zero-sit assertions were retargeted from gameUpdates to a new gameCancels list rather than loosened, since the kickoff hold is a lock acquisition and not a cancellation, so each keeps its exact original meaning. NOTE the fake cannot model row locking, so what the tests prove is the ORDER (the hold runs first) and the REFUSAL (a moved game stops everything at the hold); the lock semantics rest on Postgres. Verify after all four: typecheck 0, lint 0, guardrails 26/26, apps/web 12450 passed 97 skipped, up from 12446 by exactly the four tests added. |
| C-123 | Review round 19 on #717, Devin CONFIRMED and it is the SIXTH time a defect traced to the sibling branch of my own preceding fix. Round 15 made the SCORELESS game write in free-settlement-runner match the kickoff on its EXACT value, because a correction to a DIFFERENT PAST time satisfies lte and silently invalidates the binding. The SCORED branch of the same if/else was left on lte: settledAt, and free-score-persist's write on lte: now. In both, the chosen final was bound to the kickoff read before the network work within MAX_KICKOFF_DRIFT_MS, so a correction to another past time still passed the write predicate and the row took a score matched to a kickoff it no longer had. Both now match the exact value, which implies the started check since a future kickoff was already skipped. The runner's refusal CLASSIFIER was wrong too and is fixed: it compared the re-read kickoff against settledAt, so a correction to a different past time was reported to the operator as a SCORE conflict, sending them to look for a disagreeing final that does not exist; any change to the kickoff is now a schedule correction. | claude | DONE | #717. Negative control on both halves together: reverting the two predicates fails 4 tests, and the runner one fails as count 1 rather than 0, meaning the settlement COMMITS despite the correction - the defect reproduced, not just an assertion shape. Two existing persister assertions were tightened rather than loosened (lte: any Date became an exact Date equal to the row's kickoff); one new persister test and one new runner test, the runner's being the explicit SCORED sibling of the scoreless test round 15 added. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12452 passed 97 skipped, up from 12450 by exactly the two added. |
| C-124 | Review round 20 on #717, Devin CONFIRMED an ABBA DEADLOCK that round 18's own fix introduced. Round 18 closed a real race by holding the GAME row before the PICK in persistZeroSitVoid. Verified by reading all three settlement transactions: free-settlement-runner writes pick(212) then game(284/321), settle-backfill writes pick(394) then game(425), and zero-sit's VOID half had become game(857) then pick(868). The zero-sit and free-settlement candidate sets OVERLAP BY CONSTRUCTION - both select PENDING picks whose game has commenced - and settle-picks runs about five times an hour with no durable lease (C-99 still OPEN), so two lanes taking the same two rows in opposite orders is reachable, and Postgres resolves it by aborting one transaction. Fixed by restoring the common PICK-then-GAME order and making the game hold UNCONDITIONAL and rollback-on-refusal through a ZeroSitKickoffMoved sentinel, which is exactly free-settlement-runner's structure. Unconditional matters: SCORE_MISMATCH_CROSS_PATH and AMBIGUOUS_TEAM_NAME voids write no other game statement, so without it nothing would detect a move after the pick write for those codes. The caller maps the sentinel to WRITE_RACE_LOST, not WRITE_FAILED - a deliberate rollback is a lost race, not a poison row. | claude | DONE | #717. The test fake now MODELS ROLLBACK (undo log over the mutable fields), because persistZeroSitVoid writes the pick and then throws, and a fake that kept the write would let a test assert an outcome Postgres would never produce. Negative control: removing the hold fails the rollback test as voided 1 rather than 0, so the original round-18 defect reproduces on real behavior, not on an assertion shape. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12453 passed 97 skipped, up from 12452 by the one net added test (two rewritten, one added). LESSON, seventh instance and the first CROSS-FILE one: a fix that changes the ORDER of writes changes a global invariant, so the blast radius is every other transaction touching the same tables, not just the file being edited. |
| C-125 | C-119 MEASUREMENT COMPLETE, and it REFUTES the guard I was about to write. Founder decision needed; no code shipped. All figures read-only production SQL 2026-09-07 02:5x UTC. (1) SCOPE: 1116 published SPREAD picks; 568 (51 pct) carry a line NO book quoted for that game. By sport, not-quoted: MLB 306/725 (42 pct), NCAAF 116/161 (72 pct), NFL 79/123 (64 pct), MLS 53/83 (64 pct), NBA 12/12 (100 pct), NHL 2/12 (17 pct). (2) THE NAIVE GUARD IS WRONG. 'Refuse a line no book quoted' would remove 51 pct of the entire spread board including 72 pct of NCAAF, and it SHOULD: the mean of minus 3, minus 3.5, minus 3 is minus 3.1667, which no book quotes but which is a legitimate consensus. Football and MLB are different problems and must not share a rule. (3) MLB IS STRUCTURALLY DIFFERENT and is the real rule-1 exposure. The run line is fixed at 1.5 with 2.5 and 3.5 alternates. Published MLB abs(line): 1.5 -> 326 picks, 2.5 -> 28, 3.5 -> 16, so valid run lines are 370 of 725 (51 pct) and 355 (49 pct) are not. The invalid ones split into near-misses that are visibly means (1.375, 1.4375, 1.227272, 1.416666, 1.454545, 2.375, 1.0625, 0.681818) and far values that cannot be baseball at all (4.5, 5.5, 7.5). (4) CONTAMINATION SOURCE, and it is NOT one bad book: 8 of 12 MLB bookmakers carry impossible spreads (betrivers 3658, bovada 1461, draftkings 1069, betmgm 1050, fanduel 1012, fanatics 997, williamhill_us 974, mybookieag 891), 11112 rows of 426019 (2.6 pct), reaching abs 19.5. The 4 clean books (espn_public, betonlineag, lowvig, betus) are exactly plus or minus 1.5 with min = max = 1.5 and zero alternates. (5) MIS-KEYING REFUTED AS THE SOLE EXPLANATION: 486 of 1086 MLB games (45 pct) carry at least one impossible spread row, averaging 22.9 bad rows per affected game. That is systemic, not a handful of mis-keyed fixtures. NOT ESTABLISHED and do not repeat as fact: which event those lines truly belong to. (6) A TEST I RAN THAT PROVED NOTHING, recorded so nobody re-runs it: comparing the total column on impossible vs valid SPREADS rows to identify a foreign sport is INCONCLUSIVE BY CONSTRUCTION, because odds rows are one-market-per-row and total is NULL on all 426019 SPREADS rows. Draw nothing from it. THE DECISION, founder-only: an MLB run-line guard refusing abs(line) not in 1.5, 2.5, 3.5 removes 49 pct of MLB spread picks from the board. That is a material product change trading board volume against rule 1, since a subscriber cannot place 'Athletics minus 7.5' on a baseball game at any book. The alternative, snapping the published line to a real quoted line, is BETTER for the product but changes what the engine publishes, which is a MODEL_VERSION bump, founder-sequenced for after 2026-09-13. I shipped neither. model-freeze.mjs permits a publish REFUSAL in scoring.ts (it guards only MODEL_VERSION against a paired calibration proposal) and the soccer three-way guard at scoring.ts:864 is the precedent, so the guard is buildable the moment the founder picks. | claude | OPEN | Verified mechanism unchanged from C-119: packages/prediction-engine/src/scoring.ts:394 avgSpread is the arithmetic mean of every book's spread and :410 publishes it directly as the pick's line, so a single contaminated row drags the mean off any quotable value. Queries were SELECT-only via the Neon MCP (founder-approved read exception); no writes. |
| C-126 | C-120 DONE, and it CORRECTS MY OWN EARLIER CLAIM. C-120 called adding the settle-time score to PickSettlementEvent.payload 'a small, safe, additive change'. Verified 2026-09-07 03:1x UTC that this was WRONG for exactly the picks that matter, and shipping it as described would have been a change that passes its own tests and silently does nothing in production. settlement-outbox/worker.ts expandEvent REPLACES the payload wholesale when it freezes it, so anything a settlement transaction wrote is destroyed. VOID events escape only because they are closed to DELIVERED as non-decisive receipts without touching payload, which is why zero-sit's payload survives and misled me; every WIN/LOSS/PUSH event, the entire C-115 population, is expanded and overwritten. FIXED IN TWO HALVES, both needed: (1) the worker now carries a settledWith key ACROSS expansion instead of rebuilding it, guarded by a narrow settledWithFrom type guard so a malformed record is dropped rather than half-carried; (2) all four settlement lanes now write it inside the settlement transaction - free runner (path free), settle-backfill (free-backfill), settle-sport (paid), zero-sit (zero-sit). HONESTY NOTES ON THE RECORD ITSELF: the paid path writes sources as an EMPTY array on purpose, because its normalized score carries no per-score source ids and inventing one would put an unobserved fact into an evidence record; the lane is identified by path instead. zero-sit writes homeScore and awayScore null by construction, because a void grades against no final, which distinguishes 'voided, no score existed' from 'graded against a score we no longer have'. This unblocks C-115: from now on a settled pick that contradicts its game row can be attributed to a MIS-GRADE or to a later game-row overwrite. It is NOT retroactive - the 80 already-settled contradicting picks still have no evidence and remain unattributable. | claude | DONE | #717. Negative control on the enabling half: removing the carry-across line fails the new preservation test as undefined instead of the evidence object, which is precisely the silent-destruction failure mode. Two further worker tests pin that an event with no evidence still expands and gains no fabricated record, and that a malformed settledWith is dropped. One existing settle-sport assertion was EXTENDED to cover the new payload rather than loosened, keeping its exact-shape intent. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12456 passed 97 skipped, packages 4537 passed with only the pre-existing shallow-clone context-compiler failure (git rev-parse fcec492c, unrelated). |
| C-127 | C-85 IS ALREADY FIXED FORWARD, but NOT by the fix it prescribes, and C-117 is re-scoped as a result. Measured read-only 2026-09-07 03:5x UTC. C-85 asks to unify the ESPN externalId namespace; that is stale. game-identity.ts resolveCanonicalGame (team pair plus 18h kickoff tolerance, fail-closed) is ALREADY wired into all three writers - seed-games-from-espn.ts:90, process-sport.ts:625, settle-sport.ts:411 - and it works in production. MEASURED: surplus game rows per day, grouped on sportId + both team names + EXACT commenceTime, against games created that day so the drop is a RATE not a count: Aug 27 to Sep 5 ran 10 to 112 surplus rows a day on 25 to 118 games created; Sep 6 = 3 surplus on 85 created; Sep 7 = 0 surplus on 58 created. Creation volume held steady while surplus collapsed, so this is a real change in behaviour, not a quiet slate. TWO CORRECTIONS TO MY OWN METHOD, so nobody repeats them: (a) grouping by date_trunc day instead of exact commenceTime OVER-counts, because it merges genuine same-day different-time fixtures - it reported 2 residual duplicate fixtures on Sep 7 which the exact-time query shows as 0, and inspecting them confirmed different kickoffs (Twins v Rangers 00:10 and 20:10 UTC); (b) attributing a duplicate SET to the day its NEWEST row appeared makes the percentage exceed 100 on some days, so treat those percentages as an attribution heuristic and the exact-time surplus counts as the measurement. CONSEQUENCE FOR C-117: its own instruction was 'fix C-85 first and re-measure before adding board-level de-duplication'. That re-measure is now DONE and C-85 has stopped bleeding, so board de-duplication would no longer mask an active bug - it would correctly hide a FROZEN backlog. But it is NOT a safe unilateral change: de-duplicating means CHOOSING which row to show, and the measured symptom is the same fixture carrying contradictory confidence (FREE 57 LEAN vs PREMIUM 88 STRONG_PLAY), so picking one publishes a confidence number over another. That is a founder product decision of the same class as C-125, not a display tweak. apps/web/lib/ops/game-merge-plan.ts already computes which row is canonical and is the natural input if the founder wants it built. | claude | OPEN | Queries SELECT-only via the Neon MCP (founder-approved read exception); no writes. C-85 left OPEN rather than DONE because its literal prescription (unify the namespace) was never applied and should be CANCELLED or rewritten by whoever owns it, not silently closed by me; what is established is that the duplicate-creation it exists to stop has stopped by other means. The 936-row historical backlog is unchanged and remains F-17, founder-gated. |
| C-128 | Review round 21 on #717, Devin CONFIRMED. EIGHTH instance of the sibling-lane pattern and the last settlement lane still missing the kickoff contract. Verified against the code before acting: settle-backfill persistInTx wrote the pick on id plus result PENDING with NO kickoff predicate, its scored game write carried the score OR but no kickoff bound, and a SCORELESS settlement never reached that branch at all, so a VOID committed with ZERO game statements and therefore zero game checks. The free runner and zero-sit had all of this; this lane had none of it. Fixed with the same contract the other lanes use: the started bound rides in the pick WRITE as a relation filter, then an UNCONDITIONAL hold on the game matched on the EXACT kickoff the candidate was loaded with, throwing BackfillKickoffMoved to roll the whole settlement back rather than returning, because the pick write has already happened by then. Unconditional is the substance, not tidiness: it is the only game statement a scoreless settlement makes. Lock order stays PICK then GAME, matching free-settlement-runner and zero-sit, so this does not reintroduce the C-124 deadlock. KICKOFF_MOVED joins the UnresolvedStalePick reason union and the caller records it as held, so the pick is not dropped silently. | claude | DONE | #717. Negative control: removing the hold fails both new tests, the scored one as settled 1 rather than 0, meaning it grades a game whose kickoff moved - the defect reproduces as behaviour, not as an assertion shape. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12458 passed 97 skipped, up from 12456 by exactly the two added. |
| C-129 | C-125 DECIDED BY THE FOUNDER 2026-09-07: refuse the non-run-line MLB spreads. SHIPPED. isPublishableSpreadLine in scoring.ts refuses a baseball SPREAD pick whose abs(line) is not on the run-line ladder 1.5, 2.5, 3.5 (float epsilon 1e-9, since the published line is a mean). Baseball-only by key prefix; every other sport returns true unchanged, because a football mean of minus 3.1667 from books at minus 3, minus 3.5 and minus 3 is a legitimate consensus and a blanket quoted-line rule would have removed 72 pct of NCAAF. Refusal only - the line is NOT repaired, which would change what the engine publishes and need a MODEL_VERSION bump. Precedent and shape copied from the three-way moneyline guard at scoring.ts:864, which was added the same way without a bump; model-freeze.mjs guards only MODEL_VERSION against a paired calibration proposal, so a publish refusal is permitted. A TRAP I FELL INTO AND CAUGHT, recorded because it is the second time: my first scorer-level test used spreads -1.5,-1.5,-1.5,-16.5 (mean -5.25) and the negative control PASSED, meaning the test proved nothing - a wildly contaminated mean is ALREADY refused by the existing dispersion and edge thresholds. Rewrote it to a NEAR miss, -1.5,-1.5,-1.5,-2.5 giving mean -1.75, which is a real published production value (5 picks) and which DOES publish without the guard. Only then did the control fail. Always pick the case that the guard alone decides. Expected effect on the board, from the C-125 measurement: 355 of 725 published MLB SPREAD picks sat off the ladder, so roughly 49 pct of future MLB spread picks stop being published. That is the founder's decision, taken deliberately against rule 1. NOT ADDRESSED and still open: the same contamination family in MLB TOTALS (OVER 11.9, OVER 13.2 observed), which the founder's instruction did not cover, and the 355 ALREADY-published rows, which need a database write and stay founder-gated. | claude | DONE | #717. Negative control on the scorer discriminates only after the near-miss rewrite: with the guard removed the -1.75 case publishes a SPREAD, with it the pick is refused. 8 tests (6 predicate, 2 scorer). Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12458 passed 97 skipped, packages 4545 passed with only the pre-existing shallow-clone context-compiler failure. |
| C-130 | C-117 DEDUPE APPROVED BY THE FOUNDER 2026-09-07 and SHIPPED, but it fixes TWO of the THREE multiplicity sources and the third is still F-17. A four-lens parallel map of the read paths established the facts first, and it REFUTED my earlier plan: game-merge-plan.ts is NOT reusable at read time, because selectCanonical needs pickCount, oddsCount and oddsLineSnapshotCount aggregates a read path does not have, its grouping is O(n squared) union-find, and its import targets a package barrel that depends on @sports/db. Do not try to wire it into the board. THE THREE SOURCES, all measured from the code: (1) duplicate GAME rows for one contest, (2) GateDecision has NO unique constraint and the board query takes the latest 100 by evaluatedAt with no per-game collapse, so repeated evaluations of one game in a day each became a row, (3) in the fallback path the scoringNow query (commenceTime gte now, SCHEDULED) and the gatedToday query (commenceTime today, no published pick) OVERLAP by construction, so one game became scoring-<id> AND gate-<id>. Sources 2 and 3 are almost certainly what produced the reported 'four times as two contradictory variants', and both are now fixed. FIX: a pure dedupeBoardRows keyed on gameId plus market, applied INSIDE loadBoardStateInner BEFORE the lane split and before the counts, so openPicks, gatedToday, sportsWatched and the meta rowCounts describe the rows a viewer is actually shown - deduping at the outer chokepoint would have left the board's own numbers disagreeing with its own rows. Winner is deterministic: strongest lane (PUBLISHED over SCORING over GATED), then higher confidence, then higher edge, then later updatedAt, then lexically smallest id, so the board does not flip between loads. DELIBERATELY NOT KEYED ON THE MATCHUP STRING: matchup is built from denormalized team-name columns that differ between rows for the same fixture, and keying on names is the identity guess game-merge-plan.ts explicitly fails closed on (a bare 'Los Angeles' must never prefix-match; MLB has two LA clubs). Collapsing two genuinely different games is worse than showing one twice. ALSO: mergedIntoGameId null now filters the board's decision query and the /api/picks gameFilter. That is the database's OWN canonicity marker so it needs no guess, and gameFilter is shared by the findMany AND the totalAvailableToday count, so the free tier's published-today number cannot disagree with the rows it counts. STILL OPEN, state it plainly: two NEVER-MERGED game rows for one real contest are still two rows on the board and two picks in the API. The merge is owner-run and most duplicates are never tombstoned, so that residue is F-17 and remains founder-gated. | claude | DONE | #717. Negative control: reverting only the two dedupe call sites fails the end-to-end test as 2 rows rather than 1 - the contradictory-confidence symptom reproduces through loadBoardState, not merely in the helper. 10 tests added (8 unit on the winner rule including determinism under input reordering and null-vs-zero confidence, 2 end-to-end through loadBoardState including the aliased-row query predicate). Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12468 passed 97 skipped, up from 12458 by exactly the ten added. CORRECTION 2026-09-07 (round 22, 236f14e40): the key described above as 'gameId plus market' was WRONG and is superseded. market is redacted to the literal ALL_MARKETS for every non-premium viewer, so that key merged a game's SPREAD and TOTAL for FREE while PRO kept both. The live key is boardDedupeKey(gameId, pickType). See C-131. |
| C-131 | Review round 22 on #717, two Devin findings on settle-backfill, both CONFIRMED against the code and both fixed. (1) SILENT DROP: a persist result of {written:false, refusal:null} fell through all three branches - not counted settled, not counted held, not listed in unresolved - leaving a still-PENDING pick in no count at all, which is exactly the silent sit the no-pick-ever-sits policy forbids. Reachable three ways, not just the injected boolean the review named: defaultPersist returns it when the db shim has no $transaction, and persistInTx returns it when updateMany matches 0 rows because ANOTHER LANE settled the pick first. Now counted in a new BackfillResult.writeNotApplied and listed under a new UnresolvedStalePick reason WRITE_NOT_APPLIED. Deliberately kept APART from held: a hold is a decision this lane made and recorded a reason for, and this is the absence of one, so folding them together would report a concurrent settle as a deliberate hold - the misclassification the review itself warned against. (2) STALE KICKOFF: the KICKOFF_MOVED record reported the commenceTime and ageDays this cycle had LOADED, which is precisely the value the write had just proved the row no longer carries, so a game corrected INTO THE FUTURE could surface as weeks overdue and be escalated. Did NOT re-read the corrected time: BackfillDb exposes no game.findUnique outside the transaction and the in-transaction one selects scores only, so reading it would mean widening a shim contract every injected test double implements, and the transaction has already rolled back by then. Instead the record carries kickoffStale: true and reports olderThanGrace: false, withholding escalation for one cycle rather than raising it on a number known to be wrong; the next pass re-inspects against the corrected kickoff and escalates honestly then. Every other reason sets kickoffStale: false and the SCORE_MISMATCH test asserts that so the flag cannot quietly spread. 3 new tests. Negative control: reverting the accounting and the stale-age handling fails exactly those three and leaves the other 14 green. | claude | DONE | e8b394a68 on claude/sports-launch-round2-fixes-hk7kv9. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12473 passed 97 skipped. Devin threads r3949649293 and r3949649208 replied to and resolved. |
| C-132 | Rule 1: the scorer FABRICATED a price. scoreGame fell back to -110 when no book quoted the chosen side, in four places (spread avgPrice, both sides of the spread vig removal via ?? -110, total avgPrice). That invented number reached the subscriber as the pick's odds AND was committed into the immutable PickProofReceipt as entryOdds; -110 is plausible-looking, which is what makes it dangerous, since nothing downstream can tell it from a real quote. REACHABLE, not theoretical: sanitizeAmericanPrice (normalizer.ts) returns undefined for a missing, null, non-finite OR decimal-format price, so a row can carry a real spread with no price. NOT CURRENTLY FIRING, measured not assumed: production has 0 rows missing a price across 2,268,217 spread rows and 2,316,785 total rows, so the guard is a no-op on today's feed and cannot regress the live board. Both avgPrice paths now refuse; vig removal averages only over books quoting BOTH sides, because an overround is defined on a two-sided market. | claude | DONE | 26ae9d905. Surfaced by the launch-readiness audit as LAUNCH_BLOCKING; the severity was wrong (0 affected rows) but the defect was real. 8 tests. Verify: typecheck 0, lint 0, guardrails 26/26, model-freeze OK on v5.2.7, prediction-engine 3091 passed, apps/web 12473 passed. |
| C-133 | Review round 23 on #717. TENTH instance of the sibling-lane pattern, and the first where the sibling was code I had written ninety minutes earlier. (1) SPREAD EDGE, Devin CONFIRMED: C-132 narrowed vig removal to two-sided books but left avgPrice drawn from EVERY book. The edge is a COMPARISON of avgPrice against fairProb, so sourcing them from different book sets compares mismatched markets and a single one-sided outlier could manufacture an edge no complete market shows. I had noticed that asymmetry while writing C-132 and rationalised it as two honest answers to different questions; that was wrong. Checking it went FURTHER than the review: spreadOdds.length also fed MIN_BOOKMAKERS, market depth, the volatility penalty, bookmakerCoverageMax, risk level, the reason string 'backed by N% of N bookmakers pricing this market', and the pick's PUBLIC bookmakerCount, so an unpriced book was counted as pricing the market. One pricedOdds set is now canonical for every price- and depth-derived value; line consensus still uses every book carrying a spread, because a line without a price is still real information about where the line sits. (2) BOARD FALLBACK, two findings: published rows key on pickType and generic lanes on NO_PICK so their keys never collide and the collapse could not pair them, while the gated query excludes published games but the scoring query does not; and the scoring query selects commenceTime >= now AND status SCHEDULED, i.e. games that have NOT started, which LANE_RANK then ranks ABOVE gated, so C-117 made a pre-existing mislabel DETERMINISTIC in the wrong direction. Generic lanes are now suppressed for any fixture a more specific lane already represents; LANE_RANK is untouched because on the decision path SCORING_NOW is a real GateDecision status and should outrank gated. | claude | DONE | 6ad708bdc. CORRECTION TO THE RECORD: a C-132 test asserted an input with one incomplete book stops publishing; that pinned the half-fix, not a principle, and now asserts the incomplete book is simply absent. Also observed and NOT fixed (out of scope, no row yet): a row-construction throw in the board fallback degrades silently into an empty board rather than raising. 2 loader-level tests, negative-controlled. Verify: typecheck 0, lint 0, guardrails 26/26, prediction-engine 3092 passed, apps/web 12475 passed. |
| C-134 | Review round 23 continued, Devin CONFIRMED: settle-backfill reported three different pick states as one outcome. The settle write's predicate is id AND result PENDING AND game.commenceTime <= settledAt, so updateMany matching 0 rows means another lane already graded the pick, OR the kickoff moved past settledAt, OR the row is gone. C-131 collapsed all three into WRITE_NOT_APPLIED, which listed a COMPLETED pick as outstanding work and dropped the kickoffStale marking on the one case that needs it. The write now probes the pick inside the same transaction: result no longer PENDING becomes a new alreadySettledElsewhere counter kept OUT of both unresolved and held (nothing is outstanding about a finished pick and the lane made no decision); still PENDING with a later kickoff routes to KICKOFF_MOVED and inherits its stale-age handling; anything else keeps the undiagnosed outcome. The probe is an OPTIONAL shim method: 13 test files inject a $transaction double and requiring a new method would have broken all of them for a diagnostic none of them exercise. | claude | DONE | 8f553277c. 3 tests incl. one pinning the no-probe fallback so the optionality is a recorded decision. Negative control: removing the probe fails exactly the two race tests and leaves the fallback green, which is the correct shape. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12478 passed. All 56 review threads on #717 replied to and resolved. |
| C-135 | Review round 23 continued, two Devin findings, both on code written that hour. (1) TOTALS SCORER, the ELEVENTH sibling-lane instance and the first where I fixed one market and left its twin half-done IN THE SAME FILE: scoreTotalPick used THREE different book sets - chosenPrices from totalOdds, each implied average from its own independent one-sided filter, and every depth/risk/count value from totalOdds again. Same consequence as the spread path: the edge COMPARES avgPrice against fairProb, so mixed sets compare mismatched markets, and a book quoting neither direction was still counted in the public bookmakerCount. pricedTotals already existed for the tiebreak and is now canonical for every price- and depth-derived value, MIN_BOOKMAKERS gated on it; line consensus keeps every book carrying a total. (2) BOARD SCORING LANE: the previous commit suppressed a scoring row only when a gated row shared its id, and the gated query covers today, so a game further out had no gated twin and stayed labelled SCORING_NOW - the same false claim with a smaller blast radius, not a fix. GameStatus carries a real LIVE value, so the query now selects commenceTime <= now with status in LIVE/SCHEDULED. | claude | DONE | e5d78ebe7, merged to main in #717 as b665a270d. 4 tests. Negative controls: reverting the totals set fails exactly the outlier test; reverting the query predicate fails exactly the predicate test. Verify: typecheck 0, lint 0, guardrails 26/26, model-freeze OK v5.2.7, prediction-engine 3094 passed, apps/web 12479 passed. |
| C-136 | REVIEW ROUND 24, and TWO OF THE FOUR ARE REGRESSIONS #717 SHIPPED AND DEPLOYED. Recording that plainly: the merge was green on all 31 checks and still carried them; what caught them was a reviewer reading the diff after the merge, not CI. (1) RED, mine: C-135 changed the scoring query to select STARTED games but left the gated query covering the whole slate, so a started same-day game with no published pick satisfied BOTH, and the suppression added alongside dropped the scoring row - leaving a started game reading GATED_TODAY after kickoff, the same false label inverted. (2) mine: commenceTime lte now had NO LOWER BOUND, so any historical row still marked SCHEDULED (a game whose settlement failed) stayed eligible forever and would surface as SCORING_NOW on a quiet slate. (3) pre-existing: outranks compared confidence BEFORE the timestamp, but GateDecision rows are repeated evaluations and confidence legitimately FALLS as the line moves, so an older stronger reading beat the newer downgrade and the subscriber saw a number the model no longer stood behind. (4) pre-existing, TWELFTH sibling-lane instance: #717 gave the FALLBACK path published-suppression and left the primary GateDecision path without it. FIX: the lanes are now DISJOINT BY PREDICATE (scoring = started, within an 8h active window; gated = not started) rather than arbitrated afterwards, and the gated-versus-scoring suppression is DELETED because with the lanes correct it would re-break finding 1. Two tests from #717 are CORRECTED rather than kept: they asserted an overlapping game ends up gated, which pinned the workaround and would now pin the RED bug. | claude | DONE | c0cfa2b07 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 5 tests, each fix negative-controlled independently. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12482 passed. |
| C-137 | Founder 2026-09-07: "NO PICKS SHOULD EVER BE PENDING ON A HUMAN", plus Neon access. Built the remediation tool rather than hand-writing SQL against 586 production rows, because that is exactly what the law-7 amendment proposed an hour earlier says must stay forbidden. scripts/ops/unpublish-corrupted-picks.ts plus a tested selection module, same shape as adjudicate-stale-picks.ts: dry run by default, one bounded updateMany per population guarded on isPublished so a re-run is a no-op, nothing deleted, no result rewritten, no game row touched. MEASURED on production read-only: C-114 87, C-118 148, C-125 353, distinct 586 of 3003 published, of which only 5 sit on upcoming games - the impact is on the track record, not the live board. TWO THINGS THE MEASUREMENT SETTLED: revert-to-PENDING is ACTIVELY WRONG for C-114 because all 87 games have since played and SCORE_MISMATCH_CROSS_PATH refuses to overwrite an existing final, so the phantom score STUCK and re-grading would re-derive the same wrong answer; and the C-125 guard is HOLDING, last off-ladder pick generated 17:00 UTC against an 18:19 UTC deploy. HONESTY NOTE, stated because it is my own write moving a public number: the withdrawn set is 232W/210L (52.5pct) against an overall 53.8pct, so removing it nudges the remaining record UP about 0.3pp. That is not why to do it - the rows are wrong in either direction - but it must not pass silently. NO PUBLISH CASCADE: verified autoPublish false, published false, source none, streak 0/3, so moving the calibration sample cannot flip a public claim. | claude | BLOCKED | 9e4410997 + 1103b3b1b (npm run ops:corrupted-picks). 14 selection tests, every population with a negative control. BLOCKED ON: the agent-side production write is denied by a safety classifier (destructive SQL needs human approval) and the script needs DATABASE_URL, which law 3 forbids searching for. Owner runs: npm run ops:corrupted-picks -- --population all (dry run), then --execute. |
| C-141 | CodeRabbit on #719, VERIFIED REAL and deliberately NOT fixed in that PR - queued here so it is a unit of work rather than a dropped comment. #719 makes the two board lanes disjoint by predicate and gives the primary GateDecision path published-suppression, but that suppression is built from the GateDecision rows the query itself returns. A game whose PUBLISHED decision row falls OUTSIDE the query window (the decision was evaluated earlier and only a later GATED re-evaluation lands inside it) therefore contributes no id to publishedDecisionGameIds, and the later GATED row survives - the subscriber sees GATED_TODAY on a game that already has a published pick. Narrower than the C-136 family: it needs two evaluations of the same game straddling the window edge, and the mislabel is a stale gate rather than a false SCORING_NOW. NOT fixed in #719 because the fix changes what the decision query SELECTS (a per-game published probe, or widening the window and folding afterwards), which is a different change from the disjointness fix and would have shipped untested inside a review round. THE THREAD IS LEFT UNRESOLVED ON PURPOSE so the finding stays visible on the PR. | claude | OPEN | No code. Finding recorded on the #719 review thread with the reply stating it is real and deferred; apps/web/lib/board/state.ts publishedDecisionGameIds is the site. Entry test when claimed: a GateDecision PUBLISHED row older than the window plus a GATED row inside it must not produce a GATED_TODAY board entry. |
| C-142 | REVIEW ROUND 25, Devin on #719, CONFIRMED and it is mine from round 24. C-136 gave the scoring lane a lower bound so a historical row a failed settlement left marked SCHEDULED could not surface as SCORING_NOW forever. I applied that one eight-hour bound to BOTH status values, and the consequence for LIVE is not a mislabel but a DISAPPEARANCE: the gated lane starts at gt: now and will not take a started game, so a LIVE game more than eight hours past kickoff left the board entirely. A rain-delayed MLB game or a lightning-suspended football game runs past eight hours, and the row the ingestion layer is actively calling LIVE is exactly the one a subscriber most wants to see. The two status values are not the same evidence: SCHEDULED past kickoff is AMBIGUOUS (started, or never updated), which is why it needs the tight bound; LIVE is a positive assertion. FIX: the bound is per status - an OR of LIVE within 24h and SCHEDULED within 8h, both still lte: now so the lanes stay disjoint from gated at gt: now. LIVE keeps a bound rather than none, because a LIVE row also outlives the game when the transition to FINAL fails, and removing the bound would reinstate the C-136 defect under a different status. | claude | DONE | b16edaa7b on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 1 new test plus the lane-predicate test extended. TWO negative controls, each failing exactly the intended assertions: collapsing the LIVE window to 8h fails the regression test (LIVE at 9h expected true, got false); deleting the LIVE lower bound fails it the other way (LIVE at 25h expected false, got true). Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12507 passed. |
| C-143 | SPLIT OUT OF C-115, and it is a shipped honesty defect rather than a bug: the line a settled pick is GRADED on is not always the line its card DISPLAYS. selectGradingLine() takes clvLockLine when present and falls back to line; the card renders line. Measured on production read-only 2026-09-07 19:5x UTC over 588 published settled TOTAL picks: the two differ on 432 of them (73pct), and on 34 rows the difference is large enough to FLIP the outcome, so a subscriber reading the card computes a different result from the one we published. Worked example cmtp7olso04ci9t2po0eeh9eo, card reads OVER 83.5, actual total 77 which loses that number, graded WIN against clvLockLine 74 - correctly, by its own rule. Which number is RIGHT is a founder question, not an agent one: clvLockLine is the line at publish time and is arguably the honest one to grade, in which case the DISPLAY is what must change; line may have moved after publish. What is not defensible either way is showing one number and settling on another with no indication on the card. Not a grading defect and must not be conflated with the 47 rows of C-115 population B, which are explained by neither line. | claude | OPEN | No code, no remediation. Read-only SELECT via the Neon MCP; no writes. Figures: 588 published settled TOTAL picks with a final, clvLockLine differs from line on 432, outcome-flipping on 34 (all 34 inside the 81 that contradict the display line). Entry files: selectGradingLine and its call at apps/web/lib/data-sources/settle-backfill.ts:344, and whatever renders the pick card line. Needs a founder decision on which number is canonical before any code change. |
| C-144 | REVIEW ROUND 26, two Devin findings, both CONFIRMED and both on paths where being wrong costs real money or real history. (1) MONEY PATH: verifyEnvPriceAmount compared unit_amount and NOTHING else, so a price carrying the right number in the wrong currency, or the right number on a YEARLY interval, or the right number billed every three months, all read as a match and would have charged a monthly subscriber something other than the monthly rate the page promised. Fixed by composing three axes at the call site rather than widening stripePriceAmountMatchesAd, because that predicate is SHARED with the deploy-time gate (scripts/lib/stripe-price-check.mjs) which layers its own currency axis on top - my first attempt did widen it and broke four of that gate's tests, which is the gate telling me it owns its own composition. The interval half lands as stripePriceIntervalMatchesAd, which is the exact helper intervalMatchesAd has been reaching for by name since #612 and falling back to an inline copy for; the deploy gate now uses one predicate with the runtime instead of two. interval_count is checked, not just interval, because "every 3 months" is a month-interval price. (2) REMEDIATION TOOL: the settled-before-kickoff population selected on settledAt < commenceTime alone, and a postponed fixture settles VOID (POSTPONED_OR_CANCELLED) and is then RESCHEDULED, which moves commenceTime past a settlement that was correct when written. The tool would have UNPUBLISHED a true result in the name of cleaning up false ones - the worst failure a remediation tool has. VOID is now excluded in whereFor, in narrow, and in the database-side write predicate, all three, so a concurrent postponement cannot widen the write set the recheck was added to narrow. Neither row has been written to production yet, so this caught it before it cost anything. | claude | DONE | 424980ed6 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 6 tests. Negative controls: dropping the currency and cadence terms fails exactly the four new price tests; dropping the VOID exclusion fails exactly the two new selection tests. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12513 passed, deploy-gate suite 27 passed unchanged. |
| C-145 | REVIEW ROUND 27, three findings, and the important one is that I WROTE A FALSE PREMISE INTO A SAFETY COMMENT ONE COMMIT EARLIER. (1) THE REMEDIATION TOOL, found independently by CodeRabbit and by Devin (Devin rated it RED): C-144 re-validated the settled-before-kickoff predicate database-side and left the other two guarded on id plus isPublished only, on my written reasoning that they "key on immutable facts - the sport and the pick's own line". `line` IS NOT IMMUTABLE. packages/ingestion-pipeline/src/process-sport.ts:1092 refreshes it on every ingestion cycle for picks that already exist - `result` and `settledAt` are deliberately excluded from that update and `line` is deliberately included - so an off-ladder MLB run line can become a VALID one between select and write, and the tool would have withdrawn a pick that is no longer corrupt. The fix is not a check on the MLB branch: ALL THREE populations now re-evaluate their own predicate inside the write, driven off MLB_RUN_LINES so the SQL and the in-memory selector cannot drift, because a remediation tool must never write on a classification it made in the past. (2) The id list is bound as one Prisma parameter against a `text` column, so ANY() needs ::text[] spelled out or postgres cannot resolve it (CodeRabbit). (3) BOARD PRECEDENCE: outranks compared LANE_RANK before updatedAt, so an OLDER SCORING_NOW decision beat a NEWER GATED_TODAY one on the same fixture and the board showed a state the model had moved on from (CodeRabbit). Fixed NARROWER than the reviewer proposed: their suggestion was timestamps before lane rank unconditionally, which would let a newer GATED row displace an older PUBLISHED one - the exact false label C-136 and C-141 are about. A published pick is a FACT, not an evaluation, so it wins outright; recency decides between the two evaluation lanes; lane rank survives only as the same-instant tie-break. | claude | DONE | 2e1c2ac56 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 8 tests. THREE negative controls: reverting to lane-rank-first fails exactly the newer-gated test; applying the reviewer's literal suggestion (no published precedence) fails exactly the older-published test, which is why the narrower fix is the right one; the write-predicate tests fail if any guard is deleted. STATED LIMITATION: the raw SQL is NOT EXERCISED by the suite (no database in tests) - those tests assert the invariant is present in the source, not that postgres accepts it. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12545 passed. |
| C-146 | REVIEW ROUND 28, Devin, CONFIRMED: THE THIRTEENTH SIBLING-LANE INSTANCE, and this one is directly visible to a subscriber. The board page loads two things independently - loadBoardState, which shows published picks and gated rows, and loadBoardPasses, which shows "evaluated without publishing". c0cfa2b07 gave the STATE loader published-suppression and left the PASS loader without it, so the same fixture could appear as a live published pick in one section and as a pass in the other, on the same screen. The detail that makes it more than a missed line: passes.ts already DECLARED publishedPickRelation at line 108 and applied it only to the fallback game query at line 163 - the exclusion existed in the file and was simply never wired to the GateDecision query above it. Fixed at the QUERY, not after it, because filtering in memory would silently shrink the take:100 window and drop genuine passes off the end. Second defect found while fixing the first, not reported by anyone: the pass query took the latest 100 decisions with NO per-game collapse, so one fixture evaluated four times in a day produced four pass rows carrying different reasons and confidences - the identical contradiction C-117 fixed on the board itself, still live in the pass list. dedupePassesByGame collapses to the newest per fixture and deliberately does NOT re-sort, so dropping the query's orderBy surfaces as a failure instead of hiding. | claude | DONE | 898d1e3af on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 6 tests, new file board-passes-published-suppression.test.ts. TWO negative controls: removing the published exclusion fails exactly the query-level test; removing the collapse fails exactly the one-row-per-fixture test. Plus a positive control (a fixture with no published pick still lists) so a suppression that removed everything cannot pass. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12551 passed. |
| C-147 | REVIEW ROUND 29, Devin, CONFIRMED, and it is a LAUNCH-SEQUENCING defect rather than a cosmetic one: the board decided PUBLISHED_TODAY from GateDecision.status alone and never looked at whether the linked pick is STILL published. GateDecision records what we decided at a moment in time; Pick.isPublished records what is live now; nothing kept them in step. So a withdrawn pick kept its PUBLISHED_TODAY row AND kept suppressing that fixture's gated row, which means the board went SILENT about the game rather than showing its real state. MEASURED read-only 2026-09-07 20:5x UTC, and it is already live: of 811 PUBLISHED decisions, 84 point at picks with isPublished=false TODAY, before any remediation has run. scripts/ops/unpublish-corrupted-picks.ts withdraws 586 more, every one of which would have stayed on the board looking published - a remediation the product surface ignores is not a remediation, and this would have silently voided the whole C-114/118/125 cleanup. The same read settles the null-link question Devin asked: all 811 PUBLISHED decisions carry a resolvable pick row and all 356 GATED ones carry none, so failing closed on a missing link costs nothing today and exists only so a broken reference cannot render as a published pick that does not exist. Withdrawn rows are DROPPED, not relabelled: GATED_TODAY would claim we evaluated and passed, which is false (we published and then withdrew), and a published decision carries no reason to display. openPicks and booksPolled now count the displayable set, so the board's own numbers cannot disagree with its own rows. | claude | DONE | 09d3a8cad on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 3 new tests plus 5 existing pick fixtures made explicit about isPublished. Negative control: trusting decision.status alone fails exactly the two withdrawn-pick tests. Positive control included, so dropping every published row cannot pass. Read-only SELECT via the Neon MCP; no writes. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12554 passed. |
| C-148 | REVIEW ROUND 30, four findings, THREE OF THEM ON THE DETECTOR I WROTE AN HOUR EARLIER, and one of those makes its headline number incomplete. (1) FALSE NEGATIVE, CodeRabbit, the important one: classifySettledPick returned CONSISTENT as soon as the DISPLAYED line agreed and never evaluated clvLockLine - the line production actually grades on. MEASURED read-only 2026-09-07: 46 published TOTAL picks are right against the card and WRONG against the line settlement grades on, and the detector called every one of them clean. A detector that reports a clean bill on 46 anomalies is worse than no detector because it converts an unknown into a false assurance. Now a fourth verdict, GRADED_ON_DISPLAY_LINE, kept separate from C-143's GRADED_ON_LOCK_LINE because they have different owners: one says the CARD is wrong, the other says SETTLEMENT did not follow its own documented rule. Also from the same finding: a present-but-ungradeable lock line returned CONTRADICTS_BOTH, manufacturing an accusation out of missing data; it is UNGRADEABLE now. (2) MONEYLINE DRAWS, Devin: expectedResult refused every draw, so all 30 drawn moneylines in production went unchecked - and soccer draws are exactly the C-118 fabrication-risk population, so the detector was blind to the market it most needed to watch. Now mirrors settlement.ts:96 exactly (soccer LOSS, everything else PUSH) via a sportKey on the input. (3) FAIL-OPEN PARSERS in a fail-closed module, CodeRabbit: the spread regex made the sign OPTIONAL so "Team 1.5" parsed as team-plus-line (6 such selections exist in production), and totalSide matched OVER inside "OVERDUE" (0 in production, so defensive). Both now fail closed. (4) PASS-LIST CAP, Devin and CodeRabbit independently: dedupePassesByGame ran AFTER take:100. MEASURED: a single day has produced 305 GATED rows across 58 fixtures, so the cap was ALREADY TRUNCATING REAL DAYS and dropping genuine passes, not merely at risk of it. Raised to 500, the bound state.ts already uses for this exact reason. | claude | DONE | aed34f6f6 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 8 new tests (27 in the detector suite). Negative control: restoring the early CONSISTENT return fails exactly the right-on-card-wrong-on-graded-line test. All figures read-only SELECT via the Neon MCP; no writes. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12559 passed. |
| C-149 | REVIEW ROUND 31, two Devin findings, and the RED one lands directly on the safety I had just reported as done. (1) A COMMENT IS NOT A GUARD. I wrote the C-114 do-not-run finding as prose at the top of unpublish-corrupted-picks.ts, told the founder the tool now carried the warning at its entry point, and left --execute FULLY WIRED. `--population all --execute` would still have unpublished the 87 rows the prose said to leave alone - the rows I had just verified are mostly CORRECT. Devin rated it red and was right. EXECUTE_BLOCKED_POPULATIONS is now an executable refusal in parseUnpublishArgs, and `all` is explicitly not an escape hatch. The parser moved into the lib to be testable at all: the CLI runs main() on import, so a test that imported it would have executed the script. Dry runs of the blocked population stay available, because inspecting it is how the founder makes the re-grading decision. (2) CHRONOLOGY: C-147 dropped withdrawn PUBLISHED decisions before suppression ran, so an OLDER gated evaluation resurfaced as the fixture's current state - we evaluated, published and withdrew, and the board reverted to "we passed on this". Fixed with the SAME fact-versus-evaluation distinction outranks already uses: a LIVE publication suppresses every generic row unconditionally (a subscriber can see the pick, so a pass row is false whatever was evaluated later), while a WITHDRAWN one only resolves ORDER - it suppresses rows at or older than itself and lets a genuinely newer gated evaluation display. MY FIRST ATTEMPT WAS TOO BROAD and an existing C-117 test caught it: I applied chronology to live publications too, which would have re-broken the published-plus-gated case that test exists to pin. The suite did its job. | claude | DONE | 83c1defcb on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 7 new tests. Negative controls: emptying EXECUTE_BLOCKED_POPULATIONS fails exactly the three block tests; dropping the withdrawn-chronology branch fails exactly the older-gated-resurfaces test. Positive controls both sides: the unblocked populations still execute, and a dry run of the blocked one still works. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12565 passed. |
| C-150 | GROUND-TRUTH AUDIT, and it answers the question LAUNCH_READINESS section 5 item 7 asked: how many published results are actually wrong. Re-derived every checkable settled published pick against the ESPN public scoreboard keyed by EVENT ID (never by team-and-time, which is the field a duplicate-fixture defect corrupts). MEASURED: 68 of 590 published MONEYLINE results are WRONG against the true final, 11.5pct, and every single one of them sits on a game row carrying another fixture's score - not one wrong moneyline was found on a row holding the true final. Moneyline is the clean measurement because a moneyline grade involves no line, so no convention is in dispute. 187 of 670 checkable picks sit on a wrong-score row (MLB 169 of 491 at 34.4pct, MLS 16 of 62, NCAAF 2 of 51, NFL ZERO of 66) - the concentration in MLB rules out a generic ingestion fault. 13 MLS moneylines were settled on drawn matches, which is the C-118 population measured rather than assumed. This confirms the prediction that the contradiction detector UNDERSTATES the problem: a pick graded off a corrupt row agrees with that row and reads CONSISTENT, so only an outside source can find it. THE NEW FINDING nobody had stated: 1,529 of 2,199 settled published picks (69.5pct) sit on game rows with NO ESPN event id, so their results cannot be checked against ground truth by this or any similar method. The track record is unauditable for two thirds of its own history. Giving every game row a stable external id is a launch prerequisite, not housekeeping. A CLAIM I REFUTED BEFORE PUBLISHING IT: every settled published spread selection in production shows a minus sign (526 of 526 MLB, zero plus signs anywhere) while 266 store a POSITIVE line, which reads exactly like a renderer showing subscribers the wrong side of the spread. It is not. scoring.ts:588 renders the sign correctly and scoring.ts:651 documents that line is stored home-perspective on purpose while selection is chosen-team-perspective; they disagree exactly when the pick is on the away team, and the absence of plus signs says only that the engine never takes a spread underdog. Checking the code killed the false alarm. | claude | OPEN | docs/ops/GROUND_TRUTH_AUDIT_2026-09-07.md. All figures read-only SELECT via the Neon MCP plus ESPN public scoreboard fetches; NO WRITES. Two controls run and reported: a home-away-swapped bucket returned ZERO rows in every sport (so the truth table is oriented right), and the coincidence control was run and DISCARDED as non-discriminating (118 of 138 CORRECT MLB rows also match some other real final that day, because MLB scores are low-cardinality) - no claim rests on it. NOT ESTABLISHED and stated as such: which writer produces the wrong scores, and whether the unmeasured 1,529 carry the same rate. Entry work when claimed: give every game row a stable ESPN event id, then re-run this audit over the full 2,199. |
| C-151 | REVIEW ROUND 32, Devin, CONFIRMED, and it is a REGRESSION I INTRODUCED ONE COMMIT EARLIER in C-149. loadBoardState enters its primary decision path on `displayableDecisions.length > 0` - displayable CANDIDATES - and then returned on them too. C-149's withdrawn-publication chronology runs AFTER that branch is chosen, and it can supersede every candidate: a fixture whose only surviving row is a gated evaluation OLDER than a withdrawn publication drops out by design. When that empties dedupedDecisionRows the function returned a board with three EMPTY LANES and never ran the fallback pick and game queries, so ONE withdrawal on ONE fixture blanked the WHOLE board - live published picks on other fixtures included. Trading a wrong label for a blank board is not an improvement, and blanking the surface is the same failure mode C-147 was about: the board going silent rather than showing the real state. The evidence that this was mine and not merely missed: THE C-149 COMMENT ALREADY DESCRIBED THE INTENDED BEHAVIOUR - "the fixture drops out of the decision path entirely and the fallback lanes judge it on their own predicates" - and the code did not do it. The test that pins the chronology rule stubbed BOTH fallback queries empty, so an empty board and a fallback that never ran were indistinguishable to it; a test can only catch what its fixtures let it see. FIX: return on ROWS, not on candidates. The chronology rule is unchanged and the superseded rows still do not display; they simply stop being a reason to answer at all. | claude | DONE | 482654ad7 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 1 new test, and it is written so the two behaviours cannot both pass: the fallback lane carries a real published pick on a DIFFERENT fixture, so a blank board fails and a fallback that runs succeeds. Negative control: replacing the guard with `if (true)` (the pre-fix behaviour) fails EXACTLY that one test, 1 failed 18 passed. Positive control inside the same test: the superseded gated row still must not resurface. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12566 passed. |
| C-152 | REVIEW ROUND 33, CodeRabbit on #719, CONFIRMED REAL and deliberately NOT fixed in that round - the REMAINDER of C-151, recorded so it is a unit of work rather than a finding I quietly answered halfway. C-151 makes loadBoardState return on ROWS instead of candidates, so when chronology supersedes EVERY decision the fallback lanes run. CodeRabbit asks for the PARTIAL case too: fixture A is dropped by withdrawal chronology while fixture B still has a displayable decision, so dedupedDecisionRows is non-empty, the decision path returns, and A gets no row at all even though the fallback gated or scoring lane would describe it honestly on its own predicate. This is not a missed line in C-151. The primary decision path and the fallback lanes are EITHER/OR by construction - whenever the decision path answers, NO fixture gets a fallback row - so satisfying it means MERGING two sources that have never been merged: two sets of rows to dedupe across, and openPicks/gatedToday/sportsWatched to recount so the board's own numbers still agree with its own rows (the C-117 invariant). That is a redesign of loadBoardState, not a guard. SEVERITY, stated rather than assumed: the current behaviour on fixture A is SILENCE, which is incomplete but not false. The pre-C-149 behaviour was a resurfaced 'we passed on this' for a fixture we had actually published and withdrawn, which IS false. Silence is the better of the two, so this is an improvement to make, not a regression to undo - and it is the third finding in this PR (with C-141) that wants a structural change to the same read path in a round that already carries one. | claude | OPEN | No code. Finding recorded on the #719 review thread with a reply stating it is real and deferred, and the thread left unresolved on purpose so it stays visible. Site: apps/web/lib/board/state.ts, the `if (dedupedDecisionRows.length > 0)` return and the fallback Promise.all beneath it. Entry test when claimed: an upcoming game carrying a withdrawn PUBLISHED decision plus an OLDER gated one, alongside a second fixture with a displayable decision, must still receive its fallback row when it satisfies the fallback predicate - and the counts must match the rows shown. |
| C-153 | REVIEW ROUND 33, CodeRabbit, FOUR findings and I confirmed all four - one is a code defect, three are errors in documents I wrote tonight. (1) THE FOURTEENTH SIBLING-LANE INSTANCE, and it is the SAME defect C-149 fixed, in the SAME pair of files, one round later. loadBoardPasses excludes a game that has a LIVE published pick via `picks: { none: publishedPickRelation }`. That is the right rule for a live publication and NO rule at all for a withdrawn one: unpublishing the pick makes the relation match again, and an OLDER gated evaluation reappears as a CURRENT pass. 'We passed on this' is then false in the strongest way that list can be false, because we evaluated, published and withdrew. Fixed with the same chronology rule as state.ts, in a second query, and the suppression map asserts BOTH conditions off the row (status is PUBLISHED, pick is not live) rather than trusting the where clause that produced it - this file has now been the second half of a one-lane fix twice. (2) I labelled 87 of 670 a wrong-result count while section 4 of the same document says spread and total correctness is not well defined under two disagreeing line conventions. Relabelled as line-dependent disagreements; the defensible headline is the moneyline one, 68 of 590. (3) I wrote that the sport concentration 'rules out a generic ingestion fault'. It does not: NFL's zero is zero of 66, and MLS is at 25.8pct, which is not a clean sport. Downgraded to a prioritization signal in BOTH places it appeared (I had repeated the overreach in the summary section). (4) THE WORST ONE, because it contradicts this repository's own ledger: I wrote 'ECE is unaffected by this audit' while C-114 records that 63 moneyline rows graded before kickoff sit INSIDE the sample the published ECE 0.0524 is computed from. Corrected to the narrower and worse statement: the floor still binds, the published VALUE is not trustworthy until C-114 is re-graded or excluded, and this audit neither recomputes it nor claims a direction. Also measured while reconciling CodeRabbit's arithmetic finding: the remediation populations are 87 / 148 / 352 which sums to 587, they OVERLAP on exactly 2 rows (settled-before-kickoff with off-ladder run lines; the other two pairs are disjoint), so the true unique-row count is 585 - neither the 587 in the doc nor the 586 I had written before. | claude | DONE | b42e1a7bb on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 3 new tests in board-passes-published-suppression.test.ts. Negative control: replacing the chronology comparison with an unconditional true fails EXACTLY the older-gated test, 1 failed 8 passed. TWO positive controls both sides: a NEWER gated evaluation after the withdrawal still lists, and a STILL-LIVE publication does not suppress by chronology (that path is owned by the query, and pinning it keeps the two mechanisms separable). Overlap and union counts read-only via the Neon MCP; no writes. Verify: typecheck 0, lint 0, guardrails 26/26. |
| C-154 | BACKGROUND WORKFLOW RESULT (27 agents, launched earlier tonight), and the headline is that its TWO PROPOSED FIXES ARE BOTH ALREADY SATISFIED OR WRONG - recorded so nobody implements them. Its proposed fix (1) says settle-sport.ts logs SCORE_MISMATCH_CROSS_PATH, skips the game write, and THEN STILL GRADES every pick, and that the paid lane must be made to refuse like the free lanes. I READ THE CODE: settle-sport.ts:552 gates the entire settlement block on `score.homeScore !== null && score.awayScore !== null && !conflicts`, and the grading loop at :643 is INSIDE that block, so on a conflict the paid lane already grades NOTHING. The claim is false and the fix would be a no-op edit to a fail-closed guard. This is the SECOND agent tonight to mis-attribute this writer to the paid lane; the launch-readiness doc already carries the first as an unverified [A]. Its proposed fix (2), persisting the settle-time score, is C-120, shipped and live since 20:20 UTC today - its own measurement of '613 of 613 events carry no settleScore' is explained by the field going live after those events were written, not by the field being absent. WHAT IS VALUABLE IN IT, and it corroborates C-150 by a DIFFERENT method (team-pair-plus-start-time matching, different date windows): on 45 of 45 matchable population-B rows the stored pick result is CORRECT against ESPN and the game row is WRONG, and its CONTROL on correct-looking MLB rows found the game row disagreeing with ESPN on 55 of 95 (57.9pct). That control is the same phenomenon C-150 measures at 34.4pct on MLB by event-id matching. TWO INDEPENDENT METHODS NOW AGREE that the game-score baseline is broadly untrustworthy, which is a stronger statement than either made alone. It also states plainly that it did NOT run the corruption entry point to ground: all four in-repo score writers appear guarded against overwriting a FINAL row with a different score. | claude | OPEN | Full agent output at /tmp scratchpad task file wt22abnsa.output (session-local, not committed). No code changed for this row. The refutation is a direct read of packages/ingestion-pipeline/src/settle-sport.ts:552 and :643. Entry work when claimed: identify the writer that puts a sibling fixture's final on a game row, which remains unidentified after two independent investigations. |
| C-155 | REVIEW ROUND 34, Devin, two findings. The first is C-152 found independently a second time (superseded fixtures disappear from a MIXED board) - already recorded, already deferred with its entry test, no new action; two reviewers landing on it separately raises its priority in the queue, not its urgency in this round. The second is NEW and is a NONDETERMINISM rather than a wrong answer: dedupePassesByGame keeps the FIRST row per fixture and deliberately does not re-sort, so the query's ordering IS the tie-break, and the query ordered on `evaluatedAt` alone. Two evaluations of one fixture written in the same millisecond leave the winner to whatever order postgres happens to return, so two identical board loads could show different reasons and confidences for the same game - the C-117 contradiction again, except nondeterministic and therefore not reproducible from a screenshot or a bug report. MEASURED BEFORE FIXING, and the measurement is the honest part of this row: across ALL 356 GATED decisions in production, ZERO (gameId, evaluatedAt) pairs carry more than one row (max rows sharing a timestamp is 1). THIS HAS NEVER FIRED. It is fixed anyway because it is a one-line total order broken by a column that is already unique, and the cost of leaving a latent nondeterminism in a product whose premise is a readable audit trail is a defect nobody can reproduce. Devin also asked for a test that shuffles the input and expects a stable winner. NOT WRITTEN, deliberately: dedupePassesByGame has an existing test pinning that it does NOT re-sort, precisely so a query ordering mistake surfaces as a failure instead of being hidden by a defensive sort. A shuffle test would assert a property this code is designed not to have. The determinism lives in the query, so that is where it is asserted. | claude | DONE | 929c10aa2 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. 1 new test asserting the orderBy is the two-key total order. Negative control: reverting to the single-key orderBy fails EXACTLY that test, 1 failed 9 passed. Tie measurement read-only via the Neon MCP; no writes. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12570 passed. |
| C-156 | C-92 CONFIRMED IN THE CODE AND FIXED, and the measurement corrects a claim I made in the launch-readiness doc earlier tonight. generate-signal-slate.ts built one `shared` payload and spread it into BOTH the create and the updateMany, so `isPublished: gates.canExposePublicPicks` was rewritten on every existing row on every slate run: an operator who withdrew a live pick had it SILENTLY RE-PUBLISHED by the next run. THE SCALE CLAIM I WROTE WAS WRONG BY A FACTOR OF 146. I wrote that this 'alone would undo any remediation'. Measured: the updateMany is scoped to result PENDING and the slate writes only MONEYLINE, so of the 585 remediation rows it can reach FOUR. The settled populations and every SPREAD row were never at risk. What IS real is 70 published PENDING moneylines that could be silently re-published today, which is why it is fixed rather than downgraded. THE OBVIOUS FIX IS THE WRONG ONE and the tests pin why: writing the flag on CREATE ONLY (which is what this ledger row's own text asked for) would also remove the gate's power to CLOSE. canExposePublicPicks is an honesty boundary - when it goes false, live rows must stop being live - and create-only would leave them published forever. So the write is ONE-DIRECTIONAL. Gate CLOSED: force false on every run, no exceptions. Gate OPEN: write nothing, because 'the gate permits publishing' and 'this pick should be published' are different statements and only the second is an operator's to make. On create the gate decides outright, since there is no prior operator judgement to preserve. | claude | DONE | 5f0750b6b on claude/sports-launch-round2-fixes-hk7kv9, PR #719. New file packages/ingestion-pipeline/src/__tests__/signal-slate-publication-flag.test.ts, 4 tests. TWO NEGATIVE CONTROLS, one for each wrong fix, each failing EXACTLY the test that names it: restoring the shared flag fails the withdrawal test (1 failed 3 passed); making it create-only fails the gate-CLOSED test (1 failed 3 passed). Positive control included so a fix that simply skipped withdrawn rows cannot pass - the refresh must still update selection, modelVersion and generatedAt. Create asserted in BOTH gate directions. All counts read-only via the Neon MCP; no writes. Verify: typecheck 0, lint 0, guardrails 26/26, ingestion-pipeline 413 passed, apps/web 12570 passed. |
| C-157 | C-88 VERIFIED AT ALL THREE SITES AND PARTLY FIXED, and the three are NOT equivalent - the ledger row treated them as one item and they need different owners. THE SHARED DEFECT: `confidence` is a 0-100 Edge Index and is NOT a win probability, which this repository asserts in two independent places - compute-live-calibration-metrics.ts says 'confidence/100 is never scored' because the floors refuse it, and the HOME PAGE says it to customers in as many words, 'Not a probability the pick wins'. (1) FIXED, PUBLIC PROOF PAGE: load-proof-of-record.ts printed the gap between the market's fair probability and the Edge Index scaled to 0-1 as 'model vs market Xpp', in green or red, on the page whose entire purpose is verifiability. The comment two lines above it ALREADY forbids exactly this for SPREAD and TOTAL ('leave it null rather than print a number that mixes two unrelated quantities') - the rule was right and simply was not applied to MONEYLINE. Now always null. The field and its null-safe render path are KEPT so restoring a real model probability (rankingP, not in this query's select today) stays a one-line change. (2) FIXED, CUSTOMER COPY, two sites: the home annotated sample called it 'a calibrated 0-100 Edge Index' and the GLOBAL FOOTER, on every page, said we 'deliver calibrated market signals'. The calibration gate reads RED, ECE 0.0524 fails its floor, and per C-114 the published ECE value is itself contaminated - so 'calibrated' is an unearned claim three ways over. The adjective is dropped; the honest halves of both sentences are kept and pinned by positive controls. (3) NOT FIXED, FOUNDER DECISION, B2B API: /api/v1/probabilities publishes pModel = confidence/100. Changing what that field CARRIES is a public contract change with several viable answers (null it, carry marketFairProb, carry rankingP, remove it), so an agent should not pick one at 23:00. What IS done is additive and breaks no consumer: the payload's own disclaimer now states that pModel is a CONFIDENCE SCORE and not a calibrated win probability, that this service never scores it as one, and redirects integrators to marketFairProb and rankingP, which are already in the payload. The route also already carries claimPosture experimental_research_grade_not_verified_roi, which is why it was ranked BELOW the two unlabelled surfaces rather than above them. | claude | OPEN | c079baa76 on claude/sports-launch-round2-fixes-hk7kv9, PR #719 fixes items 1 and 2; item 3 stays OPEN and is why this row is not DONE. New file apps/web/__tests__/confidence-is-not-a-probability.test.ts, 7 tests, source pins in the proof-of-record-surface.test.ts idiom because a CLAIM is a property of the text, not of a return value. TWO NEGATIVE CONTROLS: restoring the proof-page arithmetic fails exactly its test, and restoring the footer adjective fails exactly its test (2 failed 5 passed together). THREE POSITIVE CONTROLS so deleting rather than fixing cannot pass: the modelVsMarketPp field must still exist, 'Not a probability the pick wins' must survive on the home page, and the footer must keep 'not certainty' and 'Set limits before emotion enters'. Founder decision needed on what pModel should carry. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12577 passed. |
| C-158 | REVIEW ROUND 35, Devin, CONFIRMED AND IT IS A COST OF MY OWN C-156 FIX THAT I STATED AS IF IT WERE FREE. C-156 made the slate's isPublished write ONE-DIRECTIONAL: a closed gate forces false every run, an open gate writes nothing, so an operator's withdrawal survives. Devin's point is the other half of that: GATE RECOVERY NO LONGER RESTORES. If canExposePublicPicks closes and later reopens, the rows the closed runs unpublished are never republished by the open ones, and since a pick is unique per (gameId, pickType) no new row can replace them - they stay hidden for the rest of their life. That is a real regression against the previous behaviour. My C-156 comment said 'a pick that was never published stays that way' and framed it as intent without naming the price. WHY IT IS STILL THE RIGHT DIRECTION, and this is a judgement rather than a measurement: the two failures are not symmetric. The old behaviour silently RE-PUBLISHED a pick an operator had withdrawn for being wrong or corrupt, which publishes a known falsehood. The new behaviour publishes LESS than it could. Under this product's premise the safe direction is publishing less. WHY AN AGENT CANNOT FIX IT: doing both correctly requires distinguishing 'unpublished by the gate' from 'unpublished by an operator', and `isPublished` is a bare Boolean (schema.prisma:550) with nowhere to record which. That needs a provenance column, i.e. a migration - and packages/db/prisma/schema.prisma plus migrations are FROZEN for agents by AGENTS.md law 2. So this is founder-gated by construction, not by preference. SUGGESTED SHAPE when the founder takes it: an `unpublishedReason` enum or nullable string written by whoever unpublishes (GATE_CLOSED vs OPERATOR_WITHDRAWN vs REMEDIATION), then the open-gate branch restores ONLY GATE_CLOSED rows. AMENDED 2026-09-08 by C-167: the column must also carry an `unpublishedAt` TIMESTAMP, not only a reason. Both board lanes order a withdrawal against gated evaluations using the PUBLICATION's evaluatedAt because no withdrawal time exists, and a reason-only column does not fix that. The remediation tool (scripts/ops/unpublish-corrupted-picks.ts) would stamp its own value in the same write it already does. | claude | OPEN | 733e2a38b on claude/sports-launch-round2-fixes-hk7kv9, PR #719 - code comment now names the cost, and the test suite PINS THE GAP so it cannot go quiet. The new test asserts current behaviour and says in its own body that IT SHOULD FAIL when C-158 lands, to be replaced by one asserting gate-suppressed rows restore while operator-withdrawn rows do not. No schema touched. Entry files: packages/ingestion-pipeline/src/generate-signal-slate.ts publicationUpdate, scripts/ops/unpublish-corrupted-picks.ts, packages/db/prisma/schema.prisma Pick.isPublished. Verify: typecheck 0, lint 0, guardrails 26/26, ingestion-pipeline suite green. |
| C-159 | I INTRODUCED A CRITICAL SECURITY FINDING ONE COMMIT EARLIER AND CODACY CAUGHT IT. Recording it rather than quietly fixing it, because the whole point of this ledger is that mistakes are visible. The C-157 test file opened with a helper that took a relative path as a PARAMETER and passed it to fs.readFileSync. Codacy flipped from '0 issues' on 598b43bc5's predecessor to 'Not up to standards, 1 critical Security' on 598b43bc5. That is the CWE-22 shape: a non-literal filesystem argument, in a helper that will read whatever relative path it is handed. Nothing in the file passed it computed input, so there was no live exploit - but the pattern is the hazard, and it was NEW in my diff, which is why the scanner flagged it and had not flagged the identical-purpose proof-of-record-surface.test.ts. THE EXISTING REPO IDIOM WAS ALREADY RIGHT and I departed from it for brevity: proof-of-record-surface.test.ts inlines a FULLY LITERAL path.resolve at every call site. My file now does the same, four literal constants, no parameter anywhere on the path. The finding is removed at the ROOT rather than suppressed with an ignore comment. Also worth recording as method: my first repair script asserted 'no read( remains' and failed on its own new COMMENT, which quotes the helper it was describing. The check was wrong, not the edit. Re-run with a precise regex scoped past the comment block. | claude | DONE | c18a7f4f4 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. Same 7 tests, unchanged assertions - only how each source is loaded changed. RE-VERIFIED ALL FOUR CONSTANTS ARE WIRED TO THE RIGHT FILES by negative control, because a rewrite of the loading layer could have made a test pass vacuously against the wrong file: injecting the banned arithmetic fails exactly the proof-page test, weakening the disclaimer fails exactly the API test, restoring the footer adjective fails exactly the footer test. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web 12577 passed. |
| C-160 | C-119 INVESTIGATED AND DELIBERATELY NOT IMPLEMENTED, and the measurement I ran instead found something bigger than the guard. WHY NOT IMPLEMENTED: `line: avgTotal` (scoring.ts:856) is an average across books, so it lands off any real ladder whenever books disagree. Two fixes exist and are NOT equivalent - REFUSE off-ladder lines (suppresses 56.4pct of published MLB totals and 47.6pct of MLB spreads, MEASURED) or SNAP to the nearest step (changes the graded line, therefore edge, confidence and CLV, under a frozen MODEL_VERSION). Both are founder calls and both would answer C-143 by accident, since C-143 is the prior question of which line is canonical at all. So C-119 stays OPEN with the cost of each option measured rather than guessed. THE REAL FINDING, MEASURED on settled published non-bootstrap picks with a decided result: THE PICKS YOU COULD ACTUALLY PLACE PERFORM WORST. MLB totals on a real ladder 70 of 193 = 36.3pct, off-ladder 136 of 275 = 49.5pct. MLB spreads on-ladder 127 of 294 = 43.2pct, off-ladder 116 of 232 = 50.0pct. Spreads and totals are near-even-money so break-even is about 52.4pct and win rate is approximately the profitability metric: both on-ladder cohorts are losing, and 36.3pct over 193 is roughly 3.8 sd below a coin flip, not sampling noise. TWO CONTROLS RUN: all four cohorts are isBootstrap false, so it is not a pre-canonical artifact; mean bookmakerCount is 8.32 on-ladder against 8.26 off-ladder, so the cohorts are not separated by how much data we had. THE CONFOUND, NAMED NOT BURIED: with ~8 books an on-ladder average means the books AGREED and an off-ladder one means they DISAGREED, so this contrasts market consensus as much as placeability and the two cannot be separated by this query [INFERRED]. That does not weaken the actionable part - the engine does materially worse where the market is confident, which is the opposite of where an edge should live - and the placeable subset remains the honest denominator for any public claim. ALSO NOT ESTABLISHED: these are stored results and C-150 measured 68 of 590 moneylines wrong; the corruption is not known to be balanced across these cohorts, and nobody has re-derived SPREAD or TOTAL results against ESPN because C-143 means the correct result is not yet well defined for those markets. LAUNCH WARNING that follows directly: win rate by market is NCAAF ML 94.1pct (n 102), MLB ML 62.8pct (n 608), MLB spread 46.2pct (n 526), MLB total 44.0pct (n 468). A BLENDED WIN RATE ACROSS THESE WOULD BE MISLEADING IN OUR FAVOUR - moneyline win rate is not a performance claim without price, since the engine only takes sides at fairProb >= 0.58, so a high ML win rate is what backing favourites looks like. PERFORMANCE_STATS_ENABLED is off; before it is flipped the performance surface must be checked for exactly this mixing. That is a gate, so no agent touches it. | claude | OPEN | docs/ops/PLACEABILITY_AND_PERFORMANCE_2026-09-07.md. All figures read-only SELECT via the Neon MCP; no writes; no code changed. Decision order for the founder: C-143 first (which line is canonical), then C-119 (refuse or snap), then the performance-publication question. |
| C-161 | REVIEW ROUND 36, CodeRabbit, SIX findings and ALL SIX WERE REAL. Two are defects I introduced in the two commits immediately before. (1) THE ONE THAT MATTERS, and it is the same class this file keeps producing: my C-153 withdrawal watermark was a SECOND findMany with take 500 AND NO ORDERING - a cap applied BEFORE the per-fixture collapse. Production holds 811 PUBLISHED decisions, so an arbitrary 500 would have been kept and any fixture whose withdrawal fell outside that slice would have shown its OLD gated row as a current pass: the exact false claim the query exists to prevent, reintroduced by the query itself. Replaced with groupBy by gameId taking max(evaluatedAt) IN THE DATABASE - no window to truncate, no cap to size, and the where clause is now the guarantee so nothing downstream re-asserts what the rows are. (2) COPY CONTRADICTED ITSELF THREE LINES APART: two callouts read 'a 64pct confidence signal still loses 36 of 100 times' and 'A 71-confidence signal still loses ~29 of 100' - converting the Edge Index into an outcome frequency immediately after the adjacent sentence says it is not a probability. Losing to variance is the honest half and stays; the arithmetic is the half that had to go. (3) AUDIT COVERAGE CONFLATED TWO DIFFERENT PROBLEMS: I wrote that all 1,529 uncheckable rows lack an ESPN event id. MEASURED split is 1,425 with NO id (structural, no fetch fixes it) plus 104 whose id fell outside the windows I fetched (merely incomplete, an afternoon of work). Conflating them overstates the structural problem by 7pct. (4) I RESTATED A CLAIM I HAD ALREADY CORRECTED ONCE TONIGHT: the audit said 'the picks themselves are not the problem'. What is established is the ASSOCIATION - all 68 wrong moneylines sit on wrong rows - not the DIRECTION. C-114 holds a pick settled 18h before first pitch that inherited the corrupt row and was genuinely mis-graded. C-114 re-grading stays in scope. (5) THE ECE LINE said calibration 'resolves with more settled rows, not with work'. Half true: the FLOOR is a correct gate, but the VALUE 0.0524 is computed on a sample containing C-114's 63 pre-kickoff rows and more rows do not remove outcomes nobody observed. (6) THE 117 HEADLINE was never a wrong-result count - it was the size of the CONTRADICTION population. Superseded by 68 of 590 wrong moneylines plus line-dependent spread and total disagreements. Now says so. | claude | DONE | 3781de565 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. Tests: board-passes 11 (new: the watermark is computed by groupBy with no take, asserted on the SHAPE of the call because 'there is no cap' is a property of the query); confidence-is-not-a-probability moved to .tsx and the two COPY assertions now RENDER the components and read the DOM, which is strictly better - a claim is made to a customer by the DOM, not by a string literal, so it survives the copy moving into a constant or a CMS. TWO NEGATIVE CONTROLS on the rendered tests, each failing exactly its own assertion. CODACY NOTE, recorded because it corrects my own C-159 commit message: Codacy went from 1 critical to 4 after that fix, because the four literal fs.readFileSync calls each trip the non-literal-argument rule. MEASURED: 265 test files in this repo already use readFileSync, so this is the repository's dominant idiom and not something I introduced - C-159's genuine improvement was removing a PARAMETERISED reader, which is a different and real hazard. Two source pins remain here (the proof loader needs a database and the B2B route is behind API-key auth), with the trade-off written into the file. Whether to change the rule or the idiom repo-wide is a founder call, not an agent's. Verify: typecheck 0, lint 0, guardrails 26/26. |
| C-162 | FOUNDER STEER: stop reading MLB and check every sport, especially NFL and NCAA. He was right and it surfaced a FIVE-DAY problem. MEASURED, upcoming 14 days, game rows per distinct real fixture: NFL 2.52 (78 rows, 31 fixtures, THREE namespaces), MLB 1.12, NHL 1.05, NCAAF 1.04, MLS 1.00. TOMBSTONED ROWS: ZERO, in every sport. NFL Week 1 Sunday 2026-09-13 is 12 real fixtures as 36 rows. GOOD NEWS FIRST AND IT IS REAL: all 20 published Week 1 picks sit on the namespace carrying the odds (12 of 12 fixtures at 2+ books, avg 11 books), so NFL Week 1 pricing is effectively complete and picks are landing on the right row. NFL settled history is also clean, 0 of 66 wrong rows against MLB's 169 of 491. WHY IT IS STILL URGENT: mergedIntoGameId is the canonicity marker the board's C-117 dedupe reads, and with zero tombstones all three NFL rows are canonical to every consumer - the fallback lanes can surface one fixture three times. Worse, several rows per fixture PLUS SCORE_MISMATCH_CROSS_PATH refusing to overwrite a written final is EXACTLY the structural precondition that produced 34.4pct wrong scores in MLB. NFL has both. It is clean today because its settled volume is preseason-small, not because it is protected [INFERRED], and Week 1 is when the volume arrives. AUTOMATION GAP: scripts/ops/merge-duplicate-games.ts exists as npm run ops:merge-games and is OWNER-RUN ONLY - no cron in apps/web/vercel.json, no route in apps/web/app/api/cron. That is why nothing is tombstoned anywhere, and it sits against the founder's standing 'no human step where a machine can do it'. OTHER SPORTS, so this is not another single-sport read: NCAAF is the healthiest forward book (180 rows for 173 fixtures, 82 games Saturday 09-12 with 60 priced, 106 published picks); MLS 1.00 per fixture; MLB forward coverage has COLLAPSED to 44 of 295 upcoming games with 2+ books (14.9pct), 251 with none; NHL 65 upcoming with ZERO priced and ZERO picks as its season approaches; NBA has NO forward schedule at all, furthest game 2026-06-14 which is in the past. | claude | OPEN | docs/ops/SPORT_COVERAGE_2026-09-08.md. Read-only SELECT via the Neon MCP; no writes; no code changed. I did NOT run the merge (database write, law 7) and did NOT change fixture identity (packages/ingestion-pipeline/src/game-identity.ts), because choosing a canonical namespace is a data-model decision with more than one defensible answer. NOT ESTABLISHED and it gates the next step: whether merge-duplicate-games.ts, run today, would pick the odds-bearing opaque-id row as the SURVIVOR. If it picks a bookless duplicate it is destructive rather than corrective. Answer that before running it. Founder decisions: (1) run the merge or not, (2) canonical namespace for NFL fixture identity, (3) whether the merge becomes a cron. |
| C-163 | ANSWERED C-162's NOT-ESTABLISHED ITEM, AND THE ANSWER INVERTS THE RECOMMENDATION - the merge must NOT be run before NFL Week 1 without a companion step. THE SURVIVOR RULE IS FINE: selectCanonical (apps/web/lib/ops/game-merge-plan.ts:253) sorts by pickCount desc, then oddsCount plus oddsLineSnapshotCount desc, then non-ESPN external id, then oldest createdAt. For NFL Week 1 the odds-bearing pick-bearing row wins on the FIRST tie-break, so it would be kept. That half of the C-162 worry was unfounded. THE COST IS ELSEWHERE AND IT IS LARGE: merge-duplicate-games.ts re-points odds, snapshots, signals and other children onto the canonical, but BY EXPLICIT DESIGN NEVER MOVES `picks` - they are settlement history and stay put. The alias is then tombstoned via mergedIntoGameId, and the board filters mergedIntoGameId IS NULL, so any published pick on a losing row DISAPPEARS FROM THE PRODUCT. MEASURED, replicating the tool's ordering: running it today merges 652 fixtures, keeps 791 published picks on canonicals and STRANDS 578 - MLB 420, NCAAF 90, MLS 49, NFL 19, NHL 0, NBA 0. CAVEAT ON MY REPLICATION, stated because the number drives a decision: I ordered by pickCount, oddsCount, createdAt and omitted the oddsLineSnapshots term and the ESPN-shape tie-break, so groups decided by those could split differently. Treat 578 as right to its order of magnitude, not to the unit. The tool's own --dry-run prints the exact set and is the correct instrument before any apply. | claude | OPEN | docs/ops/SPORT_COVERAGE_2026-09-08.md section 7. Read-only: SELECT via the Neon MCP plus reading apps/web/lib/ops/game-merge-plan.ts and scripts/ops/merge-duplicate-games.ts. NO writes, no merge run, no code changed. FOUNDER DECISIONS THIS CREATES: (1) re-point or deliberately withdraw the 578 alias picks before merging - re-pointing touches published history so it is not an agent's call; (2) whether the merge becomes a cron once that companion exists; (3) the canonical namespace for NFL fixture identity (packages/ingestion-pipeline/src/game-identity.ts) so three rows stop being minted at all. Entry step for whoever takes it: run the tool's --dry-run and diff its canonical choices against the 578 figure here. MECHANISM NOW IDENTIFIED and it is documented in the code itself at packages/ingestion-pipeline/src/seed-games-from-espn.ts:81 - THREE WRITERS MINT THREE IDS for one contest: espn-schedule-seed.ts:121 writes espn:<short>:<id>, espn-odds-client.ts:361 writes espn:<sportKey>:<id>, and the paid path writes a bare 32-hex Odds API hash. The seed does call resolveCanonicalGame to claim a twin, so a reconciliation attempt EXISTS and is evidently not catching every case (NFL 2.52 rows per fixture, zero tombstones). So the architecture mints up to three rows per contest BY DESIGN and the only thing that tombstones anything is an owner-run tool that hides 578 published picks. That is bigger than NFL: it is why MLB accumulated the duplicate rows carrying 34.4pct wrong finals. MEASURED on creation dates: the hash writer has stopped minting NFL and MLB rows (last NFL hash row 2026-08-22, none in 48h) while ESPN ingestion continues (NCAAF 134 rows in 48h), so the mix is shifting but nothing retires what exists. THE FIX IS A DATA-MODEL DECISION, NOT AN AGENT'S: pick one canonical fixture identity, make the other writers resolve to it BEFORE insert, and give the merge a companion that re-points or withdraws alias picks. All three touch published history or data shape. |
| C-164 | CORRECTING MY OWN COVERAGE NUMBERS, and the correction reverses the ranking of concern. C-162 counted priced coverage per game ROW. With NFL at 2.52 rows per fixture that divides by the empty duplicates and reports a figure no customer would recognise. RECOUNTED PER REAL FIXTURE (priced if ANY row for the fixture carries 2+ books): NFL next 7 days 15 of 15 = 100pct and beyond 7 days 245 of 257 = 95pct; NCAAF next 7 days 74 of 97 = 76pct falling to 4pct beyond, which is exactly how books post lines and needs no fix; MLB next 7 days 16 of 80 = 20pct and beyond 7 days ZERO of 159 = 0pct. THREE THINGS CHANGE: (1) NFL IS THE HEALTHIEST SPORT IN THE PRODUCT, not a worry - every fixture in the next seven days is priced and 95pct of the season is too; the 33pct I reported was an artifact of dividing by duplicates. (2) NCAAF is normal. (3) MLB IS THE SPORT THAT IS ACTUALLY BROKEN and cannot produce book-priced picks for most of its own slate. SO THE RANKING OF CONCERN IS THE REVERSE OF WHERE MY ATTENTION HAD BEEN. MLB dominated every query because it holds the settled-row history, and it is also the one sport whose forward pipeline has failed; NFL, the commercially decisive one, is in the best shape of any sport and its only real issue is the latent duplication in C-162/C-163. The founder's instruction to stop reading MLB was right twice over. The MLB pricing failure is not a new defect: it is the two-book problem (C-104, WP-27) and the Odds API credit governor (C-109) meeting in one place and finally showing up as a product outage in one sport. | claude | OPEN | docs/ops/SPORT_COVERAGE_2026-09-08.md section 9. Read-only SELECT via the Neon MCP; no writes; no code changed. Entry work: MLB forward pricing is the outage to fix, and it is already scoped as C-104/WP-27 rather than needing a new investigation. |
| C-165 | CORRECTING C-164, WHICH I PUSHED AN HOUR AGO: 'MLB is the sport that is actually broken' IS WRONG. MEASURED per real fixture by closeness to kickoff: MLB under 24h is 12 of 12 priced = 100pct, 24-48h 27pct, 2-4d 0pct, 4d+ 0pct. EVERY MLB FIXTURE INSIDE 24 HOURS IS PRICED. Baseball books post about a day ahead, not a week, so that curve is the NORMAL shape for the market - exactly as NCAAF 76pct falling to 4pct is normal for college football and NFL 95pct months out is normal for the NFL. THE MISTAKE, NAMED, because it is more useful than the conclusion: I compared every sport against ONE FIXED SEVEN-DAY BAR, but the horizon at which books post is different per sport, so a fixed bar measures the sport's CALENDAR rather than its health. That is the same shape as C-164's own correction, where counting per ROW let the duplicates do the distorting. TWICE IN ONE NIGHT: an aggregate whose denominator quietly encodes something other than what I claimed to be measuring. CORRECTED READ - judged against each market's own posting window, ALL FOUR IN-SEASON SPORTS ARE HEALTHY: NFL 100pct next 7 days and 95pct season; NCAAF 76pct next 7 days; MLB 100pct under 24h; MLS 86pct within 2 days. NHL 0pct is its season not having started and NBA's missing schedule is the same; neither is evidence of a defect today, both worth re-checking in October. WHAT WAS NOT WRONG and is unaffected: MLB's SETTLED HISTORY still carries 169 of 491 checked rows with another fixture's final, 62 wrong published moneylines, and 420 of the 578 strandable picks. The forward pipeline is fine; the historical data is not. RESOLVED, the 24-48h question is now CLOSED with evidence rather than left open: odds-table timestamps separate the two explanations. Under 24h has 11,450 odds rows fetched 0.0h ago; 24-48h has 408 rows ALSO fetched 0.0h ago; 2-4d and 4d+ have ZERO odds rows and have NEVER been fetched. So it is NOT a refresh lag - we are fetching those 24-48h fixtures this minute and the books simply have thin coverage that far ahead of a baseball game. The 27pct is the market, not us. The zero at 2+ days is a DELIBERATE HORIZON LIMIT (never fetched), defensible for baseball, and it does mean the product cannot show a priced MLB board beyond about two days - a product constraint rather than a defect. | claude | OPEN | docs/ops/SPORT_COVERAGE_2026-09-08.md section 10, written as a correction IN the same document rather than an edit that hides the error. Read-only SELECT via the Neon MCP; no writes; no code changed. Entry work if anyone takes it: resolve the 24-48h question by comparing bookmakerCoverageMax against odds row timestamps for the same fixtures a day apart. |
| C-166 | THE SIBLING-LANE AUDIT APPLIED TO THE CANONICITY MARKER, prompted by hitting the same defect class three times in one night. MEASURED: of the 15 files that query `games`, only THREE filter on mergedIntoGameId - board/state.ts, board/market-coverage.ts and game-identity.ts. TWELVE DO NOT, including admin/dashboard/route.ts, sitemap.ts, board/passes.ts, slate-twin/get-slate-twin.ts, free-score-persist.ts, shadow-evaluation-pass.ts, cron/generate-drafts, generate-signal-slate.ts, process-sport.ts, settle-sport.ts, freeze-slate-commitments.ts and team-game-log-repair.ts. The C-117 fix landed on the board's state loader and its coverage helper and on nothing else: one lane fixed, eleven not. ALMOST ALL OF IT IS LATENT AND SAYING SO MATTERS: zero rows are tombstoned today in any sport, so eleven of the twelve behave identically either way. These are NOT eleven live bugs and reporting them as such would be the exact overstatement this repo exists to avoid. What they are is eleven sites that would silently do the wrong thing THE MOMENT THE MERGE RUNS. ONE IS LIVE AND IT LANDS ON NFL: generate-signal-slate.ts:166 selects a 21-day horizon with take:80 and no dedupe, and duplicates exist today. MEASURED: the 21-day window holds 744 rows over 658 real fixtures; the cap reaches only 71 fixtures; NINE OF THE 80 SLOTS (11pct) are spent on duplicate rows that carry no odds and can never yield a pick; and the slate really reaches only to 2026-09-12, about four days, so the 21d comment is aspirational and the CAP is the binding constraint rather than the horizon. This is the cap-applied-before-collapse pattern for the THIRD time tonight after C-153 and C-161, now in pick generation, and NFL's 2.52 rows per fixture means the waste grows as Week 1 enters the window. THIS CHANGES THE MERGE SEQUENCE, and that is the actionable output: C-163 said the merge strands 578 published picks; this says THE FILTERS ARE A PREREQUISITE FOR THE MERGE, NOT A FOLLOW-UP. Run the merge first and settlement, score persistence, pick generation, the slate twin, the sitemap and the admin dashboard all keep operating on rows the database has marked not-real - and two of those WRITE SCORES, which is the C-115 corruption family. CORRECT ORDER: filters first, then the pick-stranding companion, then the merge. | claude | OPEN | docs/ops/CANONICITY_SWEEP_2026-09-08.md. Read-only SELECT via the Neon MCP plus reading the repo; no writes, no code changed. I did NOT add the filter to the twelve sites: it touches settlement, ingestion and pick generation in one sweep, changes nothing today, and is only correct as part of the sequence above. A twelve-file change to hot paths landed unattended at midnight on the strength of a grep is the shape of the mistakes this session spent the night correcting. The one change defensible on its own merits is the slate's take:80, and even that should be a DEDUPE rather than a bigger number - raising the cap without collapsing duplicates just buys more duplicates. |
| C-167 | REVIEW ROUND 37, Devin, CONFIRMED, AND IT IS C-158's MISSING COLUMN SHOWING A SECOND FACE rather than a separate bug. Both board lanes suppress an OLDER gated evaluation once a fixture's publication is withdrawn (C-149 in state.ts, C-153 in passes.ts). The watermark they compare against is the PUBLISHED decision's evaluatedAt - the PUBLICATION time - because Pick.isPublished is a bare Boolean (schema.prisma:550) with no timestamp beside it. There is no withdrawal time in the database to use. CONSEQUENCE, verified by reading the code: 'published T1, gated evaluation T2, withdrawn T3' is indistinguishable from 'published T1, withdrawn T2, gated evaluation T3', and only the second is honestly a pass. In the first the T2 gated row survives and the board says 'we passed on this' about an evaluation made while a subscriber could still see the pick. WHY IT IS NOT FIXED HERE: the schema and migrations are frozen for agents by AGENTS.md law 2, so the fix is founder-gated by construction. WHY THE COLUMN-FREE ALTERNATIVE IS WORSE, and this is a judgement not a measurement: suppressing every gated row on any withdrawn fixture silences the board on games we have an honest current answer for, which is the failure C-149 was written to prevent, and it breaks the C-149 positive control. Bounded and named beats broad and quiet. WHAT LANDED: the limitation is stated in the comments of BOTH lanes (the sibling-lane rule applied to documentation, not just to code) and PINNED by one test per lane, each of which says in its own body that it asserts the DEFECT and should FAIL and be replaced when the column lands. C-158's suggested shape is amended above to require `unpublishedAt`. | claude | OPEN | UNPUSHED at write time on claude/sports-launch-round2-fixes-hk7kv9, PR #719. Entry files: apps/web/lib/board/state.ts newestWithdrawnPublishedAt, apps/web/lib/board/passes.ts the same map, packages/db/prisma/schema.prisma Pick.isPublished. Verify: cd apps/web && npx vitest run __tests__/board-gate-decisions.test.ts __tests__/board-passes-published-suppression.test.ts - 32 passed. No schema touched. |
| C-168 | REVIEW ROUND 38, Devin, CONFIRMED AND FIXED, AND IT IS THE SIBLING-LANE DEFECT AGAIN WITH THE LANES SWAPPED. This time passes.ts held the correct behaviour and state.ts lacked it: passes.ts has filtered its gated query on `game.picks.none(isPublished, isBootstrap:false)` since C-153, while the state loader learned that a fixture was published ONLY from a PUBLISHED GateDecision inside today's `evaluatedAt` window (state.ts, the decision findMany bounded by todayBounds). CONSEQUENCE: a pick published yesterday and still live today, on a fixture the engine re-evaluated and gated this morning, rendered as 'we passed on this' in the gated lane while a subscriber could still see the pick. Same for any publication whose decision fell outside that query for any other reason. FIX: an existence probe on the SAME query - `game.include.picks: { where: publishedPickRelation, select: { id: true }, take: 1 }` - unioned into livePublishedGameIds. It cannot be a `where` on the query itself, because that would also exclude the PUBLISHED decisions this lane exists to show, and `take: 1` keeps it a boolean so no cap can truncate it (the cap-before-collapse class this file has now produced three times). Suppression is a fact about Pick.isPublished NOW, not about which decision rows a query happened to return. Three tests: the defect, the query shape, and a negative control that a fixture with no live pick still shows its gated row. | claude | DONE | 6bace1f96 on claude/sports-launch-round2-fixes-hk7kv9, PR #719. Entry files: apps/web/lib/board/state.ts decision query include + livePublishedGameIds, apps/web/__tests__/board-gate-decisions.test.ts. Verify: typecheck 0, lint 0, guardrails 26/26, apps/web suite 11339 passed / 97 skipped. |
| C-169 | THE ONE LIVE ITEM OUT OF C-166, FIXED, AND IT LANDS ON NFL. generate-signal-slate.ts took the next 80 GAME ROWS by kickoff and treated each as a fixture. It is not one row per fixture: three writers key `games` on three externalId shapes for the same contest, NFL carries about 2.5 rows per real fixture and ZERO rows are tombstoned, so the cap was partly spent on duplicates. MEASURED on production 2026-09-08: 744 rows over 658 real fixtures inside the slate's own window, the cap reaching only 71 fixtures, 9 of the 80 slots (11pct) on duplicate rows that can never yield a pick. A CAP APPLIED BEFORE THE COLLAPSE - the third instance of that class in one night after C-153 and C-161, and the same repair all three took: bound rows SCANNED (take 1000, and a run that fills it now WARNS instead of silently shortening the board), collapse per fixture, cap on FIXTURES. WHICH ROW SURVIVES IS NOT A NEW RULE and that is the point: it is selectCanonical's rule from apps/web/lib/ops/game-merge-plan.ts (most picks, most odds children, non-ESPN externalId, oldest createdAt), restated in the package because a package cannot import the app. So every pick this lane writes from now on lands on the SAME row the merge would keep as survivor - one fewer pick for that merge to strand (C-163). FAIL-CLOSED CASES KEPT AS TWO ROWS: a FLIPPED pair (the rows disagree about who is home, so collapsing would grade against one orientation) and an unresolvable alias chain. MLB doubleheaders are separated by game-identity's 2h baseball window and pinned by a test. `mergedIntoGameId: null` added to the same query - latent today, load-bearing the moment the merge runs. ONE TEST FIXTURE CHANGED AND IT IS WORTH NAMING: the CFB day-key batching test used a COPY of its own game 6h later as the second day key, which is the exact duplicate shape this collapse removes; it now uses a different matchup, so the assertion tests what it was written to test rather than passing by accident. | claude | DONE | PR #719. Entry files: packages/ingestion-pipeline/src/slate-fixture-collapse.ts (new, pure, 11 tests), generate-signal-slate.ts query, __tests__/generate-signal-slate-guard.test.ts (+2 tests). Verify: typecheck 0, lint 0, guardrails 26/26, ingestion-pipeline 427 passed / 6 skipped. |
| C-170 | THE FOURTH CAP-BEFORE-THE-WORK INSTANCE IN ONE NIGHT, AND IT IS ON THE LANE THAT WRITES SCORES. free-score-persist.ts read candidate rows with take:300 over a 21-day window. THE FILE'S OWN COMMENT DOES THE ARITHMETIC THAT DEFEATS IT: 'MLB alone runs about 15 games a day, so the 21-day window is already near the cap' - that is ~315 MLB rows before NFL, NCAAF or MLS are counted, and the games table holds about 2.5 rows per real fixture with none tombstoned (C-163), so 300 rows could be as few as ~120 real fixtures. Rows that can NEVER resolve (a phantom fixture, a duplicate with no feed match) keep satisfying the where for the full 21 days and, ordered oldest-first, sit at the head of the queue displacing newer games that CAN be scored. The cap produced the starvation it was written to prevent, and starved score rows surface downstream as overdue picks, not as an error. THE CAP PROTECTS NOTHING: the scoreboard is fetched per DATE (uniqueScoreboardDates, maxDays 21), not per game, so raising it costs ZERO extra requests - only a wider SELECT of seven small columns. Raised to 2000 with a warning when it fills; no silent cap, the discipline C-153, C-161 and C-169 each landed after the same defect class. SECOND AND MORE IMPORTANT OUTPUT, A CORRECTION TO C-166's OWN SWEEP: this file is on C-166's list of twelve sites missing `mergedIntoGameId: null`, and ADDING IT HERE WOULD BE WRONG. A merge never moves `picks` - Game.mergedIntoGameId's schema comment says why, `picks` is unique on (gameId, pickType) and cannot always be re-pointed - so an alias row keeps carrying real settlement history. Refuse to score it and every pick on it can never settle: C-163's stranding turned from a merge-time cost into a permanent one. The sweep is therefore NOT uniform, which is the strongest argument yet against landing it as one blind twelve-file commit. Pinned by a test that asserts the filter is ABSENT, so a later consistency pass has to argue with the test first. | claude | DONE | PR #719. Entry files: apps/web/lib/data-sources/free-score-persist.ts SCORE_CANDIDATE_LIMIT and the candidate query, apps/web/__tests__/free-score-persist-guard.test.ts (+3 tests). Verify: typecheck 0, lint 0, guardrails 26/26, free-score-persist-guard 6 passed, settle-picks-paid-scores-governor green. |
| C-171 | THE FIFTH INSTANCE, AND THIS ONE IS VISIBLE TO A SUBSCRIBER ON THE PUBLIC BOARD. Three fallback queries returned GAME ROWS and treated each as a fixture: board/state.ts scoring lane (take 8), board/state.ts gated lane (take 12) and board/passes.ts fallback (take 100). None collapses per contest - state.ts's own comment says why and says it was known: 'Two rows for one fixture carry DIFFERENT ids, so the collapse below cannot pair them; only excluding the tombstoned row can.' That was correct AND the merge it depends on has never run: zero rows are tombstoned in any sport (C-163), so the board could show ONE CONTEST TWICE, and the duplicates spent the lane's slots so it also showed FEWER fixtures than its cap. The pass lane is worse than cosmetic: `dedupePassesByGame` keys on gameId, so two rows for one contest each carried their OWN reason - the C-117 contradiction, in the lane C-117 never reached. FIX: the same repair as C-153, C-161, C-169 and C-170 - cap bounds rows SCANNED (40/60/300), collapse per fixture, then the display cap (8/12/100, all unchanged). The collapse is the C-169 helper, promoted out of the slate and shared (packages/ingestion-pipeline/src/fixture-collapse.ts, exported from the package index), so both surfaces keep the merge planner's survivor rule and cannot drift apart. `mergedIntoGameId: null` added to the pass fallback - SAFE HERE because it is a display lane, and the contrast with C-170 (where the same filter would strand settlement) is now pinned by a test on each side. TWO DEFENSIVE DETAILS WORTH KEEPING: the queries now select `sport.key`, because the collapse picks its twin window from it and baseball's is 2h so a DOUBLEHEADER stays two contests (a control test pins this); and the collapse KEEPS any row whose commenceTime it cannot read instead of throwing, because both callers are read paths whose catch returns an EMPTY board - a TypeError in the collapse would have blanked a public surface. | claude | DONE | PR #719. Entry files: packages/ingestion-pipeline/src/fixture-collapse.ts (+ index export), apps/web/lib/board/state.ts fallback lanes, apps/web/lib/board/passes.ts fallback, tests in fixture-collapse.test.ts, board-gate-decisions.test.ts, board-passes-published-suppression.test.ts, board-pass-reason-honesty.test.ts fixture. Verify: typecheck 0, lint 0, guardrails 26/26, ingestion-pipeline 428 passed, apps/web suite green. |
| C-172 | THE FIFTH TIME IS WHEN YOU BUILD THE GUARD. In one night this repo produced the same defect five times on five lanes - a cap on `games` or `gate_decisions` applied BEFORE the per-fixture collapse, on a table holding ~2.5 rows per contest with nothing tombstoned: C-153, C-161, C-169, C-170, C-171. Every one was found by review AFTER it shipped. apps/web/__tests__/fixture-query-inventory.test.ts now takes an inventory of every db.game.findMany and db.gateDecision.findMany in apps/web/{lib,app}, packages and workers - 18 queries across 15 files - and requires each to be REGISTERED as one of scan-then-collapse / single-row / unbounded / capped-by-design with a one-line reason. A new query, or a change to an existing one, fails the suite until somebody writes down which kind it is. It deliberately does NOT judge correctness: 'is this cap before a collapse' is not a regex question, and a guard that guesses gets argued with instead of read. What it enforces is that no such query is UNDECLARED. scripts/guardrails/** is frozen for agents (law 2) so it lives in the test suite, which CI runs. THE REGISTRY ALSO RECORDS TWO GAPS I DID NOT FIX, and a third test asserts they stay named: the board's two DECISION lanes (state.ts take 500, passes.ts take 500) collapse by gameId, which two rows for one contest do not share, so a fixture evaluated twice can still list twice there. C-171 fixed the FALLBACK lanes only. Collapsing the decision lanes is not the same one-line change: a PUBLISHED decision on each duplicate row is two REAL published picks, and dropping one would hide a live claim rather than a duplicate. NEGATIVE CONTROL INCLUDED, and it is not decorative - the first run of this test scanned the wrong root, found zero queries, and would have passed vacuously the moment the registry matched; it now asserts the scan found more than ten. | claude | DONE | PR #719. Entry file: apps/web/__tests__/fixture-query-inventory.test.ts. Verify: typecheck 0, lint 0, guardrails 26/26, the new suite 3 passed. |
| C-174 | CI RED ON MY OWN C-170 PUSH, PLUS THE REAL DEFECT THE REVIEW FOUND UNDERNEATH IT. TWO THINGS, AND THE FIRST IS A PROCESS MISS WORTH RECORDING: apps/web has tests in TWO places - __tests__/ and COLOCATED beside the source (lib/data-sources/free-score-persist.test.ts) - and I verified C-170 by running only __tests__/. The colocated suite pinned take:300 and went red in CI on 23d7648a0 and again on f90bd3ffa. The repo-wide `npm test` is the CI-equivalent command and is what I should have run; it reproduces in one shot. Assertion updated to 2000 with the reason, not deleted. SECOND, AND IT IS A REAL BUG (Devin Review, #719): the candidate window is an INSTANT interval, 21*24h back from now, and an instant interval that does not begin at midnight touches 22 DISTINCT Eastern calendar dates. uniqueScoreboardDates was called with maxDays 21 and keeps the NEWEST 21, while the candidate query orders games OLDEST first - so the oldest date's games were selected as candidates and their scoreboard date was never fetched. Those games stayed unscored until they aged out of the window entirely, which is the same overdue-pick symptom C-170 was fixing from the other end. Widening the candidate cap made that day reliably populated rather than sometimes empty, which is how the review surfaced it. Now maxDays 22, pinned by a test that asserts the ARGUMENT because this suite mocks the date helper - the honest assertion available in that file, and it says so in its own body. | claude | DONE | PR #719. Entry files: apps/web/lib/data-sources/free-score-persist.ts uniqueScoreboardDates call, apps/web/lib/data-sources/free-score-persist.test.ts. Verify: typecheck 0, lint 0, guardrails 26/26, and the FULL repo-wide npm test - 930 test files passed, 0 failed. |
| C-173 | FAN-06 (part of C-94): the projections badge published a claim the page contradicted. 'The projections SOURCE is live' and 'the players on this page are real' are two different facts, and ProjectionsBadge asserted the first with no way to know the second. FIVE fantasy surfaces render the FICTIONAL pool from lib/fantasy/players.ts - waivers, league-twin, trade, studio, scheme - whose own doctrine comment says the names are fictional precisely so no estimate is mistaken for a real one, and every one of them shows this badge through FantasyShell. SAY THE STATE PLAINLY: THIS IS LATENT, NOT LIVE. getLiveProjectionsMeta().live is false today because the licensed provider is founder-gated, so the badge already reads 'illustrative' everywhere. It becomes a false claim on a PAID surface the day the provider is enabled - a config change, with nothing in the code to catch it. FIX: `pool` is now a REQUIRED prop with no default, three states - real / illustrative / none. Three rather than two because collapsing 'none' lies in both directions: illustrative understates a working provider, real asserts players the page does not have. FantasyShell defaults to 'illustrative', so no page had to be edited and every unreviewed surface fails CLOSED; bestball and draft derive theirs from the SAME resolved pool their note already uses, so the badge and the note cannot disagree; /launch renders no players and passes 'none'. FOUR TESTS including the control that the discriminator cannot be used to claim live data while the provider is off. LEFT FOR THE FOUNDER, NOT EDITED: /launch says 'The Draft Assistant and the new Best Ball board run on real, cleared nflverse-graded data' unconditionally, while those tools fall back to the illustrative pool when resolveToolPoolAsync returns null. That is marketing copy and a copy decision, so it is reported rather than rewritten. | claude | DONE | PR #719. Entry files: apps/web/components/integrations/projections-badge.tsx, components/fantasy/fantasy-shell.tsx, app/launch/page.tsx, app/fantasy/{bestball,draft}/page.tsx, __tests__/projections-badge-pool.test.tsx. Verify: typecheck 0, lint 0, guardrails 26/26, full repo npm test 930 files passed. |
| C-175 | A FIXTURE WE PUBLISHED AND THEN WITHDREW WAS BEING DESCRIBED AS ONE WE NEVER LOOKED AT (Devin Review, #719). Both board lanes have a fallback query that selects games with `picks: { none: { isPublished: true } }` and labels them with `unevaluatedPassReason` - state.ts's gated lane and passes.ts's pass list. That relation excludes only a LIVE published pick, so THE MOMENT A PICK IS WITHDRAWN its fixture matches again and the surface says the game has not been evaluated. It is the same defect chronology fixed one query higher (C-149, C-153) reappearing in the fallback, and it is the strongest way these lists can be false: we evaluated it, published it, and withdrew it. FIX: both fallback queries now exclude the fixtures in the withdrawal watermark the decision lane already computes; in state.ts that map is hoisted out of the decision branch so one computation serves both paths. WHY SILENCE AND NOT A LABEL, and this is a judgement: of the options available, 'we passed' and 'not evaluated' both assert something untrue, and a third public state ('published, then withdrawn') is customer copy nobody has approved - and could not be written accurately anyway while Pick.isPublished carries no withdrawal timestamp (C-158, C-167). THIS ALSO ANSWERS DEVIN'S OTHER OPEN THREAD, 'Superseded fixtures disappear from mixed boards', WHICH PULLS THE OPPOSITE WAY: that one asks for the superseded fixture to reappear through the fallback, which is exactly the false label this row removes. The two cannot both be satisfied by the lanes as they exist; they can only both be satisfied by a fourth display state, which is founder copy plus the C-158 column. Recorded rather than silently picking one. | claude | DONE | PR #719. Entry files: apps/web/lib/board/state.ts newestWithdrawnPublishedAt hoist + gated fallback where, apps/web/lib/board/passes.ts fallback where, tests in board-gate-decisions.test.ts and board-passes-published-suppression.test.ts. Verify: typecheck 0, lint 0, guardrails 26/26, full repo npm test 930 files passed. |
| C-176 | THE PUBLIC CALIBRATION PANEL WAS THE LAST CUSTOMER SURFACE CALLING THE EDGE INDEX A PROBABILITY. Two claims lived there: (1) a per-band 'expected' marker positioned at confidence/100, which draws the diagonal a perfectly calibrated FORECAST would land on, and (2) a published Brier score computed as (confidence/100 - outcome)^2, labelled 'the single calibration number', with prose reads including 'Sharp. Confidence tracks outcomes closely.' A Brier score is only a calibration score if the number being scored is a probability, and this repo asserts everywhere else that confidence is not one (compute-live-calibration-metrics.ts 'confidence/100 is never scored'; home copy 'Not a probability the pick wins'; C-88 and C-157 removed the same claim from the proof page, the B2B API, the footer and the home annotations). SAY THE STATE PLAINLY: IT WAS LATENT, NOT LIVE. loadPublicCalibrationReport returns gated:true with an empty payload until the performance gate opens, and the gate is closed (eligibility RED), so nobody has been misled - which is exactly why it had to be fixed BEFORE the flip rather than after. WHAT STAYS: the observed decided win rate per band with its Clopper-Pearson interval, and the discrimination read - both real measurements that need no probability claim. WHAT REPLACES THE BRIER TILE: a sentence saying what the panel is (a separation read on a ranking signal) and naming the market-anchored measurement as the one the gate actually uses. Pinned by a SOURCE-level test because the defect is the presence of a particular expression in a rendered component; a render test would only catch it on whichever fixture exercised that branch. | claude | DONE | 6ef86add6 on claude/sports-launch-round2-fixes-hk7kv9. Entry files: apps/web/components/performance/calibration-panel.tsx, apps/web/__tests__/calibration-panel-not-a-probability.test.ts. Found by a parallel read-only calibration audit that also independently CONFIRMED C-28 (the public chart buckets by confidence while the gate scores market-anchored p, moneyline-only). Verify: typecheck 0, lint 0. |
| C-177 | A TEST THAT ASSERTED expect(true).toBe(true) UNDER A PRODUCT-LAW DOCSTRING. apps/web/__tests__/env-flags-founding.test.ts carried a suite named 'signal board product law' whose only case was titled 'documents dual freshness: generation SLA or upcoming pending signals' and whose body was expect(true).toBe(true) with a comment pointing at the real implementation. A test that cannot fail is worse than no test: it reported coverage of CLAUDE.md rule 5 (no stale data) while asserting nothing, so a regression in that gate shipped green AND the suite count made the area look guarded. Now exercises all four branches of isSignalBoardSlateStale - fresh inside the generation SLA, fresh on a stale generation time when an upcoming published PENDING signal exists, stale when both fail, stale when there has never been a published pick - and pins the fallback QUERY (published, non-bootstrap, non-seed, PENDING, 7-day horizon), because loosening any of those would keep the public board open on evidence it should not accept. 11 tests in the file, up from 8, and the new four can each fail. | claude | DONE | 6d081f50d on claude/sports-launch-round2-fixes-hk7kv9. Found by a parallel read-only test-gap audit. Verify: typecheck 0, lint 0, suite green. |
| C-178 | THE UPGRADE PROMPTS A FREE VISITOR MUST READ WERE BELOW WCAG AA, MEASURED NOT ESTIMATED. text-ultraviolet (#7B61FF) on a bg-ultraviolet/20 panel over the obsidian ground computes 3.88:1, and on bg-ultraviolet/10 it computes 4.34:1; AA for normal text is 4.5:1. I recomputed both ratios myself with the WCAG 2.1 formula rather than accepting the audit's numbers - they matched. Those exact pairings carried 'Full board is a Pro feature' (11px bold uppercase), the free-tier teaser explanation, and the Pro-to-Elite alerts teaser: the conversion path. text-ultraviolet-glow (#9F87FF) on the same two grounds measures 5.74:1 and 6.41:1, so the swap is the whole fix - no new token, no layout change. Applied to every ultraviolet-tinted text site found, not only the picks page. The regression test RECOMPUTES the ratios from the token hexes instead of hard-coding them, so changing a token recalculates and fails on its own, and keeps a negative control asserting the OLD pairing fails so a later revert has to argue with the arithmetic. | claude | DONE | 7245eb75b on claude/sports-launch-round2-fixes-hk7kv9. Entry files: apps/web/app/picks/page.tsx, app/fantasy/baseline/page.tsx, components/parlay/parlay-genome.tsx, components/cards/result-card.tsx, __tests__/paywall-cta-contrast.test.ts. Verify: typecheck 0, lint 0, guardrails 26/26. |
| C-179 | A ZERO A CUSTOMER CANNOT TELL APART FROM AN OUTAGE IS A FALSE STATEMENT. Every count on the signed-in dashboard failed soft - .catch(() => 0) and .catch(() => []) - so one dead query could never take the page down, which is right and stays. But the fallbacks ARE the displayed values, so a database outage rendered '0 settled, 0 wins, no picks today' and a member read it as an empty account rather than a broken load. /board and /picks already surface DB_UNREACHABLE out loud; the surface a paying member actually lands on did not. The fallbacks now set a degradation flag AS THEY FIRE, so the flag cannot drift from the catches it describes, and the page renders a role=status banner naming the cause and disclaiming the numbers. Pinned at the source because the property is 'no fallback bypasses the flag' - a render test only proves the branch it exercises, and the risk is the eleventh query somebody adds later with a bare catch. The test keeps the control that every query still has a catch, so 'be honest about the outage' cannot turn into 'crash on it'. | claude | DONE | 44dbda4b6 on claude/sports-launch-round2-fixes-hk7kv9. Entry files: apps/web/app/dashboard/page.tsx, apps/web/__tests__/dashboard-outage-honesty.test.ts. Verify: typecheck 0, lint 0, guardrails 26/26. |
| C-180 | THE PASS-LIST API RETURNED THROUGH A BARE NextResponse.json, NOT jsonNoStore. `.claude/rules/nextjs-caching.md` rule 2 requires every route on the picks/passes surface to return through the helper, INCLUDING error and gate responses, because a cacheable error is exactly as dangerous as a cacheable success. /api/board/passes did neither: the 200 and the 429 both went out bare. `dynamic = 'force-dynamic'` governs Next's own render cache and promises nothing about an intermediary, and this body carries the suppression/degradation state that has to be able to change the instant a kill switch fires - a cached 200 here is a stale public board, which is the failure CLAUDE.md rule 5 exists to prevent. The 429 matters for the same reason in the other direction: a cached rate-limit response keeps refusing a caller whose window has already reset. Both now return via jsonNoStore. | claude | DONE | 13bd970fe on claude/sports-launch-round2-fixes-hk7kv9. Entry file: apps/web/app/api/board/passes/route.ts. Found by a parallel read-only money-path audit. Verify: typecheck 0. |
| C-181 | A PAID FEATURE IS DEAD ON THE PASS-LIST ROUTE, AND I DELIBERATELY DID NOT WIRE IT AT 3AM. /api/board/passes calls loadBoardPasses() with NO options, so `includeNoBetDetail` takes its default of false and the paid No-Bet forensic detail is never served through this route regardless of the caller's tier. IMPORTANT DIRECTION: this FAILS CLOSED. It is a dead paid feature, not a leak, so it costs the product a feature rather than costing it a paywall. WHY IT IS NOT FIXED HERE: wiring it means adding per-viewer entitlement resolution to a route that is currently PUBLIC and ANONYMOUS, and the failure mode of getting that gate wrong is the opposite and far worse one - paid forensic detail served to an unauthenticated caller, which is CLAUDE.md rule 3. That is a reviewed change with its own tests and an explicit read of .claude/rules/api-gating.md, not a drive-by at the end of a long session. The route now carries the gap in a comment so the next reader does not have to rediscover it. | claude | OPEN | Entry files: apps/web/app/api/board/passes/route.ts (the loadBoardPasses call), apps/web/lib/board/passes.ts LoadBoardPassesOptions, apps/web/lib/api-entitlement.ts for the gate helpers. Found by a parallel read-only money-path audit; branch claude/sports-launch-round2-fixes-hk7kv9. |
| C-182 | THE APEX HOST WAS BEING WRITTEN INTO GENERATED PUBLIC CONTENT. CLAUDE.md states one canonical host: every absolute URL derives from apps/web/lib/seo/site-url.ts, which resolves to the WWW host and NEVER the apex. lib/studio/load.ts and app/api/cockpit/studio/generate/route.ts both hardcoded `https://galaxysportsedge.com` - the apex - as brandConfig.publicUrl, and that value is interpolated into generated X threads, TikTok scripts, newsletter blocks, sponsor blurbs and fan explainers. A published draft therefore carried the non-canonical host out into search and social, which is expensive to take back. THE HARDCODE EXISTED IN TWO PLACES THAT DO NOT SHARE CODE, so fixing one would not have fixed the other - the same duplicate-lane shape as the board defects earlier tonight. Both now read SITE_URL. | claude | DONE | 0ab160cf6 on claude/sports-launch-round2-fixes-hk7kv9, PR #720. Found by a parallel read-only SEO audit; verified by me before the edit. Verify: typecheck 0, lint 0, guardrails 26/26, full repo npm test 934 files passed. |
| C-183 | A FALSE STATEMENT IN THE PRIVACY POLICY. apps/web/app/privacy/page.tsx section 4 said 'You can delete your account at any time from the dashboard.' MEASURED: there is no delete-account UI, no deletion route, and no user delete anywhere in apps/web (repo-wide grep; the only hits are unrelated watchlist and outbox internals). A privacy policy that describes a path the reader cannot take is a false statement in a legal document, and under CCPA/GDPR it is the kind of promise a regulator reads literally. The copy now describes the mechanism that actually exists - the legal contact address, which the same section already uses for every other data right - and the 30-day deletion commitment and retention carve-out are unchanged. BUILDING THE SELF-SERVICE CONTROL IS THE BETTER FIX AND IS FOUNDER-GATED: it needs a deletion path across auth, Stripe and the settlement history that must be retained. Flagged for founder review because it is a legal-document edit. | claude | DONE | 9c2f4f01c on claude/sports-launch-round2-fixes-hk7kv9, PR #720. Found by a parallel read-only legal/compliance audit. |
| C-184 | /api/ops/daily-truth SET NO maxDuration WHILE EVERY OTHER CRON DOES. MEASURED across apps/web/app/api/cron/*: ten routes set 300, seven set 60, one sets 30, and daily-truth set none - so it ran under the platform default while fanning out across the calibration surface, scheduler liveness and canonical-sample posture in one pass. The failure is quiet in the worst way: the daily truth record is simply not written, and the surface that would tell an operator something is wrong is the one that failed. Set to 300, matching the ceiling the other heavy ops jobs use. | claude | DONE | 9c2f4f01c on claude/sports-launch-round2-fixes-hk7kv9, PR #720. Found by a parallel read-only cron/ops audit, which also reconciled all 22 schedules against their routes: NO orphan schedules, no drift between apps/web/vercel.json and the root mirror (byte-identical), and four unscheduled cron routes that docs/ops/CRON_MATRIX.md documents as deliberate. |
| C-185 | robots.txt AND THE SITEMAP DISAGREED ABOUT /stats. sitemap.ts adds /stats, /stats/compare, /stats/ask, /stats/proof and /stats/expert-board the moment isStatsPublic() is true; robots.ts disallowed /stats and /stats/ UNCONDITIONALLY. Submitting a URL you also forbid is the 'Indexed, though blocked by robots.txt' state - the page is neither properly crawled nor cleanly excluded, and Search Console reports it as an error against the site. Both now derive from the same gate. LATENT today because STATS_PUBLIC is off, which is exactly when it is cheap to fix. ALSO IN THIS ROW: the pricing page's JSON-LD Offer carried a RELATIVE url ('/pricing'). metadataBase resolves relative URLs inside Next's own Metadata fields; it does not touch a hand-built JSON-LD object serialised into a script tag, so a structured-data parser saw the literal string. Now absoluteUrl('/pricing'). | claude | DONE | 0ab160cf6 on claude/sports-launch-round2-fixes-hk7kv9, PR #720. |
| C-186 | GSE-SEC-071 IN THE SIBLING LANE, AND THE PRECEDENT FIX WAS SITTING IN THE NEXT FILE OVER. apps/web/lib/loss-autopsy/draft.ts rethrew `error.message` verbatim into a LossAutopsyDraftError, and the route returns that message to the caller. ClaudeMessagesError is constructed as `Claude API error: ${status} - ${await response.text()}`, so its message carries the RAW upstream Anthropic response body: request ids, account and quota detail, model names, internal error text. apps/web/lib/pick-explainer/explain.ts fixed exactly this under GSE-SEC-071, with a comment explaining why, and the fix was never carried across. Severity is limited by the route being admin-only, not eliminated. Now: full error to Sentry with the upstream status and model name, generic message to the caller, and the HTTP_<status> ledger entry above it is untouched so no diagnostic detail is lost. | claude | DONE | 9c2f4f01c on claude/sports-launch-round2-fixes-hk7kv9, PR #720. Found by a parallel read-only Claude-API audit, which ALSO verified the two hard product claims hold: content publishing is genuinely draft-only (no path sets publishedAt or status PUBLISHED, and scripts/guardrails/draft-only.mjs enforces it in CI) and no Claude output ever writes a pick, line, confidence or result. |
| C-187 | A REAL DATA LEAK IN THE FEATURE PIPELINE: THE FORM WINDOWS HAD NO DATE BOUND. getAtsForm and getHeadToHeadForm (packages/data-ingestion/src/context-enrichment.ts) took 'the most recent N settled games' with NO cutoff, while their two siblings in the SAME FILE - computeRestDays and computeScheduleDensity - both filter `gameDate: { lt: gameDate }` and always did. The correct pattern and the incorrect one sat side by side. Unbounded is safe ONLY while every call happens in real time, because a game that has not been played cannot be in TeamGameLog yet; on any reprocess, backfill or re-run after later fixtures settled - and this repo's history includes backfill-ordering bugs - the window silently starts including results from AFTER the game being predicted. That is the classic leak: a model that looks excellent in testing because it read the future. FIX: `asOf` is REQUIRED, not optional-with-a-default, so a future caller cannot forget it and the type error is the reminder; the call site passes the fixture's own kickoff. NO LIVE BEHAVIOUR CHANGE: for live generation the excluded rows do not exist yet, so no pick moves - the leak closes for every other path. Pinned by a test asserting the bound is applied IN THE DATABASE and is strictly `lt` (a row stamped at kickoff is the game itself), with a control that the window still returns a reading so the fix cannot silently delete a feature. | claude | DONE | 30cdcd65b on claude/sports-launch-round2-fixes-hk7kv9, PR #720. Found by a parallel read-only leakage audit run against the discipline in the founder's Leak Check prompt. Verify: typecheck 0, lint 0, guardrails 26/26, full repo npm test 934 files passed. |
| C-188 | MEASURED ANSWER TO 'DOES THE ENGINE BEAT THE MARKET', AND TO 'IS IT SEEING THE FUTURE'. Read-only SELECT against production; full method and caveats in docs/ops/BASELINE_AND_LEAK_CHECK_2026-09-08.md. LEAKAGE, THE HIGHEST-STAKES QUESTION, IS CLEAN: publish-time-market-p.ts:150 bounds each pick's market probability at `fetchedAt <= that pick's own generatedAt`, so the number the whole PROVEN gate rests on is not reading the future. BASELINE: on n=565 settled published moneylines resolved to real books at publish time (avg 5.24 books), model hit rate 67.79pct against a mean de-vigged market probability of 65.60pct on the SAME selections - an edge of +2.19pp with SE 1.88pp, t=1.17. THE HEADLINE HIT RATE IS ALMOST ENTIRELY THE MARKET: the engine picks favourites and favourites win. By sport: MLB +3.37pp t=1.42 (n 371), NCAAF +8.32pp t=2.92 (n 74), NFL +15.01pp t=1.60 (n 27, noise), MLS -11.10pp t=-2.17 (n 93, significantly WORSE - the known C-118 two-way-price-on-a-three-way-market error). Excluding MLS: n=472, +4.81pp, SE 1.99, t=2.41, against a mean overround of 3.62pp - so roughly +1.2pp NET OF VIG. THE MLS EXCLUSION IS POST-HOC and I say so: defensible a priori (the construction error is documented and the engine already refuses to publish those picks) but chosen after seeing the number. In-sample, no time split, selection-affected. Promising, NOT proven. | claude | OPEN | docs/ops/BASELINE_AND_LEAK_CHECK_2026-09-08.md. Founder-gated follow-up: a SKILL floor beside the calibration floors. |
| C-189 | THE PROVEN GATE CERTIFIES CALIBRATION, NOT SKILL, AND A ZERO-SKILL FORECASTER PASSES MOST OF IT. Derived from the decomposition in packages/prediction-engine/src/probability-calibration.ts: a forecaster that predicts the base rate on every pick puts every prediction in ONE bin, so Murphy reliability = sum nk(fk-ok)^2/n = EXACTLY 0, ECE = sum (nk/n) times abs(fk-ok) = EXACTLY 0, and resolution = 0 - it distinguishes nothing. Two of the four floors in apps/web/lib/ops/calibration-eligibility.ts are passed PERFECTLY by a forecaster that knows nothing, and the sample-size floor is not a quality bar at all. CORRECTING A PARALLEL AUDIT THAT WENT FURTHER: it stated a constant forecast passes all four. That is base-rate dependent, not universal - Brier = p(1-p), which at the measured base rate 0.652 is 0.2269 and FAILS the 0.22 floor, while at 0.678 it is 0.2184 and passes. The accurate claim is narrower and still damning: only Brier retains any bite and its bite is a function of the base rate rather than of skill. CONSEQUENCE: a product can be perfectly calibrated with no edge at all - which is exactly what 'agrees with the market' looks like - and nothing in the eligibility check would notice. PROPOSED FIX, FOUNDER-GATED: a Brier Skill Score floor against the publish-time de-vigged market, 1 - Brier_model/Brier_market, required positive with a CI excluding zero. The primitives already exist (brierDecomposition; market-backtest.ts already reads the market baseline); nothing currently computes model, constant and market side by side, so nothing can fail for lack of skill. NOT an agent change: adding a floor is a product decision about what PROVEN means. | claude | OPEN | docs/ops/BASELINE_AND_LEAK_CHECK_2026-09-08.md section 3. |
<!-- LEDGER:END -->
