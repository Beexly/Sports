# Agent ledger

One row per unit of work. **Every agent working on this repo reads this file first,
claims its row, and writes its evidence when done.**

**Also read the live truth surface before acting:**
`https://www.galaxysportsedge.com/api/ops/public-surface-truth` — deployment SHA,
gates, credit stack, Jynx lane plan, calibration. On 2026-08-19 it disproved a
"leaked Anthropic key" alarm (the value was Vercel placeholder text; the real
value is `auto`) and would have prevented a week-old open PR (#369) from being
rebuilt twice by different agents. Dashboards and prose reports drift; this
endpoint and this ledger are the two ground truths.

Validated by `scripts/ops/check-agent-ledger.mjs`, which runs in CI via
`apps/web/__tests__/agent-ledger.test.ts`. A malformed or dishonest row fails the
build.

## Why this exists

Four agents work on `Beexly/Sports` — a local Hermes runner, GitHub Copilot, a
browser agent, and Claude Code sessions. They share no memory. On 2026-08-19 that
cost three separate collisions in one day:

- Copilot and Hermes each built the same fabricated `signup-workflow` feature,
  independently, neither knowing the other had.
- A Claude session began reimplementing `H-L` while it was assigned to Hermes.
- Hermes read "run the queue" as a CSV import queue and processed a census file
  instead of the task queue.

Git is the only substrate all four agents actually touch — none of them can reach a
Postgres or Redis memory store. So the ledger is a file, and the enforcement is a
guard, not a convention.

Related prior art, and why it is not used: `Beexly/fablechain` carries an
`AgentMemory` (Postgres + Redis) and a `TaskBacklog`. Over its last 200 commits the
agent implemented `feat(faucet): implement testnet faucet backend` **nine times** with
an identical subject line, because `TaskBacklog.ts` is a `const` array and completing
a task never removes it. Persistent memory alone does not prevent duplicate work.
Evidence does.

## Rules

1. **Claim before you start.** Set `Owner` and `Status: CLAIMED` in the same commit
   that begins the work. An unclaimed row is fair game; a claimed row is not yours.
2. **Never edit a row you do not own**, except to add a `BLOCKED` note explaining a
   dependency.
3. **`DONE` requires evidence.** Put a commit SHA (7+ hex chars) or a `#123` PR
   reference in `Evidence`. The guard resolves SHAs against the repo — a hash that
   does not exist fails the build. "I completed it" is not evidence.
4. **If you cannot push, you are `UNPUSHED`, not `DONE`.** Record the local branch
   and SHA so the work can be recovered. Eight rows sit in this state right now;
   every one of them exists on exactly one laptop.
5. **`CANCELLED` requires a reason** in `Evidence`, not a hash.
6. **Titles must be unique.** Two rows describing the same work is the exact failure
   this file exists to catch, so the guard rejects it.
7. **One row, one unit of work.** If scope grows, add a row; do not widen a claimed one.

## Owners

| Owner | What it is |
|---|---|
| `hermes` | Local Hermes runner (OpenRouter free models), on the founder's machine |
| `copilot` | GitHub Copilot coding agent, runs on a GitHub runner |
| `browser` | Browser agent with dashboard access (Vercel, Anthropic console) |
| `claude` | Claude Code session (remote container) |
| `founder` | Garrett — owner-gated decisions only |
| `—` | Unclaimed |

## Statuses

`OPEN` · `CLAIMED` · `BLOCKED` · `UNPUSHED` · `DONE` · `CANCELLED`

`UNPUSHED` is the honest state for work that is finished but lives only on one
machine. Hermes is instructed never to push, so its deliverables sit on a local
branch whose SHAs no other clone can resolve. That is not `DONE`: one laptop is
the only copy, and nobody else can verify or build on it.

## Ledger

<!-- LEDGER:BEGIN -->

| ID | Title | Owner | Status | Evidence |
|---|---|---|---|---|
| H-A | Isotonic PAV calibration | hermes | DONE | 62e32730 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-B | Cron matrix generator | hermes | DONE | 4b961782 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-C | E-process sequential test | hermes | DONE | f53b229e on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-D | markClosingSnapshots behind a flag | hermes | DONE | 0a447f98 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-E | CLV census on a Neon branch | hermes | UNPUSHED | output CSV on founder machine: docs/ops/calibration/2026-08-18-clv-census.csv, 1161 rows; Neon branch since deleted |
| H-F | DOC_DRIFT.md audit | hermes | DONE | e668c4c4 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-G | Suppression curve vs random and oracle baselines | hermes | DONE | 63e84c32 on origin/hermes/sprint-backup-20260819, verified ancestor 2026-08-19 |
| H-K | charge.refunded revocation behind REFUND_REVOKES_ACCESS | claude | DONE | e69aa93c on origin/hk/refund-revocation — REIMPLEMENTED by fleet after Hermes confirmed "never implemented". 19 new tests, 72/72, default-OFF structural. Adversarial review confirmed 1 major (active-subscription refund lock-in) + hardening list; revision is C-11 before any merge |
| H-L | health-alert stateless cooldown + portable payload | claude | DONE | b9ec799 |
| H-M | Cron no-op audit | hermes | DONE | cd5086f7 on origin/hermes/h-m-cron-audit; data-driven early-return proposal only, ~60/day off-season, 576/day figure withdrawn |
| H-N | Env-shape validator | hermes | CLAIMED | — |
| H-O | Repair the @/lib/stripe test mock | claude | DONE | ab9f9c2; superseded by #369 single-seam version — the fix had been sitting mergeable in #369 for 12 days and was rebuilt twice for lack of a ledger |
| H-P | Triage the 73 CI test failures | claude | DONE | #434: every cluster root-caused; fixes shipped for checkout, copy, nflverse, waitlist, kill-switches, PAVA |
| C-5 | PAVA forward-violation bug in calibration core | claude | DONE | 9627379 |
| H-Q | Fix remaining singleton test failures (triage notes in v7 handoff) | hermes | DONE | 14b74b5c, merged via 3e997adf; full apps/web suite ZERO failures locally for the first time this session |
| F-4 | Authorize one-time Hermes push of its local sprint branch (8 UNPUSHED deliverables, single-copy risk) | founder | DONE | 63e84c32 (sprint-backup tip) + cd5086f7 (h-m tip) both on origin, independently verified; grant spent |
| C-1 | vercel.json guard drift + drift test | claude | DONE | 657a7f1 |
| C-2 | Land v5.2.6 calibration evidence | claude | DONE | 175c44f |
| C-3 | Declare appliedPauseGroups + RUN_GENERATE_SIGNAL_SLATE | claude | DONE | 1d39021 |
| C-4 | Agent ledger + guard | claude | DONE | 65e6474 |
| X-1 | signup-workflow scaffolding | copilot | CANCELLED | fabricated feature; auth is Google OAuth only via PrismaAdapter, no signup flow exists |
| X-2 | Delete CLAUDE_PROVIDER to fix AI routing | browser | CANCELLED | doubly wrong. (1) Original diagnosis false: value is "auto", not a key. (2) Deletion is NOT inert in production: with clouds configured and no JYNX_MODE, empty -> mode "anthropic" -> zero cloud attempts -> CASH Anthropic, dropping the azure/vertex credit lanes that "auto" selects. provider-mode-failsafe.test.ts covers UNRECOGNISED values only — citing it for deletion was a category error (fable). Prerequisite for any future deletion: set JYNX_MODE=auto first |
| F-1 | Rotate the Anthropic key found in CLAUDE_PROVIDER | browser | CANCELLED | false premise: live truth surface returns claudeProvider "auto"; the sk-ant-api03-aB text was a Vercel placeholder misread, then propagated by the orchestrator. No Anthropic key was ever exposed there. Browser agent correctly refused the rotation script |
| F-2 | Decide REFUND_REVOKES_ACCESS default | founder | OPEN | decision made (fable): stays OFF through H-K merge; flip to true after ONE observed production refund revokes correctly in logs; the env flip itself is a hands task |
| F-3 | Promote or remove apps/web/app/api/v1 | claude | DONE | 149e469f — RULING (delegated authority): PROMOTE. The tree is not a shadow — signals/probabilities/openapi are functioning, key-authed, rate-limited, RED-honest B2B routes; deleting working revenue surface mid-launch is malpractice, and permanent-red trains everyone to ignore CI. Promotion executes the machinery's OWN ceremony (promotion-readiness.ts gates: owner-approval-recorded via founder delegation, rollback evidence = git revert of the route commits, raw-key-absence proof), never bulldozes it. Implementation is C-8 |
| C-8 | Execute the api-v1 promotion ceremony: record doc, guard route-tree check to exact-3-route allowlist (new routes still fail), promotion-readiness gate + ~13 tests aligned to post-promotion model — own PR, small diff | claude | DONE | 149e469f; 69/69 across ten api-v1 suites, full suite 41 to 11 failures, guard negative-tested with a rogue route, no assertion weakened |
| C-7 | Triage the residual failure set: cockpit-jarvis-trend-api, cockpit-picks-glance, honest-degraded-states, picks-daily-limit-meta | claude | DONE | 41a971a0 — four stale assertions, zero real defects: a regex that could not cross a quote, a premium constant misread as a paywall regression, a query shape replaced by a clearer derivation, and honest copy improved past its pinned wording. 36/36. NOTE: the original "CI-only" framing was wrong — these fail locally too; gse-waitlist/backfill-independent-trueprob/espn-odds-client are genuinely CI-only and remain unowned (see C-9) |
| C-9 | Genuinely CI-only failures (pass locally, fail in CI): gse-waitlist 6, backfill-independent-trueprob 2, espn-odds-client 1 — almost certainly env divergence between the CI runner and dev containers | claude | DONE | e2c3c9f7 + 2f229d3b — the "env divergence" framing was wrong on all three. backfill: test pinned the pre-RES-lift plain average; the sharpness-weighted blend is deliberate; suite lives in packages/ingestion-pipeline which apps/web-scoped local runs never executed. gse-waitlist: CI uses the durable Postgres limiter which resetRateLimits() cannot clear; 11 POSTs shared one "anon" key at 5/60s, flapping on the window boundary — each POST now has its own client IP. espn-odds-client: fixture commence date 2026-08-10 aged out of the client's -6h..+21d window on Aug 11 — deterministically red since; fixture date now dynamic. CI-green confirmation pending the run on 2f229d3b |
| C-6 | Drive PR #369 to green and merge (invoice.paid alias, auth-aware smoke, single-seam checkout mock) | claude | DONE | #369 |
| R-1 | Rotate the ~25 Hermes .env credentials exposed in a session transcript (Fireworks, Together, DeepSeek, OpenRouter x2, Vercel first); delete the browser agent's own mistyped 401 gateway key | browser | OPEN | APPROVED (fable) 2026-08-19; these are the real exposure, not the phantom sk-ant key |
| R-2 | Set JYNX_MODE=auto in Production+Preview (belt-and-braces so a future CLAUDE_PROVIDER deletion is genuinely inert) | browser | OPEN | APPROVED (fable) 2026-08-19 |
| R-3 | Redeploy and confirm contentPlanPrimary flips off cerebras_free to the OpenRouter secondary (CEREBRAS_API_KEY removed; live surface still shows the old plan) | browser | OPEN | ELEVATED 2026-08-19: this redeploy ALSO activates THE_ODDS_API_KEY (truth surface shows oddsKeyPresent false — env saved after the b71f7e2 build) — NFL odds path for preseason; do it first |
| R-4 | Verify TheRundown 429s stop after THE_ODDS_API_KEY activates (dashboard request log clean for 1h); if 429s persist, report cadence for review — do not change any cron | browser | OPEN | 429 observed on /api/v2/sports/11/events/2026-08-20 at 05:22 UTC while ingestion still succeeded same minute (partial throttle, not blackout) |
| S-1 | Evaluate TheSportsDB as a free schedules/results redundancy source (v1 key 123, 30 req/min; NOT an odds source) — must pass source-rights registry classification before any adapter work | — | OPEN | founder surfaced docs 2026-08-19; folds into H-S |
| H-S | Free sports-data provider map (L-5 in the v8 queue): one doc, fixed schema, every candidate classified — odds sources separated HARD from schedules/results/stats sources, free-tier limits verified with at most 2 live calls each using documented public keys only, source-rights classification per CLAUDE.md for each. No signups, no credential creation, no adapters — map first | hermes | DONE | fc4587114 — docs/ops/data-sources/FREE-PROVIDER-MAP-2026-09-03.md, 8 providers classified (7 real + OrcaSports confirmed vaporware), all with tier/limits/source-rights per CLAUDE.md taxonomy |

| C-10 | Guard hardening from fleet review: ledger guard fetch-by-sha in shallow CI (fabricated SHAs currently pass), api-v1 route walker must not honor SKIP_DIRS and must flag symlinks, promoted-routes listFiles must not throw on dangling symlinks — each fix carries the reviewer reproduction as a regression test | claude | DONE | b7471fc1 (fix/ledger-shallow) + 0451eb97 (fix/apiv1-walker), both merged. Ledger guard: validate() now fetches unresolved SHAs from origin (--depth=1) before failing, so a fabricated DONE SHA can no longer pass in a shallow CI clone; "unverified" now survives ONLY for a repo with no origin remote at all (strictly narrower than before). Verified end-to-end against a genuinely shallow repo: real ledger exits 0, scratch ledger citing deadbeefcafe exits 1 via an actually-refused fetch. api-v1 guard: dedicated walkRouteTree replaces the shared walker under the promoted tree — descends EVERY directory (SKIP_DIRS no longer hides app/api/v1/build/route.ts) and flags any symlink or non-regular dirent as its own violation id; promoted-routes listFiles no longer statSyncs, so dangling symlinks return a verdict instead of crashing 10 suites. PROMOTED_ROUTES literal unchanged, walkFiles untouched for all other scans — strictly more detection, nothing relaxed. Re-attack agent confirms all 5 exploits closed with live reproductions. 123/123 across the three affected suites, typecheck 0 |
| C-11 | H-K revision per attackers: never revoke while Stripe subscription still active (retrieve + status check, log for human), log-only mode makes ZERO Stripe API calls, transient retrieve failure in enforce mode 500s (structural unresolvability still 200s), status guard on revoke write | claude | DONE | 53200c5c (fix/hk-revision) on base e69aa93c, merged — brings H-K itself plus all four attacker findings fixed. (1) MAJOR lock-in closed: ENFORCE mode retrieves the resolved subscription and revokes ONLY on Stripe status canceled/incomplete_expired; any live status (active/trialing/past_due/unpaid/paused/incomplete) mutates nothing and escalates via console.error naming charge id, sub id and status, acking 200. (2) Log-only mode makes ZERO Stripe API calls. (3) Transient retrieve failures propagate to a 500 with the webhook event UNRECORDED so Stripe redelivers (test proves the second delivery revokes); structural dead-ends stay log-and-200. (4) revokeSubscriptionAccess WHERE now carries status: { not: 'CANCELED' } so a later distinct refund event cannot re-stamp canceledAt. 82/82 in stripe-webhook-route.test.ts, 200/200 across adjacent billing suites. Flag remains structurally default-OFF (=== "true"), verified post-merge — production behavior is unchanged until F-2 |
| C-12 | Edge Roadmap + gate-split map committed | claude | DONE | #435 — docs/ops/edge/2026-08-19-edge-roadmap.md + calibration-gate-split-map.md |
| F-5 | Founder reads the Edge Roadmap and picks the QUICK experiments to greenlight; enabler E1 (clean-room prod export) needs founder hands — agents cannot reach the DB | founder | OPEN | roadmap section 2-3; every experiment preregistered against CLV 52.4 percent under the e-process, kill criteria included |
| L-6 | CLV analysis on the census (the measurement that should have been L-4) | hermes | DONE | 830ff6df on origin/hermes/l6-clv-analysis, resolved 2026-08-19; 909/1161 graded pairs; headline 24.86% beat is NOT accepted as a performance number — inputs under C-14/L-9 provenance audit first |
| C-13 | Integrate origin/hermes/sprint-landing (e8286086: H-A..H-G toolkit + L-4/L-5 docs + main merged) into the designated branch, fully verified | claude | DONE | e00e526b, 373 files. Founder explicitly authorized proceeding after I read (not assumed) every sealed-path diff: CI adds one additive dependency-audit job (no existing job touched) + a secretless external-watchdog workflow polling the already-public status endpoint; new pre-push hook runs the existing secret scanner; .claude/settings.json only tightens this agent's own permissions; the Prisma migration is purely additive (1 enum, 2 new currently-unread tables, no ALTER/DROP/backfill) and its FILE landing here does not apply it to any database. Re-verified independently: typecheck 0 errors across 22 workspaces, both guards pass, full apps/web suite 11313/11314 (the 1 failure is ai-control-plane-sealing-guard.test.ts, a pre-existing environmental false positive from this session's .claude/worktrees/ convention predating that guard's SKIP_DIRS list — confirmed unrelated to merge content, would fail identically on any commit right now; real minor guard gap noted for a future authorized SKIP_DIRS fix). Two conflicts (espn-odds-client, backfill-independent-trueprob tests) resolved to already-verified C-9 content |
| C-14 | CLV-grading forensics: the L-6 magnitudes are diagnostic of a possible computation artifact (ML mean CLV -27.4pp is implausible as real market movement; spread beat-rate 10.31% with median CLV 0; inverse strategy clears 52.4% on decided picks) — audit settle-sport CLV sign conventions, ML price units, side attribution, MATCHED semantics before ANY conclusion ships | claude | DONE | docs/ops/edge/2026-08-19-clv-forensics-verdict.md. Verdict: MIXED/medium confidence. CLV math itself verified correct (sign conventions, American-odds handling); NOT de-vigged (methodology fact). Forensics found a real, ALREADY-FIXED side-derivation bug (0e56c477, 2026-07-17) matching the SPREAD/ML-low vs TOTAL-honest pattern — but adversarial refutation showed that story doesn't fit the magnitude (a rare name-collision can't crash an aggregate to 7-10%) and, critically, missed two REAL, CONFIRMED, still-UNPATCHED bugs on the exact same code path: no staleness bound on the closing snapshot (8e2af6f1 on origin/claude/hotfix-settle-refresh-races, unmerged) and truncated book coverage at close (take:80 vs needed take:240, fix at 6f0353e1 on origin/claude/galaxy-sports-edge-pdcswh, unmerged). Neither headline number (TOTAL 58.5%, ML -27.4pp) ships as a claim until these are applied and the census is re-graded |
| R-6 | Rename THE_ODDS_API_KEY_FREE to the canonical THE_ODDS_API_KEY in Vercel Production (code enumerates the canonical name; oddsMatchedEnv null proves the mismatch), verify CEREBRAS_API_KEY presence/absence, redeploy, re-read truth surface | founder | DONE | executed by founder hands 2026-08-19 ~07:39 UTC (not the browser agent), redeployed. Verified live (no-store, age:0): oddsKeyPresent=true, oddsMatchedEnv="THE_ODDS_API_KEY" — preseason NFL odds path unblocked. deployment.sha unchanged (b71f7e28) as expected: same-commit Redeploy with new env, not a new push. contentPlanPrimary still reads "cerebras_free" but this is a STALE LABEL not a bug: jynx.ts:165 names the field "cerebras_free" whenever isFreeLaneEnabled() is true via EITHER CEREBRAS_API_KEY OR the OpenRouter secondary (free-lane-policy.ts:14-21); the dispatcher itself (free-lane.ts:59-91) correctly skips Cerebras with zero wasted calls when the key is absent/falsy and falls through to the OpenRouter secondary host — money-correct, cosmetic-only. THE_ODDS_WIDGET_KEY also added (feeds C-17) |
| L-7 | CLV data-path forensics: 10-row hand spot-check, ML price-format census, spread side-vs-movement buckets, MATCHED semantics, per-book + June/July slices | claude | CLAIMED | Hermes's part done and pushed: f87146bb on origin/hermes/l7-clv-forensics but NOT YET merged into the designated branch: real conflict on 10 test files (canonical-sample-posture, cockpit-jarvis-trend-api, cockpit-picks-glance, cqr, glass-ledger-page, honest-degraded-states, picks-daily-limit-meta, player-stats-backfill-plan, refresh-player-stats-route, backfill-independent-trueprob/espn-odds-client) — this branch shares the same old pre-C-7/C-9-fix lineage as hermes/sprint-landing. Merge attempted, correctly aborted rather than hand-resolved under time pressure; likely resolves cleanly once C-13 lands sprint-landing (same root divergence). Do not rename to DONE until actually merged |
| L-8 | Commit + push the L-7 forensics artifacts with a formulas README | hermes | DONE | commit content confirmed on origin/hermes/l7-clv-forensics (f87146bb); integration into designated branch tracked under L-7 above, not blocking this row |
| L-9 | CLV verdict slices: decided-only beat rates with CIs, TOTAL deep-dive, lock provenance audit (book quote vs batch mean vs model-derived), ML monster-lock provenance, spread sign-flip classification | hermes | DONE | 93b92db1 on origin/hermes/l9-clv-slices, merged c19d2e06. 3/5 items computed: decided-only beat by market x month (TOTAL 58.5% [52.8,63.9] clears 52.4%, SPREAD/ML do not), sign-flip 57/388 SPREAD (toward 40/away 109/unchanged 239), spot-check 0/10 mismatches. **CRITICAL finding for C-14: 909/909 picks have ZERO matching odds_batch rows at clv_captured_at — every lock price appears model-derived, not a real book quote.** 2/5 items (lock provenance, ML monster-lock join) correctly left BLOCKED — no hermes_ro DB credential reachable anywhere on the Hermes machine (DATABASE_URL_UNPOOLED, local Postgres, neonctl all masked); required queries saved in clv-slices.json for a session with real DB access. Hermes correctly refused to escalate to NEON_API_TOKEN or unmask credentials per standing rule |
| L-10 | Free-provider live probes for the H-S map: 2 calls max per candidate, latency/shape/limits, rights-registry classified, no adapters no signups | hermes | DONE | 8c1c6143 on origin/hermes/l10-provider-probes, merged 5d358e90. Cleared, zero-cost, live-verified: nflverse (open-license CSV/JSON, ~280ms), Open-Meteo (open-license, ~550ms), Sleeper API (public, ~27ms). ESPN public API 403'd both calls (Akamai bot-block, was previously 200 — investigate before relying on it). TheSportsDB + MLB Stats API both respond 200 but are vendor_candidate/gated per registry — Hermes correctly did NOT mark them usable, needs registry promotion first. FFC-ADP DNS failure, likely transient |
| L-11 | Verify the DeepSeek-pasted affiliate/partnership-tooling repo list against GitHub ground truth before any adoption | hermes | DONE | 7817d56a on origin/hermes/l11-affiliate-tooling-verification, merged. 21 checked: only 3 real-and-matches (Refferq, Google Meridian, Dub); 4 do not exist at all (Income Generator Hub, SponsorFit, ClawMarketing/growth-os, xAmplify PRM, prathammahajan/affiliate-management-system — that's actually 5 names, table lists 5 under a "4" mislabel, not rechecked further); remaining ~13 real but exaggerated or mismatched vs the DeepSeek description. Confirms the original caution in docs/ops/edge/2026-08-19-affiliate-tooling-research-triage.md — treat as leads only |
| C-29 | C-15 fleet round 1 REJECTED at verification — 5 blockers, all 3 adversarial lenses refuted; implementation 9099ace2 exists in worktree wf_2645e948-ec2-5 and is NOT merged. (1) CORRECTNESS: STALE_CLOSE is unreachable in production — the SQL floor gte commenceTime-6h in closing-odds.ts:65-73 excludes stale batches BEFORE the deriver sees them, so findFirst returns null and you get NO_ODDS with closeAgeMs null; the exact measurement the preregistration depends on is systematically censored. (2) The shadow re-grade caps its window at 24h so the preregistered maxCloseAgeMs=Infinity cell is byte-identical to the 24h cell by construction — that sensitivity row is not evaluable and the closeAgeMs distribution is right-censored. (3) DOCTRINE: the commit ships a NEW public performance claim (decided-only beat-close rate + Wilson CI on the public /clv page AND unauthenticated /api/clv) gated only on "allowed", BEFORE the shadow re-grade that its OWN preregistration doc in the SAME commit says must gate it. (4) scripts/clv/shadow-regrade-census.ts has no DATABASE_URL check, no stub-mode guard and no empty-population guard — run without a live DB the stub client returns [] and it writes a complete, schema-stamped, preregistration-citing artifact full of zeros: a fabricated-evidence generator. (5) SUFFICIENCY: MIN_CLOSE_DISTINCT_BOOKS is pooled cross-market, counted at clv-capture.ts:179 before the per-market split at :203-215, so a batch with TOTALS from one book, H2H from another and SPREADS from a third passes a 3-book floor while every market has exactly one — proven by probe test | claude | OPEN | do NOT merge 9099ace2 as-is. Genuine wins to keep from round 1: both defects re-confirmed live on HEAD (clv-capture.ts:96 no staleness bound; settle-sport.ts:363 still take:80 — note the line is 363, not 321 as previously recorded), and a proven reproduction that HEAD accepts a batch captured 7 HOURS pre-kickoff as "the close" |
| C-30 | PREMISE CORRECTION to C-15/C-20 as previously written: commit 6f0353e1 does NOT contain the take:80 to take:240 change — that was authored by 8e2af6f1 (and independently by 7c1276f8); 6f0353e1 merely inherited 240 from its own ancestor. Consequence: 6f0353e1 is the tip of a 44-commit stale branch (Worldline/OTS/MCP/fantasy campaign) whose parent is NOT an ancestor of HEAD and CANNOT be taken. 8e2af6f1 by contrast is a clean single-commit candidate (parent 821d0ca3 already an ancestor, HEAD..8e2af6f1 = exactly 1 commit) | claude | DONE | c789fe40 lineage verified by the C-15 fleet. ALSO CRITICAL: cherry-picking 8e2af6f1 WHOLESALE would REGRESS HEAD — its M-F6 half conflicts because HEAD already carries an independently-derived equivalent (GSE-SEC-043, pick.updateMany scoped to result PENDING), and taking theirs would strip HEAD's newer bookDisagreementAtLock write-once capture. The landable slice is M-F7 only: clv-capture.ts + clv-capture.test.ts + settle-sport.ts take:240, 3 files, zero conflicts, typecheck 0, prediction-engine 203 files/2352 tests pass, ingestion-pipeline 17 files/197 tests pass |
| C-15 | CLV measurement integrity fix, three parts now scoped by the C-14 verdict: (1) pull in two pre-built unmerged fixes — 8e2af6f1 closing-snapshot staleness bound, 6f0353e1 take:80->take:240 book coverage; (2) lock-price provenance fix (real book quote at lock time vs model-derived, per L-9's 0/909-match finding); (3) re-grade the census under both and compare before any claim | claude | OPEN | verdict + concrete fix path landed in docs/ops/edge/2026-08-19-clv-forensics-verdict.md; this is the gate to any honest ESTABLISHED (>=52.4% CLV) claim; not dispatched tonight per cost-conscious mode, needs a real design pass |
| C-16 | Frontier research night: six verified-citation literature threads (anytime-valid/e-process + conformal e-prediction, calibration/CORP/Venn-Abers, pooling/extremization, market efficiency + CLV literature, independent models for MLB totals, Kelly/abstention decision layer) mapped onto the codebase as preregistration-compliant proposals, citation-audited and doctrine-audited, synthesized into a ranked dossier | claude | DONE | 66aee571 — docs/ops/edge/2026-08-19-research-frontier-dossier.md (72,704 chars). 11 agents, 6 literature threads, 17 engineering proposals; citation audit checked 90 citations across the surveys and 38 more against the repo — ZERO fabricated, ZERO doctrine violations, 4 proposals flagged for overstated framing only. Central correction to our own prior reading: holdout Brier 0.2556 sits ABOVE UNC 0.2499, so under the exact CORP decomposition DSC-MCB is about -0.0057 — NEGATIVE net skill versus the base rate, not merely zero resolution. Governing result: calibration-only methods (Venn-Abers/IVAP, temperature, Platt, beta, plain isotonic) are score-only monotone transforms — ranking-invariant, therefore provably CANNOT create resolution; only methods injecting new conditioning information can (IDR, multicalibration-as-boosting, an independent overdispersed totals model, decorrelation-from-market objectives). Top three extracted as C-20/C-21/C-22 |
| C-23 | Anytime-valid certification protocol v1: composite bet-specific null (H0,i: p*_i <= b_i = 1/D_i, NOT the fixed 0.524), mixture e-process over unknown edge, deterministic pre-placement inclusion rule, frozen append-only hash-chained ledger, per-model-version tracks, preregistered kill criteria | — | OPEN | Design + audit in docs/ops/edge/2026-08-19-deepseek-adversary-round2.md (7d425f47). Our current fixed-0.524 null is MISSPECIFIED and inflates certification: at -115 the true break-even is 0.5349, so a losing 0.530 reads as skill. Supermartingale property re-derived and verified. KNOWN GAP to close before build: the price-space null is E[delta] <= 0 on a CONTINUOUS quantity, and no e-process was supplied for it — needs a bounded-mean betting martingale (Waudby-Smith/Ramdas), not the binomial likelihood ratio. Existing repo crypto (proof/receipts, verification-spec, sealed-holdout, pedersen-opener) is a candidate substrate for the frozen ledger |
| C-20 | Grade TOTAL/SPREAD CLV in PRICE space, not points-only (the Bickel-Kim fix) — dossier rank 1, days of effort | — | OPEN | SCOPE EXPANDED + HARD CONSTRAINT ADDED 2026-08-19 (7d425f47): (a) capture BOTH sides' decimal odds + ms timestamp + book at BOTH lock and close; de-vig with Shin (already in repo), NOT proportional; (b) the null changes from a win-rate null to a price-space mean null E[delta]<=0, which needs the e-process gap in C-23 closed first; (c) **NEVER re-grade the historical census in place** — anytime-valid guarantees are void if E_n is recomputed over corrected history (certification probability can be driven arbitrarily high by re-grading until the path looks good). Fix forward only; corrected history becomes a separate, clearly-labelled audit stream that never feeds certification. Original dossier finding: our totals/spread CLV is computed on point-line movement alone while the actual over/under juice sits UNUSED in columns the schema already has. Bickel & Kim 2014 show MLB totals conclusions FLIP once real juice replaces an assumed price. Consequence: the 58.5% totals beat-close number is not evidence of anything, in either direction, until this lands. MUST be coordinated with C-15 (measurement integrity, fleet in flight) — same code path, and C-15's scope did not include this defect |
| L-12 | Compute the grouping-loss go/no-go gate on the 909-pick graded census | hermes | DONE | c789fe40 on origin/hermes/l12-grouping-loss, merged. Result is an HONEST BLOCKED, and the blocker is the most clarifying finding of the session: **the (score, outcome, features) triple does not exist because there IS no model probability.** outcome present (WIN/LOSS); features present but market-metadata-only (pre-lock odds, no game-state covariates — those live on Game/PickSignalSnapshot/OpeningLine and are not exported); model probability MISSING ENTIRELY. schema.prisma:602 documents PickProofReceipt.modelProb as "calibrated win prob 0-1 — null until one genuinely exists". Hermes refused to substitute confidence_pct (heuristic composite) or market_fair_prob (the MARKET's number, and populated on only ~561/1161 rows), and refused to invent numbers. Full estimator implemented and ready (equal-count bins 5/10/20, from-scratch k-means on logit-standardized features, permutation null) — it runs the moment a real model probability exists |
| C-28 | **The calibration number measures the market, not our model.** ECE 0.0044 was computed on confidence/100: compute-live-calibration-metrics.ts:119 ("p from confidence/100 provisional; internal eligibility only until publish policy"), calibration-regression-snapshot.ts:23, public-confidence.ts:51. Confidence is substantially a market-structure echo (scoring.ts:486-494), and murphy-res-definition.ts:48 ALREADY names the correct basis as "Independent modelProb / edge ranking (not confidence/100 market echo)". So the excellent calibration is largely the MARKET's calibration re-measured through our heuristic — which coherently explains the whole picture: near-perfect reliability AND near-zero resolution is exactly what you get when your score is the market. Decide the publish posture and either emit a genuine modelProb or restate the calibration claim to say precisely what it measures | — | OPEN | discovered via L-12's blocked gate 2026-08-19. IMPORTANT MITIGATION: no false claim has shipped — the code gates this as "provisional; internal eligibility only until publish policy", so the honesty machinery held. But every downstream conclusion resting on "our calibration is excellent" needs restating, and C-21/C-22/C-26 are all gated behind a real modelProb existing |
| C-21 | Grouping-loss lower bound within existing confidence buckets as the go/no-go gate before any multicalibration/IDR spend — dossier rank 2, days of effort | — | OPEN | BLOCKED IN PRACTICE by C-28: cannot compute until a genuine model probability exists (see L-12). Original scope: Answers the question the entire roadmap turns on, cheaply: is RES ~0 because the market is efficient wrt the features we have (=> stop, go find new data) or because the pipeline collapses real signal into one flat number (=> build)? Perez-Lebel/Le Morvan/Varoquaux 2023 (arXiv:2210.16315) gives a recoverable-resolution lower bound. One day of diagnostic before a week-plus model build. WARNING (7d425f47): do NOT implement the grouping-loss formula supplied by the external adversary — it gives within-bin OUTCOME variance, which for binary outcomes is ~p(1-p), near 0.25 always, and is useless. True grouping loss is the variance of the TRUE conditional probability within a score bin (expected within-bin variance of P(Y=1 given X), conditioned on the score S(X)) and requires the paper's partition/clustering lower-bound estimator over features within each bin. Its accompanying <0.01 / 0.01-0.05 / >0.05 threshold table is invented, not sourced. Citation VERIFIED 2026-08-19: ICLR 2023, arXiv:2210.16315, and the authors publish working estimator code at github.com/aperezlebel/beyond_calibration — USE THEIRS, do not reimplement from a description |
| C-22 | Independent MLB totals model on an overdispersed negative-binomial run distribution (not soccer-shaped Poisson), wired end-to-end — dossier rank 3, week+ | — | OPEN | Per the dossier the ONLY surveyed proposal that CREATES resolution rather than measuring/preserving/shrinking it, because it conditions on real game-to-game variation (park, weather, starter rest) that a collapsed confidence score structurally cannot carry — those covariates move tens of percent game to game where our score moves 0.17%. Targets the one market segment with any live signal. Gated behind C-20 and C-21. PRIOR UPDATE 2026-08-19 (downward, from verifying our own key citation): Bickel & Kim 2014 (Applied Financial Economics 24(18), doi:10.1080/09603107.2014.925052) — the very paper that justifies the price-space fix — did exactly that fix with real run-line and money-line prices and found LITTLE EVIDENCE the MLB over-under market is inefficient. Our angle (weather-park interaction, umpire zone, bullpen fatigue, and pricing the full run DISTRIBUTION incl. alternates rather than the mean) is not what they tested, so this is not fatal — but we are hunting a field a competent study already swept. Carry a lower prior into C-22 and require the prospective test, not the backtest |
| F-7 | **URGENT, FOUNDER HANDS: redeploy production.** Live deployment SHA is b71f7e28, HEAD is 112 commits ahead (both verified independently 2026-08-19). At the DEPLOYED SHA three surfaces serve premium content to ANONYMOUS visitors: /board (state.ts renders market selection and rankingP with no viewer check), /intelligence/engines (anonymous WebFetch returned full analytics tables verbatim, no gate panel), /observatory (get-slate-twin called with no entitlements, loader has zero occurrences of canSeeConfidence/canSeePremiumPicks). HEAD already gates ALL THREE (state.ts:112 GSE-SEC-026, engines page 12-13/159-178, observatory 62-63 + get-slate-twin.ts:109,214-225). So the fix is ALREADY MERGED and merely UNDEPLOYED — this is a redeploy, NOT a code change and NOT a gate/env flip | founder | DONE | Pro's entire headline value (full board, selections, confidence, model probabilities) is currently free on three public URLs, and it is a live CLAUDE.md rule 3 violation (no frontend-only paywalls). Smoke-test the money path after deploying: 112 commits land at once. docs/ops/edge/2026-08-19-gse-launch-optimization-plan.md — RESOLVED 2026-08-19: production deployed at 90aa7652 (Vercel Ready, www alias live, verified via public-surface-truth). Root causes: strict-allow-scripts npm blocker (cdd91b58) plus fsevents deny (90aa7652, Codex, founder-authorized) |
| C-31 | Ship the five verified launch fixes from the optimization fleet, in rank order: (2) free teaser returns 1 pick not 2 — api/picks/route.ts:128 applies take BEFORE the selective-publish filter at :135-142, so survivors can only be fewer than the cap; over-fetch a bounded pool then slice AFTER filter+sort, server-side only. (3) /faq:89 and :43 answer "Every pick, free" and "Free gets every pick" — FALSE (enforced limit is 2, live renders 1) and published as FAQPage JSON-LD; rewrite from the entitlement constant, add a test asserting no Free answer contains "every pick"/"all of them". (4) /pricing renders NO RiskDisclosure while 29 other page.tsx files do — add it, but do NOT pass includePastPerformance (implies a track record) and do NOT hardcode 21+ (terms:64-72 says legal age in your jurisdiction). (5) checkout hard-400s on browsers without crypto.randomUUID — subscribe-button.tsx:65-69 mints a non-UUID fallback, CLIENT_INTENT_ID_RE is UUID-only, route.ts:74-82 rejects; server already has a token-less branch at checkout-attempt.ts:417-421, so return null and omit the field. Do NOT widen the server regex. (6) daily-slate counts have NO date bound (route.ts:78-85) while /api/picks does (route.ts:103-106), so /picks shows four irreconcilable numbers on one screen; extract one shared predicate | claude | DONE | bd60fc71: all four shipped (fix 5 was C-39/0c190bfe earlier) — 69 raw findings, 10 falsified in adversarial verification, these 5 survived with file:line evidence. docs/ops/edge/2026-08-19-gse-launch-optimization-plan.md |
| C-32 | PERMANENT DO-NOT-DO list from the launch audit — proposals killed in verification, recorded so no future session re-raises them: (a) NEVER write universal "every pick is sealed with a receipt before kickoff" copy — three separate audits independently proposed it and it is FALSE as written, since process-sport.ts:846-853 mints a receipt only when marketFairProb is in (0,1) AND entryOdds is non-zero, and mint failure is non-fatal at :888-894. (b) NEVER publish a win rate, ROI, hit rate or beat-close rate — holdout Brier 0.2556 sits ABOVE base-rate uncertainty 0.2499; this includes wiring daily-slate recentRecord to real data without sample size and a Wilson band. (c) NEVER re-select which pick is free on the theory that free picks are the weak ones — the split is confidence >= PREMIUM_CONFIDENCE_THRESHOLD and with resolution near zero there is NO evidence high-confidence picks win more; that would institutionalise an unproven discrimination claim on the highest-traffic free surface | — | OPEN | keep this row permanently; it exists to stop good-sounding proposals being re-litigated |
| F-6 | FOUNDER DECISION, RESOLVED 2026-08-20: no sportsbook or DFS affiliate revenue, permanently — option (b) from the original framing, scoped precisely to the conflicted category rather than all partnerships. The competitive-intel corpus's red-team argument (handoff section 1/8, master dossier 4.5: "the moment GSE books meaningful affiliate revenue it becomes the thing it attacks") applies to sportsbook/DFS specifically, where the operator profits when the user loses money betting; it does not extend to non-gambling partner categories (creator tools, sports-data APIs, cloud/AI tools, local sponsors), which the codebase already separates by risk class (`HIGH_RISK_PARTNER_CATEGORIES = [sportsbook, dfs]` in lib/revenue/partner-types.ts) and which carry no such conflict | founder | DONE | 45d3f1f7 — pledge published on /how-we-make-money and /terms sec.7 ("no sportsbook or DFS affiliate links, permanently"); C-17 and R-7 cancelled below; general /partners disclosed-partner program (zero live partners today) explicitly continues, unaffected |
| C-26 | Audit the Kelly staking chain for market-echo contamination: kelly.ts:154-155 gates on confidence AND edgeScore — the AND with an edge gate means it does not fire on chalk (the corpus's stated fear) but it DOES discard edge-positive picks purely for low confidence, which under calibrated probabilities is where variance-adjusted value lives. More concerning, kelly.ts:181 derives a "bounded +5% edge proxy" from edgeScore and line 70 concedes the probability is an ESTIMATE derived from edgeScore — so given confidence is substantially a market-structure echo, stakes may be sized off market structure rather than independent edge | — | OPEN | verified in code 2026-08-19; doctrine per competitive-intel handoff is fire on calibrated edge e = p - q, never on confidence kappa. Gated behind C-21 (is there orthogonal information at all) — no point retuning a staking rule if there is no edge to size |
| C-27 | Quarantine the five stale files still carrying the superseded "70 percent win-rate north star" so no agent or copy generator reads them as current: gse-competitive-intel README line 18, _expansion-targets line 6, rotowire-engine-teardown line 256, and two fantasyguru docs. Superseded 2026-06-30 by "prove CLV edge, do not claim it until it clears breakeven" — but all five were written AFTER the walk-back and still carry it. Also flag _competitor-mistakes-lessons line 18 which scripts the literal marketing line "We get you a proven edge" | — | OPEN | that repo is read-only to this session (public clone, no push). Remediation is either a founder-side commit there or a local do-not-read note; the risk is real because these are exactly the files an agent asked to "write GSE marketing copy" would find first |
| C-24 | Claude Code operating-cost and reliability config: CLAUDE_CODE_SUBAGENT_MODEL for mechanical fleets, maxTurns cap on every subagent frontmatter, effort per unit, CLAUDE_CODE_RESUME_INTERRUPTED_TURN=1 + CLAUDE_ENABLE_STREAM_WATCHDOG=1 (directly fixes the 5-hour silent fleet stall we hit tonight), PermissionDenied hook returning retry:true (fixes the classifier denials that blocked two commands tonight), and an audit/prune of the ~40 always-loaded skill descriptions which are a permanent per-turn context tax | — | OPEN | sourced from /workspace/beexly/claude-code-best-practice; NOT applied mid-flight with ~15 agents running — apply when the board is quiet. NOTE: autoMode is deliberately NOT read from project .claude/settings.json (repo-injection guard), so permission fixes must land in user/local settings, which is a founder action not an agent one |
| C-25 | Ledger guard hardening round 2 — two holes our SHA rule structurally cannot catch: (1) INVERSE COMPLETENESS: we verify every claimed DONE has a resolvable SHA, but nothing verifies the reverse, that no row exists which no evidence supports; (2) NO ESCALATION SLA: an item can sit OPEN/CLAIMED forever. Add a rule that after N consecutive checks a stale row must resolve, be re-owned, or be deleted. Also adopt status-with-mandatory-reason (COMPLETE/INVALID/ON HOLD) so an agent has a legitimate slot for "this finding was wrong" instead of a fake DONE | claude | DONE | f4d6b2c97: ON HOLD status with mandatory owner+reason, SLA warnings (CLAIMED-no-evidence, OPEN-evidence-no-owner) on every CLI run, 36/36 negative tests; guard caught 3 real defects in this ledger (missing SHAs C-68/C-69, pipe-parse break C-67); background: claude-code-best-practice verification-checklist.md; SHIPPED on this branch: ON HOLD status with mandatory owner+reason, SLA warnings (CLAIMED-no-evidence, OPEN-evidence-no-owner) surfaced on every CLI run, negative-tested 36/36 in agent-ledger.test.ts, guard caught 3 real defects in this ledger on first run (missing SHAs C-68/C-69, pipe-parse break C-67); original: claude-code-best-practice verification-checklist.md rules 1F/10B/10C, whose own origin note records an item sitting ON HOLD for 6 runs because no rule checked the reverse direction. Cheap to add, closes two real gaps in scripts/ops/check-agent-ledger.mjs |
| C-17 | Design (not implement) The Odds API affiliate widget integration: founder added THE_ODDS_WIDGET_KEY to Vercel; distinct from THE_ODDS_API_KEY (prediction source-of-truth, not for sale) — the widget is bookmaker odds + our affiliate link, a monetization surface. Design against the existing apps/web/lib/revenue/ partner/offer/disclosure model and the sealed affiliate-structural-separation + partner-offer-compliance-scan guards, without editing either guard | claude | DONE | 9b8fa121 (docs/ops/edge/2026-08-19-odds-widget-integration-design.md). Two real blockers found: (1) site CSP script-src (next.config.mjs:103) allow-lists only Clarity+Stripe, blocks any vendor widget script until the real host is added; (2) evaluateOfferEligibility fails closed on unknown userState for high-risk offers and no visitor-state signal exists in the repo — widget cannot legally render for anonymous traffic without one. Category must be sportsbook not sports_data (triggers the full compliance bundle). 8-item founder-review list + 7-item safe-to-PR list (~half day) in the doc — CANCELLED 2026-08-20 by F-6: sportsbook affiliate widget is in the killed category, permanently |
| C-19 | Cosmetic: rename the misleading contentPlanPrimary="cerebras_free" label — it fires whenever ANY free lane is enabled (Cerebras OR the OpenRouter secondary), not only when Cerebras is actually used; already caused one false-alarm re-investigation during R-6 verification | hermes | DONE | 4df9e6b9e — renamed cerebras_free → content_free in JynxLane type, credit pool, and all tests (6 files, 30 tests pass, typecheck 0) |
| C-18 | Design S2S (server-to-server) postback/conversion tracking for revenue partners: apps/web/lib/revenue/ stops at "offer disclosed," nothing records whether a click-through actually converted (signup/deposit) — applies to every affiliate partner, not just the odds widget | — | OPEN | surfaced 2026-08-19 while researching C-17; needs its own design pass (webhook endpoint, HMAC/shared-secret verification, idempotency, which network(s) support it) |
| R-7 | Start (not complete) sportsbook affiliate program applications at every major US-regulated book (DraftKings, FanDuel, BetMGM, Caesars, ESPN Bet, Fanatics, bet365, Hard Rock Bet, Bally Bet): fill all public/non-sensitive fields, hard-stop before SSN/EIN/banking/payout fields or any T&C-acceptance click, produce a per-book punch-list of what remains | browser | CANCELLED | founder-directed 2026-08-19 night; boundary is deliberate — no agent submits tax/banking info or accepts a binding affiliate contract unsupervised; feeds C-17 (widget design) and the RevenuePartner/disclosure model already in apps/web/lib/revenue/ — CANCELLED 2026-08-20 by F-6: sportsbook affiliate applications are in the killed category, permanently |
| C-33 | DeepSeek round-5 audit: ADOPT N_eff arithmetic and certification calendar (NFL cannot certify solo in a season; MLB ~37 weeks; with ~6 weeks left in MLB season, certification is a 2027 event on every sport — launch and certification are decoupled, launch rides the transparency floor), preseason exclusion rule, key-number label handling, single-track disclosure language. Five errors rejected: regime-mixing "breaks the e-process" mislabelled PROVEN (costs power, not validity); empirical-Bernstein bet formula unstable across rounds (implement from WSR paper only); rank table mis-sorted for the THIRD straight round (BookDisagreementAtLock 0.200 ranked below 0.1875); alpha-table effort column decorative (all seven weights recomputed, divisor is 1.0 everywhere) with invented per-sport N_eff; NFL 3-markets assumption unverified (1H totals may not be ingested). Unification: opener-vs-close is the intercept-only special case of close-pred — one program, preregistered baseline, not two tracks | claude | OPEN | doc: docs/ops/edge/2026-08-19-deepseek-round5-audit.md; adoption of build steps gated on L-14 census |
| L-14 | Line-archive label census v2 (read-only, hermes_ro): per sport AND per market type (spread, total, 1H total, moneyline) — clean-label counts under C-14 contamination criteria, snapshot cadence, earliest-snapshot depth before start, NFL preseason vs regular-season split. Gates the close-pred backtest, the preregistration date, and the E-5 NFL volume question | hermes | DONE | abf0e3f4 on origin/hermes/l14-label-census, merged here; headline: MLB 241 clean closes per full-game market, NFL 0 clean and 0 preseason odds, first-half totals not ingested, go is no |
| C-34 | F-7 blocker found and fixed: production deploy has been failing on EVERY commit since `.npmrc`'s `strict-allow-scripts=true` (added 2026-08-16, real npm ≥11.10 feature, verified against npm docs) landed in this branch's lineage — it hard-fails any dependency install script (ESTRICTALLOWSCRIPTS) unless approved in package.json's `allowScripts` map, which did not exist. Vercel's build npm enforces it; this sandbox's npm 10.9.7 silently no-ops it, which is why local installs looked fine and masked the bug. `.npmrc` is not even in `b71f7e28`'s ancestry — the last successful prod deploy predates this control; every build since has failed at `npm install`, not from F-7's 112-commit backlog itself | claude | DONE | cdd91b58: approved all 7 real pending packages via `npm approve-scripts` (verified list via `npx npm@11 approve-scripts --allow-scripts-pending`): prisma@5.22.0, unrs-resolver@1.11.1, @prisma/client@5.22.0, @prisma/engines@5.22.0, @sentry/cli@2.58.6, esbuild@0.21.5, esbuild@0.28.1. Reproduced Vercel's exact `npm install --include=dev` with real npm 11 — exit 0, Prisma generates cleanly. package-lock.json touched only by npm's own 1-line fsevents dev-flag normalization (verified trivial, no version/integrity change) — kept, not agent-authored content |
| C-35 | Root-caused the real reason NFL has zero odds — NOT cadence, NOT a dead cron, NOT the 112-commit deploy lag (the three theories in play). SEASON_WINDOWS in packages/data-ingestion/src/config.ts gated americanfootball_nfl to Sep-Feb, so getInSeasonSports() filtered NFL out of every 15-min refresh for all of August, the weeks Week 1 lines are posted and preseason runs. Explains L-14's "last snapshot 2026-06-17, 0 clean closes, 84 future games with no odds" exactly. Widened to Aug-Feb; spring stays closed so added credit spend is confined to August | claude | DONE | 1454d8c3 — 6 regression tests in season-windows.test.ts (August in-season, off-season still closed, year-end wrap, backfill override); 0 type errors, 287 data-ingestion + 197 ingestion-pipeline tests pass |
| C-36 | Ingest NFL preseason odds (implemented as H-F3 preseason ingestion) | hermes | DONE | 8731a472 (H-F3, verified live in DOD-1 smoke) |
| C-37 | LINE_ARCHIVE_ENABLED is OFF, which is why odds_line_snapshots has 0 rows — deliberate, not broken (line-archive.ts:143 "Default OFF — founder flips it"). CORRECTION to my first version of this row: I wrote that line history is therefore "lost permanently". FALSE. The `odds` table is APPEND-ONLY — no unique constraint, id is a cuid, every refresh cycle INSERTs, 1,368,288 rows across 717 MLB games is roughly 1,900 rows per game. Raw price history is being preserved right now and nothing overwrites it. What the archive flag actually adds is the structured per-side snapshot with OPEN/INTERIM/CLOSE phase tagging, which is what makes close identification reliable rather than reconstructed. Real value, but not a data-loss emergency, and DeepSeek round-6 action 2 ("back up the live odds table now, it may already be disappearing") rests on the same false premise | claude | OPEN | schema.prisma:409-438 Odds model — indexes only, zero unique constraints |
| F-8 | FOUNDER: set LINE_ARCHIVE_ENABLED=true in Vercel. Corrected justification — this is NOT about preventing data loss (the odds table is append-only and is preserving raw prices already, see C-37). It is about getting phase-tagged OPEN/INTERIM/CLOSE snapshots so closes are identified at capture time instead of reconstructed after the fact, which is exactly the contamination class that ranked #1 in the round-3 failure table. Worth doing, not an emergency, and it does NOT block the feasibility tests — those run on the existing 1.37M append-only rows tonight | founder | OPEN | gate-flip is owner-only by standing rule; code path shipped and test-covered (H-D, line-archive.ts) |
| C-38 | DeepSeek round-6 audit, WITH A CORRECTION OF MY OWN FALSE ACCUSATION. I flagged its "1,368,288 live odds rows" as fabricated. It was not. That figure is line 3 of Hermes RESULTS.md, along with 717 MLB games and 569 eligible — the founder had pasted the full census to it. I wrote the accusation before reading the file I had just merged, which is the same failure mode I spent the night catching in others. Standing rule added: never call a number fabricated until it is grepped against every artifact in hand. Its two REAL errors stand: preseason key backwards (C-36) and mis-diagnosing the founder-gated archive flag (C-37). ADOPTED and materially good: each clean-close game yields MULTIPLE entry labels (24h/12h/6h/3h/1h), so the usable label count is far above 241 while the independent-game count for grouped CV stays 241; Ridge with grouped-by-game CV, never gradient boosting, at this sample size | claude | OPEN | correction verified by grep of docs/ops/hermes/l14-label-census/RESULTS.md line 3 |
| L-15 | MLB close-prediction FEASIBILITY tests on the existing append-only odds corpus — the fastest possible go/no-go, runs tonight on data already in hand, no new ingestion and no waiting. Tests 0-4 are pure SQL over the 1.37M odds rows (mean CLV by entry window; dispersion predicts absolute movement; deviation-from-median predicts direction; short-horizon momentum; cross-market spread-leads-total lead-lag). Test 5 is Ridge with grouped-by-game CV. Decision rule fixed BEFORE seeing results: grouped-CV Spearman r >= 0.15 promising, r < 0.10 kill MLB close-pred | hermes | DONE | 5f524a98 on origin/hermes/l15-close-pred-feasibility, merged here; RESULTS.md, headline: kill MLB close-pred; totals Ridge r=0.49 is p_entry artifact given corr(entry, close)=0.40; H2H r=0.09 R2 negative |
| C-39 | C-31 fix 5 SHIPPED — checkout was hard-400ing on every browser without crypto.randomUUID. A prior pass stopped the TypeError but replaced it with a non-UUID `ci_` fallback that the server's UUID-only CLIENT_INTENT_ID_RE rejected, so Safari under 15.4, older Chrome, and any insecure context could not subscribe at all and were shown a network error blaming themselves. Returns null now and omits the field; the server's existing token-less branch mints the durable intent. Server regex deliberately NOT widened | claude | DONE | 0c190bfe — 5 rewritten tests that previously pinned the bug now assert null plus a never-returns-a-rejectable-string guard; checkout suite 72 pass; apps/web typecheck clean |
| L-16 | Book-level microstructure tests, the two mechanisms L-15 did NOT test and which cannot suffer the corr(X, Y-X) artifact that voided the totals result. A: per-book persistent shading versus the cross-book median close, clustered by game. B: cross-book lead-lag or copy hierarchy at 19-min cadence. Both are book-selection rules rather than game-outcome prediction, so they can pay even though the market-level close is unpredictable | hermes | DONE | f2b485d3 on origin/hermes/l16-book-microstructure, merged here; branch hermes/l16-book-microstructure, RESULTS.md, headline: both dead; totals max clustered t 1.48; 0 lead-lag pairs; minute-cadence MLB totals closed |
| C-40 | L-16 verdict absorbed: BOTH book-level mechanisms DEAD, minute-cadence MLB totals is now closed. A (per-book shade): 11 books, 863-947 labels each, largest absolute t is betus 1.48 — nobody clears the pre-registered bar. B (lead-lag): 110 ordered pairs, zero raw leads, zero after Benjamini-Hochberg; largest rho-1 is bovada to draftkings 0.075 with a positive reverse, so not one-way. Hermes correctly REFUSED a tempting positive: anti-shade CLV showed +1.2 to +1.8pp on the deviation subset, and it called that fade-the-outlier noise rather than persistent shade because the first kill clause had already fired. Do not build a copier or a fade-the-soft screen on this corpus | claude | DONE | f2b485d3; RESULTS.md at docs/ops/hermes/l16-book-microstructure/ |
| C-41 | ONE exploratory survivor from L-16, explicitly NOT a claim: fanatics MONEYLINE shade at t=-2.18, +0.8pp CLV over 163 bets, which clears both the t and the 150-bet bars. It is quarantined because the pre-registered primary market was TOTALS — this is a secondary market surfaced while testing something else, which is precisely the multiple-comparisons trap that killed the original 58.5pct totals signal. It may only be pursued as a PROSPECTIVE pre-registered track on moneyline with its own frozen inclusion rule, never as a retrospective claim on this corpus | — | OPEN | L-16 RESULTS.md moneyline paragraph; three other absolute-t-above-2 moneyline books fall below the 150-bet bar |
| L-17 | FINAL edge experiment on this corpus, pre-registered before results: six path-geometry features (pre-entry-only realized variation, increment autocorrelation, sign-change rate, dispersion decay half-life, cross-sectional skew, staleness fraction) into Ridge with grouped-by-game CV predicting realized CLV on the 241 clean-close games. r at or above 0.15 continue; r below 0.10 the edge program STOPS on this corpus, no appeal. Each feature must pass its decimation/jitter separation test before entering the model (round-7 Part 1 artifacts) | hermes | DONE | e2986412 on origin/hermes/l17-l18-geometry-metrics, merged here; RESULTS.md, headline: STOP edge program; totals grouped-CV r=0.091 R2 negative |
| L-18 | PRODUCT metrics on the 241-game corpus, needed regardless of L-17 outcome: BPQI (per-book mean signed deviation from cross-book median close, pre-close snapshots only, clustered by game) and BURS (per-book fraction of polls with a changed quote). Output feeds the public metrics surface. Honest-claim / forbidden-claim pairs fixed in round-7 Part 3: state measured deviations, never "exploitable", never "fade targets" | hermes | DONE | e2986412 on origin/hermes/l17-l18-geometry-metrics, merged here; RESULTS.md, headline: 11-book BPQI/BURS table; totals max clustered t fanatics -2.05; measured deviations only |
| C-42 | Master plan committed: two tracks (R revenue-first, E one final pre-registered experiment then silence), agent-by-agent assignments, do-not-do list, five sign-off criteria. The alignment artifact all four agents read via git since they share no other memory | claude | DONE | docs/ops/MASTER-PLAN-2026-08-19.md landed in 286b1f3e |
| C-43 | Business plan v1 committed: GSE as "the proof layer for sports prediction" — competitors' product classes (picks, tools, props, fantasy, odds intel) on a proof spine none can bolt on afterward. Moat stated honestly: the un-buyable start date of the append-only archive and published record, the zero-affiliate incentive structure, and the L-15/L-16 negative knowledge; trademarks and trade-secret handling for the rest; no pretense that publishing-calibration is secret. GSE TruthMetrics family named (BookGrade, PulseScore, Consensus Clock, Line DNA, Glass Ledger). Revenue math at near-zero cost base; NFL Week 1 is the launch window; F-6 recommended zero-affiliate permanently; 30/60/90 mapped to agents; founder-only action list of five | claude | DONE | docs/business/GSE-BUSINESS-PLAN-2026.md landed in 7587c5f3 |
| C-44 | TRACK E CLOSED — the pre-registered L-17 kill fired: totals grouped-CV r=0.091 (rule: below 0.10 stop, no appeal), R² negative, n=203 games. All six path-geometry features survived decimation (real path shape, not cadence noise) and still do not predict realized CLV — the strongest possible form of the negative: the geometry is real and uninformative. Spreads r=0.209 with NEGATIVE R² is quarantined same as C-41 (secondary market, zero explained variance). The edge program ends on this corpus. No second experiment, no booster, no fleet. Remaining research lives only as: (a) C-41 prospective moneyline track if ever activated with frozen rules, (b) forward NFL archive accumulating for a future, separately pre-registered program. All effort now on Track R | claude | DONE | e2986412 (L-17 RESULTS.md); MASTER-PLAN Track E clause executed as written |
| B-1 | /kill-ledger public page | hermes | DONE | pushed `a43cdaa` on `hermes/b1-kill-ledger` |
| B-2+B-3 | BookGrade+PulseScore v1 + metric-honesty glossary component | hermes | DONE | pushed `5e589d41` on `hermes/b2-bookgrade-pulsescore` |
| B-4 | Per-pick Verify button UI on public pick cards | hermes | DONE | 935b59e1 on origin/hermes/b4-verify-button, merged here; client component reveals contentHash, committed timestamp, /verify link; no new crypto |
| B-QUEUE | Hermes autonomous build queue v1 (B-1..B-4 shipped as individual DONE rows; B-5 pledge page folded into B-QUEUE-2) | hermes | CANCELLED | B-1 a43cdaa, B-2+B-3 5e589d41, B-4 935b59e1; queue doc superseded by BUILD-QUEUE-2026-08-20B.md (B-QUEUE-2) |
| C-45 | AGENTS.md was stale and actively misdirecting agents: it pointed autonomous sessions at handoff/LEDGER.md and docs/ops/hermes/CONTINUOUS.md, both frozen since 2026-08-17/18 (a prior session, branch claude/fable-5-ultracode-plan-ptru4e) and containing none of tonight's work. This is the most likely cause of the fresh Hermes session that could not find the build queue and had no origin remote -- it was bootstrapping from stale instructions. Repointed THE LOOP at docs/ops/AGENT_LEDGER.md (the live multi-agent ledger) and the latest docs/ops/hermes/BUILD-QUEUE-*.md; softened the absolute never-push law, which the founder had already explicitly overridden tonight | claude | DONE | 5fe69ad5 |
| C-46 | Grok "self-regularizing residual-information e-process" audited: algorithmic core is real and correctly stated (Liu-West 2001 kernel, cubature Kalman, RBPF, negative-binomial hierarchy — converges with our own adopted C-22 design); REJECTED its novelty claim (capital-modulated lambda is a predictable betting strategy the existing anytime-valid theory already licenses — GRAPA-family adaptive bets are the published cousins), its residual-information gate (sequential conditional MI at a few hundred binary outcomes is estimator noise, unspecified estimator), its step-1 run on the closed 241-game corpus (Track E closed, C-44, and its own power math prices that run at zero), and its sandbox capital-896 as evidence (synthetic planted edge, smoke test only). ADOPTED as the candidate engine for the one open door — the future separately pre-registered prospective program — via synthetic-first validation with a mandatory null test | claude | DONE | b1416b92 (docs/ops/edge/2026-08-20-grok-stack-audit.md) |
| B-QUEUE-2 | Hermes build queue 2 issued: B-6a/b/c Glass Ledger made true (chain append wiring behind LEDGER_CHAIN_ENABLED default OFF, public chain-export endpoint, real loadLedgerView), B-7 Consensus Clock + Line DNA libraries against fixtures (pages data-gated until the phase-tagged archive holds 7 days), R-9 Grok engine synthetic-only with a hard never-touch-real-data rule and an alpha-level null-test acceptance bar | hermes | OPEN | docs/ops/hermes/BUILD-QUEUE-2026-08-20B.md in this commit; launch items strictly before the research tail |
| B-7 | Consensus Clock (dispersion half-life fit) + Line DNA (per-game path summary) libraries against synthetic fixtures | hermes | DONE | 1bacc671 on hermes/b7-consensus-clock-line-dna; libs + 26 tests vs fixtures only; pages unshipped |
| C-47 | Grok round-2 absorbed before founder sign-off: implementation depth adopted into R-9 (Laplace-for-NB conditional, Liu-West ordering, house-filter conventions from the verified-in-repo team-strength-filter.ts — 42KB shadow bootstrap PF that R-9 now extends instead of greenfielding); DML causal prototype adopted as R-10, shadow-only, one treatment (QB out), time-aware cross-fitting, overlap/placebo/sensitivity mandatory, compared against existing TeamIntervention magnitudes, never a public claim. Grok sandbox artifacts remain spec-only; round-1 rejections stand | claude | DONE | 7534bbf1 |
| B-6a | Glass Ledger chain-append wiring | claude | BLOCKED | Hermes correctly stopped instead of looping (AGENTS.md two-attempts rule) on a process-sport.ts anchor mismatch, but that was not the real bug. The store it built (ledger-store.ts, origin/hermes/b6a-chain-append 7f3822c4, NOT merged) is in-memory-plus-optional-JSON-file, and process-sport.ts runs inside apps/web/app/api/cron/refresh-odds/route.ts, a Vercel serverless function, so module state does not survive a cold start. Flipping LEDGER_CHAIN_ENABLED would have shipped a chain that silently restarts from GENESIS_HASH on nearly every invocation while presenting as continuous: a fake audit trail on the platform whose entire identity is a real one. Root cause and the exact proposed Postgres schema, matching the existing PickProofReceipt/SlateCommitment DB-adapter pattern the file's own header already specified, are in docs/ops/edge/2026-08-20-ledger-chain-durability-proposal.md. Requires a new Prisma migration, a sealed path, founder review required before any code lands |
| F-9 | FOUNDER: review and approve (or amend) the LedgerChainEntry Prisma model proposed for the Glass Ledger's durable hash chain | founder | OPEN | docs/ops/edge/2026-08-20-ledger-chain-durability-proposal.md; blocks B-6a/b/c only, nothing else in the queue |
| R-9 | Grok-derived NB-RBPF engine, synthetic-only, house-filter conventions, null-test acceptance | hermes | DONE | cc939ea3 on hermes/r9-grok-engine; null 0/200 capital>20; planted median max 2.1385 vs open-loop 1.0000; RESULTS.md; shadow only |
| R-10 | DML causal prototype, QB out/limited, shadow-only, time-aware cross-fitting | hermes | DONE | 5436aa07 on hermes/r10-dml-qb-out; synthetic panel (no nflverse dump in workspace); ATT -0.0111 CI includes 0; placebo contains 0; RESULTS-dml.md |
| F-10 | FOUNDER OVERRIDE, recorded honestly: the CEO-agent charter (uploaded 2026-08-20) orders ONE real-data MVE on the 241-game corpus — a fundamentals-vs-market outcome model with a fractional e-process — despite C-44's no-appeal closure. The closure was ours; the gate is his. Binding, pre-registered outcome: capital at or below 2 ends the edge program permanently with a published Kill Ledger entry; above 20 produces a prospective pre-registration DRAFT for founder signature; anything between publishes as did-not-survive and closes the program identically. One cycle, no reruns, no tuning after seeing the path, primary window 6-3h with one bet per game | founder | DONE | f986dfdb (FINAL-RUN doc + charter adoption); executed by Hermes as H-F5 |
| Q-FINAL | FINAL RUN H-F queue (closed by C-61: all seven H-F tasks merged and verified; superseding run doc FINAL-RUN-2026-08-20.md) | claude | CANCELLED | C-61 (2e016327) closed this queue DONE; row retired to stop agents re-running it |
| C-48 | DeepSeek MVE pre-run statistics audit VERIFIED AND ADOPTED into H-F5 as a binding STATISTICAL RULING: the fractional e-process E_t = 1 + 0.3(Yq/m + (1-Y)(1-q)/(1-m) - 1) is a fair martingale under the POINT null (market probability exactly correct) but NOT a valid e-process under the composite null p<=m (verified: for q<m the increment's conditional expectation exceeds 1 at small true p). Bound consequences: point null is THE pre-registered null; result language never says "positive expected value" (vig is outside this test); hyperparameters frozen-or-online-only; entry prices held to the close-quality cleanliness bar with exclusions counted; window 6-3h and lambda 0.3 fixed pre-result with no alternates computed even diagnostically; any >20 prospective track certifies against the vig-inclusive composite null (b_i = 1/D_i) instead. DeepSeek's pre-drafted kill entry and preregistration template stand as the outcome-doc templates | claude | DONE | 3f565cab (FINAL-RUN H-F5 ruling block) |
| C-49 | Grok Build 4.6 integrated as a governed grunt seat: .grok/config.toml ([permission] allow/deny mirroring session law — no main pushes, no force-push, no rm -rf, no prisma migrate/db push, no install-scripts approve, sealed paths + .env unreadable/uneditable) and .grok/sandbox.toml ([profiles.gse] extends "workspace", denies .env/.pem/.ssh). Schema verified against the actual xai-org/grok-build source (Apache-2.0, cloned read-only): [permission] allow/deny rules, [mcp_servers]/[plugins] tables, and sandbox profile extends="workspace" all confirmed in crates/codegen/xai-grok-config and xai-grok-sandbox. Grok Build reads CLAUDE.md/AGENTS.md natively, so the full rule system binds it with zero duplication. Pricing recorded honestly: the client has free included usage (2x-first-week promo dated Aug 12, expiring ~now); the underlying API is paid — Grok Build is NOT unconditionally free | claude | DONE | 3f565cab (.grok/config.toml, .grok/sandbox.toml) |
| C-50 | MVE STATISTICAL RULING v2, superseding v1 BEFORE any computation (pre-registration still amendable; v1 symmetric point-null retired unrun). DeepSeek round-2 correction VERIFIED: the asymmetric fractional increment 1 + 0.3(Y q/m + (1-Y)(1-q) - 1) has conditional expectation linear in true p, <= 1 at both p=0 and p=m, hence a valid supermartingale under the composite null p <= m, with increments >= 0.7. Claude's amendment: DeepSeek's form was over-side-only — structurally blind to under-side edges and losing on them in expectation, fatal in a one-shot final experiment — so the pre-registered primary is SIDE-ADAPTIVE (increment oriented to the predictably-chosen bet side; one process, no multiplicity; founder may veto before the run). Adopted operational rules: entry book-quoted <= 15 min old from >= 3 books with exclusions counted; checkpoints every 50 from n=50; certification E >= 20 at a scheduled checkpoint; kill E <= 0.10 at any checkpoint; early abort < 0.01 after 50. Prospective-track template frozen with disclosure language at docs/ops/edge/2026-08-20-mve-prereg-v2.md; queued for whichever agent holds the build seat at H-F5 | claude | DONE | 2cad335f |
| C-51 | MVE pre-registration FINALIZED before first computation: DeepSeek round-3 review returned PROVEN on Claude's side-adaptive amendment ("a predictable side-selection rule combined with the asymmetric increment yields a nonnegative supermartingale under the composite null") and the side-selection rule is now frozen deterministic — q>m bet OVER, q<=m bet UNDER (ties UNDER), under bets use complements — with disclosure language adding side selection to the frozen list. FINAL-RUN verifier lane rewritten as a seat-swap rule (builder never verifies own work; H-F5 audit stays cross-model: DeepSeek statistics + Hermes code). FOUNDER DIRECTIVE 2026-08-20: Grok Build is the default seat for ALL grunt work while its included usage lasts ("use grok for everything"), Hermes fallback; Grok must update the ledger per task like every agent. Ops fix shipped with it: sandbox deny is fs-level and inherited by child processes, so the env file was removed from the sandbox deny (node scripts need runtime env for DB; model-level Read/Edit denies stand) and node/npx-tsx under scripts/ allowed for the MVE runner | claude | DONE | 9078f838 |
| C-52 | BUILD-BLOCKING BUG, real and verified before fix (grepped the file for all usages, ran eslint and tsc directly, read the failing rule's config): apps/web/app/kill-ledger/page.tsx line 127 declared EXPECTED_EVIDENCE_PATHS and never referenced it anywhere in the file. @typescript-eslint/no-unused-vars is "error" severity in .eslintrc.json and apps/web/next.config has eslint.ignoreDuringBuilds=false, so `next build` genuinely fails on this line — not a false alarm. Introduced in a43cdaa7 (Hermes's original B-1 commit); missed by Claude's own c80b1a92 review-fix to the same file; every commit since inherited the failure, meaning production deploys have been blocked since B-1 landed. Deleted the dead declaration; eslint and tsc both clean after (verified directly, not by report) | claude | DONE | b820cc9c |
| C-53 | LEAN BACKSTOP WAKE finding: two Hermes branches ready and content-clean (both pure additions, both self-report DONE) — hermes/hf1-fable-dashboard (H-F1, 6fc36451, /fable proof dashboard) and hermes/hf2-pledge (H-F2, 2b427adc, /pledge + affiliate-free endpoint). Dry-run merge of EACH against current base conflicts only on this ledger file's own append line (both branches append a DONE row at the same spot as each other and as C-52). Per the lean-backstop protocol, not hand-resolving in this pass — needs the standard union-resolve (keep both rows, keep DONE) on the next full pass | claude | DONE | 7ea77a5a (H-F1 merge); H-F2 merge follows |
| H-F1 | FABLE proof dashboard section | hermes | DONE | af111172 |
| H-F2 | Zero-affiliate pledge page | hermes | DONE | 1642d202 |
| C-54 | Root-caused the deploy blocker PROPERLY instead of stopping at the one reported line. C-52 fixed a single unused var; a full `eslint . --ext .ts,.tsx` sweep over apps/web (run with a REAL exit code, not piped — piping through tail had masked the true status earlier this session) found THREE more no-unused-vars errors of the identical build-blocking class: lib/truthmetrics/consensus-clock.ts newtonStep declared `n` and never referenced it (verified by reading the whole function, not by trusting the linter alone); consensus-clock.test.ts imported an unused ConsensusClockResult type; bookgrade.test.tsx imported an unused PULSE_SCORE_V1. Because apps/web lint runs --max-warnings=0 and next.config sets eslint.ignoreDuringBuilds=false, every one of these fails `next build` — so C-52 alone would NOT have unblocked the deploy. All three removed; eslint over the whole app now exits 0; the two touched suites pass 19/19. Lesson recorded: fix the CLASS, not the reported instance | claude | DONE | 16dabcf8 |
| C-55 | PRODUCTION BUILD VERIFIED GREEN locally before any deploy — the first time this has been proven rather than assumed this session. `npm run build` (full next build, vercel.json's real command path) exits 0 with zero errors or warnings; note the harness reported "exit 0" for the wrapper too, which was meaningless (trailing echo), so the true status was read from BUILD_EXIT written inside the log — same pipe-masking trap as the earlier ledger-guard incident. Route manifest confirms all four launch surfaces compiled: /fable, /kill-ledger, /bookgrade, /pledge, /api/pledge/affiliate-free. deploy:ready reports 18 env-var failures that are SANDBOX ARTIFACTS, not blockers (no prod secrets in this container; they live in Vercel env) — its meaningful checks pass: 20 cron schedules, 3 security-header rules, gate sequencing | claude | DONE | 16dabcf8 (build run against this tree) |
| DOD-1 | DEFINITION-OF-DONE ITEM 1 CLOSED — production redeployed from latest main after being the standing blocker all session. Deployed SHA 7294739c, status Ready (exit 0, ~4 min build), aliased to https://www.galaxysportsedge.com, 53 migrations found and none pending. Executed by the Grok build seat under F-11 (Claude lacked any production-deploy tool). VERIFIED LIVE, not taken on report: post-deploy smoke all green (13 public pages 200, robots/sitemap/opengraph, /api/health, 3 security headers); the four launch surfaces each return 200 (/pledge, /fable, /kill-ledger, /bookgrade) plus /calibration; and /api/pledge/affiliate-free returns the frozen payload character-for-character. Mandatory-language spot-check on the live HTML: ECE confidence-echo caveat renders verbatim on /fable, BookGrade "quality score, not a betting signal" renders verbatim | founder | DONE | 7294739c |
| C-56 | Honesty-gate gap caught by checking the LIVE page rather than the source or the report: the charter lists four mandatory verbatim public lines; three rendered exactly (ECE caveat, BookGrade, PulseScore) but /kill-ledger carried a paraphrase ("We test things so you do not have to learn the hard way") instead of the mandated "These are things we tested and found not to work. We publish them so you do not pay for the same mistakes." Copy corrected to the charter wording (house style keeps "do not" over the contraction). Not a launch blocker and the old line was not dishonest — but mandatory means mandatory, and a paraphrase in one of four is how a spec quietly erodes. kill-ledger suite 4/4, trust-gate and eslint clean. Rides the next deploy; not worth a redeploy cycle on its own | claude | DONE | 10c8d655 |
| C-57 | Delegation skills installed and made PERSISTENT. `npx skills add amElnagdy/delegate-skills --skill grok-delegate --skill claude-delegate --global` succeeded (both repos real; the earlier 403 on github.com was the agent proxy on unauthenticated HTML, not absence) but wrote to /root/.agents/skills in this ephemeral container — the founder asked for them "for future use", which that install does NOT deliver since the container is reclaimed at session end. Vendored to .claude/skills/ so git carries them to every future session and to the founder's local checkout; .gitignore extended in its own established pattern (.claude/* ignored with explicit ! allowlist for shared assets — commands/, settings.json, now skills/). SUPPLY-CHAIN REVIEW DONE BEFORE LANDING, since these run with full agent permissions and the installer says so: verified by grep, not by trusting the file header — Node built-ins only, zero third-party deps, zero network calls (no fetch/http/socket anywhere), spawns limited to git/grok/claude/taskkill; process.env is forwarded so the child CLI can authenticate, harmless given no network path. secret-scan and trust-gate clean. NOT runnable here: the /plugin marketplace commands are Claude Code CLI built-ins the founder must type locally | claude | DONE | 539e3c1e |
| F-11 | FOUNDER OVERRIDE, recorded honestly: the CEO charter's non-negotiable "Do not let Hermes or any agent touch Vercel" is overridden for the build seat, at the founder's explicit direction ("im giving you permission to redeploy" + "pretty sure grok can though"). Claude could NOT execute it — the only Vercel tool in session is create_git_project, which makes a PREVIEW deploy and could create a duplicate project, and there is no .vercel linkage, no list_teams tool, and no Vercel CLI; declining was a capability limit, not policy. Scope of the override is deliberately narrow in .grok/config.toml: allow `vercel --prod`/`deploy --prod` plus read-only ls/inspect/logs; DENY vercel env, remove, rm, project rm, domains, alias — the seat may ship, never mutate env or destroy the project. Deploy is only safe because C-55 proved the build green first | founder | DONE | 08d3abed (.grok/config.toml scoped allow/deny) |
| H-F4 | Daily honest-record post generator | hermes | DONE | 2d850547 |
| C-58 | LEAN BACKSTOP WAKE: four new build-seat branches found. MERGED H-F4 only (clean merge, zero conflicts; typecheck 0, trust-gate 0, draft-only guard 0 — that guard is the law for this task since publishing stays founder-gated — 36/36 tests). NOT merged, each for a stated reason: (a) hermes/hf3-preseason conflicts on this ledger file's append line only, code content clean — per lean-backstop protocol not hand-resolved in this pass, needs union-resolve next full pass, and note it self-expires ~Aug 30; (b) hermes/hf5-mve held back because FINAL-RUN makes an independent cross-model audit MANDATORY before any H-F5 merge and the builder may not verify its own work; (c) hermes/f9-ledger-chain-schema NOT merged — it edits packages/db/prisma/schema.prisma plus a new migration, a SEALED PATH, while F-9 (the founder's schema review) is still OPEN | claude | DONE | 223c5202 (SHA corrected; 40c341ab was a pre-merge hash that resolves nowhere) |
| C-59 | H-F5 MVE DID NOT RUN, and the build seat handled it correctly — recording this because a stalled one-shot experiment is exactly what gets quietly faked. RESULTS.md reports the cycle BLOCKED before first observation: runtime DATABASE_URL/DIRECT_URL point at localhost Postgres, pg failed 28P01 password authentication for user "sports", DATABASE_URL_UNPOOLED unset, no Neon URL in process env. It invented no capital, no n, no exclusion count; printed no secrets; computed no alternate window, lambda or variant; and left the pre-registration un-amended. Frozen artifacts DID land on the branch (side-adaptive e-process + unit tests, seed 20260820, nParticles 24, runner scripts/edge-lab/run-mve.ts, and the outcome drafts written BEFORE any corpus look) — which is correct pre-registration hygiene. Needs founder hands: re-run on the same branch with a working Neon URL, no retuning, one cycle | hermes | BLOCKED | founder must supply working DATABASE_URL and re-run; origin/hermes/hf5-mve 0035e3b4 |
| C-60 | GOVERNANCE FLAG on hermes/f9-ledger-chain-schema (a28e1d67): the branch appends a NEW row "F-12" with owner=founder and status=DONE, self-marking founder-owned work complete without the founder having acted — F-9 remains OPEN. Mitigating and verified by reading the diff, not assumed: the change is deliberately inert (it does not apply the migration, does not flip LEDGER_CHAIN_ENABLED or PUBLISH_LEDGER, and loadLedgerView stays four-leg empty), and it does implement the durable Postgres store the B-6a proposal specified instead of the in-memory fake that was rejected. Still not mergeable by an agent: sealed path plus an open founder gate. Founder decides whether to adopt; if adopted, the F-12 row should be rewritten as agent-owned with F-9 recording his actual approval | claude | BLOCKED | founder review required; not merged |
| H-F3 | NFL preseason ingestion (C-36) | hermes | DONE | 8731a472 |
| H-F6 | SEO/metadata audit pass | hermes | DONE | a3ee39cd |
| H-F7 | Archive liveness check (odds_line_snapshots) | hermes | DONE | 38b2bb8a — total 37402; MLB 11318 (OPEN 144 / INTERIM 11174 / CLOSE 0); NFL 9864 (OPEN 96 / INTERIM 9768 / CLOSE 0); hermes_ro on gse-postgres main |
| C-61 | FINAL RUN H-F QUEUE CLOSED. Merged hermes/hf3-preseason, hf6-seo, hf7-archive into the working branch (union-resolve on this file each time; the resolve initially duplicated C-53 and the ledger guard caught it — stale BLOCKED copy from hf3's older base deleted, current DONE kept, per the keep-DONE convention). Verified after merge, not assumed: tsc 0, eslint over apps/web 0, trust-gate 0, data-ingestion 295/295, launch-page suites 29/29. Also fixed a stale docblock in kill-ledger/page.tsx whose "Copy rules" comment still quoted the pre-C-56 closing line while the rendered copy, metadata and JSON-LD had all moved to the mandated wording — a comment that lies about the copy rule is how the next agent reintroduces the old line | claude | DONE | 2e016327 (SHA corrected; 5077a89d was a pre-merge hash that resolves nowhere) |
| C-62 | ARCHIVE LIVENESS (H-F7) read from Neon as SELECT-only hermes_ro: odds_line_snapshots = 37,402 rows; MLB 11,318 (OPEN 144 / INTERIM 11,174 / CLOSE 0); NFL 9,864 (OPEN 96 / INTERIM 9,768 / CLOSE 0). Table grew mid-query (37,384 -> 37,402), which proves LINE_ARCHIVE_ENABLED is bound and writing. CLOSE=0 is NOT yet evidence of a bug: close stamps are written at settle time by markClosingSnapshotsIfEnabled, and the flag only bound at the 7294739c deploy, so no game has completed a full pre-deploy-to-settle cycle under it. It IS the next thing to check — if CLOSE stays 0 after MLB games settle post-deploy, the close-stamp path is broken and DoD item 7 is not actually satisfied. Recorded as a pending observation, not a pass | claude | OPEN | recheck CLOSE counts after the next MLB settle cycle |
| F-13 | FOUNDER DIRECTIVE 2026-08-20: build seat moves from Grok Build to HERMES RUN ON LAGUNA going forward. Grok completed H-F1..H-F7 and steps back to fallback + verifier. FINAL-RUN doc updated with seat history and current holder. Consequence recorded because it is load-bearing: since Grok BUILT the H-F queue, Grok may not verify it, so the outstanding mandatory H-F5 MVE audit belongs to Laguna-on-Hermes or DeepSeek | founder | DONE | 2e016327 (evidence SHA corrected; 5077a89d resolves nowhere. Row content untouched) |
| T-Q1 | q-integrity: slugPlayer collision fix + credit-cap kickoff sort + line-archive N+1 batch | hermes | DONE | 9a619bf4 on origin/hermes/ox-alpha-q-integrity; draft PR #525; 257 tests passing (34 tq1-specific) |
| T-Q2 | Wire markClosingSnapshots behind existing LINE_ARCHIVE_ENABLED flag in settle-sport.ts | hermes | DONE | 0a447f98 (hermes-H-D) is ancestor of HEAD on hermes/ox-alpha-q-integrity; markClosingSnapshotsIfEnabled called in settle path with try/catch (never fails settlement); 55 settle-sport tests + 15 line-archive tests passing; fix stale docblock noting it's wired (commit fad55c53) |
| T-Q3 | Kalshi listing snapshot harness: 429 → one retry → success; 429 exhaust → KalshiError(429) STOP; never last_price as q; exponential backoff verified | hermes | DONE | kalshi-snapshot-harness.test.ts created (0906ec0c on origin/hermes/ox-alpha-q-integrity); 4/4 tests pass; persist path already in #516 (gateKalshiListing refuses last_price, yes_bid+no_bid complement) |
| T-Q3a | Typecheck fix for T-Q3: kalshi-snapshot-harness.test.ts lines 109-110 — with noUncheckedIndexedAccess, fv.sides[0]/sides[1] are possibly undefined. Added non-null assertions (sides[0]!, sides[1]!) since toHaveLength(2) already guards. Fix at cc905a83 on origin/hermes/ox-alpha-q-integrity; tsc --noEmit exits 0; CI Test job 97040591997 PASS (7m54s), all checks green, PR #525 ready for review | hermes | DONE | cc905a83 on origin/hermes/ox-alpha-q-integrity; PR #525 |
| H0.6 | H0 slice #6 — remaining process covariates from PROP_COVARIATE_GAP: rush-yards bind + INT bind on hermes/h0-rush-yards-covariate-bind, PR #576. Binds leak-safe NGS weekly means (week t for t+1, week=0 excluded, fail-closed, never imputed) into RushSample (pctAttemptsGte8Defenders + avgTimeToLos) and IntSample (avgTimeToThrow + aggressiveness) via covariate-bus. priced:false throughout. Post-review fix (2308f468): single latestPriorRow scan per request instead of nextGameCovariate x2 per field; CovariateCells built from BUS_CELL template for single-source grain/provenance. 22/22 tests pass, tsc --noEmit 0 errors, all CI checks green on PR #576 | hermes | DONE | 2308f468 on origin/hermes/h0-rush-yards-covariate-bind; PR #576 |
| H0.5 | RZ-share on top of Dirichlet (K11) — BLOCKED. K11 (dirichlet-multinomial kernel slot) is defined in KERNEL_SLOT_CARDS.md but NOT yet implemented on origin/main (no kernel/slots/ dir). EDGE-HUNT-LAUNCH.md gate: 'you bind it only if slot already on main.' Do not fill K1–K13 slots (fleet does that). scoring-zone.ts already has rzShare field — ready to bind once K11 lands on main. | hermes | BLOCKED | N/A — waiting on fleet K11 landing |
| LAUNCH-0902 | Final launch pass (owner-directed, 2026-09-02): free-first settlement law with starved-cycle detection; alias tombstones + dry-run merge tool for duplicate games (2h baseball window); blocking CI migration replay + drift check; baseline seeds non-destructive; market-coverage and confidence-tail monitors on the truth surface and launch:ready; owner runbook (`npm run ops:runbook`) and 10-minute launch-day runbook; review-fix pass on the Devin/cubic findings; guard hardening (selftest 91/7/45). Calibration floors and record gates untouched (D2/D3/D8 in docs/ops/CLAUDE_DECISIONS_20260902.md). | claude | DONE | PR #685 (stacks on PR #684); report LAUNCH_FINAL_20260902.md; decisions docs/ops/CLAUDE_DECISIONS_20260902.md |
| C-63 | ZERO GAPS AUDIT 2026-09-03: Comprehensive codebase audit with all gaps closed. Fixed 2 failing guardrails (ai-control-plane-sealing: skip Sports/ nested dir copy; ai-council: use node not npm spawn for Windows PATH compat). Added descriptions to 18 @sports packages (ai-council through util). Verified: tsc=0, lint=0, git-clean, guards=26/26 (was 24/26), all package READMEs present. Gap analysis report at docs/ops/GAP_ANALYSIS_2026-09-03.md documenting 31 remaining OPEN ledger items + 2 production TODOs (documented future work in compliance package). | claude | DONE | 3f47b0e88 |
| C-64 | settle-backfill defaultPersist score-disagreement guard (dual-audit verified) | claude | DONE | 6a2ec0d0d + regression test in settle-backfill.test.ts |
| C-65 | settle-backfill capReached off-by-one fix: fetch cap+1 rows so capReached (rows beyond cap) is decidable; exactly-cap=false, cap+1=true; graded set still sliced to cap | claude | DONE | merge 4adc43f2b + capReached rework in settle-backfill.ts, 11/11 tests |
| C-66 | backfill-team-efficiency + ingest-player-stats unpublished-season guards | claude | DONE | 968c49522: both routes fall back to floor only on isUnpublishedSeasonSignal (404/ok-zero-rows); 5xx outage recorded failed; 49/49 route tests; tests for outage-no-fallback + ok-zero-rows fallback added |
| C-67 | merge-duplicate-games computeCanonicalFillData score-pair branch (AND to OR) | claude | DONE | ea893d5a8: extracted pure rules to scripts/ops/game-merge-score-fill.ts (OR semantics) + 10 node:test cases via test:merge-score-fill |
| C-68 | game-merge-plan canonicalPickId → referencePickId rename (repo-wide) | claude | DONE | f4709df22: PickConflict.referencePickId; 3 code refs + 1 test ref renamed, game-merge-plan tests 17/17 |
| C-69 | smoke-prod cold-start timeout retry logic | claude | DONE | 5ba852598: one 2s-settle retry per non-200 route, global cap 8, LAST_CODE global (subshell counter bug caught in self-test); bash -n OK, counter verified |
| C-70 | VERIFIED FIXES 2026-09-03: 6 real bugs from dual-audit verification (DeepSeek + Claude independent scans agreed on 6/12 real, 6/12 false/stale). Shipped as C-64 through C-69 with one commit each plus verify block. False positives NOT touched: confidence-tail pickType (already wired), external-watchdog set -e (already omitted with comment), owner-runbook dual-secret check (already present), agent-bash-guard env -S/--output (already handled with selftests), review-pr.md/canary.md (already correct), process-sport.test.ts (doesn't exist). | claude | DONE | 47037c6d1..5ba852598 on claude/verified-fixes-2026-09-03; gates at close: tsc 0 errors, lint clean, guardrails 26/26, test:fast 251/251, settle-backfill 11/11, game-merge-plan 17/17, merge-score-fill 10/10 |
| H-N1 | All-night Wave 1 — consolidate the landing: merge #695→#696→#697→#698→#699→#694→#692 in order onto hermes/night-2026-09-04, full verify block after each, one draft PR, then re-add the soccer scoring assertion named in historical-replay-sport-key.test.ts | hermes | DONE | 5bccb568e1c9eaccbf3bb8db50e7846cfa31e1c5..6a9c9d694cf78586b9ec8335f86fc64526c37822 (7 merges in §2 order; #696→#698 carried #695 as merge base). Verify block after EVERY merge: typecheck 0, lint 0, test:fast 0, guardrails 26/26 (dependency-audit flaked red twice on heads with zero manifest changes — D4 flake, waivers stay per §2, fix is forbidden path, noted in merge message). One conflict: AGENT_LEDGER.md append line, union-resolved keep-both (H-N1 + ARCH-1). Soccer suppression assertion from historical-replay-sport-key.test.ts NOTE re-added post-#694 with NFL control: 7/7 green. Draft PR: #700 |
| ARCH-1 | ARCHITECT RECON 2026-09-03: verified launch state and triaged the unmerged backlog. main=0db5ef808 with CI run 33799645214 SUCCESS; typecheck 0, lint 0, guardrails 26/26 on that head; production LIVE on the same SHA (Vercel dpl_GoYrLkrJ2vTC28dYaXbXhEni9PbJ, target=production, READY). 789 remote branches, 640 diverging from main. KEY FINDING: 50 open PRs from the 2026-08-25 audit wave were never merged and none of their content is on main; dry-run merge of each against 0db5ef808 with git merge-tree gives 43 CLEAN, 7 CONFLICT, 0 absorbed. These are named access-control, money-path and honesty-surface fixes, including #597 (two BLOCKER findings: paid-board bypass, free-grace exploit). Landing queue, per-batch verify block, preview-deployment verification recipe and an explicit not-verified list are in docs/ops/LAUNCH_COMMAND_2026-09-03.md. Textual merge cleanliness is NOT semantic compatibility: the full apps/web suite was NOT run against any merged combination, and the doc says so rather than implying otherwise. Also corrected an earlier stale-ref reading in this session that reported main at bb0e7df | claude | DONE | #690 (4d42ffc5d696feecd775ac38291285646c466b27); doc is docs/ops/LAUNCH_COMMAND_2026-09-03.md |
| H-N2 | All-night Wave 2 — make metric-source-payload-rights.test.ts and metric-evidence-report-markdown.test.ts cwd-independent (resolve from import.meta.url, not process.cwd()); no test deleted or skipped | hermes | DONE | 648071a8c22a466a564db0121435e287435b5c84: walk-up repo-root helper (tsconfig.base.json marker, unique to root) because package tsconfig pins module:CommonJS so import.meta is TS1343; controls added vs vacuous pass; verified green from BOTH repo root and package dir (10/10 each). SECOND ROOT CAUSE beyond cwd: untracked nested clone at Sports/ (git-ignored, no unique commits vs main, clean) was collected by the root glob and always failed from root; moved to C:/Users/Garrett/_staged_for_deletion_2026-09-02/stale-sports-clone-from-sports-Sports_2026-09-04 (reversible). Gates: tsc 0, lint 0, guardrails 26/26, test:fast 0, targeted 0. Also in 61fd629c0: H-N1 DONE + PR #700 |
| H-N3 | All-night Wave 3 (first item, per addendum A3) — write docs/data/CONVERGENT_CALIBRATION_EVIDENCE_2026-09-04.md setting live tail (40% at ≥80), live resolution (0.005 on 1,663), replay AUC (0.4965 on 13,646) side by side, citing CLAUDE_DECISIONS_20260902.md D2/D3 + NFL_REPLAY_CALIBRATION_2026-09-04.md; state plainly the ≥80 tier wins less often than board average, corroborated; then continue Wave 3 main build (replay-calibration.ts) | hermes | DONE | 25772b36a (A3 convergent-evidence doc; every number traced to its recorded source) + Wave 3 build in fe3223f6085a7cf5659316d9e48b3ce517a772ff: scripts/analytics/replay-calibration.ts — walk-forward 10 folds (2016-2025), Murphy decomposition + reliability + ECE (equal-width AND adaptive) with seeded 2000-resample bootstrap CIs, isotonic/Platt/beta vs identity on held-out folds, variable-based leaves (favourite strength, era). RESULT: closing line already calibrated — no calibrator beats identity beyond +0.00007 mean Brier; market Brier 0.2106 CI [0.2050,0.2172] on n=2750 held-out; favourite-strength 6.5-9.5 leaf drifts 65.9%→57.1% (flagged for follow-up, no product change). Corpus honestly bounded: 5,281 games 2006-2025 (closing MLs start 2006), stated in script output and docs/data/MARKET_CALIBRATION_2026-09-04.md. Run exit 0; tsc 0, lint 0, guardrails 25/26 (dependency-audit D4 flake, 304s degraded npm audit), no test gates affected (analysis script, no test suite claims it) |
| H-N4 | All-night Wave 4 — build packages/prediction-engine/src/clv-harness.ts reusing clv.ts primitives (never reimplement), (openLine, closeLine, side, entryOdds) → CLV in points AND probability; known-answer tests computed by hand with controls; runner injecting seeded synthetic open labelled SYNTHETIC OPEN — NOT A REAL CLV RESULT everywhere; document required real-archive columns | hermes | DONE | dee68cf900d11541bbdf75bd4f274667e5f283d2: clv-harness.ts delegates all CLV math to clv.ts primitives (summarizeClv/clvFromEntry), SYNTHETIC OPEN labelling on every output section, requiredArchiveColumns() contract + entry==close degeneracy documented; clv-harness.test.ts 11/11 hand-computed with controls (one test fixed during build: +150 misapplied favourite formula, harness was right); clv-synthetic-runner.ts exit 0, n=744/market over 3 nflverse seasons, mean CLV ~0 pts as symmetric seeded jitter requires — SELF-CAUGHT pre-commit: season cap compared season numbers (2025..) to SEASON_CAP=3 and broke after one game (n=1, vacuous); fixed to distinct-season tracking and re-run before committing. Gates: tsc 0, lint 0, guardrails 25/26 (dependency-audit D4 flake 3rd occurrence tonight, 304s, manifest-unchanged head), test:fast 0, targeted clv-harness+clv 0 |
| H-N5 | All-night Wave 5 — totals tie-break: strict opt-in (context.totalsTiebreak="strict" threaded via HistoricalReplayOptions; only discriminating books vote, exact tie => no pick), legacy DEFAULT untouched (pinned tests stay green; default flip = owner's MODEL_VERSION decision), before/after replay, proposal doc under docs/calibration-proposals/, no MODEL_VERSION bump, no live-path merge | hermes | DONE | da1ab0b763cd303d743fe1d0a207c2beac79d627 (on hermes/wave5-totals-tiebreak; merged into hermes/night-2026-09-04): scoreTotalPick strict branch + GameContextInput.totalsTiebreak field + replay plumbing; scoring.test.ts strict suite 5/5 incl. A/B test proving the flag changes behaviour on identical input; pinned totals-consensus-tiebreak.test.ts UNCHANGED and green; historical-replay* 4 files green. Replay evidence (totals-tiebreak-replay.ts, seasons 2023-2025, 816 REG games, identical target both modes): legacy 816/816 total picks, 100% OVER, mean confidence 67, decided 0.5068 (411-400-5); strict 0 picks — every replayed total pick was manufactured by the <= tie-break. Doc: docs/calibration-proposals/2026-09-04-totals-tiebreak-strict.md (status PROPOSED, modelVersion v5.2.7 declared = current, no bump; model-freeze unaffected). Gates: tsc 0, lint 0, guardrails 25/26 (dependency-audit D4 flake 4th occurrence tonight, 40s, manifest-unchanged), test:fast 0, targeted 6 files 0. SELF-CAUGHT ×2: (1) first season slice assumed newest-first encounter order (archive oldest-first) processed 1 season/248 games — honest-corpus guard aborted; (2) JSON seasons field printed slice(0,3) not the processed target. Both fixed before commit |
| H-N6 | Re-audit of the 2026-09-04 night in a fresh session: re-verify every gate claim on merged head 432dbc055 with fresh tool exits, then hunt missed/under-leveraged/unpolished items | hermes | DONE | ba389e792 (docs commit carrying the night-log RE-AUDIT line + this row lands alongside; no second code commit): verification re-run correctly-invoked and green (typecheck 0, lint 0, test:fast 0, guardrails 26/26 exit 0 — first harness attempts used wrong runner path/bare tsc, self-caught and discarded before any claim); Wave 5 replay JSONs pinned from %TEMP% to docs/calibration-proposals/evidence/ with doc citation repointed; SPREAD pick'em sibling defect confirmed by throwaway probe (all-PK board publishes phantom pick conf 59, side AWAY by exclusion, consensus 1.0; 50/50 opposing lines correctly null) — recorded as re-audit addendum in the proposal doc + _dispatch3 Forecast List, NOT fixed (D1 scope + D5-adjacent live-path rule); D4 dependency-audit root-caused as degraded-response false STALE (next/high verified still live at 10:32 CST) — full diagnosis + proposed fix for the forbidden-path owner in docs/ops/DEPENDENCY_AUDIT_D4_DIAGNOSIS_2026-09-04.md; night log RE-AUDIT line appended; totals-tiebreak default-flip decision remains with the owner (unchanged); EXTENDED same session to all waves: W1 all 7 PR SHAs + 170aa1682 ancestors, fix code spot-verified, targeted suites 127/127 engine + 7/7 web; W2 metric tests 10/10 package-dir, nested clone confirmed removed; W3 replay-calibration re-run exit 0 reproducing every published number exactly (Brier 0.2106 CI [0.2050,0.2172] n=2750, isotonic +0.00007, leaf 65.86->57.12) with log pinned docs/data/MARKET_CALIBRATION_2026-09-04-reproduction.txt; W4 clv 11/11 + SYNTHETIC OPEN wrapping verified + archive contract printed, real-archive acquisition tracked in F-5; flagged 6.5-9.5 leaf drift noted as having no dedicated follow-up owner (D1-scoped, left unclaimed) |
| C12 | Close the pass (scripts/ops/chaos/c12-close-the-pass.txt): PART 2 7/7 verifications closed (cron auth enumerated 26/26, email_verified confirmed installed-package-level, D-4 columns confirmed, webhook dedupe confirmed, npm-audit matrix waived with reasons + dates, Neon PITR honestly NOT-RUN-console-only, 3/10 calibration-checked); five blockers fixed-or-decided — S1 liveBoardOn de-hardcoded (4 sites), S2 copy corrected + push opt-in mounted (D-2), S3 always-on 21+ attestation gate (16 prefixes, middleware + /age-verify, loop/redirect-safe), S10 ESPN disclosure on /data (feed use founder-ratify), #16 precision-corrected (comment not copy) with gate kept structural via free-only; PART 4 PAID_CHECKOUT_OPEN server-side choke on the single checkout path + one-line revert; PART 5 D-item artifacts (D-1/2/3/4/6/7/8/10; C11's D-0..D-15 text unrecoverable — honest gap recorded); PART 6 re-scores (free-only 6/10, paid 4/10); PART 7 2am handoff | hermes | DONE | this commit carries code+tests+docs/ops/launch/C12-02..06 (C12-01 landed in 4e5a58963); verify block real exits: typecheck 0, lint 0, lint:brand 0, targeted web 116/116 (board-classify-state, middleware-contract, subscriptions-checkout-route, auth, age-verify surface, deploy-readiness-stripe-prices), push stack 35/35; D-4 backfill dry-run REFUSED exit 2 with gate off (control verified live); no sealed path touched, no gate flipped, no Stripe flow exercised, no test skipped |
| H-N7 | Frontier night 2026-09-05 (hermes/night-2026-09-05): PR 704 opened for c12-close-the-pass; archived 12 genuinely-orphaned engine modules to attic/ after falsifying 5 of LEVERAGE 19 dead-code claims as transitively live (elo-estimator, hawkes-steam, nflverse-replay-parser, projection-evaluation, tweedie-aci); narrowed eprocess-property.test.ts to live forecast-skill props; closed research-lab algorithm gap (10 briefs mapped, paths existence-checked); ingested Wave-2 extract-data JSONs to gse-competitive-intel waves/ on branch hermes/wave2-ingest-2026-09-05 + remote added; hunts all NOT FOUND (cron auth 25/25 via cronAuthError incl. generate-signal-slate, Stripe webhook hardened, dev/state prod-gated, JSON-LD escaped, NEXT_PUBLIC clean, no SSRF-shaped routes, rate-limit infra present) | hermes | DONE | c92964d23 on main (squash #705; supersedes branch SHAs a69a3d435 + 8ed40dc6c, unreachable after --delete-branch); engine 275 files/3072 tests green, typecheck 0, lint 0, guardrails 26/26 |
| H-N8 | Frontier night 2026-09-05 closeout (hermes/night-2026-09-05): full web 12010 passed/0 failed, engine 275 files/3072 tests green, guardrails 26/26 exit 0, typecheck/lint clean (c12 branch); Wave-3 research-lab 41 mapped paths existence-checked, Rumor Triage row clarified to metrics/market/ canonical paths; diagnosed 21:54 full-run 10 failures as branch-mixing artifact (C12 test file on main-tree worktree) + sealed-holdout 60s load-flake, neither a real regression | hermes | DONE | c92964d23 on main (squash #705; supersedes e9d2ebf80, unreachable after --delete-branch); web 12010/0, engine 3072/3072, guardrails 26/26 |
| C-80 | Settlement CRITICAL root cause (36 overdue, 2026-09-05): ESPN limit=1000 collapses the board to 25 events (limit 300 + FBS/FCS + D-I groups), matcher held or mis-graded on bare fc/sc/united tokens, 2-3 letter abbreviation containment and union-side matching (now bipartite, exact abbreviations, diacritics folded), runner skipped within-grace rows whenever one hold existed, backfill kept the newest dates | claude | DONE | 2294db2 6880f18 6cd5eda f987259; live ESPN probes and the real 2026-08-29 MLS board replay (13/13 settle, zero holds) in the tests; SQL read-only: 22 MLS + 13 MLB + 1 NFL, zero settlement observations for any of them |
| C-81 | Signal slate overwrote book-priced MONEYLINE picks every cycle (selection "(model signal)", line 0, bookmakerCount 0, marketFairProb null while the receipt kept the book) and showed FREE viewers the gated number as "@ NN%"; guard added, teaser scrubbed at write and read, confidence-tail byMarket populated | claude | DONE | 31564d9 ef24e77 67730a6; SQL: MLB 450 signal rows vs 19 book-priced, 46 NCAAF signal rows written 15:54-16:05 UTC over today's slate |
| C-82 | C-28 Phase 0/1: calibration loaders read the receipt's publish-time market probability when the factor breakdown lost it (coverage was 28/738 settled ML = 3.8%), score bake-off reported per market, operator note restated to the real p hierarchy | claude | DONE | 8a8f292 3eaf058 9a11cc2; floors unchanged; no gate touched |
| C-83 | Operator tooling for the backlog: GET /api/ops/settlement-rca (Bearer CRON_SECRET, dry-run of the production grader, reason per overdue pick, read-only), npm run ops:stale-picks (SELECT-only list of the 20 stale published picks) + OPERATOR_TASKS STALE-PICKS, generate-signal-slate and autonomy-cycle raised to the 300s cron ceiling (583 Vercel timeouts at 120s) | claude | DONE | a06d59f 0e9835c e957482 |
| C-84 | Finish-line plan and v5.2.8 proposal: docs/ops/LAUNCH_FINISH_LINE_2026-09-05.md (production facts, fixes, founder-only actions, WP-1..WP-12 with acceptance commands) and docs/calibration-proposals/2026-09-05-market-anchored-display-probability-v5.2.8.md (status PROPOSED; model-freeze exit 0) | claude | DONE | e14463b and the docs commit carrying this row |
| F-14 | FOUNDER (DECIDED 2026-09-05 by delegation, revised 19:05 UTC: YES, NOW; Phase 2 = WP-1 starts immediately as the PROVEN unlock; production read-only SQL on settled MONEYLINE picks with receipts: n 150, Brier 0.1692, Murphy REL 0.0050, ECE 0.0552 on 10 bins; excluding soccer two-way moneylines by the engine's own publication rule: n 115, Brier 0.1444, ECE 0.0440, Murphy REL 0.0044, all four floors met today; founder approved the switch and cron triggering 19:20 UTC; recorded in the proposal doc section 1 and LAUNCH_FINISH_LINE sections 3c and 3d; measurement side shipped fbc3784c7 with receipt-first scoring, MONEYLINE-only pooled floors and a basis-aware streak; receipts carry a mean-implied proportional de-vig, not Shin, and the proposal wording was corrected; display label and claim copy remain as C-107): decide v5.2.8 (market-anchored displayed win probability; confidence stays a 0-100 selection score never rendered as a percent; restated public calibration claim). YES flips the proposal doc to IMPLEMENTED and dispatches WP-1 | founder | OPEN | docs/calibration-proposals/2026-09-05-market-anchored-display-probability-v5.2.8.md section 1; read provenPath.scoreBakeoffByMarket (marketFairProb on MONEYLINE) after the next calibration-metrics cron before deciding |
| F-15 | FOUNDER (TOP ITEM): The Odds API account is in a payment state: HTTP 402 payment circuit open on every hourly settle cycle since 2026-09-03 20:20 UTC (31 groups per sport, Vercel), 401 INVALID_KEY on NFL until 2026-09-04 23:20; the shared breaker stopped paid book odds at 2026-09-05 00:30 UTC (last fanduel/draftkings/betmgm rows) and zero book-priced picks were generated in the 18h after (SQL 18:00 UTC); fix the account for Week 1 only if WP-27 (C-104, the completely free two-book board) cannot land before Thursday; do not upgrade the tier; check account/plan/quota before NFL Week 1, the truth surface only reports that a key value exists | founder | DONE | DONE 2026-09-06 02:37 UTC by the browser agent (#709, plan 3e script A and 3f): 20K key rotated on the dashboard, THE_ODDS_API_KEY set in Vercel Production, redeploy Ready 02:37:12 UTC; one 402 on the old key at 02:37:15 UTC during rollover, none in the 24 minutes after (Vercel logs); dashboard usage 0 to 112 credits by 03:02 UTC; truth surface oddsInserting lastSuccessAt 03:03 UTC with 242 rows; follow-up C-109 (credit governor) because that burn exhausts the plan before Week 1 ends; history: mcp Vercel get_runtime_errors 48h, 2026-09-05 15:4x UTC; REVISED 2026-09-06 02:25 UTC from founder dashboard screenshots: account Active on the 20K plan ($30/mo, next invoice Sep 22), 0 of 20,000 credits used this period, free key 0 of 500, so the production key is stale, not unpaid; founder is setting THE_ODDS_API_KEY in Vercel Production to the 20K key and redeploying (breaker is process-local, redeploy resets it); browser agent verifies only (plan 3e script A); no tier change, no key pasted anywhere |
| F-16 | FOUNDER (DECIDED 2026-09-05 by delegation: UNPUBLISH every row the selector finds; REVISED 2026-09-06: automated by WP-29 (C-106, coder priority 1) so no human runs it; the owner tool e8a3731dc stays as fallback only; 18 stale rows on the live surface at 02:08 UTC): adjudicate the 20 stale published PENDING picks on unstarted NFL/NCAAF games before Thursday; list with npm run ops:stale-picks; procedure in OPERATOR_TASKS STALE-PICKS | founder | OPEN | truth surface stalePendingPicks.count=20 on 2026-09-05 |
| F-17 | FOUNDER: run the owner game-merge tool over the duplicate Game rows (same fixture under odds-api hash, espn:<short>: and espn:<oddskey>: namespaces; MLB 268 fixtures with 2-3 rows, NCAAF 102, MLS 75, NFL 34 in the 21d window, zero merged) after WP-3 stops new duplicates | founder | OPEN | SQL read-only 2026-09-05; apps/web/lib/ops/game-merge-plan.ts, npm run ops:merge-games |
| C-85 | WP-3: unify the ESPN externalId namespace for new Game rows (espn-schedule-seed.ts:121 writes espn:<short>:<id>, espn-odds-client.ts:361 writes espn:<oddsKey>:<id>); one form, upsert on it, never a third row per fixture; existing rows are F-17 | — | OPEN | docs/ops/LAUNCH_FINISH_LINE_2026-09-05.md section 5 |
| C-86 | WP-7: MLB and MLS TOTAL picks are 0 with 41/51 games in the 72h window while pause groups cover MONEYLINE/SPREAD only; find the exact gate (process-sport totals path, selective runtime) and fix or label it on the coverage surface | — | OPEN | truth surface marketCoverage 2026-09-05 |
| C-87 | WP-2/4/5/6/8/11/12 settlement and honesty follow-ups: cockpit reason column from settlement-rca, board fetch memoization + 240s soft deadline in settle-picks, doubleheader tie window 90m, CROSS_PATH_SCORE_CONFLICT rca code, CLV entry price for rows first written by the signal slate, hadOddsSignal from bookmakerCount, CONFIDENCE_DISPLAY_MODE wired | — | OPEN | each has files and an acceptance command in LAUNCH_FINISH_LINE section 5; one PR per package |
| C-88 | WP-9/10: B2B /api/v1/probabilities publishes confidence/100 as pModel and the public proof page computes model-vs-market from confidence/100; Edge Index home copy says "calibrated" while maps are OFF | — | OPEN | apps/web/app/api/v1/probabilities/route.ts:88-91, apps/web/lib/proof/load-proof-of-record.ts:262-269, components/home/annotated-sample-signal.tsx:35 |
| C-89 | Money path, picks and public frontend honesty batch: reconcile-entitlements and repair-checkout-attempts at distinct minutes (45 and 50), publicEdgeScore withholds the edge number when it would reveal a gated probability, dashboard post-checkout banner truthful for FREE, US-Eastern slate-day window on /api/picks and daily-slate (80 of 91 NCAAF book picks were hidden), signal-slate soccer skip and neutral copy, "book-priced" receipt claim, dashboard confidence as NN/100, SentientShell unmounted from public routes, pricing and picks copy derived from the PUBLIC_PICKS gate | claude | DONE | a7ca36aef 00931f08c 486b0a34a 19937d8e5 66316461c 246fc9efe 994a2fa88; typecheck 0, lint 0, trust-gate 0; full web suite 12135 passed with 2 stale source invariants, both repaired in 994a2fa88 |
| C-90 | WP-25: gate the ESPN Power Index fetch in the signal slate behind the rights registry (fail closed, attribution) until the founder clears FPI use (F-21); copy never names the source to anonymous viewers | claude | DONE | 44735ad04 package-level gate (independent-source-rights.ts, ESPN_POWERINDEX_LICENSED unset = off, client never called; injectable env; 304 tests green; adversarial review verdict SHIP); anonymous copy stopped naming the source in 31564d9 |
| C-91 | WP-13..19 money-path hardening: Stripe unpaid never access-granting, Stripe-side double-subscribe guard, no-store on every entitlement gate body, env-path price amount verification, 503 on missing webhook secret, isPremium helper replaces inline tier predicates, durable rate limiter on checkout and portal | — | OPEN | files and acceptance per package in LAUNCH_FINISH_LINE section 5; 174 money-path tests green on the branch |
| C-92 | WP-20..24 pick payload: signal slate batches per sport and refreshes its own rows, no game.dataQualityScore write from the slate, line null and priceSource for book-less moneylines, isPublished on create only, per-market totals drop reasons on the coverage hint | — | OPEN | LAUNCH_FINISH_LINE section 5; WP-23 must not write the gate into pickUpdateData (would re-publish owner-unpublished rows) |
| C-93 | FE-02/05/08/09/10/11/12/13/14/15/17/18 public frontend launch quality: one floating launcher per corner, "No book price attached" pill and suffix stripped on signal cards, checkout resume after the sign-in bounce, main-content id on every page, plain-language gate copy, PICK_GRADE_LABELS on the dashboard, sitemap and noindex for illustrative pages, no double brand in titles, design-token drift, FAQ and About match the paywall, computed annual savings | — | OPEN | LAUNCH_FINISH_LINE section 5 FE table; FE-01/03/04 shipped in 994a2fa88 and 486b0a34a; no screenshots possible from the sandbox (connection reset through the proxy) |
| C-94 | FAN-01/02/03/05/06/07/08/10/11/12/13/14 fantasy suite honesty: label the 2025 basis season on every live surface and advance it when 2026 REG rows exist, feed the synced Sleeper roster into Start-Sit Waivers Trade, projections badge off above fictional tools, FREE trimmed-pool disclosure and upsell, hub readiness derived from providers, pricing Fantasy features match canUseFantasyFull, GM Ledger demo banner, per-game units on Start-Sit, studio pinned to the demo pool, scheme fit removed from live copy, projection-method decision, baseline text | — | OPEN | LAUNCH_FINISH_LINE section 5 FAN table; FAN-04 and FAN-09 shipped in 74e8e381f (host.ts disclosure, lib/fantasy/sleeper-season.ts) |
| C-95 | NFL-05/06/07 Week 1 follow-ups: free grader and backfill stamp line-archive CLOSE (markClosingSnapshotsIfEnabled after a successful settle write), hasRegRows probe so the nflverse display season advances to 2026 once REG rows land, ESPN schedule seed covers every Eastern day in the horizon | — | OPEN | packages/ingestion-pipeline/src/settle-sport.ts:716 is the only CLOSE caller today; packages/data-ingestion/src/nflverse-season.ts:29-83; espn-schedule-seed.ts:134-152 |
| C-96 | NFL-04 code half: surface the last refresh's Odds API remaining-request header on /api/ops/public-surface-truth and the health alert, and skip the paid getScores supplement for out-of-season sports in settle-picks (free path unchanged) | — | OPEN | packages/data-ingestion/src/config.ts:52-69 and :101; apps/web/app/api/cron/settle-picks/route.ts:246; founder half is F-26 |
| F-18 | FOUNDER: set the public Terms URL in the Stripe Dashboard, then STRIPE_TERMS_CONSENT_ENABLED, then redeploy; watch the consent checkbox in the live checkout smoke | founder | OPEN | docs/ops/OPERATOR.md section 5 ordering rule; LAUNCH_FINISH_LINE section 4 item 9 |
| F-19 | FOUNDER: confirm no Founding Payment Link (scripts/ops/create-founding-payment-link.mjs) is active or shared; they charge without granting access by construction | founder | OPEN | LAUNCH_FINISH_LINE section 4 item 10 |
| F-20 | FOUNDER: the Stripe webhook endpoint must subscribe to all ten handled events (the go-live checklist lists seven; missing checkout.session.expired, invoice.paid, charge.refunded) | founder | OPEN | apps/web/app/api/webhooks/stripe/route.ts handled-event list; LAUNCH_FINISH_LINE section 4 item 11 |
| F-21 | FOUNDER (DECIDED 2026-09-05 by delegation: gate the fetch fail-closed at the package layer, ESPN_POWERINDEX_LICENSED unset = off; NFL and NCAAF signal picks stop until a license exists): ESPN Power Index rights decision | founder | OPEN | rights registry allows ESPN facts only; C-90 is the engineering floor; remaining founder action is only the license itself |
| F-22 | FOUNDER: one live production checkout smoke end to end, then refund | founder | OPEN | LAUNCH_FINISH_LINE section 4 item 4 |
| F-23 | FOUNDER: the X handle @GalaxySportsAI is stamped into twitter:site and creator on every page and linked in the footer; rename or register a handle matching the domain, then update lib/brand.ts SOCIAL.x and derive ORG_HANDLE from it | founder | OPEN | apps/web/app/layout.tsx:89, apps/web/lib/brand.ts:62-67, components/layout/footer.tsx |
| F-24 | FOUNDER: first-visit media weight about 10.5 MB plus an 8s full-screen video interstitial (gse-reveal.mp4 4.16 MB) before any content; decide montage default off for organic visits and a hero still under 250 KB | founder | OPEN | apps/web/components/landing/montage-entrance.tsx; ls -la apps/web/public/brand/gse-reveal.mp4 |
| F-25 | FOUNDER: every /fantasy/* URL 307s anonymous visitors to the 21+ age gate (live curl 2026-09-05); decide whether /fantasy stays in AGE_GATED_PREFIXES or only contests and props do | founder | OPEN | apps/web/lib/age-verify/surface.ts:44, apps/web/middleware.ts:53-57 |
| F-26 | FOUNDER: The Odds API plan tier vs projected burn (about 34.6K credits per 30 days in September at */15 with 4 in-season sports and 3 markets, about 51.8K in October); confirm the plan on the vendor dashboard and upgrade or reduce non-NFL cadence | founder | CANCELLED | superseded 2026-09-05 by the delegated decision in LAUNCH_FINISH_LINE section 3b item 1 (fix the account, do not upgrade the tier, keyless path becomes primary: C-103); config.ts:52-69 and :101 remain the burn reference |
| F-27 | FOUNDER: Contest Bay bootstraps gse_contest_entries and gse_contest_settlements with runtime DDL outside Prisma migrations; land a migration before CONTESTS_PUBLIC ever opens | founder | OPEN | apps/web/lib/contests/store.ts:113-146; contestsPublic false on the live truth surface |
| F-28 | FOUNDER: the home hero has no path to /picks or /pricing and the IA ships two competing today surfaces (/board and /picks) plus two calibration-named proof pages; decide the primary CTA and which surface is the product | founder | OPEN | apps/web/app/page.tsx:129-137 and :170; nav-auth.tsx:98 |
| C-97 | OPS-02/04/06/08/09 operations hardening: durable rate limit on public-surface-truth and health, autonomy observation built from the real loaders (no hard-coded boardSuppressed true), withErrorCapture around checkout webhook and picks, autonomy executor timeout budget inside maxDuration, delete or test-fence the unreachable cron auth dual mode | — | OPEN | apps/web/app/api/ops/public-surface-truth/route.ts:150-166; health-alert/route.ts:165-167; autonomy-cycle/route.ts:121-136; lib/cron/authorize.ts:10-24 |
| C-98 | OPS-01 code half: alerting posture booleans (health webhook configured, Sentry DSN configured) on the truth surface and cockpit so a CRITICAL band that pages nobody is visible | — | OPEN | apps/web/app/api/cron/health-alert/route.ts:87-110 and :215-252; mirror lib/ops/autonomy-posture.ts; founder half is F-29 |
| C-99 | OPS-03 code half: durable lease on settle-picks (JarvisMemoryEvent pattern from traffic-heartbeat.ts) and a recency guard in the autonomy kernel so three schedulers stop double-firing refresh-odds and calibration-metrics | — | OPEN | vercel.json; .github/workflows/external-cron.yml:28-44 (frozen for agents); lib/autonomy/operating-kernel.ts; the founder picks the primary scheduler (F-30) |
| C-100 | OPS-05/07 alert copy and runbook drift: hours-in-band and overdue count in the health-alert head line, escalation by band age, LAUNCH_DAY_RUNBOOK and GO_LIVE aligned with strict health, settlement-rca and autonomy posture | — | OPEN | docs/ops/LAUNCH_DAY_RUNBOOK.md:89-103; docs/ops/CRON_MATRIX.md:27-36; apps/web/lib/ops/health-alert-decision.ts:69-72; the post-publish calibration drift alert (durable marker, health-alert unhealthy signal, truth surface) shipped in fbc3784c7 |
| C-101 | TCI-3/6/8 test-suite gaps: anonymous /api/picks teaser contract by invoking the route, explicit timeout on the sealed-holdout repo walk and node environment for route tests, worker smoke tests (TCI-2 signal-slate publish-rule assertions and TCI-5 packages/db test script shipped in 3da74b4de) | — | OPEN | apps/web/__tests__/picks-stale-kill-switch.test.ts is the mocking precedent; apps/web/__tests__/sealed-holdout-open-scan-guard.test.ts:56-61; workers/* have no tests |
| F-29 | FOUNDER: confirm HEALTH_ALERT_WEBHOOK_URL (or ALERT_WEBHOOK_URL) and SENTRY_DSN are set in Vercel Production and as the GitHub Actions secret; today no surface shows whether a CRITICAL band pages anyone | founder | OPEN | health-alert logs BLIND or UNDELIVERED to console only; NOT VERIFIED from the repo (env read-denied) |
| F-30 | FOUNDER: three schedulers drive settle-picks about six times an hour (Vercel cron, GitHub external-cron.yml, autonomy executor); pick the primary and trim the others; also enable the four env-gated Postgres suites and point the ci.yml brand-safety step at npm run test:brand-safety (workflow files are frozen for agents) | founder | OPEN | .github/workflows/external-cron.yml; ci.yml test job env (CHECKOUT_ATTEMPT_DB_TEST_URL, PR159_PG_URL, AI_CLAIM_PG_URL, SLATE_OPENING_PG_URL); brand-safety step lists 7 files vs 19 |
| C-102 | SEC-01/02/03/04/05/06/08 security follow-ups (no P0 or P1 found): admin trigger-refresh server action calls the handler directly under requireAdminActor (the fetch-to-self drops the session cookie), emailVerified stamp awaited with a fail-closed catch in the jwt callback, clientIp() in the five routes keying on raw X-Forwarded-For, safeEqualSecret in the waitlist Basic Auth gate, expectedMainFeatures and provider env-var names off the anonymous truth surface, nonce-based CSP, /api/promotions via jsonNoStore | — | OPEN | apps/web/app/admin/page.tsx:112-122; lib/auth.ts:93-97; lib/waitlist/access-gate.ts:59-62; api/ops/public-surface-truth/route.ts:610 and :815; next.config.mjs:87-88; api/promotions/route.ts:48-53 |
| F-31 | FOUNDER: open PR triage (30 open, read-only git against origin/main 1a3f00d05): close the 9 merged or superseded (#697 #698 #696 #625 #686 #687 plus the merge-train branches), update and merge the 5 clean production-relevant branches (#665 #668 #666 #660 #669) after CI, split #693 by original PR onto fresh branches (11 of its 12 re-landed money and access-control fixes never reached main; 11 conflicts), close the 4 mega-branches (#663 #674 #672 #675) with the two-item salvage list, treat #670 as a v5.3.0 calibration proposal not a cleanup, hold #437 until after Week 1, rebase #667 yourself (sealed guardrail path) | founder | OPEN | LAUNCH_FINISH_LINE section 5 (PR triage); NOT VERIFIED: GitHub PR metadata, only git refs were read |
| F-32 | FOUNDER: one secret (CRON_SECRET) authorizes both cron mutations and the read-only operator surfaces (settlement-rca, daily-truth, detailed truth); add OPS_READ_SECRET in Vercel once lib/ops/ops-auth.ts accepts it (C-102 companion) | founder | OPEN | apps/web/lib/ops/ops-auth.ts:12-14 |
| C-103 | WP-26 BRIDGE ONLY and optional (founder position on PR #680: not Rundown; the product path is C-104): TheRundown quota discipline: Phase A durable 429 cooldown (IngestionRun marker rundown:429, RUNDOWN_429_COOLDOWN_MINUTES default 60), freshness throttle from the newest Rundown-affiliate odds row (RUNDOWN_FALLBACK_MIN_INTERVAL_MINUTES default 30), provider and providerNote per sport, oddsInserting.lastProviderNote on the truth surface; Phase B board-fill skips the second odds pass inside the interval and Rundown daySpan follows the board horizon; Phase C re-land the PR #680 core after Week 1 | — | OPEN | NOT STARTED: the implementation run was stopped 2026-09-05 18:30 UTC to save usage; the design is complete in LAUNCH_FINISH_LINE WP-26 Phase A. ROOT CAUSE (production logs 2026-09-05, every 15-minute cycle, all four in-season sports): rundown empty (2d): HTTP 429 rate_limited; ESPN core odds returns exactly one provider (DraftKings) for NFL CFB MLB MLS so MIN_BOOKMAKERS = 2 cannot hold without Rundown or the paid feed |
| C-104 | WP-27 GSN API activation, the completely free two-book board (founder position on PR #680: we are the provider, not Rundown, not The Odds API): (1) re-land the PR #680 core onto main (GalaxySportsApiOddsProvider reading ESPN inline odds via site.web.api, de-vig fair_prob, 8s timeouts, galaxy-espn-inline registry entry, provider selection); (2) select the Galaxy provider also when the paid circuit is open, not only when THE_ODDS_API_KEY is unset; (3) feed galaxy-kalshi-book.ts from the PredExon Kalshi catalog (predexon-client.ts on main, verdict use-with-caution so assertIngestible passes, PREDEXON_INGEST plus PREDEXON_API_KEY, catalog cached per cycle for the 1 rps and 1k per month free tier) instead of the direct Kalshi client; (4) extend the Kalshi book from H2H to NFL SPREAD and TOTAL using series KXNFLSPREAD and KXNFLTOTAL in kalshi-series.ts; (5) acceptance: with the paid key blank in a test, processSport mints MONEYLINE and NFL SPREAD/TOTAL picks with bookmakerCount 2 from espn_public plus kalshi, none when either side lacks a live quote, data-ingestion and ingestion-pipeline suites green | — | OPEN | origin/hermes/galaxy-keyless-odds is 197 behind main and its PR #680 CI was green; nothing on main consumes predexon-client.ts yet (git grep 2026-09-05); ESPN core odds returns one provider (DraftKings) verified live 2026-09-05; the founder holds a PredExon dashboard key (registry note 2026-08-21) |
| F-34 | FOUNDER: set PREDEXON_API_KEY (the dashboard key you already hold) and PREDEXON_INGEST=true in Vercel Production once WP-27 (C-104) lands; then blank THE_ODDS_API_KEY if you want the keyless Galaxy path to take over immediately (after WP-27 step 2 it also switches on its own while the paid circuit is open) | founder | OPEN | packages/data-ingestion/src/predexon-client.ts env names; PredExon free tier 1 rps and 1k requests per month, Kalshi list-markets documented free and unlimited |
| F-35 | FOUNDER: ESPN inline odds rights posture for the Galaxy path: the app registry espn-public-api entry is facts only with commercial_display_allowed false, while the PR #680 package entry galaxy-espn-inline is use-with-caution with commercialUse true; reconcile the two (an ESPN license, or a written posture that relayed sportsbook prices are facts with attribution) before the keyless path is the only book-priced source | founder | OPEN | apps/web/lib/scraping/source-rights-registry.ts espn-public-api; origin/hermes/galaxy-keyless-odds packages/data-ingestion/src/source-registry.ts galaxy-espn-inline |
| C-105 | WP-28 publish-time market probability from the odds table for the 610 receipt-less settled moneyline picks: third fallback in live-calibration-p.ts after the factor breakdown and the receipt, odds rows with fetchedAt at or before generatedAt, latest snapshot per bookmaker, at least MIN_BOOKMAKERS books, de-vig with consensusNoVig, picked side; read time only, zero writes; loaders in proven-path-seed.ts, calibration-eligibility-durable.ts and calibration-metrics/route.ts pass the rows or a resolver | claude | DONE | fbc3784c7 (publish-time-market-p.ts pure resolver plus one-query loader, provenance counted, same mean-implied proportional de-vig as the receipts, adversarially reviewed); query shape reused from apps/web/lib/settlement/free-path-clv.ts:68-90; acceptance: fixture test with three bookmaker rows, no-rows pick stays excluded, scoreBakeoffByMarket MONEYLINE coverage above 0.7 on the next cron run; LAUNCH_FINISH_LINE section 3c step 2 |
| C-106 | WP-29 zero-sit settlement policy (founder decision 2026-09-05: no pick ever sits): PENDING more than 24h after commenceTime with rca reason NO_FINAL after every free source and the backfill lane is VOIDED through the settlement outbox lane (PickSettlementEvent VOID with the RCA code in the payload, never deleted); published PENDING picks on unstarted games past STALE_PENDING_PICK_MAX_AGE_DAYS are unpublished by the same cron via scripts/ops/lib/stale-pending-picks-selection.ts; both counted on the truth surface and the health alert; owner tool kept for manual overrides | claude | CLAIMED | code landed b4885f214 (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): apps/web/lib/settlement/zero-sit-lane.ts runs at the end of every settle cycle: stale published PENDING picks on unstarted games past STALE_PENDING_PICK_MAX_AGE_DAYS unpublished and recorded; PENDING picks more than 24h past commence VOIDED through the settlement outbox lane with RCA NO_FINAL, SCORE_MISMATCH_CROSS_PATH (24h unresolved), AMBIGUOUS_TEAM_NAME (cause MULTIPLE_FINALS for doubleheaders) or FIXTURE_NOT_FOUND (ESPN board fetched with strictEspn, all division groups required, fetch failure voids nothing; rows CANCELED only when neither team appears); preferLongerTeamName in the process-sport upsert update branch; stale-pick-policy.ts and the truth-surface hint state the automatic policy; tests settlement-zero-sit-lane.test.ts, free-adapters.test.ts, multi-source-scores-dated-clearance.test.ts, settle-picks-free-first.test.ts (60 passed); typecheck 0, lint 0, guardrails 26/26; adversarial review approved after one fix (fail-open on a partial ESPN board). DONE flips when the truth surface reads overduePending 0 and stalePendingPicks 0 after the first settle cycle post-deploy (law 5: the acceptance is a production reading). CODER PRIORITY 2 after C-109 (2026-09-06: the founder wants no human step; the stale half ships first, the void half second); 2026-09-06 03:20 UTC diagnosis of the 16 overdue (read-only SQL plus Vercel logs, plan 3f item 2): 10 MLB SPREAD picks (v5.2.7, 2026-09-01 to 03) on game rows with city-only names (Tampa Bay v New York, Chicago v Milwaukee, Los Angeles v New York, Los Angeles v St. Louis, Houston v Chicago; 32-hex externalIds), 7 of them refused every cycle as [free-settle] SCORE_MISMATCH_CROSS_PATH (existing 3-1 vs incoming 3-4 and similar, "left PENDING for human review"), which is what ambiguous names produce when two feeds match two different real games; the paid scores window (daysFrom 3) has passed for 9 of the 10, so only the void lane clears them: VOID with RCA AMBIGUOUS_TEAM_NAME or SCORE_MISMATCH_CROSS_PATH after 24h, never human-review forever; also apply preferLongerTeamName in the plain upsert update branch (process-sport.ts:609-612), which today overwrites a full name with a feed's city-only name; the other 6 overdue are the phantom fixtures in C-111; stale count 26 at 03:02 UTC; replaces the owner-decides stance in apps/web/lib/board/stale-pick-policy.ts:16-20; entry points settle-picks/route.ts, root-cause-analysis.ts, settle-backfill.ts; acceptance: 25h NO_FINAL pick voided with an outbox event, 23h pick untouched, stale unstarted pick unpublished, overduePending and stalePendingPicks read 0 after one cycle |
| F-36 | BROWSER AGENT (revised 2026-09-06, founder wants no human step): the streak runs on the schedule (40 */6 UTC) and the durable publish receipt is written automatically at streak three (evaluateAndPersistEligibility auto-publish); after the truth surface shows that receipt AND C-107 is deployed, set PERFORMANCE_STATS_ENABLED=true and PRICING_PHASE=PROVEN in Vercel Production and redeploy (plan section 3e script B) | browser | OPEN | CALIBRATION_ELIGIBILITY_STREAK default 3 (apps/web/lib/ops/calibration-eligibility-durable.ts:237); floors measured 2026-09-05 19:05 UTC in LAUNCH_FINISH_LINE section 3c; agents never run a cron with a secret |
| C-107 | WP-1 display side of v5.2.8 Phase 2: the pick card and the picks API show the market-implied win probability on book-priced two-way moneyline picks with the verified label (mean implied probability across books, proportional de-vig, fixed at publish time in the receipt), the public calibration claim is restated with the MONEYLINE-only scope, then the proposal status flips to IMPLEMENTED and MODEL_VERSION to v5.2.8 in the same commit | claude | CLAIMED | display half landed f06be6b31 (2026-09-06 04:55 UTC): pick card and /api/picks show "Market-implied win probability NN%" with the verified label on receipted two-way moneyline picks, N from the immutable signal snapshot bookmakerCount, under the confidence entitlement and never in the FREE branch, nothing on signal-slate, SPREAD or TOTAL picks (apps/web/lib/picks/market-implied-display.ts, tests market-implied-display.test.ts, pick-card-market-implied.test.tsx); the public claim is restated MONEYLINE-only but reworded to "The calibration we measure ourselves on is ..." because the public /calibration chart still buckets by confidence (apps/web/lib/calibration/report.ts:68), so the founder accepts the rewording or a later row re-scopes that chart to marketFairProb; FE-05/FE-10/FE-15 shipped in the same commit (preview page displaySelection and the ConfidenceBadge percent path under CALIBRATION_ADJUSTMENTS are follow-ups). CLAIMED 2026-09-06 03:50 UTC by the launch session on claude/sports-prediction-launch-rtiexc for the DISPLAY half only (pick card and picks API label, MONEYLINE-only claim copy); the IMPLEMENTED flip and the MODEL_VERSION v5.2.8 bump stay sequenced after the first clean NFL Sunday (2026-09-13) per the standing decision and the model-freeze guard; measurement side shipped fbc3784c7 and merged in cff3e72d7; label and claim text in docs/calibration-proposals/2026-09-05-market-anchored-display-probability-v5.2.8.md section 1; never write the banned four-letter betting word into copy or tests (trust-gate); signal-slate picks keep showing no percentage |
| C-109 | WP-30 The Odds API credit governor (decision 2026-09-06, delegated: the 20K plan must last the month, no tier change): (1) honor the existing spend guard in packages/ingestion-pipeline/src/settle-sport.ts:181-187, which today logs "paid scores fetch not justified" and proceeds, so paid getScores runs only when the free path reported NO_FINAL for that sport's overdue picks, and at most once an hour; (2) settle-picks is invoked five times an hour (the :20 cron plus the autonomy cycle RUN_FREE_SETTLE target at 7,22,37,52 via apps/web/lib/autonomy/execute-autonomy-cycle.ts:29), so the paid supplement must not follow that cadence; (3) refresh-odds iterates all seven SUPPORTED_SPORTS every 15 minutes (packages/data-ingestion/src/config.ts, apps/web/app/api/cron/refresh-odds/route.ts:121-123) including NBA, NCAAB and NHL out of season: skip a sport with no event in the next 48h on the free ESPN scoreboard; (4) pace to the plan: x-requests-remaining is already parsed (packages/data-ingestion/src/odds-api-client.ts:255), hold paid odds calls when remaining divided by hours to month end is below the current hourly rate; (5) surface remaining credits and projected exhaustion on the truth surface oddsInserting.dualPath; acceptance: steady state at or below 600 credits a day measured on the dashboard over 24h, unit tests for the pacing math, no change to MIN_BOOKMAKERS or any gate | claude | DONE | DONE 3359e072a (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): pure governor packages/data-ingestion/src/odds-credit-governor.ts (600 a day budget, hold below pace to UTC month end, one odds call per live sport per hour kept, one probe per sport per hour on a stale zero, header-less 0/0 never recorded), append-only credit ledger odds-credit-ledger.ts (JarvisMemoryEvent pattern), settle-sport.ts honors the spend guard with justification from the settle-picks route (apps/web/lib/odds/paid-scores-justification.ts) and at most one paid scores call per sport per hour across the cron and the autonomy cycle, refresh-odds skips a sport with no ESPN event in 48h (fetch failure never skips), truth surface oddsInserting.dualPath.credits; tests odds-credit-governor.test.ts, settle-sport.test.ts, refresh-odds.test.ts, paid-odds-governor.test.ts, paid-scores-justification.test.ts, settle-picks-paid-scores-governor.test.ts (164 passed); typecheck 0, lint 0, guardrails 26/26; adversarial review approved after one fix (zero-credit deadlock). Disclosures: paces to UTC month end while the invoice date is the 22nd (conservative late in the period); the once-per-hour marker is read-then-write, not atomic under true concurrency; governor-held sports report ok:true with note credit_governor_skip; the 600-a-day acceptance is measured on the dashboard over the first 24h post-deploy. Observed 2026-09-06 (browser agent, dashboard): 112 credits used between 02:41 and 03:02 UTC, about 320 an hour against 20,000 a month (645 a day); at that rate the key exhausts around 2026-09-08; at the schedule-implied rate (7 sports x 4 odds calls x 3 credits plus 5 x 7 x 2 score credits an hour, about 154 an hour, assuming the provider's documented per-call costs; confirm from x-requests-used deltas) around 2026-09-11, NFL Week 1 kickoff; Vercel logs 02:04 to 03:14 UTC show settle-picks runs at 02:07, 02:20, 02:22, 02:37, 02:52, 03:07. CODER PRIORITY 1, ship before 2026-09-08 00:00 UTC |
| C-110 | WP-28b single-book publish-time market probability (decision 2026-09-06, delegated): marketPFromOddsTable left 223 games unresolved as insufficient_books (one stored book at or before generatedAt); resolve them from that one book with the same proportional de-vig, tag pSource market_p_single_book, report it in pSources and bySource, and bump the streak basis tag to market_anchored_v2 so the streak restarts honestly on the new sample definition; the 263 no_rows games stay excluded; floors, bins (10 equal-width, apps/web/lib/calibration/ece.ts) and the streak length do not change | claude | DONE | DONE 23a0a3a0f (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): publish-time-market-p.ts resolves one real two-sided book with the same de-vig, pSource market_p_single_book (two or more books keep market_p_from_odds_table), unresolved reasons no_rows / no_usable_book / no_side with insufficient_books held at 0 for compatibility, loader stats resolvedSingleBook, MARKET_ANCHORED_P_BASIS market_anchored_v2 written by calibration-eligibility-durable.ts and compute-live-calibration-metrics.ts so the first v2 run (08:40 UTC) resets the streak once by design; tests publish-time-market-p.test.ts, market-anchored-calibration-sample.test.ts, calibration-drift-alert.test.ts (57 passed); typecheck 0, lint 0, guardrails 26/26; review approved with no blocking finding. Disclosure: production no_rows may read lower than 263 because rows with only non-book or one-sided quotes now count as no_usable_book. Truth surface 2026-09-06 03:02 UTC: n 223, Brier 0.1617, ECE 0.0553 with bootstrap 95% CI 0.0365 to 0.1142 (200 resamples), Murphy REL 0.0071; pSources proof_receipt 122, market_p_from_odds_table 101; exclusions three_way_market 120, non_moneyline_market 976, no_market_probability 486 (263 no_rows + 223 insufficient_books); by sport MLB n 175 ECE 0.0509, NCAAF n 35 ECE 0.131, NFL n 13 ECE 0.3006. At n 223 the floor sits inside the estimator's own interval; more real rows is the honest lever. CODER PRIORITY 4 |
| C-111 | Fixture confirmation guard (found 2026-09-06): three NCAAF game rows created from May listings (externalIds a6dfbee1…, e17a2675…, bcad21f3…; Ole Miss v Louisville, Illinois v UAB, Washington v Washington State, all dated 2026-09-05) do not exist on ESPN's 2026 schedule for that date (ESPN public scoreboard 20260905, 68 events, none present; 20260912 shows Duke at Illinois, Charlotte at Ole Miss, Utah State at Washington, Washington State at Kansas State), yet generate-signal-slate wrote MONEYLINE "model signal" picks on them at 15:54 and 18:53 UTC on 2026-09-05 and three v5.0.0 spreads from 2026-05-22/23 still sit published: (1) the slate and the board must not generate or publish a pick for a game absent from the day's free ESPN scoreboard for its sport; (2) a game row whose kickoff has passed with no ESPN fixture within 24h is marked CANCELLED and its picks VOIDED with RCA FIXTURE_NOT_FOUND through the outbox lane; (3) game rows older than 30 days re-confirm date and opponent against ESPN before any pick is generated | claude | DONE | DONE b4885f214 (2026-09-06 04:55 UTC, branch claude/sports-prediction-launch-rtiexc): packages/ingestion-pipeline/src/fixture-confirmation.ts (FixtureConfirmer, ESPN board per sport per cycle with all division groups, groups 80+81 for CFB and 50 for NCAAB, any failed group means not confirmable rather than not listed) wired into generate-signal-slate.ts and process-sport.ts before any pick write; unlisted fixture skipped and logged (fixtureUnconfirmed counter), scoreboard failure skips the sport for the cycle, rows older than 30 days re-confirm and take ESPN's kickoff when it differs by more than 15 minutes; tests fixture-confirmation.test.ts, generate-signal-slate-guard.test.ts, process-sport.test.ts; typecheck 0, lint 0, guardrails 26/26; review approved after one fix (division groups). Disclosures: exact-name college matching depends on ESPN and the odds feed spelling displayName identically (watch fixtureUnconfirmed on the first NCAAF Saturday); the guard runs before line snapshots and enrichment so an unconfirmed game loses those derived writes for the cycle; two ESPN fetch batches per sport per cycle (process-sport and the slate); events.length at limit 300 not yet treated as truncation. The settlement-side void of the six phantom picks rides on C-106. 6 of the 16 overdue picks at 2026-09-06 03:10 UTC are this cohort (read-only SQL: picks joined to games, result PENDING, commenceTime older than 6h); ESPN public scoreboard queries 2026-09-06 03:20 UTC; plan 3f item 2. CODER PRIORITY 3 (with C-106) |
| F-37 | BROWSER AGENT handoff (founder instruction 2026-09-06: no human step where a machine can do it): script A The Odds API billing (clear the invoice, no tier change, copy nothing); script B Vercel Production env (HEALTH_ALERT_WEBHOOK_URL, SENTRY_DSN, redeploy; later PERFORMANCE_STATS_ENABLED and PRICING_PHASE after the preconditions); script C Stripe live mode (Terms URL, then STRIPE_TERMS_CONSENT_ENABLED, three missing webhook events, deactivate Payment Links); script D live checkout with the founder's card then refund; script E read-only verification | browser | DONE | DONE 2026-09-06 03:02 UTC (#709, report in plan 3e scripts and 3f): A DONE (key swap, redeploy 02:37 UTC), C DONE (Privacy URL set to /privacy, Terms already /terms, STRIPE_TERMS_CONSENT_ENABLED already true since Jul 12, webhook /api/webhooks/stripe now 10 events with invoice.paid and charge.refunded added, 5 Payment Links deactivated), E DONE (truth surface read 03:02 UTC); B alerting and D checkout SKIPPED by founder decision (no Slack, no Sentry, no checkout smoke); the two public flips remain for a later prompt after the receipt and C-107; LAUNCH_FINISH_LINE section 3e; supersedes the manual halves of F-15 F-18 F-19 F-20 F-22 F-29; never paste a secret into chat, docs or commits; never blank THE_ODDS_API_KEY before WP-27 deploys |
| F-33 | FOUNDER: close hermes/settlement-token-fix after PR #707 lands; its containment minimum of 3 still lets "chi" match "kansascitychiefs" and it conflicts with 6880f18 (minimum 4, exact abbreviations, bipartite sides, NFL regression cases) | founder | OPEN | git merge-tree of the two branches reports a content conflict in apps/web/lib/data-sources/free-settlement.ts |
<!-- LEDGER:END -->
