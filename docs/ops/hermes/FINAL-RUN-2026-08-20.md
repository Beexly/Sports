# FINAL RUN — the queue that ends at DONE

Source of authority: the founder's CEO-agent charter (2026-08-20, uploaded)
and its 14-item Definition of Done. This queue maps every remaining item to
an owner. **Hermes: work this top to bottom and DO NOT STOP between tasks.
The stop conditions are exactly two: (1) every H-F task below is DONE or
BLOCKED-on-founder, or (2) your runner hits its iteration cap — in which
case the founder relaunches you with the one-liner at the bottom and you
resume from the ledger, exactly where you left off. Never ask what to do
next. Never wait for a merge to start the next task.**

Standing rules (unchanged, non-negotiable): branch per task from latest
`origin/claude/cron-config-placement-verify-qsl19t`, push feature branches
when each task's guards pass — do NOT wait for permission to push feature
branches on this run — never push main, guards with real exit codes
(`npx tsc --noEmit -p apps/web`; `node scripts/guardrails/trust-gate.mjs`;
`node scripts/ops/check-agent-ledger.mjs`; check `$?`), sealed paths
untouched, mandatory metric language from the charter's HONESTY GATES
section verbatim, report only what your commit diff proves.

## Definition-of-Done scoreboard (do not redo the DONE items)

| # | Item | State | Owner of remainder |
|---|---|---|---|
| 1 | Redeploy from latest main | PENDING | browser + founder |
| 2 | /fable dashboard: calibration curve, Brier decomposition, BSS, ECE caveat, Kill Ledger links | PARTIAL — pieces exist on separate routes | **H-F1** |
| 3 | BookGrade live, totals-only | merged to main; needs deploy | deploy only |
| 4 | PulseScore live | merged to main; needs deploy | deploy only |
| 5 | Glass Ledger + chain UI | BLOCKED on F-9 (founder schema review) | founder, then Claude issues the task |
| 6 | Receipts + Verify on every pick | merged; receipts always-on | deploy only |
| 7 | Phase-tagged archive writing | wired + flag set; confirm rows post-deploy | **H-F7** (verify only) |
| 8 | NFL ingestion incl. preseason | regular-season live; preseason key unmapped | **H-F3** |
| 9 | Stripe live test checkout | PENDING | founder |
| 10 | Machine-verifiable zero-affiliate pledge page | F-6 decided → unblocked | **H-F2** |
| 11 | Terms/Privacy/RiskDisclosure | DONE (45d3f1f7, bd60fc71) | — |
| 12 | SEO/JSON-LD complete | mostly done; audit new pages | **H-F6** |
| 13 | Daily honest-record posts | content engine exists, unwired | **H-F4** |
| 14 | Real-data MVE, binding outcome | founder-ordered (F-10) | **H-F5** |

## H-F1 — /fable proof dashboard section (`hermes/hf1-fable-dashboard`)

Add a "Proof Dashboard" section to the existing public /fable page: render
the live calibration curve data (reuse loadPublicCalibrationReport), Brier
decomposition (REL/RES/UNC — murphy machinery exists in
lib/calibration/), Brier Skill Score vs base rate, the model ECE from the
durable artifact with THIS language verbatim: "This ECE largely measures
the market's calibration through our confidence echo. It is not evidence
of independent skill." Render the LIVE value, never hardcode 0.0044. Link
cards to /calibration, /kill-ledger, /bookgrade. Honest-empty states where
data is not yet published (readiness gates stay untouched). Tests: section
renders, caveat present, no hardcoded ECE literal.

## H-F2 — Zero-affiliate pledge page (`hermes/hf2-pledge`)

Route `/pledge`: the dated pledge ("Galaxy Sports Edge does not carry
sportsbook or DFS affiliate or commission links, and never will —
published 2026-08-20"), what it means, why it exists (a company paid when
its users lose cannot credibly claim unbiased picks), violation clause
("any violation will be published on this page within 24 hours").
Machine-readable: `/api/pledge/affiliate-free` returning `{pledged: true,
category: "sportsbook_dfs_affiliate", since: "2026-08-20",
enforcement: "guardrail affiliate-structural-separation runs on every
commit"}` — read the posture, do not modify the guard. Footer + sitemap.
Tests: page + endpoint + no banned words.

## H-F3 — NFL preseason ingestion (C-36) (`hermes/hf3-preseason`)

The Odds API serves preseason under `americanfootball_nfl_preseason`. Our
48 August NFL games are seeded under `americanfootball_nfl` from ESPN, so
add a key-mapping: fetch the preseason key, map incoming events onto
existing games by team pair + commence time (the espn-schedule-seed
matching helpers exist), tag rows normally. Season-window: preseason key
active July–August only. Do NOT add it to SUPPORTED_SPORTS as a new
board sport — ingestion-only. Note the hard expiry (~Aug 30): if it can't
land cleanly in one pass, mark BLOCKED and move on — it self-expires.

## H-F4 — Daily honest-record post generator (`hermes/hf4-honest-posts`)

Wire the existing content engine to emit one DRAFT post per day from real
data only: yesterday's settled record (wins/losses/pushes from the DB, or
"no settled picks yesterday" honestly), one Kill Ledger rotation feature,
one BookGrade/PulseScore highlight with its interpretation line. DRAFTS
ONLY — the draft-only guard is law; publishing stays founder-gated. Tests:
generator produces a draft with zero banned phrases, refuses to fabricate
when the DB has no rows.

## H-F5 — THE MVE (founder-ordered, F-10, ONE cycle) (`hermes/hf5-mve`)

You have DB access; this is yours. Run the charter's spec exactly:
241 clean-close MLB totals games; m_t = Shin no-vig over prob from
cross-book median at entry; PRIMARY window 6–3h (others diagnostic only —
one bet per game, no dependence inflation); hierarchical outcome model for
total runs from PRE-GAME data only (team rolling metrics via MLB Stats
API, SP FIP/recent form, bullpen 3-day usage, park, Open-Meteo forecast,
umpire zone history, rest/travel) — reuse the R-9 NB machinery, walk-
forward, never trained on a game's own future; q_t from the predictive
distribution; fractional e-process λ=0.3:
E_t = 1 + 0.3·(Y_t·q_t/m_t + (1−Y_t)·(1−q_t)/(1−m_t) − 1), capital = ΠE_t
chronologically. Report: final capital, max drawdown, threshold crossings
at 2/5/10/20, the full path in RESULTS.md. BINDING OUTCOME, pre-registered
here before you look: capital ≤ 2 at the end → the kill is final — write
the fifth Kill Ledger entry draft (page copy PR'd on the same branch) and
the edge program is closed for good; capital > 20 → write a prospective
pre-registration draft (frozen rules, checkpoint cadence, certification
and kill thresholds) — do NOT open the track, the founder signs it;
anything between → publish the path with the verdict "did not certify,
did not survive" and the program closes exactly as if killed. No middle
state persists. ONE cycle. No reruns, no tuning after seeing the path.
If MLB Stats API/weather data is missing for early games, shrink the
window honestly and report the real n.

STATISTICAL RULING v2 (supersedes v1; amended 2026-08-20 BEFORE any MVE
computation — no data has been looked at, so the pre-registration is
still amendable; v1's symmetric point-null design is retired unrun).
DeepSeek's corrected ASYMMETRIC fractional e-process is adopted,
verified by Claude, with one correction of our own: DeepSeek's formula
as written was over-side-only, which would be blind to under-side edges
and lose on them in expectation — fatal in a one-shot final experiment —
so the pre-registered PRIMARY is the side-adaptive orientation:

E_t = 1 + 0.3·(W_t·(q_bet/m_bet) + (1−W_t)·(1−q_bet) − 1)

where the bet side (over/under) is chosen per game from the model BEFORE
outcome (predictable; one bet per game), q_bet and m_bet are the model
and Shin-de-vigged market probabilities OF THE BET SIDE, and W_t
indicates the bet side hit. Side-selection rule (frozen, DeepSeek
round-3 confirmed PROVEN): q_t > m_t → OVER; q_t <= m_t → UNDER (ties
UNDER); under bets use q_bet = 1−q_t, m_bet = 1−m_t. Full frozen
pre-registration: docs/ops/edge/2026-08-20-mve-prereg-v2.md. Validity (verified algebra): the increment's
conditional expectation is linear in the true side-probability p and
is <= 1 at both p=0 and p=m_bet, hence on all of p <= m_bet — a
supermartingale under the composite null "the market's quoted probability
of each bet side is an upper bound on its true probability" — and every
increment is >= 0.7 > 0. Per-game orientation is a predictable function,
so this is ONE process, not two: no multiplicity.

Binding operational rules (DeepSeek spec, adopted): (a) entry price must
be book-quoted inside the 6-3h window, age <= 15 min, from >= 3 books —
games failing this are EXCLUDED and the exclusion count reported; (b)
checkpoints every 50 graded picks starting at n=50 (50/100/150/200/241);
certification = E >= 20 at a scheduled checkpoint; kill = E <= 0.10 at
any checkpoint; early abort = capital < 0.01 after 50 graded picks
(abort, publish the kill, close the program); (c) outcome mapping onto
F-10's binding branches: E >= 20 at a checkpoint → prospective
pre-registration DRAFT (template:
docs/ops/edge/2026-08-20-mve-prereg-v2.md) for founder signature; kill /
early-abort threshold or final capital <= 2 → the fifth Kill Ledger
entry and the program closes for good; anything else → "did not certify,
did not survive" and the program closes identically. Unchanged from v1:
all hyperparameters, hierarchy depth, and shrinkage FROZEN before the
walk-forward or updated strictly online — no empirical-Bayes fit over
the full series; the 6-3h window and lambda=0.3 fixed here before any
result — no other window, lambda, or e-process variant may be computed,
even as a diagnostic; result language states evidence against the
composite null above — that the model finds sides whose true probability
exceeds the DE-VIGGED market probability — and NEVER net-of-vig
profitability or "positive expected value": vig is not in this test; if
the prospective track opens, its certification null is the vig-inclusive
composite null per the round-2 protocol (b_i = 1/D_i).

## H-F6 — SEO/metadata audit pass (`hermes/hf6-seo`)

One pass over /kill-ledger, /bookgrade, /pledge (after H-F2), /fable:
metadata, OG, canonical, JSON-LD where the page type warrants it,
sitemap entries present. Fix gaps only; no redesigns.

## H-F7 — Archive liveness check (read-only, after the founder redeploys)

One query: `odds_line_snapshots` row count and per-phase counts, MLB and
NFL. Report numbers in the ledger row. If zero rows >24h after deploy,
mark BLOCKED with the finding — that means the env flag didn't bind.

## Build seat + independent verification (seat-swap rule)

The BUILD SEAT is held by whichever agent the founder launches on this
queue — as of 2026-08-20 that is GROK BUILD (launched `grok --sandbox
gse` in the repo; it reads CLAUDE.md/AGENTS.md/.grok natively and these
same H-F specs govern it verbatim; every "Hermes" instruction in the
task sections binds the build seat, whoever holds it). Hermes is the
fallback seat when Grok's included usage runs dry — same queue, same
ledger, zero handoff cost.

Independent verification (charter execution-loop step 4): a DIFFERENT
agent checks each build against its acceptance criteria, so Claude's
tokens are spent on merges and decisions, not line-by-line review. The
verifier never merges, pushes, or edits the ledger; verdicts are
advisory input to Claude's merge. WHOEVER builds a task cannot verify
it: if Grok builds, Hermes (relaunched for the audit) or DeepSeek
covers verification, and vice versa. For H-F5 (the MVE) the independent
audit is MANDATORY before merge: check the e-process arithmetic, the
frozen side-selection rule applied exactly, walk-forward causality (no
feature computed from a game's own future), the one-bet-per-game
discipline, and that the binding outcome rule was applied without
post-hoc modification.

## DeepSeek statistics lane (parallel, founder-run, advisory)

DeepSeek audits STATISTICS while Grok audits CODE — two independent
reviewers with different known failure modes (DeepSeek has mis-sorted its
own rank tables and confabulated citations; Grok inflates novelty), so
each covers the other's blind spot. DeepSeek never sees merge authority.
Assignments: (1) pre-draft BOTH MVE outcome documents BEFORE the result
exists — the Kill Ledger entry text for capital <= 2 AND the prospective
pre-registration template for capital > 20 — so the result cannot shape
the framing; (2) audit the finished MVE's statistics (e-process validity,
the fractional form's supermartingale property under the composite null,
drawdown interpretation, honest n); (3) on any public metric page, check
the interpretation guide states what the number does NOT measure. All
DeepSeek output is audited by Claude before adoption, as all session.

## Completion self-audit (run when every H-F task is DONE/BLOCKED)

Walk the 14-item scoreboard. For each: state DONE with evidence (SHA or
URL) or name the founder-gated blocker. Write the completion report to
docs/ops/hermes/FINAL-RUN-REPORT.md, update every ledger row, push. THEN
stop — that is the only earned stop.

## Founder relaunch one-liner (paste this any time Hermes's runner dies)

> Pull origin/claude/cron-config-placement-verify-qsl19t, read
> docs/ops/hermes/FINAL-RUN-2026-08-20.md and docs/ops/AGENT_LEDGER.md,
> find the first H-F task not DONE/BLOCKED, and continue the FINAL RUN
> without stopping between tasks.
