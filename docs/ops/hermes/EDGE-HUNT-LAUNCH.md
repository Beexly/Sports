# Hermes overnight — NFL H0 grunt (not kernel slots)

You are Ox-class grunt for Galaxy Sports Edge. Product is independent `p`, then `e = p − q`. Not website chrome.

Model may be `z-ai/glm-5.2:free` tonight: OpenRouter 404s `stealth/ox-alpha` on this account (data-policy / not in /models/user). Same job.

## Do not touch
- `C:\Users\Garrett\Sports` (junction checkout). Work only in this worktree.
- `.github/**`, prisma schema+migrations, Odds historical, scrape, DK/FD HTML, billing, keys.
- `grok/**` branches and PRs #546 #555 #556 #557 (other Grok session).
- Kernel `contract.ts` / `numeric.ts` / `conformance.ts`. Do not fill K1–K13 slots (fleet does that).
- Never `git push --force`. Never merge main. One PR per item on `hermes/<slug>` from origin/main.
- `priced: false`. Independent `p` uncontaminated. Never κ. Never last_price.

## H0 remaining (top-down). Skip anything already OPEN as grok/h0-*
Already on main: covariate bus, SEP, YAC, CPOE binds, kernel contract, masterplan, doctrine.
Already OPEN (do not steal): #555 harness CONFLICTING, #556 TPRR green, #557 kneel green.

Next slices, in order, one PR each:
1. NFL change-point / regime detector (leak-safe, week t for t+1). Fail closed.
2. Ladder + boost scanners (softness map) — detect, do not fire live `p`.
3. CLF v0 (calibration layer, shrinkage) — not a tout score.
4. Incentive + rule-change calendar as covariates, not copy.
5. RZ-share on top of Dirichlet (K11 is fleet; you bind it only if slot already on main).
6. Remaining process covariates from PROP_COVARIATE_GAP that are not already bound.

If a file fails 2 times, BLOCKED that file — revert, record the exact error, skip to next. Never a third attempt. Never idle. Never invent Odds remaining. No historical 10×.

## Verify
`cd packages/prediction-engine && npx vitest run <your tests> && npx tsc --noEmit`, then
the full repository gate before any handoff: `npm run lint` and the repo
typecheck command from the repo root, and a successful build. Completion
requires tests, types, and a successful build — not package-scoped checks alone.

Commit locally on `hermes/<slug>`, stop that item, start the next. Never
`git push` — hand the branch to the owner for review and push. YOLO tool use.
ASCII logs.
