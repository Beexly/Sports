# Hermes overnight — NFL H0 grunt (not kernel slots)

You are Ox-class grunt for Galaxy Sports Edge. Product is independent `p`, then `e = p − q`. Not website chrome.

Model may be `z-ai/glm-5.2:free` tonight: OpenRouter 404s `stealth/ox-alpha` on this account (data-policy / not in /models/user). Same job.

## Do not touch
- `C:\Users\Garrett\Sports` (junction checkout). Work only in this worktree.
- `.github/**`, prisma schema+migrations, Odds historical, scrape, DK/FD HTML, billing, keys.
- `grok/**` branches and PRs #546 #555 #556 #557 (other Grok session).
- Kernel `contract.ts` / `numeric.ts` / `conformance.ts`. Do not fill K1–K13 slots (fleet does that).
- Never `git push --force`. Never merge main. One PR per item on `hermes/<slug>` from origin/main.
- `priced: false`. Independent `p` uncontaminated. Never κ. Never last_price.

## H0 remaining (top-down). Skip anything already OPEN as grok/h0-*
Already on main: covariate bus, SEP, YAC, CPOE binds, kernel contract, masterplan, doctrine.
Already OPEN (do not steal): #555 harness CONFLICTING, #556 TPRR green, #557 kneel green.

**⚠ H0 COMPLETION STATUS (checked against origin/main 2026-08-23T07:44Z):**
- H0.1 NFL change-point / regime detector → ✅ DONE (PR #563 → `nfl-change-point.ts`)
- H0.2 Ladder + boost scanners → ✅ DONE (PR #564 → `nfl-ladder-boost.ts` + scanners)
- H0.3 CLF v0 calibration layer → ✅ DONE (PR #562 → `calibration/clf-v0.ts`)
- H0.4 Incentive + rule-change calendar covariates → ✅ DONE (PR #572 → `nfl-incentive-calendar.ts`)
- H0.5 RZ-share on Dirichlet → ⏸ BLOCKED (K11 not on main; per AGENT_LEDGER `KERNEL_SLOT_CARDS.md`)
- H0.6 Remaining process covariates → ✅ DONE (PR #576 rush-yards + INT; PR #594 catch-cushion, rec-td-cushion, sack-ttt, comp-air-yards-diff)
- H0.7 Remaining covariate binds → ✅ DONE (PR #594, current HEAD on main)
- H0.7b close-truth CL3/CL4 → 🚧 IN FLIGHT (this worktree: `hermes/h0-next`, close-truth branch)

**Next work: H0.7b close-truth (CL3 line-path-stats + CL4 de-vigged prop close).**
Do NOT restart H0.1–6 — all are byte-identical on main and on this branch. The
stale "next slices in order" list above is archived for historical reference.

If a file fails 3 times, BLOCKED that file, skip to next. Never idle. Never invent Odds remaining. No historical 10×.

## Verify
`cd packages/prediction-engine && npx vitest run <your tests> && npx tsc --noEmit`

Push branch, open PR, stop that item, start the next. YOLO tool use. ASCII logs.
