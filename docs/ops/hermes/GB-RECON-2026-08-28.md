# Green Board Dispatch — Reconnaissance (GB-RECON-2026-08-28)

**Branch:** `hermes/green-board-1` off `origin/main` @ `bb0e7dfc0`
**Date:** 2026-08-28
**Author:** Hermes
**Status:** Complete — foundation for GB-1..GB-6 + D-1..D-5
**Source dispatch:** `docs/ops/hermes/GREEN-BOARD-DISPATCH-2026-08-28.md` (read from
`origin/claude/fable-5-registry-sync-ejp6pf` @ `d87e458fe`; the file is NOT on
`hermes/green-board-1` itself — this recon brings the spec in via this document
and the subsequent GB-* commits)

---

## 1. Existing state on `hermes/green-board-1` (verified live, 2026-08-28)

### 1.1 GB-1 — predicate ALREADY implemented (with gaps to close)

- `packages/prediction-engine/src/green-board.ts` exists, exports match the dispatch
- `packages/prediction-engine/src/green-board.test.ts` exists with 6 tests
- Signature: `greenBoardEligible(input: GreenGateInput): GreenGateResult`
- Tier logic: `null` → "GREEN" (p≥0.70) → "PRIME" (p≥0.80)
- Reasons: failed-gate names ("G1".."G4"); `["GREEN"]` on success
- G3 semantics: `ind < calibratedP - INDEPENDENT_DISSENT_BAND` (strict > 6pts
  gap = veto; at exactly 6pts, no veto — matches the dispatch's `> 6pts` language)
- **Gaps to close in GB-1:**
  - `PRIME_P_MIN = 0.80` is buried inside the function body, not exported
  - Module-level JSDoc is missing on the file, function, and gates
  - Test coverage gaps:
    - PRIME gate-fails → `tier: null` (e.g. `{calibratedP: 0.85, bookmakerCount: 1}` → tier null)
    - All-four-fail reasons shape: `["G1","G2","G3","G4"]`
    - FPI and Poisson dissent triggers (existing test only covers elo)
    - Input validation: NaN, out-of-range, non-finite values
  - No inline provenance comment pointing to GB-2's `vetoFlags` vocabulary

### 1.2 GB-2 — SITREP vetoes NOT implemented — **DATA FIELDS DO NOT EXIST**

The dispatch requires GB-2 to extract four flags from "existing covariates":
- `REST_DEFICIT` (rest differential ≤ −2 days)
- `QB_OUT` (starting QB out/doubt from injuries data)
- `HIGH_WIND_TOTAL` (wind > 20mph)
- `REVERSE_LINE_MOVE` (line moved opposite price-implied direction)

**Verified absent on this branch (no exact matches):**
```
search "wind|injuries|qbStatus|qbOut" in packages/prediction-engine/src  → 0 hits
search "wind|injuries|qbStatus|qbOut" in apps/web/lib                    → 0 hits
```

The only related field is `restAdvantageScore` (–10 to +10) in
`packages/prediction-engine/src/game-context.ts`, but it is a *score*, not a
day-differential. No `wind`, no `injuries`, no `qbStatus`, no opening/current
snapshot pair stored on a row anywhere the prediction engine reads.

**GB-2 BLOCKER:** The dispatch says "pure extraction from existing covariates,"
but the covariates do not exist. This is a real blocker, not a missing-piece-
I-can-derive-from-surrounding-code. Per the two-strike rule, I will:
- **Strike 1 (GB-2 attempt):** design `SitrepVetoInput` with the four flags
  and a thin wrapper around the rest-score conversion (the only derivable
  field). Ship as the v1 minimum; document the missing three as honest
  "absent = no flag" in provenance.
- **Strike 2 (if founder still wants real data):** call out the data-path
  requirement explicitly in the BLOCKED note. Cannot synthesize the
  injuries/wind/line-snapshot data — the existing machine does not carry it.
- This is an honest BLOCKED, not a fabricated "DONE."

### 1.3 GB-3 — read-side lane NOT implemented; route found

- Route: `apps/web/app/api/picks/route.ts` (328 lines, rate-limited 60/min)
- Page: `apps/web/app/picks/page.tsx` (calls route via direct handler import)
- The route already uses `passesPublicSelectiveFilterAsync` from
  `@/lib/calibration/selective-publish-runtime` and a `confidence: desc` orderBy
- **Implementation path:** add a `?lane=green` query param; the handler runs
  `greenBoardEligible` over each pick's `confidence` (used as calibratedP
  proxy) + per-sport bookmaker count (via `proofReceipt.oddsCount` or
  per-pick) + freshness (already gated upstream by `isPublicPicksSurfaceStale`)
  + independent votes (need to look up `PickSignalSnapshot` if present)
- **P-0 dependency in code comment:** `calibratedP` here is `pick.confidence`,
  which per the schema (`PickProofReceipt.confidence`) is "heuristic, NOT a
  prob." Until P-0 lands (clean de-vigged consensus), the GREEN lane is
  computed against a degraded number. Document loudly in the route.

### 1.4 GB-4 — retro script NOT implemented — **VERIFIED DEVIG PATH MISSING**

- Verified path the dispatch mandates: `docs/ops/calibration/2026-08-28-res-night-verified/`
  - **NOT on this branch** (only on `origin/claude/fable-5-registry-sync-ejp6pf`)
- What exists here: `docs/ops/calibration/2026-08-19-l9-clv-slices/` (909 graded
  picks, per-market beat rates — that is the L-9 honest record, not the
  GB-4 retro)
- `docs/ops/calibration/2026-08-19-l10-provider-probes/` (provider-level probes)
- **Pre-flight decision required:** either merge the verified branch to bring
  `res_backtest.py` here, or find an equivalent verified devig path on
  `bb0e7dfc0`. The retro cannot run honestly without it.

### 1.5 GB-5 — page NOT implemented; shared lib extraction point identified

- The "record ticker" must be a shared function (`packages/prediction-engine/src/record-ticker.ts`
  candidate) consumed by both the retro script and the public page
- The page itself goes at `apps/web/app/green/page.tsx` (server component)
- Entitlement gating: read `auth()` then `getUserEntitlements(userId)` —
  existing helpers, no new logic needed
- Empty state copy: "no green today — the gates said no, that is the product
  working" — must pass the existing `no-unsupported-performance-claims`
  guardrail (it has no win-rate claim, so it should pass; will verify in GB-5)

### 1.6 GB-6 — guardrail vocabulary — **AGENTS.md conflict to resolve**

- Existing guard: `scripts/guardrails/no-unsupported-performance-claims.mjs`
  (349 lines). Forbidden list at lines 77-91. Pattern list at lines 48-63.
  Test file: `no-unsupported-performance-claims.test.mjs` (93 lines).
- **Conflict:** AGENTS.md (line 51-52) says "NEVER modify: ... scripts/guardrails/** ..."
  The dispatch says to "extend the claims guardrails ... with an ALLOWED
  vocabulary ... performance-claim bans stay fully intact."
- **Resolution:** the dispatch is a founder-issued task spec; the AGENTS.md
  "NEVER modify" list is a default-deny policy. Add the ALLOWED vocabulary
  additively (new patterns in a new constants section), never remove from the
  forbidden list. Record the dispatch as authorization in a code comment
  on the new section header. This is consistent with the standing
  founder-directive-overrides-AGENTS-default pattern (per memory).

---

## 2. Phase D — web surface state (verified live)

### 2.1 D-1 — Nova equivalent identified

- **Component:** `apps/web/components/explainers/page-explainer.tsx` exports
  `PageExplainerAuto` — described in its own header as "Nova's 'how this
  page works' guide, on every registered page." Launcher: bottom-5 left-5,
  "Guided by Nova · {durationLabel}."
- **Mounted in:** `apps/web/app/layout.tsx` (root layout) — see the import
  and the body-mount in the layout. **The dispatch's D-1 = unmount this.**
- **Replacement:** `components/welcome-video.tsx` (new) — poster frame +
  play button; renders poster state when no video asset.
- **Keep the file:** the dispatch says "Keep the component file if other code
  imports it." Grep needed before deletion decision (the explainer registry
  in `lib/explainers/registry` likely references it). Defer the keep/delete
  decision to D-1 execution.

### 2.2 D-2 — current IA = 4 doors, dispatch says 5

- **Current nav (`apps/web/components/ui/nav.tsx`):**
  1. Board ▾ (Today's Picks, Today's Board, The House, Mission Control, Daily Briefing)
  2. The Lab (direct link, single)
  3. Intelligence ▾ (Intelligence Engines, Galaxy Twin)
  4. Fantasy & Daily ▾ (Draft Assistant, Best Ball, Start-Sit, Waivers, Trades, Optimizer, DFS, Pick'em)
  5. GSN ▾ (The Beat, The Studio, The Academy)
  6. Proof (direct link)
- **Comment at line 7-14 of nav.tsx:** "Four doors, not ten. The 2026 IA
  condenses every public surface into four primary doors... plus two prominent
  standalone links (The Beat, Proof)."
- **Dispatch's 5 doors:** Today's Picks · The Record · How We Prove It ·
  Pricing · Learn. The proof, record, and how-we-prove-it are currently
  *spread across* the existing Board, Proof, and GSN menus. This is a
  larger IA shift than the 4→5 suggests — it's "consolidate three
  credibility surfaces into one door" + "make Today's Picks the single daily
  door" + "introduce a new Learn door for Math Room."
- **159 public page.tsx files** (excluding admin/account/dashboard/cockpit/
  api/fantasy/intelligence/embed/auth paths). D-2 requires mapping every
  one to a door or a redirect. The simplify directive (5-second test,
  ≥50% word cut on touched pages) means a lot of pages move *behind* the
  doors as lenses, not as nav items.

### 2.3 D-3 — copy diet, no infrastructure

- Existing trust-gate: `no-unsupported-performance-claims.mjs` already scans
  the public surface. After GB-6 extends the ALLOWED vocabulary, D-3 copy
  rewrites can use band-definition language without tripping the guard.
- Flesch-Kincaid ≤ 7 on public surfaces: no test infra exists. Need to
  add a small `scripts/ops/copy-readability.mjs` + a test fixture.

### 2.4 D-4 — type & contrast

- **Current body size:** `--t-body: 400 15px/1.55 var(--f-body)` in
  `apps/web/styles/design-tokens.css:259`. Dispatch says ≥17px.
- **`--t-body-lg` already exists at 17px** (line 258) — the bump can be
  `15px → 17px` on `--t-body` OR rebind the body rule to `--t-body-lg`.
- Contrast: design tokens already document WCAG AA ratios for every text
  color (lines 26-41, 44-50). The "audit" is a verification task, not a
  fix-it task. Need a `scripts/ops/contrast-audit.mjs` to assert ratios
  per token pair.

### 2.5 D-5 — palette proposals

- Three previews as flag-gated routes (env var, e.g. `PALETTE_PREVIEW_ENABLED=true`).
- Each shows the same homepage mock under a different palette.
- No commit until founder picks — this is a deliverable presentation, not
  a state change.

---

## 3. Found files (the lookup table for every other task)

| What | Path | Lines | Notes |
|---|---|---|---|
| Predicate (GB-1) | `packages/prediction-engine/src/green-board.ts` | 35 | Already exists |
| Predicate tests | `packages/prediction-engine/src/green-board.test.ts` | 68 | Already exists |
| Pick list route (GB-3) | `apps/web/app/api/picks/route.ts` | 328 | Rate-limited 60/min |
| Pick list page | `apps/web/app/picks/page.tsx` | 802 | Calls route directly |
| Selective-publish filter | `apps/web/lib/calibration/selective-publish-runtime.ts` | ? | Used by /api/picks |
| Performance-claim guard (GB-6) | `scripts/guardrails/no-unsupported-performance-claims.mjs` | 349 | Forbidden list L77-91 |
| Guard tests | `scripts/guardrails/no-unsupported-performance-claims.test.mjs` | 93 | Fixture-driven |
| Layout (D-1 mount) | `apps/web/app/layout.tsx` | ? | Mounts PageExplainerAuto |
| Nova equivalent | `apps/web/components/explainers/page-explainer.tsx` | 181 | "Guided by Nova" |
| Nav (D-2) | `apps/web/components/ui/nav.tsx` | 118 | 4 doors + 2 standalones |
| Design tokens (D-4) | `apps/web/styles/design-tokens.css` | 376 | `--t-body` is 15px |
| Home page | `apps/web/app/page.tsx` | 404 | 4 hero + many sections |
| Game context (GB-2) | `packages/prediction-engine/src/game-context.ts` | ? | Has `restAdvantageScore` only |
| Verified devig path (GB-4) | `docs/ops/calibration/2026-08-28-res-night-verified/` | n/a | **NOT on this branch** |
| L-9 CLV results | `docs/ops/calibration/2026-08-19-l9-clv-slices/` | 909 picks | Honest internal record |
| SECURITY-POSTURE-2026-08-28 | `docs/ops/hermes/SECURITY-POSTURE-2026-08-28.md` | 276 | Pushed earlier today |
| AGENT_LEDGER | `docs/ops/AGENT_LEDGER.md` | 218 | H-SEC-0 DONE @ d603d7f87 |

---

## 4. AGENTS.md conflicts surfaced by this recon (to be resolved per task)

1. **scripts/guardrails/** — NEVER modify per AGENTS.md; GB-6 needs to extend.
   Resolution: additive-only changes, dispatch as authorization, never remove
   from forbidden list. Will be recorded in code comment at the new section.

2. **.gitignore** — NEVER modify per AGENTS.md. D-2/D-4 may need to add
   `screenshots/` and `playwright-report/` patterns. These are already in
   .gitignore per the Aug-28 read (line 110-115), so no change needed.
   Good.

3. **package-lock.json** — NEVER modify. GB-1..GB-6 + D-1..D-5 should not
   require any package adds. D-5 palette proposals are token-only.

4. **schema.prisma / migrations/** — NEVER modify. GB-1..GB-6 are pure logic
   + read-side; no schema changes. GB-3's lane filter is read-only.

5. **Push authority** — AGENTS.md default is "never push unless explicitly
   told." Founder's standing directive (per memory) is "push after every
   cycle's commit." Resolved: push to `hermes/green-board-1` only, never
   to `main`, after every commit.

---

## 5. Execution order with pre-flight decisions

1. **GB-1** — verify + close gaps (PRIME export, JSDoc, 4 new tests). Verify
   block must pass. **Pre-flight: none.**
2. **GB-2** — design `SitrepVetoInput`; v1 with rest-score (only derivable
   field) + 3 honest-miss placeholders. Add 8-10 tests. **Pre-flight:
   BLOCKED note if rest-score mapping is ambiguous after 2 attempts.**
3. **GB-6** — read `no-unsupported-performance-claims.mjs` first; design
   additive ALLOWED section; add forward-looking vs backward-looking tests.
   **Pre-flight: AGENTS.md conflict resolution comment.**
4. **GB-3** — find the read-side fields (`calibratedP` resolver,
   `bookmakerCount` source, `freshnessOk` source, `PickSignalSnapshot`
   location). Wire the lane filter. **Pre-flight: P-0 dependency comment.**
5. **GB-4** — **pre-flight decision required: merge
   `origin/claude/fable-5-registry-sync-ejp6pf` to get the verified
   devig path, OR find an equivalent on `bb0e7dfc0`, OR BLOCK with
   the exact error.** Then build the script, run it, write RESULTS.md.
6. **GB-5** — extract the shared record-ticker lib; build the /green
   page; entitlement gating; tests. **Pre-flight: GB-4's lib.**
7. **D-1** — unmount PageExplainerAuto from layout; build
   `components/welcome-video.tsx`; tests + screenshots.
8. **D-2** — full route→door mapping; rewrite Nav to 5 doors; build
   Proof Stack hero; one-plank motifs. Tests + screenshots.
9. **D-3** — copy rewrites on touched pages; Flesch-Kincaid test.
   Trust-gate + check-claims must pass. Screenshots.
10. **D-4** — bump `--t-body` to 17px; contrast audit; animation
    review. Tests + screenshots.
11. **D-5** — 3 palette preview pages, flag-gated, no commit until
    founder picks.
12. **Final** — push; file PR; Claude clean-room verify of GB-4.

---

## 6. Honest gaps (where this plan is NOT confident)

- **GB-2 will likely BLOCK after 2 attempts.** The data fields genuinely
  do not exist. The honest output is BLOCKED + a clear "you need a data
  source for wind/injuries/line-snapshots" message, not a fake "DONE"
  with empty-flag defaults.
- **GB-4 depends on a branch merge or an equivalent path.** I do not
  yet know if `origin/claude/fable-5-registry-sync-ejp6pf` has anything
  that conflicts with `hermes/green-board-1`. The merge may surface
  conflicts that need founder input.
- **D-2 is the largest single task.** 159 pages mapped to 5 doors is
  a half-day of work, not a one-commit change. The recon is honest
  about that; the plan above does each door as a separate touch with
  a separate commit, but no test can verify "5 doors" without
  re-running the full nav test. I will design the test first.
- **D-5 is a no-decision deliverable** (the decision is the founder's).
  I will build the previews and stop, not push.

---

## 7. The end state we are building toward

Per the doctrine, the Green Board is a 70% selective board whose every
picked line is:

- **Calibrated** (clean de-vigged consensus, after P-0)
- **Receipted** (cryptographic hash chain, public verify)
- **Interval-aware** (the rate ships with its Wilson CI)
- **Situated** (SITREP dossier explains the gate decisions)
- **Audited** (every loss has a public autopsy)

GB-1..GB-6 + D-1..D-5 is the minimum loud product. The retro (GB-4) is
the diagnostic that proves the system can hit 70% historically. The
public record starts at zero on launch day. The retro never appears on
any public surface.

This recon is the foundation. Next: GB-1.
