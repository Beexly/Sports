# Platt scaling + Brier decomposition

Platt MAP IRLS: `platt-scaling.ts`. Murphy: Brier = REL − RES + UNC.
Live RES≈0.002 blocks PROVEN. Bake-off writes `calibration-map-bakeoff.json` from cron.
Apply OFF until ranking improves.
