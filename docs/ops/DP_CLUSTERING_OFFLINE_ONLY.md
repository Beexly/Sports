# Dirichlet process clustering — offline EDA only

DP / HDP regime discovery is **not** on the production calibration apply path.
Reasons: label switching, frozen-map versioning, MCMC cost, PROVEN audit trail.

Use fixed `sport|market` keys + **EB τ** hierarchical intercepts instead.
Optional research notebooks may cluster; never wire DP into cron artifacts as SUCCESS maps.
