# Research audit — partial, not complete

## Scope and provenance

User requested independent research of every topic in spreadsheet `1Gv3hFGPEdgez1uK87jNmsnGLwXyJVodhurYa9qle1AU`, including all four exported tabs, and repository improvements backed by tests. Direct anonymous export returned HTTP 401. Opening the authenticated XLSX export in existing Chrome succeeded; fresh download `gse_research_analysis (1).xlsx` yielded SHA-256 `a418ffb72c8d1db341da1b062758028584eae0759aa537a23af75cc909df1e5b`. Parsed snapshot: `live-export.json`. The earlier local exports omitted two tabs and were not sufficient on their own.

Ranked and triage tabs contain the same 1,343 distinct literal IDs. Original tiers (not independently validated relevance judgments): High 58, Medium 402, Low 882, Unresolved 1. All 58 High IDs match the dossier tab. Build Plan has 24 nonempty rows after its heading, including context/headings rather than 24 independently actionable recommendations. Its mention of approximately 50 additional Pass-3 IDs is NOT an inventory of those IDs; their absent referenced separate document remains a reconciliation gap. Never claim those citations are reviewed.

## Completed layer: metadata, not research completion

`coverage.json` resolves 1,343/1,343 IDs against the official arXiv API. Raw XML is retained under `arxiv-metadata/`, with hashes in each metadata record. One HTTP429 batch succeeded on a later retry. The initially unresolved source row `cs/0210021` resolved; missing source title caused a reproducible KeyError, repaired without changing the literal ID. Metadata flags: 40 normalized-title mismatches needing contextual review; 3 withdrawal notices needing source-level disposition; 1 source claim of unresolved metadata contradicted by retrieval. A title change alone does not establish fabrication.

Confirmed withdrawal comments: 2312.11067, 2503.08883, 2412.18204. FightTracker's short-window profit result is explicitly disclaimed by its author after longer evaluation. The build-plan tab already warned about this; do not portray every correction as newly discovered by us.

## Deep source/code reviews delivered

Eight reports exist: 1701.05976, 2208.08598, 2608.12291, 2311.03490, 1710.02824, 2008.01485, 2501.02505, 2406.19563. They contain source citations, exact equations/results, availability checks, existing-code mapping and new-build deltas. They are NOT eight reproduced experiments or independently checked mathematical proofs. The per-row machine registry deliberately still says NOT_REVIEWED until review acceptance is recorded, rather than silently inferring completion from file presence.

Material findings needing implementation judgment:

- 1701.05976 models Gaussian observations of normalized-moneyline LOGITS, not normal score margins. Main sampler settings are 40k draws after 4k burn-in, not the dossier's 20k/2k. Public driver references missing/restricted dependencies. Existing GSE outcome-likelihood particle filter is related but not the paper model.
- 2208.08598 uses augmented-refit signed-residual full conformal predictive distributions, not a frozen generic calibration wrapper. The dossier omits the candidate's own residual/tie term. Conformal coverage is not calibrated point probability or proof of predictive edge.
- 2608.12291 is originally Low tier, not a High dossier. Optimal stopping assumes a true posterior martingale and specified costs; it does not establish a useful pregame lock policy or CLV uplift.
- 2311.03490's grouped/full-model bootstrap and decision stability are not equivalent to resampling already-fitted calibration predictions. Existing bootstrap-calib-ci.ts is IID; a separate stationary bootstrap module exists.
- 1710.02824 studies cross-book price discrepancies, not a CLV-versus-Brier relationship. Its reported monetary totals and live ROI require careful reconciliation, not unconditional endorsement.
- 2008.01485 studies economic crowd forecasts, not sports-market efficiency; it does not support the build plan's cited inference.

`deep/clv-local-diagnostic.py` and JSON document no usable joined local rows for CLV + locked model probability + settled outcome within the inspected artifacts. That diagnostic is NOT_RUN, not a null association. No production DB export was performed.

## Real tests and branch distinction

Audit branch starts from `origin/main` at `2676759b0`; first audit commit `449434a76`. Original workspace was `hermes/last-plan-2026-09-15` at `992424b2c`; its test failures must NOT be automatically attributed to main. Fresh audit-branch targeted runs after dependency installation: ingestion publication/series tests 11/11 pass; metric-source-payload-rights 8/8 pass. Earlier no-deps invocations and an invalid reporter invocation were startup failures, not test outcomes. An initial npm-ci run failed ENOSPC; piping to tail masked its exit code. npm cache verify garbage-collected unused cache, and a subsequent unpiped npm ci succeeded (821 packages). Full baseline `npm test` finished with exit 1, recorded at `449434a76` in `test-suite.result.json`. The later saved web rerun also exited 1: 1,015 passed files, 9 failed, 12 skipped; 13,922 passed tests, 29 failed, 97 skipped. Its result JSON lacks command, HEAD and timestamps, and the worktree changed during the run: do not call this an exact-final-HEAD verification. No fresh full suite, typecheck, lint, build or complete guardrail battery was run in this second-look pass.

## Remaining acceptance criteria

Every ID needs independent relevance reassessment, primary-source claim review, limitations/data-rights checks, repo mapping, and an explicit disposition. Low-ranked and unresolved inputs are not excluded. Every actionable build-plan row needs its own falsifiable recommendation and dependency disposition. Empirical claims need executable artifacts and real available data; absent inputs produce NOT_RUN, never synthesized results. Any production change requires focused regression tests, repo guards, and preserved frozen model/gates. No betting, publishing, deployment, DB mutation, pricing, environment flags or frozen paths changed by this audit.

## Second-look audit: verified gaps and priority

Checked against branch HEAD `05a5ec3fd` before these documentation/checker changes. Remote main still matched base `2676759b0`; no rebasing or production changes were performed.

1. **Missed duplicate mathematical defect.** `apps/web/lib/calibration/cqr.ts:9-15` has a second `conformalQuantile` that still clamps rank to `n-1`. The correction in `conformal-calibration.ts` did not reach it. At n=5, alpha=0.1, the required augmented order statistic is +Infinity, not the observed maximum. `apps/web/__tests__/cqr.test.ts:5-9,15-23` explicitly expects/justifies the clamp. Its green result is not correctness evidence. Follow-up acceptance: a failing small-sample regression first, both implementations aligned, infinite CQR interval behavior tested, and a separate sufficiently large-sample test preserving legitimate negative-qhat interval shrinkage. Keep product flags OFF. Also review the empty/mismatched-input fallback at `cqr.ts:31-32`; it returns unchanged intervals with qhat=0, not evidence of calibrated coverage. The Mondrian comment in `conformal-calibration.ts:160` incorrectly calls an infinite threshold “always surprise”; an infinite threshold is non-informative, not automatic surprise.
2. **Correctness outranks copy-test cleanup.** The saved DFS failure group has 12 failing cases, including suboptimal lineups, invalid feasibility, and nondeterminism. This is distinct from stale copy. Diagnose against the existing brute-force oracle; do not change expected optimum or accept invalid slots to obtain green tests. Of the remaining eight failed files, entitlement-fail-closed-audible was freshly reproduced (four failures) and repaired by adding the missing customer `isAdminEmail: () => false` mock after checking upstream commit `34961f326`; all 16 tests then passed. No assertions or production auth code changed. The other seven files are not collectively cleared by that result.
3. **Evidence coverage is still shallow.** The offline verifier checked all 1,343 metadata records against hashes, version URLs and text fields in 34 saved XML files. Eight report files cover seven input-High papers and one input-Low paper. Thus 1,335 IDs have no deep report: 51 High, 402 Medium, 881 Low, 1 Unresolved. The earlier approximately 456 Medium handoff figure was wrong. All registry review states remain NOT_REVIEWED; `summary.json`'s zero completed deep reviews is an acceptance-state claim, not the number of delivered reports. Add explicit report links and acceptance states rather than infer scientific approval from file presence.
4. **Build-plan recommendations need a row-level decision record.** The cheap iWinRNFL and Poisson diagnostics, bootstrap-gate experiments, and selection-bias diagnostic remain NOT_RUN. Build Plan cells A15/A22 are contradicted by the rank reports: rankings/ties do not replace absolute event probabilities or confidence gates. A19's sliding window does not by itself restore exchangeability. A16/A27's CLV/Brier premise is not established by the cited papers. Prioritize falsifiable chronological, leakage-safe baselines and data availability over new ensemble architecture. Do not silently adopt the original tier scores or pass consensus as independent evidence.
5. **Reproducibility and provenance need improvement.** `scripts/research-audit-inventory.py` hardcodes local Downloads filenames and does not reproduce the live export command. The original authenticated XLSX bytes were not verified in this second look; agreement between saved hash claims is not a rehash of the workbook. `scripts/research-audit-metadata.py` overwrites review/disposition fields with NOT_REVIEWED/OPEN on rerun, so later acceptance decisions need a separate preserved registry. Raw paths use Windows separators; the new read-only checker normalizes them without changing source identifiers. Test result JSONs should carry command, cwd, start/end HEAD and times, dirty-state evidence, and log hashes. ANSI escape sequences must be stripped before parsing Vitest summaries; naive matching incorrectly returned zero failures during this pass.
6. **Underused evidence and unclosed gates.** Existing rank counterexamples and local algorithms should constrain recommendations instead of merely sitting beside unsupported spreadsheet claims. Lack of usable CLV/probability/outcome joins in inspected local artifacts does not prove the production data are absent. A scoped read-only, rights-cleared data availability check is still needed before declaring that experiment impossible. Full suite, typecheck, lint, build and full guardrails remain unverified on final HEAD; disk pressure remains a practical risk. Pushed does not mean reviewed, integrated, deployed or scientifically validated.

### Fresh checks in this second-look pass

- `python scripts/research-audit-verify.py`: exit 0; 1,343 unique IDs, 34 XML files, matching metadata and summary claims, eight report files, no integrity errors. Offline integrity only: not fresh arXiv retrieval, workbook-byte validation, deep-review approval or empirical validation.
- From `apps/web`: `npm test -- lib/calibration/conformal-calibration.test.ts __tests__/cqr.test.ts __tests__/honest-degraded-states.test.ts __tests__/preview-page-paywall.test.tsx __tests__/analytics-instrumentation.test.tsx`: exit 0, five files and 36 tests passed at source HEAD `05a5ec3fd`. Includes the CQR test whose assertion is itself wrong; passing it does not clear the mathematical defect above. Database reads used the documented stub, not production.
- From `apps/web`: `npm test -- __tests__/entitlement-fail-closed-audible.test.ts`: exit 1 (four failed / 12 passed) before the mock repair; exit 0 (16 passed) after. Same command, no assertions removed.
- The older broad web result remains historical evidence: 9 files / 29 tests failed. It was not rerun in full here.
- `AGENTS.md` still contains stale six-report/no-rerun checkpoint text. An attempted correction was blocked by protected-file approval timeout; no alternative write was attempted. Use this status document for the newer evidence.

This file is a checkpoint, not an assertion that the exhaustive user request is finished.
