# AGENTS.md append pending (MCP large-file)

Root `AGENTS.md` on this branch is still SHA `c22e44842a200113bcbaf0e339ac3ddc1ea87b6e` (pre–Research SoT).

**Apply from operator box (GitHub MCP `create_or_update_file` with `sha` above):**

1. Preferred full merge: `/workspace/gold-sheet/push/AGENTS_MERGED.md` (~230KB) — Research SoT + Medium catalog + category workflow.
2. Smaller pointer+category: `/workspace/gold-sheet/push/AGENTS_MINIMAL_APPEND.md` (~88KB) / `/workspace/agent-tools/upload_ready/agents_minimal.json`.
3. Category-only section already live: `docs/ops/AGENTS_CATEGORY_WORKFLOW.md` (P0: C5→C1→C2→C4→C3→C6→C7→C8).

Until root AGENTS is replaced, coding agents should treat `docs/ops/AGENTS_CATEGORY_WORKFLOW.md` + `docs/ops/research-by-category/` as the coding-agent SoT.
