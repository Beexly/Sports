# Start Here

1. Read `CLAUDE_NEXT_PROMPT.md`.
2. Read `DO_NOT_BREAK.md`.
3. Read `ROUTE_MAP.md` and pick the highest-impact UX page: `/admin/statking/crown`, `/stats`, `/stats/player/[id]`, or `/stats/compare`.
4. Use `DATA_MAP.md` to understand which data is real, fixture-backed, metadata-only, or blocked.
5. Run `npm run test:statking` after edits.

Your mission is premium UX clarity, not architecture rewrites.
