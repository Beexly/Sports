# Grok — Solve & Forecast (2026-07-02)

This prompt turns Grok on our REAL current state: what we have, what we're
blocked on, the open questions, and what breaks next. Copy-paste block below the
marker. It carries every anti-fabrication rule earned across the session.

────────────────────────── COPY BELOW ──────────────────────────

You are GSE's R&D division. This is not edge-hunting — it is SOLVE-AND-FORECAST
on our actual state. Attack our real blockers, answer our open questions, find
the issues we can't see, and forecast what breaks next. Read the current state
first, then go to the limit.

READ FIRST (real state, so you work on OUR problems not generic ones):
https://github.com/BeeXly/Sports/blob/claude/night-shift/handoff/claude/overnight-2026-07-01/SESSION-FULL-AUDIT-LEDGER.md
and the triage docs in that same folder (GROK-EDGE-TRIAGE-R1..R4, GROK-STATS-
TRANSCRIPT-TRIAGE, TRACKING-10HZ-PLAYBOOK, LOOPHOLE-EDGE-MAP, EDGE-ANGLES-
AGGRESSIVE, RECONSTRUCTION-SYNTHESIS-TRIAGE).

═══════════ WHAT WE HAVE (built, tested, on night-shift) ═══════════
- Reconstruction engine: empirical-Bayes shrinkage (James-Stein, recency-
  weighted, position-stratified), ridge covariate model (inert until
  calibrated), k-fold CV fidelity gate (fully out-of-sample), skill-score
  graduation rule, compile-time RECONSTRUCTED provenance (runtime-enforced
  allowlist of cleared sources). FIRST LIVE SURFACE just shipped: de-noised
  receiver separation tendency + interval from public NGS, dark behind a flag.
- Honest stats: Wilson CIs (win rate) + BCa bootstrap CIs (ROI/units,
  deterministic/seeded so a public band reproduces from the sealed ledger) +
  a public ROI policy that claims profit ONLY when the lower bound clears
  break-even.
- Proof: SHA-256 pick receipts, public /verify (tamper check covers displayed
  fields, withholds fields on failure), Merkle primitives.
- CLV capture, Shin devig, conformal intervals, edge-lab sealed-vault backtest
  harness, crypto + Stripe billing (dark), RSS news wire.

═══════════ OUR REAL BLOCKERS (attack these with concrete unblock paths) ═══════════
1. RECONSTRUCTION IS DATA-STARVED: the surface renders empty until NGS
   separation accrues over weeks. What is the FASTEST legal path to a usable
   historical NGS aggregate backfill (nflverse historical seasons)? Cite the
   exact nflverse datasets/seasons + the load method.
2. CALIBRATION NOT ACTIVATED: we have the validator + edge-lab harness but have
   not run them (needs prod settled-pick volume). What is the minimum settled-n,
   and the exact pre-registered protocol, to HONESTLY activate calibration
   without overfitting — and what does "it PASSed" have to look like to survive
   a hostile statistician?
3. DEVIG METHOD UNRESOLVED: which de-vig (proportional / Shin / power / logit)
   best matches realized outcomes PER SPORT and PER MARKET? This is unanswered
   in the literature and is proprietary if we settle it. Give the exact
   experiment (data, metric, confound = favorite-longshot bias) to decide it.
4. "PREDICT THE CLOSE EARLY" NOT BUILT: the audit reframed our goal from
   beat-the-close to predict-the-close-early. Give the exact, hostile-proof
   statistical protocol (lead-lag, look-ahead trap, multiple-comparison
   control) to prove our T-24h number leads the close, receipt-anchored.
5. NGS HIGHLIGHTS CALIBRATION SET: asonty/ngs_highlights is real 10fps coords
   (highlighted plays, NO license). Is INTERNAL, transformative, never-re-served
   calibration use defensible? Quote the actual terms if any exist; separate
   copyright from CFAA from contract; mark the conclusion counsel-required.
6. SKILLCORNER PROCUREMENT: not started. What is the real 2026 path — who to
   contact, what license class (analytics, non-team), realistic pricing band
   (cite anything published), and the exact data schema they deliver?

═══════════ THE CONTRACT (enforced — a fabrication voids the round) ═══════════
- Primary source per claim (quote it) or [SPECULATION]. Reality-Ladder tag
  everything. NUMBERS LAW: cited numbers with units, or none.
- Legal conclusions about GSE conduct = [PROPOSED, counsel-required]; only a
  cited case/statute holding is [VERIFIED]; separate CFAA from contract/ToS;
  quote vendor/platform terms or mark [UNVERIFIED]. (You have twice cited real
  substance under wrong labels — verify labels too.)
- MANUFACTURE-THE-DATA: an empty record is a dataset to BUILD; give inputs +
  method. CONFOUND CONTROL: every proposed test names its #1 confound + control.
- No fake code / file trees / "shipped" claims — you research, we build.

═══════════ FORECASTING (the part only a great model does) ═══════════
A. WHAT BREAKS NEXT: forecast the failures we haven't hit yet — at 10x pick
   volume, at first real crypto payment, at first public performance claim, at
   first hostile-journalist audit, at first book C&D. For each: the trigger, the
   blast radius, and the cheapest pre-emptive mitigation.
B. WHAT WE'LL NEED: the data, legal sign-offs, and infra we don't have yet but
   will, ranked by lead-time (the thing with the longest lead-time to acquire is
   the thing to start now).
C. BLIND-SPOT AUDIT: read our state and name what we are STRUCTURALLY unable to
   see about our own project — the wrong assumption baked into the whole thing.
D. THE ONE MOVE: if you were the founder, the single highest-leverage next
   build, defended against its own strongest objection.

═══════════ EXECUTION ═══════════
Depth over breadth. For each blocker: the primary source, then the source that
CONTRADICTS it, reconciled. Self-adversarial: propose, then red-team your own
answer, keep only what survives. Answer the 6 blockers, the 4 forecasting
sections, and state exactly where verification ran out. Do not stop early. Go.

────────────────────────── COPY ABOVE ──────────────────────────
