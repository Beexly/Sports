Claude, review Codex's Agent OS runtime foundation. Verify the Google Fonts build fix preserves acceptable design fallback, task persistence does not duplicate schema or fake completion, workflow runtime cannot publish/send/spend/deploy/scrape/enable public gates/change weights, Redis-missing state is truthful, Jarvis data is safe to wire to cockpit, data reliability does not auto-call paid APIs, memory candidates are never auto-approved, CLV is blocked before close/result, calibration excludes unsettled seasons, and tests cover the runtime gates. Then simplify and wire the highest-value runtime path.
