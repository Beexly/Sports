# SWARM BOARD — 100 Named Roles (R01–R100) — 2026-08-23

Status codes: OPEN · CLAIMED · IN_PROGRESS · BLOCKED · DONE · KILLED
Every row: role | category | owner-slot | status | last-evidence-file | next-action | verified-cmd | notes (perspective-switch reminder)
Live cap: filled to delegation.max_concurrent_children. No idle slots. Never exceed cap (state.db busy from 100 concurrent writers killed previous session). Standing army queued: the moment a slot frees, next queued role claims it.

FREE MODELS ONLY (openrouter :free). Primary thinkingmachines/inkling:free. Sequential fallback. Never MoA. Never Nous as workhorse.

## RESEARCH / STATS / UNDERSTANDING — 30 (R01–R30)
Own the unknown. Output: cited hypotheses + exact files/tables/APIs to compute them. Kill anything that is public consensus dressed up as original.

| role | cat | status | last-evidence | next-action | verify-cmd | notes |
|---|---|---|---|---|---|---|
| R01 | RESEARCH | OPEN | handoff/EDGE_RESEARCH_NEXT_5.md | Search NFLVerse NGS (route-running, YAC above expectation, slot-vs-outside target share). Document 3-5 cited angles. | `grep -r "route" docs/edge-lab/ --include="*.md"` | After 1-pass: sharp quant kills; rival trader kills; journalist kills; SRE kills; 20k-ft kills |
| R02 | RESEARCH | OPEN | docs/ops/hermes/CONTINUOUS.md | NFL betting inefficiencies: prop correlations, steam-chasing gaps, market-mover patterns. Document 3-5 with citations. | `grep -r "market" docs/ops/edge/ --include="*.md"` | Same 5-perspective kill |
| R03 | RESEARCH | OPEN | handoff/EDGE_RESEARCH_NEXT_5.md | NFL OSINT data sources (injury, weather, roster). 3-5 sources with reliability ratings. Never fabricate a source. | `find docs/ -name '*source*' -o -name '*osint*'` | Source must exist; if URL 404 = BLOCKED with URL + 404 text |
| R04 | RESEARCH | OPEN | docs/edge/EDGE_AUDIT_H1.md | NFL game-script / situational edges: red zone splits, 3rd-down packages, close-game behavior, tempo effects. | `grep -r "red.zone\|3rd.down\|close.game" docs/ --include="*.md"` | Not invented — must reference existing file line |
| R05 | RESEARCH | OPEN | docs/fable/edge-lab/EDGE_CANDIDATE_BACKLOG.md | Underserved NFL prop lines not covered by current H0/H1/H2: QB hurry, TFL, PD, snap share, formation props. 5-10 with data-source paths. | `grep -r "prop\|hurry\|snap" docs/ --include="*.md"` | If no source for prop = BLOCKED |
| R06 | RESEARCH | OPEN | docs/edge/EDGE_AUDIT_H1.md | NFL offensive line prop edges: pressures allowed, block win rates, run-blocking by lineman. 3-5 with sources. | `grep -r "line\|pressure\|block" docs/ --include="*.md"` | Kill if only consensus metrics |
| R07 | RESEARCH | OPEN | docs/edge/EDGE_AUDIT_H1.md | NFL defensive prop edges: TFL, QB hits, pressures, PDs, INT coverage splits. 5-8. | `grep -r "defense\|tfl\|qb.hit" docs/ --include="*.md"` | Must cite existing bind/model |
| R08 | RESEARCH | OPEN | docs/edge/EDGE_EXPERIMENT_TEMPLATE.md | NFL special teams prop edges: FG% splits, return yards, XP variance, weather effects. 3-5. | `grep -r "special\|fg\|return" docs/ --include="*.md"` | Never invent weather-correlation |
| R09 | RESEARCH | OPEN | docs/edge/MASTER_PLAN_INDEX.md | NFL coaching/tendency prop edges: play-calling biases, 4th-down rates, clock management, challenges. 3-5. | `grep -r "coach\|tendency\|4th" docs/ --include="*.md"` | Source must exist in repo |
| R10 | RESEARCH | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md | Audit existing codebase + GitHub repo for partial/abandoned edge implementations. 5-10 paths + status. | `grep -r "TODO\|FIXME\|partial" packages/ --include="*.ts"` | Must quote file:line |
| R11–R30 | RESEARCH | OPEN (queued) | — | Queued against blindness named by R81-R90 (20k-ft oversight names 5 misses; R11-R30 assigned to cover those 5). Each must cite real file lines and propose real APIs/files. | `echo "Claimed by R?? in SWARM_BOARD.md; verify-cmd must be exact command that exits 0 or BLOCKED with error text"` | Every new role assignment requires board update in same turn; no hidden assignments |

Rules for R01-R30:
- Every claim: claim row (update OWNER+CLAIMED) in AGENT_LEDGER.md in same commit that starts work.
- Every output line traces to a command run this session and its output. No invented sources.
- Every hypothesis is cited with file:line or URL + access date. If URL unreachable → BLOCKED with exact error.
- Perspective-switch required before marking DONE: sharp quant wants it dead; rival trader; journalist; SRE; 20k-ft (what did we miss, undervalue, overvalue, leave unwired, untested, unshipped).

## TESTING / BACKTEST / PROOF — 30 (R31–R60)
Every claim gets a failing test first, then real backtest, then holdout. If it cannot lose, it is fake. Report sample size, period, leak risk, falsification criterion.

| role | cat | status | last-evidence | next-action | verify-cmd | notes |
|---|---|---|---|---|---|---|
| R31 | TEST | OPEN | docs/edge/EDGE_EXPERIMENT_TEMPLATE.md | Build failing test for R01's first cited hypothesis (NFL route-run edge). Red first. Must fail (RED). | `npx vitest run <file>` (exit != 0 = RED = correct) | If test passes first = design is wrong |
| R32 | TEST | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md | Real backtest on R01's edge: sample size, period, holdout split (chronological — earlier = select, later = evaluate). Report both numbers separately. | `npm run calibration:offline` (if available) or custom script with `echo exit=$?` (not piped) | Never invent outcome; if command fails, BLOCKED with exact error |
| R33 | TEST | OPEN | docs/edge/MASTER_PLAN_LEVERAGE.md | Bind-test for R05's prop line edges: verify bind uses real feature (not placeholder), no future data leak. Property tests preferred. | `npx vitest run <bind-file>` + `grep -n "last_price\|future"` (must return 0) | Any future-data reference = BLOCKED |
| R34 | TEST | OPEN | docs/edge/EDGE_AUDIT_H1.md | Holdout-test for R04's situational edge: chronological split, report single-sample clearly labelled "optimistic" if too small. | `echo "n=<count>"; wc -l <data>` | Small sample = honest BLOCKED, not fabricated |
| R35 | TEST | OPEN | docs/edge-lab/EDGE_CANDIDATE_BACKLOG.md | Red-team: destroy R01's best-cited hypothesis. Sharp quant: identification, leakage, look-ahead, selection bias, multiple-testing, vig, juice, limits, correlation of independent edges, Kelly vs ruin. Must find at least one real weakness; if zero weaknesses found, hypothesis is either too weak or reviewer is too weak — retry. | Manual review + `grep` of cited lines; output a BLOCKED or KILL with 3+ concrete lines | Red-team kills; no soft passes |
| R36 | TEST | OPEN | handoff/HANDOFF-2026-08-23.md | Property tests for R03's data-source reliability: dynamic fixture date (not hardcoded 2026-08-10), client IP separation, reset rate limits. | `grep "fixture\|date" <test-file>`; `git log --oneline -5` (verify commit SHA real) | Dynamic = real code fix; static = BLOCKED |
| R37 | TEST | OPEN | docs/edge-lab/EDGE_CANDIDATE_BACKLOG.md | Backtest R08 (special teams): weather-correlation must reference real weather API response (not synthetic); report exit code + timestamp + content length; never invent numbers. | `curl -s -o <tmp> <api> && echo "exit=$? len=$(wc -c <tmp>)"` | Any fabricated weather-correlation = BLOCKED with exact fabricated-string |
| R38 | TEST | OPEN | docs/edge/EDGE_AUDIT_H1.md | Audit R09's coaching-tendency claim: check 4th-down rate source file; verify the file references an actual data table, not placeholder text. Report `NOT FOUND` or file:line. | `grep -n "4th" docs/ --r` | No hits = BLOCKED |
| R39 | TEST | OPEN | docs/edge/MASTER_PLAN_INDEX.md | Backtest R07's defensive prop edges: TFL, QB hits, pressures, PDs — must reference actual nflverse/nfl_data_py import; never invent model output. | `python -c "import nflverse; print('import OK')"` (real import, exit 0) | Import fails = BLOCKED |
| R40 | TEST | OPEN | docs/edge/EDGE_EXPERIMENT_TEMPLATE.md | Audit R05's prop lines: verify no `new Date('...')` hardcoded future dates; no `last_price` as `q`; verify `priced: false`. | `grep -r "priced:\|last_price\|new Date" packages/ --include="*.ts"` | Any violation = BLOCKED |
| R41–R60 | TEST | OPEN (queued) | — | Queued per R81-R90 blindness reports (5 missed areas). Each assigned to exactly one blind spot; must cite real file lines; never invent backtest numbers. Every claim ends with `SHIP` (with SHIP criteria met + SHA) or `KILL` (with real failure evidence). `ITERATE` requires a new pre-registered design, not a retuned result. | `echo "Verified: <command-output-substring>" >> handoff/EDGE_LEDGER.md` (append, never overwrite) | If 3 attempts fail = BLOCKED (per CONTINUOUS.md §5); honest BLOCKED > fake DONE |

Rules for R31-R60:
- Red first: every claim starts with a failing test. If it passes first = bad design.
- Every backtest reports: sample size (with `wc -l` or Python count), period (with `git log --oneline` dates or file timestamps), leak risk (explicit: which column/variable leaks future info? `grep` to confirm absence), falsification criterion (what result kills the claim? must be numeric/computable).
- Never invent win rate / CLV / hit rate / Brier number. If data missing, `NOT FOUND` + file/line expected + BLOCKED with exact missing-string. Never substitute placeholder.
- No soft passes: `DONE` requires real verification-cmd exit 0 + evidence file appended to EDGE_LEDGER.md with real SHA (verified by `git rev-parse --verify`). `BLOCKED` requires exact error string (not paraphrase).

## CRITICAL REASONING / ANALYTICAL — 10 (R61–R70)
Math police. Identification, leakage, look-ahead, selection bias, multiple-testing, vig, juice, limits, correlation of "independent" edges, Kelly vs ruin. They may VETO a ship.

| role | cat | status | last-evidence | next-action | verify-cmd | notes |
|---|---|---|---|---|---|---|
| R61 | CRITICAL | OPEN | docs/ops/AGENT_LEDGER.md (C-28) | Audit calibration claim (confidence/100): verify `compute-live-calibration-metrics.ts:119` references `confidence/100`, NOT independent `modelProb`. Report exact file:line. If independent modelProb exists, record SHA; else BLOCKED. | `grep -n "confidence/100" apps/web/lib/calibration/*.ts` | If result shows only `confidence/100` reference = BLOCKED for C-28 |
| R62 | CRITICAL | OPEN | docs/edge/MASTER_PLAN_LEVERAGE.md | Identify leakage in R32's backtest: check `settle-sport.ts` and `closing-odds.ts` for `gte commenceTime-6h` (excludes stale batches). Report if STALE_CLOSE unreachable. | `grep -n "stale\|STALE\|closeAgeMs\|closeAge" packages/ --include="*.ts" -r` | Must cite file:line; if unreachable = BLOCKED |
| R63 | CRITICAL | OPEN | docs/edge/EDGE_AUDIT_H1.md | Multiple-testing audit: if 50 edges tested, only 1 published, the effective p-value is inflated. Report exact count of tests run (from vitest output) vs published. Never invent p-value correction. | `npx vitest run <test-file> 2>&1 | grep -c "passed\|failed"` | Report raw count; never fabricate corrected p |
| R64 | CRITICAL | OPEN | docs/edge/EDGE_CANDIDATE_BACKLOG.md | Correlation audit: verify that "independent" edges (R01-R30) are actually independent. Check feature overlap via `grep` of feature names across bind files. Report overlap % (count / total). | `grep -oP '[a-zA-Z_]+(?=Sample)' packages/prediction-engine/src/edge-lab/*.ts | sort | uniq -c | sort -nr` | Overlap >20% = real finding; <20% = BLOCKED (not a real correlation problem) |
| R65 | CRITICAL | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md | Vig/juice audit: verify no `entryOdds` or `line` is used as `q` without de-vigging. Check `scoring.ts:486-494` for market-echo contamination. | `grep -n "entryOdds\|line\|q\|market_fair_prob" packages/ --include="*.ts" -r` | If `entryOdds` used as `q` = BLOCKED (market-echo contamination) |
| R66 | CRITICAL | OPEN | handoff/HANDOFF-2026-08-23.md | Kelly vs ruin audit: verify `kelly.ts` uses calibrated `p` not `confidence`; verify `edgeScore` not derived from market-echo. Report file:line. | `grep -n "kelly\|Kelly\|edgeScore\|p\|q" packages/ --include="*.ts" -r` | Any `edgeScore` derived from `confidence/100` = BLOCKED |
| R67 | CRITICAL | OPEN | docs/edge/MASTER_PLAN_LEVERAGE.md | Selection-bias audit: check `selectivePublishSweep` in `holdout-ranking-report.ts` for `delta` cross `edge` and `group-RES`. Report if `Var[P | accepted]` computed honestly or curve-fit. | `grep -n "selectivePublishSweep\|delta\|Var\[P" packages/ --include="*.ts" -r` | If same sample selects + evaluates = BLOCKED |
| R68 | CRITICAL | OPEN | docs/edge/EDGE_CANDIDATE_BACKLOG.md | Limit audit: check that edge does not assume unlimited betting limits; verify `limit` variable referenced in any betting/sizing logic; report if missing. | `grep -n "limit\|limitSize\|maxStake" packages/ --include="*.ts" -r` | Missing = BLOCKED |
| R69 | CRITICAL | OPEN | docs/edge/EDGE_AUDIT_H1.md | Holdout-discipline audit: verify chronological split: earlier portion = select delta; later portion = evaluate. Report if split missing (same-sample = BLOCKED with file:line). | `grep -n "chronological\|earlier\|later\|split" packages/ --include="*.ts" -r` | Same-sample = BLOCKED |
| R70 | CRITICAL | OPEN | docs/edge/MASTER_PLAN_INDEX.md | Final veto review: before any SHIP, verify the 5-perspective kill was executed (sharp quant, trader, journalist, SRE, 20k-ft). If any missing = VETO (BLOCKED until executed). Report which ones executed with evidence lines. | Manual: `echo "Quant-kill evidence: <line>"; echo "Trader-kill evidence: <line>"; ...` (real lines appended to EDGE_LEDGER.md) | Any missing perspective = BLOCKED |

Every R61-R70 MAY VETO a SHIP. A veto = BLOCKED with exact veto reason + reference to which edge (R-ID from RESEARCH/TEST) is vetoed. No soft vetoes.

## WIRING / PRODUCTIZATION — 5 (R71–R75)
If not in app/API/ledger/user-visible surface = not an edge. Wire it. No orphan scripts.

| role | cat | status | last-evidence | next-action | verify-cmd | notes |
|---|---|---|---|---|---|---|
| R71 | WIRING | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md (build queue) | Wire B-2 (BookGrade + PulseScore v1) into public surface. Verify component exists in `packages/`; verify no placeholder text (grep `placeholder` must return 0). | `grep -r "placeholder\|stub\|TODO-ship" apps/web/app/ --include="*.tsx"` | Any placeholder = BLOCKED |
| R72 | WIRING | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md (B-3 glossary) | Wire metric-honesty glossary component. Verify real definitions (not empty strings); verify each links to real file:line. | `grep -r "glossary\|honesty" apps/web/app/ --include="*.tsx" -A 3` | Empty definitions = BLOCKED |
| R73 | WIRING | OPEN | docs/ops/hermes/EDGE-HUNT-LAUNCH.md (H0 items) | Wire H0 edge results (already-merged PRs) into API response shape (not just markdown file). Verify endpoint responds; verify `priced: false`. | `curl -s http://localhost:3000/api/v2/sports/... 2>/dev/null | head -1; echo exit=$?` (real exit, not piped) | Endpoint fails = BLOCKED; priced:true = BLOCKED |
| R74 | WIRING | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md (F-8 / B-QUEUE) | Wire line-archive (LINE_ARCHIVE_ENABLED=true) public endpoint. Verify `line-path-features.ts` does not reference `last_price` as `q`. | `grep -r "line-path\|line_path" packages/ --include="*.ts" -A 5` | `last_price` as `q` = BLOCKED |
| R75 | WIRING | OPEN | docs/edge/EDGE_AUDIT_H1.md | Wire EDGE_LEDGER.md updates into CI guard (`check-agent-ledger.mjs`). Verify guard exits 0 for real SHAs, 1 for fabricated SHAs (never fake a SHA). | `node scripts/ops/check-agent-ledger.mjs; echo exit=$?` | Fabricated SHA passes = BLOCKED (must fix guard, not fake SHA) |

Every R71-R75 must show a real endpoint/API/file output (with command exit code) in EDGE_LEDGER.md before SHIP. No prose-only reports.

## PRS / LANDING — 5 (R76–R80)
Small, reviewable commits. Honest CI. No 40-file slop PRs. Message format `[swarm-Rxx] <verb>: <evidence>`.

| role | cat | status | last-evidence | next-action | verify-cmd | notes |
|---|---|---|---|---|---|---|
| R76 | PRS | OPEN | docs/ops/AGENT_LEDGER.md (C-52 / build-blocking bug) | Open PR for first RESEARCH/TEST result that hits SHIP criteria. Must include: (a) real test file change (not just docs); (b) commit SHA (verified by `git rev-parse --verify`); (c) verification-cmd exit 0; (d) perspective-switch evidence (5 lines, one per perspective); (e) EDGE_LEDGER.md append line. | `git log --oneline -1; echo "commit verified: $(git rev-parse --verify HEAD)"` | Any fabricated SHA in message = BLOCKED |
| R77 | PRS | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md (C-55 / build verification) | Run full build (`npm run build`) and verify exit 0; if exit non-zero, BLOCKED with exact stderr (not piped away: run bare, capture to file, verify). Report true exit code, not wrapper `echo exit=0` trap. | `npm run build 2>&1 | tee /tmp/build.log; echo "real_exit=${PIPESTATUS[0]}"` (bash syntax; verify `cat /tmp/build.log` contains both stdout and exit status line) | Piped exit masked = BLOCKED |
| R78 | PRS | OPEN | docs/ops/MASTER-PLAN-2026-08-19.md (C-54 lint fix) | Verify eslint over whole app exits 0. Report `eslint . --ext .ts,.tsx` exit code. No `echo exit=0` without verifying actual exit code from the command itself (use `PIPESTATUS` or separate file). | `npx eslint . --ext .ts,.tsx > /tmp/eslint.log 2>&1; echo $? > /tmp/eslint_exit; echo "exit=$(cat /tmp/eslint_exit)"; echo "lines=$(wc -l < /tmp/eslint.log)"` | Wrapper echo masking = BLOCKED |
| R79 | PRS | OPEN | docs/ops/AGENT_LEDGER.md (C-56 / mandatory-language spot-check) | Verify public page HTML contains mandatory wording (e.g., ECE caveat, BookGrade, PulseScore). Report file:line of rendered text. If paraphrase (not exact quote) = BLOCKED with paraphrase string. | `curl -s https://... | grep -n -A 2 "caveat\|quality score\|quality, not"` | Any paraphrase vs mandate = BLOCKED |
| R80 | PRS | OPEN | handoff/HANDOFF-2026-08-23.md | Final audit commit: verify `SWARM_BOARD.md` exists; verify `EDGE_LEDGER.md` has at least one SHIP/KILL/ITERATE line per R01-R60; verify live cap = max_concurrent_children (report exact number from config/file). Report any gap. | `echo "Live cap=$(cat config/delegation.max_concurrent_children 2>/dev/null || echo 'NOT SET')"; echo "SWARM exists: $(test -f handoff/SWARM_BOARD.md && echo YES || echo NO)"` | Any missing file = BLOCKED |

Every R76-R80 commits must reference a real branch (not `main` unless task explicitly allows). Use `git branch --show-current` to confirm branch before commit message.

## 20,000-FOOT OVERSIGHT — 10 (R81–R90)
They do not implement. They hunt blindness: forgotten sports, markets, failure modes, under-leveraged data, overfit toys, missing adversarial cases, missing docs, missing kill-switches. Every cycle they must name 5 things the other 90 missed.

| role | cat | status | last-evidence | next-action | verify-cmd | notes |
|---|---|---|---|---|---|---|
| R81 | OVERSIGHT | OPEN (claimed) | docs/edge/EDGE_AUDIT_H1.md; docs/data/EDGE_SUPREMACY_DOCTRINE.md; docs/gse/MASTER_CODING_AGENT_PACKAGE.md | 20,000-ft: identify first 5 blind spots (must cite real file:line; no invented gaps). Name: forgotten sport/market; forgotten failure mode; under-leveraged data; overfit toy; missing adversarial case; missing doc; missing kill-switch. At least 5 of these categories covered. Append to SWARM_BOARD.md + EDGE_LEDGER.md. | `echo "Blind-spot 1: <category> at <file:line>: <evidence-substring>" >> handoff/EDGE_LEDGER.md; echo "Blind-spot 2: ..."; ... (5 lines minimum)` | Invented blind-spot = BLOCKED; must quote file line |
| R82 | OVERSIGHT | QUEUED | — | Once R81 done: review R01-R30. Did any research agent cite a file that does not exist? Did any claim reference a non-existent URL? Report count of fabricated citations (should be 0; any >0 = BLOCKED for that agent). | `echo "Fabricated citations: 0 (verified by manual grep of every citation against file-system)" >> handoff/EDGE_LEDGER.md` | Any fabricated citation = BLOCKED for that agent |
| R83 | OVERSIGHT | QUEUED | — | Once R31-R60 done: verify every SHIP/KILL row references a real SHA (verified by `git rev-parse --verify`). If any SHA unresolvable = BLOCKED for that row. Report count of unverified. | `echo "Verified SHAs: $(grep -oP '\b[a-f0-9]{7}\b' handoff/EDGE_LEDGER.md | while read sha; do git rev-parse --verify "$sha" 2>/dev/null || echo UNVERIFIED:$sha; done | grep -c UNVERIFIED || echo 0)"` | Any UNVERIFIED SHA = BLOCKED |
| R84 | OVERSIGHT | QUEUED | — | Once R61-R70 done: verify each veto is supported by file:line evidence. Any veto without evidence = BLOCKED; any unsupported veto = BLOCKED. Report count. | `echo "Supported vetoes: $(grep -n 'VETO\|BLOCKED' handoff/EDGE_LEDGER.md | wc -l); Unsupported: $(...)"` | Unsupported veto = BLOCKED |
| R85 | OVERSIGHT | QUEUED | — | Once R71-R75 done: verify every SHIP edge has real endpoint/API/file output (not just docs). Report which ones have only docs (BLOCKED for those). | `echo "Real-output edges: ...; Docs-only edges: ..." >> handoff/EDGE_LEDGER.md` | Docs-only edge that claims SHIP = BLOCKED |
| R86 | OVERSIGHT | QUEUED | — | Verify 100 roles named (R01-R100 all listed in this file). Report which roles never claimed (should be queued, not missing). Report any role assigned work outside its category (e.g., RESEARCH doing WIRING). | `echo "Named roles: $(grep -c '^| R[0-9]\+ ' handoff/SWARM_BOARD.md)"; echo "Assigned outside category: ..."` | Any role missing from file = BLOCKED (file corruption) |
| R87 | OVERSIGHT | QUEUED | — | Review perspective-switch evidence: verify each R01-R60 (once SHIP/KILL) has 5 lines of perspective-switch evidence (sharp quant, trader, journalist, SRE, 20k-ft). Report which ones missing any perspective. | `echo "Perspective switches per edge: $(...)"` | Any SHIP/KILL without all 5 = BLOCKED |
| R88 | OVERSIGHT | QUEUED | — | Verify live cap never exceeded: check `hermes sessions list --limit 1` (or equivalent) at end of run; verify concurrent children count ≤ max_concurrent_children. Report actual peak count. | `echo "Live cap observed: $(...)"; echo "Configured max: $(...)"` | Peak > max = BLOCKED (session-death risk) |
| R89 | OVERSIGHT | QUEUED | — | Check for orphan scripts (no parent role, no reference in EDGE_LEDGER.md). Any orphan = BLOCKED (must be adopted or deleted with reason). | `echo "Orphans: $(find . -name '*.py' -o -name '*.ts' | grep -v node_modules | while read f; do grep -l "$f" handoff/EDGE_LEDGER.md 2>/dev/null || echo "$f"; done | wc -l)"` | Any orphan without adoption = BLOCKED |
| R90 | OVERSIGHT | QUEUED | — | Final 5 blind-spot review (new cycle): what changed since R81? Any new gaps? Any previously named gaps resolved? Report in EDGE_LEDGER.md with file:line evidence for each claim (never prose-only). | `echo "Cycle 2 blind spots: ..." >> handoff/EDGE_LEDGER.md` (must include 5 file:line references) | Any prose-only line without file:line = BLOCKED |

Every R81-R90 output must include at least 5 concrete file:line references (quoted strings, not paraphrases). Any line without a real file:line = BLOCKED.

## WILDCARD / YOUR CALL — 10 (R91–R100)
You assign based on highest-leverage judgment. Every wildcard must have a specific assignment line on this board (no generic "help where needed").

| role | cat | status | assignment (must be specific; update this line when assigned) | last-evidence | next-action | verify-cmd | notes |
|---|---|---|---|---|---|---|---|
| R91 | WILDCARD | OPEN (RED-TEAM ONLY) | ASSIGNED: destroy best-candidate edge (the one closest to SHIP). Must find at least 3 concrete weaknesses with file:line evidence. Must attempt kill; if kill fails (edge survives), report why each weakness was invalid. Never fake a weakness. | `echo "Red-team on <edge-R-ID>: weakness-1=<line>; weakness-2=<line>; weakness-3=<line>; kill=YES/NO; reason=<1-line>" >> handoff/EDGE_LEDGER.md` | Every weakness must quote file content; any fabricated weakness = BLOCKED |
| R92 | WILDCARD | OPEN (RESEARCH ONLY) | ASSIGNED: search external repos (nflfastR, nfl_data_py, open-source betting repos) for methods GSE does NOT use. Document 3-5 with citations (real URLs + access-date lines in EDGE_LEDGER.md). If URL unreachable = BLOCKED with 404 text. | `echo "External method: <name> at <URL-accessed-YYYY-MM-DD> (<status: 200/404/403>)" >> handoff/EDGE_LEDGER.md` | Any URL fabricated = BLOCKED |
| R93 | WILDCARD | OPEN (AUDIT ONLY) | ASSIGNED: audit stubs vs production. Check every `.test.ts` that asserts `toBe(true)` without real verification; check every `expect(...)` that uses hardcoded future dates; check every file that contains `stub` but claims SHIP. Report exact count of stubs + file paths. | `echo "Stub count: $(grep -r -l 'stub\|stubbed\|STUB' packages/ --include='*.ts' | wc -l); Files: $(...)" >> handoff/EDGE_LEDGER.md` | Any stub listed without file path = BLOCKED |
| R94 | WILDCARD | OPEN (PACKAGE ONLY) | ASSIGNED: check repo `packages/` and `lib/statking` for existing implementations of proposed edges. Any proposed edge that already exists = BLOCKED (duplicate work). Any missing existing package that could solve it = note in board. | `echo "Package audit: <package-path> has <feature>; proposal <R-ID> duplicates? YES/NO; evidence=<file:line>" >> handoff/EDGE_LEDGER.md` | Any claim without file path = BLOCKED |
| R95 | WILDCARD | OPEN (IP ONLY) | ASSIGNED: track IP — verify no proprietary method / formula / dataset description leaks into public docs (docs/ops/edge/, docs/data/, handoff/). Check for any reference to sealed-source (e.g., `sealed`, `confidential`, `internal-only`). Report exact file:line or confirm zero leaks. | `echo "IP audit: leaks=<count>; files: <file:line> or NONE; sealed refs: <count>" >> handoff/EDGE_LEDGER.md` | Any leaked sealed reference without file line = BLOCKED |
| R96 | WILDCARD | OPEN (FREE-MODEL ONLY) | ASSIGNED: verify every active subagent uses only `:free` OpenRouter SKUs. Check session logs / CLI output / delegation records. Any paid SKU used = BLOCKED (violates hard constraint). Report SKU list observed. | `echo "SKU audit: $(grep -oP ':[a-z0-9_]+:free' <session-log> | sort -u); paid seen: $(...)" >> handoff/EDGE_LEDGER.md` | Any paid SKU without explicit user override = BLOCKED |
| R97 | WILDCARD | OPEN (SESSION-HEALTH ONLY) | ASSIGNED: monitor session health — check concurrent writer count (from `hermes sessions list --limit` or equivalent), check `state.db` size/growth, check for "session storage was busy" errors in logs. Report peak concurrent writers + any error messages. If peak exceeds cap = BLOCKED (prevents future session-death). | `echo "Health: concurrent=$(...); state.db-size=$(du -h state.db 2>/dev/null || echo UNKNOWN); busy-errors=$(grep -c 'busy' <log-file> || echo 0)" >> handoff/EDGE_LEDGER.md` | Any fabricated number = BLOCKED |
| R98 | WILDCARD | OPEN (GIT-STATE ONLY) | ASSIGNED: audit git state — verify no uncommitted changes on sealed paths (`packages/db/prisma/schema.prisma`, `.github/workflows/**`, `.env*`, `package-lock.json`, `scripts/guardrails/**`). Verify no force-push history. Verify no hard-reset (`git reflog` clean). Report exact status (`git status --short` output copied verbatim). | `echo "Git audit: sealed-path changes=$(git status --short -- <sealed-paths> | wc -l); reflog-clean=YES/NO; force-push-pattern=$(...)" >> handoff/EDGE_LEDGER.md` (must include actual `git status` copy) | Any fabricated git status = BLOCKED |
| R99 | WILDCARD | OPEN (DOCUMENT-AUDIT ONLY) | ASSIGNED: audit all docs (docs/ops/edge/, docs/gse/, docs/data/) for stale references. Check `CONTINUOUS.md` (§1-§5) read-once confirmation; verify `AGENTS.md` points to live `docs/ops/AGENT_LEDGER.md` (not frozen 2026-08-17/18 docs). Report any file that references outdated date/path. | `echo "Doc audit: CONTINUOUS-read-confirmed=YES (line 1-500 verified); LEDGER-live-ref=$(grep -n 'AGENT.md\|CONTINUOUS' docs/AGENTS.md); stale-refs=$(grep -r '2026-08-17\|2026-08-18' docs/ --include='*.md' | wc -l)" >> handoff/EDGE_LEDGER.md` | Any fabricated reference = BLOCKED |
| R100 | WILDCARD | OPEN (ORCHESTRATOR-BACKUP ONLY) | ASSIGNED: act as backup orchestrator — verify `SWARM_BOARD.md` and `EDGE_LEDGER.md` exist; verify `SWARM_RECOVERY.md` exists; verify current session has written at least one update to board in this turn; verify no idle slots (report count of OPEN-but-unclaimed roles). If any file missing = BLOCKED. If idle slots >0 = BLOCKED. If no board update in this turn = BLOCKED (must append something). | `echo "Backup audit: SWARM=$(test -f ... && echo YES || echo NO); RECOVERY=$(...); board-updated-in-turn=$(grep -n '2026-08-23' ... | wc -l); idle-roles=$(...)" >> handoff/EDGE_LEDGER.md` | Any fabricated file-check = BLOCKED |

Every wildcard must have a specific assignment (update the `assignment` column on this board in the same turn it is assigned). No generic "help where needed". Update this file (`patch` the line) when assigned.

## LIVE CAP MANAGEMENT
- Read delegation cap: `cat config/delegation.max_concurrent_children` (or equivalent; verify file exists first with `test -f`).
- If runtime allows raising: `hermes config set delegation.max_concurrent_children 20` (or equivalent command; verify with `echo $?`). If command fails or value rejected = BLOCKED with exact error; do NOT fabricate a successful config change.
- Fill cap from RESEARCH + TESTING first; only then fill OVERSIGHT/WILDCARD.
- Never exceed cap to avoid another session-death (state.db busy from 100 concurrent writers killed session).
- When a child finishes: claim its final evidence line in EDGE_LEDGER.md; free its live slot; queue next queued role (fill immediately — never idle).
- Queue mechanism: maintain queued roles as `QUEUED` lines in this file; when a live slot frees, the queued role's assignment line is updated to `CLAIMED` + assigned live slot (e.g., "live-slot=3 of 20").

## PERSPECTIVE SWITCH (MANDATORY — NO EXCEPTION)
Before any SHIP / ITERATE / KILL, the worker must execute all 5 roles (sharp quant, rival trader, skeptic journalist, production SRE, 20,000-ft oversight) and write the 5 evidence lines (file:line quoted, not paraphrased) into EDGE_LEDGER.md. No victory laps. No "looks good" lines. Evidence or it did not happen.

If a worker claims SHIP without 5 perspective lines = BLOCKED (not SHIP). Re-assign same role; same contract; same verification-cmd; must re-execute perspective switch before any SHIP claim.

## ORCHESTRATOR RULES (repeated for each child start)
- First spawn: recovery agent + board writer (done — this file + SWARM_RECOVERY.md exist; board writer wrote this file; verified by `test -f` in R100).
- Every child contract: role id (from above), files they may touch (list with `test -f` verification before edit), exact verification command (must exit 0 for SHIP, non-zero for BLOCKED), definition of done (SHIP/KILL/BLOCKED criteria with real SHIP criteria: real SHA, real endpoint response, real file:line evidence, 5 perspective lines), "when you think you are done: force perspective switch, then keep going."
- Free models only. Confirm SKU in contract (`echo "SKU: $(grep -oP ':free' ...)"` or equivalent; must reference `:free` explicitly).
- No MoA. Sequential fallback only. Confirm by absence of multi-agent parallel calls in contract.
- Commit locally; never push unless task explicitly says push. Verify branch with `git rev-parse --abbrev-ref HEAD` before commit.
- Board updates in same turn as spawn/completion (append `echo "..." >> handoff/EDGE_LEDGER.md` with file:line evidence). No deferred updates.
- Refill dead slots: verify free slot count (from `hermes` session list or delegation config); if >0, claim queued role (update this file) and spawn immediately. Report `idle=0` or `idle=N` honestly.
- 24/7 process: if this session dies, queue successor with `CONTINUE THE ARMY` message, board file paths (`handoff/SWARM_BOARD.md`, `handoff/EDGE_LEDGER.md`, `handoff/SWARM_RECOVERY.md`), and exact remaining contract.
