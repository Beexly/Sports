# Dependency + Supply-Chain Health — P7-09

Generated by: `npm audit --omit=dev --json > handoff/npm-audit-current.json` (raw output).

## Severity totals (prod only, excluding devDependencies)

source: `handoff/npm-audit-current.json` · `metadata.vulnerabilities`

| Severity | Count |
|----------|-------|
| critical | 0     |
| high     | 2     |
| moderate | 0     |
| low      | 0     |
| info     | 0     |
| **total**| **2** |

Dependency census at audit time: 280 prod, 517 dev, 119 optional, 59 peer (892 total).
source: `npm-audit-current.json` · `metadata.dependencies`

## Findings

### HIGH — `next` (14.2.35) — 30+ advisories, fix requires a SemVer-major bump

source: `handoff/npm-audit-current.json` → `vulnerabilities.next`
declared version: `apps/web/package.json` line 35 → `"next": "^14.2.15"`
installed version: `node_modules/next/package.json` → `14.2.35`
fixAvailable: `next@16.3.1` with `isSemVerMajor: true` (current 14.x → target 16.x)

This single dependency accounts for every advisory surfaced: 30 advisories across
`next` and its nested `postcss` (node_modules/next/node_modules/postcss), including:

- GHSA-h25m-26qc-wcjf — high — HTTP request deserialization DoS via RSC
- GHSA-q4gf-8mx6-v5v3 — high — DoS with Server Components
- GHSA-8h8q-6873-q5fj — high — DoS with Server Components
- GHSA-c4j6-fc7j-m34r — high — SSRF via WebSocket upgrades (CVSS 8.6)
- GHSA-p9j2-gv94-2wf4 — high — SSRF in rewrites via attacker-controlled destination
- GHSA-m99w-x7hq-7vfj — high — DoS in App Router using Server Actions
- GHSA-89xv-2m56-2m9x — high — SSRF in Server Actions on custom servers
- GHSA-36qx-fr4f-26g5 — high — Middleware/Proxy bypass in Pages Router i18n
- GHSA-9g9p-9gw9-jx7f — moderate — Image Optimizer remotePatterns DoS
- GHSA-3x4c-7xq6-9pq8 — moderate — unbounded next/image disk cache growth
- GHSA-1114897  — moderate — HTTP request smuggling in rewrites
- GHSA-ffhc-5mcf-pf4q — moderate — XSS in App Router with CSP nonces
- GHSA-3g8h-86w9-wvmq — low — Middleware/Proxy redirect cache poisoning
- GHSA-vfv6-92ff-j949 — low — RSC cache-busting collision cache poisoning
- GHSA-gx5p-jg67-6x7h — moderate — XSS in beforeInteractive scripts
- GHSA-h64f-5h5j-jqjh — moderate — DoS in Image Optimization API
- GHSA-wfc6-r584-vfw7 — moderate — cache poisoning in RSC responses
- GHSA-68g3-v927-f742 — moderate — cache confusion for requests with bodies
- GHSA-4633-3j49-mh5q — moderate — cache confusion with invalid UTF-8
- GHSA-4c39-4ccg-62r3 — moderate — unbounded Server Action payload on Edge
- GHSA-955p-x3mx-jcvp — moderate — unauthenticated disclosure of Server Function endpoints

**The only fix npm-audit offers is a Next.js 14.x → 16.x upgrade, which is a SemVer-major bump.
No non-major patch currently resolves any of these advisories.**

### HIGH — `postcss` (transitive, nested under `next`)

source: `handoff/npm-audit-current.json` → `vulnerabilities.postcss`
location: `node_modules/next/node_modules/postcss` (nested, transitive via `next`)
effects: `["next"]` — only present because of the `next` dependency above
fixAvailable: `next@16.3.1` with `isSemVerMajor: true`

Advisories: GHSA-qx2v-qp2m-jg93 (XSS), GHSA-6g55-p6wh-862q (arbitrary file read, CVSS 7.5),
GHSA-fxqj-rqcc-2cmp (incomplete fix follow-up), GHSA-r28c-9q8g-f849 (path traversal, CVSS 7.5).
**Unfixable without the same Next.js major bump described above.**

## Supply-chain / integrity check

source: `package-lock.json` → `packages` map (declared versions) + `node_modules` (installed)

- No `lite11m`, `reroute-guard`, `pareto-bandit`, `securellm-agentguard`, `care-shell`,
  `teia-cognitive-router`, `basilisk-ai`, `t3mp3st`, `mtrouter`, `freecad-api`, or
  `laravel-crm-client` present in `package-lock.json`. These 11 packages are confirmed absent
  from npm (per the fraud watchlist in P7-08's verification) — none were pulled in.
- All 892 installed dependencies resolve to entries in `package-lock.json` (no extraneous
  packages sitting in `node_modules` outside the lockfile).
- No dependency was installed from the "Sports Intelligence OS" document package set; no
  packages from `C:\Users\Garrett\Downloads` entered the tree.

## Conclusion / classification

Both HIGH-severity findings share a single root cause: **Next.js 14.x → 16.x major upgrade**.
This is explicitly OWNER-GATED per the sprint queue scope guard:

> "GSE-SEC-059/060/003 (Next.js major) — dependency majors." — `handoff/SPRINT_QUEUE.md` PHASE 8 SCOPE GUARD

`npm audit` offers no non-major fix for either package. Therefore:

- **SAFE-DIRECT fix available?** No. The required fix is a SemVer-major upgrade, which the
  scope guard classifies as owner-gated. Applying a `next` major bump in this session would:
  (a) cross the scope guard, (b) rewrite JSX/transform output across `apps/web/`, and
  (c) likely break the build — not a smallest-change, reversible task.
- **NEEDS-OWNER:** Yes. The owner must decide whether to schedule the Next.js 14→16 major
  upgrade. Per `handoff/SPRINT_QUEUE.md` P9-03, dependency majors are a ship-readiness item for
  the owner, not a sprint-executed fix.

**Net:** 0 CRITICAL, 0 MODERATE, 0 LOW, 2 HIGH (one root cause), all owner-gated → no
sprint-executed code change is warranted. This document is the deliverable for P7-09.

## VERIFY

- [x] `npm audit --omit=dev --json` executed; raw output saved to `handoff/npm-audit-current.json`.
- [x] Severity totals read from `metadata.vulnerabilities` and confirmed by counting
      `vulnerabilities` entries (2).
- [x] `next` declared version traced: `apps/web/package.json:35` → `^14.2.15`.
- [x] `next` installed version traced: `node_modules/next/package.json` → `14.2.35`.
- [x] `postcss` confirmed nested/transitive at `node_modules/next/node_modules/postcss`
      with `effects: ["next"]`.
- [x] fixAvailable confirmed `isSemVerMajor: true` for both → no non-major fix exists.
- [x] scope-guard citation confirmed: `handoff/SPRINT_QUEUE.md` PHASE 8 block, line ~1012.
- [x] 11 fabricated-package watchlist confirmed absent from `package-lock.json`.
