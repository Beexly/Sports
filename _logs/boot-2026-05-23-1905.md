# Boot Report — 2026-05-23 19:05 UTC

## Environment
- Repo: `/home/user/Sports` (ephemeral remote container)
- Branch: `claude/keen-ptolemy-d0pbK` (per session contract)
- Working tree: clean, no uncommitted changes
- Last commit: `7900d41` — "Make daily smoke workflow state check shell-portable"

## Recovered State
- `_logs/`: did not exist; created
- `CLAUDE.md`: present (project rules)
- `apps/web/.env`: did NOT exist; created stub from `.env.example` (DATABASE_URL=stub, DEV_FAKE_ADMIN=true, all secrets blank/stub)
- `packages/db/.env`: did NOT exist; created stub
- `CLAUDE_PICKUP.md`, `V6_HANDOFF.md`, `PHASE_9_REPORT.md`: present, reviewed
- No `_logs/CHANGELOG.md`, `_logs/DECISIONS.md`, or `_logs/SESSION-SUMMARY.md` from prior sessions

## Repo Map
- `apps/web/app/`: 26 route groups (admin, cockpit, picks, blog, brief, observatory, vault, vs, etc.)
- `apps/web/app/api/`: 14 namespaces (admin, auth, cockpit, cron, dev, health, picks, performance, promotions, subscriptions, webhooks, etc.)
- `apps/web/lib/`: 17 modules incl. content-engine, content-generator.ts, cockpit, calibration, source-intelligence
- `apps/web/app/cockpit/`: agents, brief, calibration, content, history, jarvis, media, promotions, review, sources, tasks
- `packages/`: db (1331-line schema.prisma), prediction-engine, data-ingestion, ingestion-pipeline, types
- `workers/`: content-publishing (disabled by triple kill switch), data-refresh, pick-generation

## Health Check
| Check | Result |
|---|---|
| `npm install` | OK (593 packages, 13 vuln) |
| `npx prisma generate` | OK |
| `npm run typecheck` | **GREEN** (all 9 workspaces) |
| `npm run lint` | **GREEN** (exit 0, --max-warnings=0) |
| `npm test` | **GREEN** — 1578 tests passing: web 1342 / data-ingestion 11 / prediction-engine 197 / types 28 |
| Dev server route check | SKIPPED — ephemeral container, no real DB; typecheck + 1578 passing tests is the green signal |

## AI Surface Inventory (from background survey)
- **Only one Claude call site**: `apps/web/lib/content-generator.ts` (116 lines) — raw `fetch` to `api.anthropic.com`, `claude-sonnet-4-6`, regex JSON extraction, no retries/timeouts/typed errors. **Violates Hard Rule §6**: "All Anthropic API calls must include retry logic, timeout, and structured error handling. Use the SDK's built-ins; don't roll your own."
- Status check: `apps/web/app/api/dev/state/route.ts:35` (just `!!process.env.ANTHROPIC_API_KEY`)
- Jarvis gate: `apps/web/lib/cockpit/jarvis-data.ts:46` (same)
- Prediction engine, calibration, Jarvis synthesis: pure logic, no Claude
- Workers: content-publishing is intentionally idle (triple kill switch); data-refresh + pick-generation don't use Claude

## SDK Reference Mining (extracted earlier)
- `/home/user/anthropic-sdks-reference/anthropic-sdk-typescript-main/` — direct fit
- High-priority patterns to import: official client, prompt caching, structured outputs (`output_config.format` json_schema), batches, streaming, typed errors
- Lower priority: Files API, tool runners, vision (no near-term need)
- Skip: Ruby SDK (no Ruby), Claude Agent SDK (not for content), managed agents (not applicable)

## Stop Conditions Check (§7)
None triggered. No destructive migrations, no paid services, no vendor decisions, no Stripe live-mode work pending.

## Cycle 1 Target
Upgrade `content-generator.ts` to the official Anthropic SDK. Highest leverage:
1. Resolves Hard Rule §6 violation (retries/timeouts/typed errors)
2. Replaces brittle regex JSON extraction with `output_config.format` json_schema (no malformed-response failure mode)
3. Establishes the SDK-client + cached-client pattern every future AI cycle will reuse
4. Adds a real test for the parsing path (none exists today)
