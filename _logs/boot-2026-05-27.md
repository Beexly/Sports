# Boot 2026-05-27T08:30Z

## Branch
`claude/gsn-claude-md-setup-lsMvR` — clean working tree at boot, `7900d41 Make daily smoke workflow state check shell-portable` at HEAD.

## §2.1 — CLAUDE.md
v1 ("Sports Prediction Platform", 115 lines) replaced with v2 GSN per the boot prompt. Prior `_logs/SESSION-SUMMARY.md` / `_logs/DECISIONS.md` / `_logs/CHANGELOG.md` did not exist — created fresh.

## §2.2 — .claude/ validation
At boot: `.claude/` missing. Created per §16:
- `settings.json` (model, permission_mode, allowed_tools, mcp_servers, plugins_required)
- `agents/pick-generator.md`, `agents/grader.md` (full per §16.2/§16.3)
- `agents/stat-researcher.md`, `agents/injury-monitor.md`, `agents/line-watcher.md`, `agents/settler.md`, `agents/content-writer.md`, `agents/social-clipper.md`, `agents/email-blaster.md`, `agents/operator-reviewer.md` (stubs per §16 parenthetical — each cites ≥1 cookbook)
- `commands/gsn-cycle.md`, `commands/gsn-handoff.md` (full per §16.4/§16.5)
- `hooks/pre-tool-use.json` (per §16.6)
- `mcp.json` (per §16.7)

Deferred (not first-boot mandatory):
- `commands/gsn-ship.md`, `gsn-pick.md`, `gsn-settle.md`, `gsn-calibration.md` — declared in §5.3, scaffold in next cycle
- `hooks/post-tool-use.json`, `session-start.json`, `session-end.json`, `user-prompt-submit.json` — declared in §5.5, scaffold in next cycle
- `skills/pick-generator/SKILL.md` + subdirs — §5.4, build via `skill-creator` when that plugin is installed
- `agnix` lint pass — plugin not installed yet

## §2.3 — Env files
Not inspected this cycle (no app-code work). Repo has `.env.example` and `.env.production.example` at root; `apps/web/.env` and `packages/db/.env` not verified — defer to Cycle 2 health pass.

## §2.4 — Health pass
Deferred to Cycle 2. Branch scope is CLAUDE.md + `.claude/` setup; running `npm`/`pnpm` checks would expand scope beyond the branch name.

Note: CLAUDE.md v2 §2.4 / §9 / §10 reference `pnpm` but the existing repo uses `npm` (per `package-lock.json` and the v1 CLAUDE.md). This is a reconciliation question for Cycle 2 — either migrate to pnpm or update CLAUDE.md commands to `npm`. Logging here so it's not lost.

## §2.5 — Route pass
Deferred to Cycle 2 (no dev server in this cycle).

## §2.6 — MCP audit
Active in session: Notion (via configured MCP), plus a handful of unrelated servers (Lawvable, Hotels, etc.) that are out of scope per CLAUDE.md §5.6. Core GSN MCPs not all confirmed connected: Google Drive, Gmail, Higgsfield, Canva, Figma — surface to Garrett.

## §2.7 — Plugin audit
None of the §4.2 / §4.3 plugins are installed in this session. `settings.json` declares them as `plugins_required` for future sessions to satisfy.

## Cycle 1 outcome
Setup committed and pushed to `claude/gsn-claude-md-setup-lsMvR`. See `_logs/CHANGELOG.md` and `_logs/DECISIONS.md`.
