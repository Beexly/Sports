---
description: Review confidence thresholds vs outcomes
allowed-tools: Read, Grep, Glob, Bash(git diff*), Bash(git log*), Bash(git status*)
---
Review confidence thresholds and edge/selection logic against graded-pick outcomes. Identify where thresholds over- or under-fire relative to realized accuracy. Propose data-backed adjustments with the supporting numbers. Recommend only — no silent changes.
