---
description: Audit empty / loading / error / locked states
---
Audit every data-driven card for its empty, loading, error, and locked states.
Specifically: anything showing a blank or bare dash (e.g. the gated WIN RATE card) must read as deliberately locked, not broken. Propose explicit treatments — lock glyph, "LOCKED until N settled", skeleton loaders, clear error fallbacks.
List each component, its current state handling, and the proposed fix. Report first.
