---
description: Verify GSN hard stops are enforced
---
Verify the GSN hard stops are enforced in code and config:
1) no destructive DB operations possible in normal flows, 2) Stripe is test-mode only, 3) no automated production deploys without explicit approval, 4) no public accuracy claims unsupported by graded data.
Report each as enforced / at-risk with file:line evidence.
