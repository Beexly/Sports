---
description: Full visual consistency audit of the GSE (Galaxy Sports Edge) cockpit
allowed-tools: Read, Grep, Glob, Bash(git diff*), Bash(git log*), Bash(git status*)
---
Audit the cockpit UI for visual consistency. Do NOT edit yet.
Check: spacing-scale adherence, alignment across cards/grids, typography hierarchy (size + weight), border/elevation/glow usage, and whether each accent color is used consistently.
Preserve the existing dark command-center identity. Report issues per component with file:line and the fix, ranked by how much each hurts perceived polish.
