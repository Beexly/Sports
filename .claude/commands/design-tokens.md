---
description: Extract hardcoded styles into tokens
---
Find hardcoded colors, spacing values, radii, and shadows/glows across the cockpit components. Map them to a single token set (or the existing one if present) and flag one-off values that break the scale.
Report duplicates and near-duplicates (e.g. #1a1a1f vs #181820). Propose a consolidated token list + a migration plan. Apply nothing yet.
