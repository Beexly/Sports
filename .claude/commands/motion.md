---
description: Tasteful micro-interactions
allowed-tools: Read, Grep, Glob, Bash(git diff*), Bash(git log*), Bash(git status*)
---
Propose restrained motion for the cockpit: gauge ring fill on load, number count-up on stat cards, smooth hover/focus transitions, and subtle state-change animation in the decision queue.
Keep it premium and fast — no bounce, no long durations — and respect prefers-reduced-motion. List each proposed interaction + where it goes. Don't implement until approved.
