---
description: TypeScript strictness audit
allowed-tools: Read, Grep, Glob, Bash(git diff*), Bash(git log*), Bash(git status*), Bash(npm run typecheck*)
---
Find weak typing across the repo: `any`, unchecked `as` casts, non-null `!` assertions, and unsafe narrowing — especially around picks, grading, odds, and payments.
List file:line with a safer typing for each. Don't change code yet.
