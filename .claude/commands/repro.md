---
description: Build a minimal reproduction
argument-hint: [describe the bug]
---
Build the smallest reproduction for: $ARGUMENTS
Identify the minimal inputs, state, and call path that trigger it. Produce a runnable repro (test or script) and the exact steps. Don't fix yet — just isolate.
