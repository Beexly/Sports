# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/
dist/

# Environment variables — ignore ALL .env* (real secrets land in .env.production,
# .env.staging, etc. per the deploy flow); re-include only the committed templates.
.env*
!.env*.example

# Prisma
prisma/migrations/dev.db

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# Type coverage (test/type-coverage output dirs only — not StatKing data)
coverage/
!data/statking/coverage/
!data/statking/coverage/**

# Vercel
.vercel
**/.vercel/

# Turbo
.turbo
apps/web/tsconfig.tsbuildinfo

# Local agent/runtime debris
.agent/logs/
scrapes/
tmp/
.cache/

# Sandbox / launch-night debris — these get created when the operator
# runs the recovery recipe and should never land in a commit.
_speedtest/
_speedtest.bad/
_disktest
_disktest.bad
node_modules.bad/
node_modules.partial/
_overnight_quarantine/
.synthetic-monitoring/

# Accidental nested repository clone from Phase 0 cleanup.
Sports/


# Local launch secrets — never commit
.launch-secrets/
VERCEL_ENV.txt
.env.vercel
.env*.local

# Playwright visual-review artifacts (generated by scripts/screenshot.mjs)
screenshots/
/test-results/
/playwright-report/
test-results/
playwright-report/

# Ignore local .claude config, but DO track shared project commands.
.claude/*
!.claude/commands/

# Local session helper scripts — never commit
*.bat
*.ps1
*.cmd

# GSE local waitlist runtime data — local-file fallback store, never commit
.gse-local/
packages/prediction-engine/node_modules/
machina-templates/

# Local npm-audit / route-inventory scratch dumps — never commit
scratch_audit_*.json
scratch_audit_err.txt
dashfiles.json
# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/
dist/

# Environment variables — ignore ALL .env* (real secrets land in .env.production,
# .env.staging, etc. per the deploy flow); re-include only the committed templates.
.env*
!.env*.example

# Prisma
prisma/migrations/dev.db

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# Type coverage (test/type-coverage output dirs only — not StatKing data)
coverage/
!data/statking/coverage/
!data/statking/coverage/**

# Vercel
.vercel
**/.vercel/

# Turbo
.turbo
apps/web/tsconfig.tsbuildinfo

# Local agent/runtime debris
.agent/logs/
scrapes/
tmp/
.cache/

# Sandbox / launch-night debris — these get created when the operator
# runs the recovery recipe and should never land in a commit.
_speedtest/
_speedtest.bad/
_disktest
_disktest.bad
node_modules.bad/
node_modules.partial/
_overnight_quarantine/
.synthetic-monitoring/

# Accidental nested repository clone from Phase 0 cleanup.
Sports/


# Local launch secrets — never commit
.launch-secrets/
VERCEL_ENV.txt
.env.vercel
.env*.local

# Playwright visual-review artifacts (generated by scripts/screenshot.mjs)
screenshots/
/test-results/
/playwright-report/
test-results/
playwright-report/

# Ignore local .claude config, but DO track shared project commands and the
# project-wide agent safety policy (permissions deny/ask + PreToolUse guard).
# settings.local.json stays ignored — that is the per-developer override file.
.claude/*
!.claude/commands/
!.claude/settings.json

# Local session helper scripts — never commit
*.bat
*.ps1
*.cmd

# GSE local waitlist runtime data — local-file fallback store, never commit
.gse-local/
packages/prediction-engine/node_modules/
machina-templates/

# Hermes overnight-agent scratch output (reports, journals, inventories) —
# read in the morning, never committed. See docs/ops/hermes/README.md.
handoff/
