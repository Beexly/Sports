{
  "name": "@gse/formal-foundry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GSE Formal Foundry — dormant, lab-only IC3 / Apalache control plane (ITF codec, Apalache JSON-RPC client, CTI lifting/MIC, IC3 controller, safety ledger, velocity dashboard). Silent-launch / proof-gated: zero production wiring. Tests the RPC client against a self-built mock JSON-RPC server, not a real Apalache instance (none is available in this environment).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
