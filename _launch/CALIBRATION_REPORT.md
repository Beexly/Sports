# CALIBRATION REPORT — the gate that decides if GSE can charge money

> AUTO-GENERATED by `scripts/generate-calibration-report.mjs` — do not hand-edit.
> Regenerates after every settlement cycle (data-refresh worker, non-fatal) and on demand via `npm run calibration:report`.
> Math mirrors `apps/web/lib/calibration/compute.ts`, parity-pinned by `apps/web/__tests__/calibration-report-generator.test.ts`.

- **Generated:** 2026-06-10T16:26:01.723Z
- **Data status:** STUB — no DATABASE_URL configured; no database was read
- **Sample filter:** settled (WIN/LOSS/PUSH), `isPublished=true`, `isBootstrap=false`, seed model excluded (`v5.0.0-seed`). Pending/void picks and bootstrap-era picks never count.

## Verdict

**Good enough to charge money? Not yet, because there is no calibration sample at all — 0 graded picks.** No database is configured (stub mode), so the pipeline has never settled a published canonical pick. Nothing here is a judgment on the model; there is simply nothing to judge yet.

**What unblocks this, in order:**

1. **GA-01** — provision the production Postgres (~15 min; migrate-in-build applies the schema automatically).
2. **GA-02** — fix/pay The Odds API key (~15 min; no live odds = no picks = no sample).
3. Then ~3-6 weeks of daily generate->settle accumulation to reach the 150-graded-pick gate (the one clock nobody can compress).

## Eligibility gate

| Gate | Required | Current | Status |
|---|---|---|---|
| Graded canonical picks | >= 150 | 0 | NOT MET |

## Sample size & hit rate

- **Graded picks:** 0 (0W – 0L – 0P)
- **Hit rate (pushes excluded):** — on 0 decisive picks
- **95% CI on hit rate (Wilson):** — (no decisive picks)
- **Break-even reference:** 52.4% at standard -110 vig. The verdict requires the CI **lower bound** to clear it, not the point estimate.

## Brier score

- **Overall:** — (lower is better; 0.25 = uninformative coin; verdict requires < 0.25)

### Per sport

No graded picks — no per-sport breakdown exists yet.

### Trend over time windows

No graded picks — no trend exists yet.

## Calibration curve (predicted vs actual)

No graded picks — the curve is empty. Every bucket below is structural, not evidence.

| Confidence bucket | Sample | Predicted win rate | Actual win rate | Delta | Bucket Brier |
|---|---|---|---|---|---|
| 50-59 | 0 | — | — | — | — |
| 60-69 | 0 | — | — | — | — |
| 70-79 | 0 | — | — | — | — |
| 80-89 | 0 | — | — | — | — |
| 90-100 | 0 | — | — | — | — |

_Buckets need >= 30 picks before drift is judged; drift >= 0.12 blocks the verdict._

## Closing-line comparison (CLV)

**No closing-line data exists yet.** The CLV capture pipeline is wired (ClosingLine snapshots + nullable `pick.clv*` columns, computed non-fatally at settlement), but no settled pick carries a computed CLV value. This section will populate on its own once settlement runs against live odds — nothing is estimated or backfilled in the meantime.

---

_This report is evidence, not a decision. Charging money, changing prices, or bumping MODEL_VERSION remain founder-gated human calls (see `_launch/GARRETT_ACTIONS.md`)._
